[package]
name = "wasm-ffi"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
build = "build.rs"

[lints.rust]
unsafe_code = "allow" # Raw Wasm exports decode host pointers and manage result handles.
unused_attributes = "warn"
unused_lifetimes = "warn"

[lints.clippy]
all = "warn"

[lib]
name = "faust_wasm_ffi"
crate-type = ["cdylib", "rlib"]

[dependencies]
codegen = { path = "../codegen" }
compiler = { path = "../compiler" }
parser = { path = "../parser" }
ffi-common = { path = "../ffi-common" }

[dev-dependencies]
serde_json = "1"
