[package]
name = "ffi-common"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[lints.rust]
unsafe_code = "allow" # shared ABI helpers require raw-pointer operations
unused_attributes = "warn"
unused_lifetimes = "warn"

[lints.clippy]
all = "warn"
