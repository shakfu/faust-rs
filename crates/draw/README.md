# draw

Graph/diagram rendering crate — scaffold placeholder.

## Intended role

- Host visualization helpers for internal IRs (boxes, signals, FIR), including
  deterministic debug views used by parity investigations.
- Provide reusable drawing backends for tooling and documentation generation.

## Current status

Scaffold only. No graph rendering API is exposed yet.

## Public API

| Item | Description |
|---|---|
| `crate_id()` | Returns the stable crate identifier |
