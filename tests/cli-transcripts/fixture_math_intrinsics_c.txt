#ifndef  __math_intrinsics_H__
#define  __math_intrinsics_H__

#ifndef FAUSTFLOAT
#define FAUSTFLOAT float
#endif

#ifdef __cplusplus
extern "C" {
#endif

#if defined(_WIN32)
#define RESTRICT __restrict
#else
#define RESTRICT __restrict__
#endif

#include <math.h>
#include <stdint.h>
#include <stdlib.h>

#ifndef FAUSTCLASS
#define FAUSTCLASS math_intrinsics
#endif

#ifdef __APPLE__
#define exp10f __exp10f
#define exp10 __exp10
#endif

static inline int faustmini(int a, int b) { return (a < b) ? a : b; }
static inline int faustmaxi(int a, int b) { return (a > b) ? a : b; }


typedef struct {
    int fSampleRate;
} math_intrinsics;

math_intrinsics* newmath_intrinsics() {
    math_intrinsics* dsp = (math_intrinsics*)calloc(1, sizeof(math_intrinsics));
    return dsp;
}

void deletemath_intrinsics(math_intrinsics* dsp) {
    free(dsp);
}

void metadatamath_intrinsics(MetaGlue* m) {
    m->declare(m->metaInterface, "faust-rs", "module-first c backend prototype");
}

int getSampleRatemath_intrinsics(math_intrinsics* RESTRICT dsp) {
    return dsp->fSampleRate;
}

int getNumInputsmath_intrinsics(math_intrinsics* RESTRICT dsp) {
    (void)dsp;
    return 1;
}

int getNumOutputsmath_intrinsics(math_intrinsics* RESTRICT dsp) {
    (void)dsp;
    return 1;
}

void classInitmath_intrinsics(int sample_rate) {
    (void)sample_rate;
}

void instanceConstantsmath_intrinsics(math_intrinsics* dsp, int sample_rate) {
    dsp->fSampleRate = sample_rate;
}

void instanceResetUserInterfacemath_intrinsics(math_intrinsics* dsp) {
    (void)dsp;
}

void instanceClearmath_intrinsics(math_intrinsics* dsp) {
    (void)dsp;
}

void instanceInitmath_intrinsics(math_intrinsics* dsp, int sample_rate) {
    instanceConstantsmath_intrinsics(dsp, sample_rate);
    instanceResetUserInterfacemath_intrinsics(dsp);
    instanceClearmath_intrinsics(dsp);
}

void initmath_intrinsics(math_intrinsics* dsp, int sample_rate) {
    classInitmath_intrinsics(sample_rate);
    instanceInitmath_intrinsics(dsp, sample_rate);
}

void buildUserInterfacemath_intrinsics(math_intrinsics* dsp, UIGlue* ui_interface) {
    (void)dsp;
    (void)ui_interface;
}

void computemath_intrinsics(math_intrinsics* dsp, int count, FAUSTFLOAT** RESTRICT inputs, FAUSTFLOAT** RESTRICT outputs) {
    FAUSTFLOAT* input0 = inputs[0];
    FAUSTFLOAT* output0 = outputs[0];
    for (int i0 = 0; i0 < count; i0 = i0 + 1) {
        output0[i0] = ((fmax(fmin(pow(fabs(((double)(input0[i0]))), 0.5), 1.0), -1.0) * 0.5) + (-atan2(sin(((double)(input0[i0]))), cos(((double)(input0[i0]))))));
    }
}


#ifdef __cplusplus
}
#endif

#endif
EXIT=0
