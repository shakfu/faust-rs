/* ------------------------------------------------------------
name: "math_intrinsics"
Code generated with Faust 0.8.0 (https://faust.grame.fr)
Compilation options: -lang cpp -single
------------------------------------------------------------ */

#ifndef  __math_intrinsics_H__
#define  __math_intrinsics_H__

#ifndef FAUSTFLOAT
#define FAUSTFLOAT float
#endif

#include <algorithm>
#include <cstddef>
#include <cmath>
#include <cstdint>
#include <cstring>
#include <exception>
#include <new>

#ifndef FAUSTCLASS
#define FAUSTCLASS math_intrinsics
#endif

#ifdef __APPLE__
#define exp10f __exp10f
#define exp10 __exp10
#endif

#if defined(_WIN32)
#define RESTRICT __restrict
#else
#define RESTRICT __restrict__
#endif


class math_intrinsics : public dsp {
private:
    int fSampleRate;
public:
    math_intrinsics() {
    }

    math_intrinsics(const math_intrinsics&) = default;

    virtual ~math_intrinsics() = default;

    math_intrinsics& operator=(const math_intrinsics&) = default;

    virtual int getNumInputs() {
        return 1;
    }
    virtual int getNumOutputs() {
        return 1;
    }
    static void classInit(int sample_rate) {
        (void)sample_rate;
    }
    virtual int getSampleRate() {
        return fSampleRate;
    }
    virtual void instanceConstants(int sample_rate) {
        fSampleRate = sample_rate;
    }
    virtual void instanceResetUserInterface() {
    }
    virtual void instanceClear() {
    }
    virtual void init(int sample_rate) {
        classInit(sample_rate);
        instanceInit(sample_rate);
    }
    virtual void instanceInit(int sample_rate) {
        instanceConstants(sample_rate);
        instanceResetUserInterface();
        instanceClear();
    }
    virtual math_intrinsics* clone() {
        return new math_intrinsics(*this);
    }
    virtual void metadata(Meta* m) {
        (void)m;
        m->declare("filename", "math_intrinsics.dsp");
        m->declare("name", "math_intrinsics");
    }
    virtual void buildUserInterface(UI* ui_interface) {
        ui_interface->openVerticalBox("math_intrinsics");
        ui_interface->closeBox();
    }
    virtual void compute(int count, FAUSTFLOAT** RESTRICT inputs, FAUSTFLOAT** RESTRICT outputs) {
        FAUSTFLOAT* input0 = inputs[0];
        FAUSTFLOAT* output0 = outputs[0];
        for (int i0 = 0; i0 < count; ++i0) {
            output0[i0] = ((std::fmax(std::fmin(std::pow(std::fabs(((double)(input0[i0]))), 0.5), 1.0), -1.0) * 0.5) + (-std::atan2(std::sin(((double)(input0[i0]))), std::cos(((double)(input0[i0]))))));
        }
    }
};

#endif
EXIT=0
