/* ------------------------------------------------------------
name: "simple"
Code generated with Faust 0.8.0 (https://faust.grame.fr)
Compilation options: -lang cpp -vec -lv 0 -vs 8 -single
------------------------------------------------------------ */

#ifndef  __mydsp_H__
#define  __mydsp_H__

#ifndef FAUSTFLOAT
#define FAUSTFLOAT float
#endif

#include <algorithm>
#include <cstddef>
#include <cmath>
#include <cstdint>
#include <cstring>
#include <exception>
#include <new>

#ifndef FAUSTCLASS
#define FAUSTCLASS mydsp
#endif

#ifdef __APPLE__
#define exp10f __exp10f
#define exp10 __exp10
#endif

#if defined(_WIN32)
#define RESTRICT __restrict
#else
#define RESTRICT __restrict__
#endif


class mydsp : public dsp {
private:
    int fSampleRate;
    FAUSTFLOAT fHslider0;
public:
    mydsp() {
    }

    mydsp(const mydsp&) = default;

    virtual ~mydsp() = default;

    mydsp& operator=(const mydsp&) = default;

    virtual int getNumInputs() {
        return 1;
    }
    virtual int getNumOutputs() {
        return 1;
    }
    static void classInit(int sample_rate) {
        (void)sample_rate;
    }
    virtual int getSampleRate() {
        return fSampleRate;
    }
    virtual void init(int sample_rate) {
        classInit(sample_rate);
        instanceInit(sample_rate);
    }
    virtual void instanceInit(int sample_rate) {
        instanceConstants(sample_rate);
        instanceResetUserInterface();
        instanceClear();
    }
    virtual mydsp* clone() {
        return new mydsp(*this);
    }
    virtual void metadata(Meta* m) {
        (void)m;
        m->declare("compile_options", "-lang cpp -vec -lv 0 -vs 8 -single");
        m->declare("filename", "simple.dsp");
        m->declare("name", "simple");
    }
    virtual void instanceConstants(int sample_rate) {
        fSampleRate = sample_rate;
    }
    virtual void instanceResetUserInterface() {
        fHslider0 = 0.5f;
    }
    virtual void instanceClear() {
    }
    virtual void buildUserInterface(UI* ui_interface) {
        ui_interface->openVerticalBox("simple");
        ui_interface->addHorizontalSlider("g", &fHslider0, FAUSTFLOAT(0.5), FAUSTFLOAT(0.0), FAUSTFLOAT(1.0), FAUSTFLOAT(0.01));
        ui_interface->closeBox();
    }
    virtual void compute(int count, FAUSTFLOAT** RESTRICT inputs, FAUSTFLOAT** RESTRICT outputs) {
        FAUSTFLOAT* input0 = inputs[0];
        FAUSTFLOAT* output0 = outputs[0];
        if ((count - (count % 8)) > 0) {
            for (int vindex = 0; vindex < (count - (count % 8)); vindex += 8) {
                int vcount = 8;
                for (int i0 = vindex; i0 < (vindex + vcount); i0 += 1) {
                    output0[i0] = ((FAUSTFLOAT)((((float)(fHslider0)) * ((float)(input0[i0])))));
                }
            }
        }
        if ((count - (count % 8)) < count) {
            int vindex = (count - (count % 8));
            int vcount = (count - (count - (count % 8)));
            for (int i0 = vindex; i0 < (vindex + vcount); i0 += 1) {
                output0[i0] = ((FAUSTFLOAT)((((float)(fHslider0)) * ((float)(input0[i0])))));
            }
        }
    }
};

#endif
EXIT=0
