/* ------------------------------------------------------------
name: "simple"
Code generated with Faust 0.8.0 (https://faust.grame.fr)
Compilation options: -lang c -single
------------------------------------------------------------ */

#ifndef  __mydsp_H__
#define  __mydsp_H__

#ifndef FAUSTFLOAT
#define FAUSTFLOAT float
#endif

#if defined(__GNUC__) || defined(__clang__)
#define FAUST_UNUSED __attribute__((unused))
#else
#define FAUST_UNUSED
#endif

#ifdef __cplusplus
extern "C" {
#endif

#if defined(_WIN32)
#define RESTRICT __restrict
#else
#define RESTRICT __restrict__
#endif

#include <math.h>
#include <stdint.h>
#include <stdlib.h>
#include <string.h>

#ifndef FAUSTCLASS
#define FAUSTCLASS mydsp
#endif

#ifdef __APPLE__
#define exp10f __exp10f
#define exp10 __exp10
#endif

static inline FAUST_UNUSED int faustmini(int a, int b) { return (a < b) ? a : b; }
static inline FAUST_UNUSED int faustmaxi(int a, int b) { return (a > b) ? a : b; }


typedef struct {
    int fSampleRate;
    FAUSTFLOAT fHslider0;
} mydsp;

mydsp* newmydsp() {
    mydsp* dsp = (mydsp*)calloc(1, sizeof(mydsp));
    return dsp;
}

void deletemydsp(mydsp* dsp) {
    free(dsp);
}

void metadatamydsp(MetaGlue* m) {
    m->declare(m->metaInterface, "compile_options", "-lang c -single");
    m->declare(m->metaInterface, "filename", "simple.dsp");
    m->declare(m->metaInterface, "name", "simple");
}

int getSampleRatemydsp(mydsp* RESTRICT dsp) {
    return dsp->fSampleRate;
}

int getNumInputsmydsp(mydsp* RESTRICT dsp) {
    (void)dsp;
    return 1;
}

int getNumOutputsmydsp(mydsp* RESTRICT dsp) {
    (void)dsp;
    return 1;
}

void classInitmydsp(int sample_rate) {
    (void)sample_rate;
}

void instanceConstantsmydsp(mydsp* dsp, int sample_rate) {
    dsp->fSampleRate = sample_rate;
}

void instanceResetUserInterfacemydsp(mydsp* dsp) {
    dsp->fHslider0 = 0.5f;
}

void instanceClearmydsp(mydsp* dsp) {
}

void instanceInitmydsp(mydsp* dsp, int sample_rate) {
    instanceConstantsmydsp(dsp, sample_rate);
    instanceResetUserInterfacemydsp(dsp);
    instanceClearmydsp(dsp);
}

void initmydsp(mydsp* dsp, int sample_rate) {
    classInitmydsp(sample_rate);
    instanceInitmydsp(dsp, sample_rate);
}

void buildUserInterfacemydsp(mydsp* dsp, UIGlue* ui_interface) {
    ui_interface->openVerticalBox(ui_interface->uiInterface, "simple");
    ui_interface->addHorizontalSlider(ui_interface->uiInterface, "g", &dsp->fHslider0, (FAUSTFLOAT)0.5, (FAUSTFLOAT)0.0, (FAUSTFLOAT)1.0, (FAUSTFLOAT)0.01);
    ui_interface->closeBox(ui_interface->uiInterface);
}

void computemydsp(mydsp* dsp, int count, FAUSTFLOAT** RESTRICT inputs, FAUSTFLOAT** RESTRICT outputs) {
    // signal_fir_fastlane_step2a: executable base slice
    // io: inputs=1 outputs=1
    // signals: 1
    float fSlow0 = ((float)(dsp->fHslider0));
    FAUSTFLOAT* input0 = inputs[0];
    FAUSTFLOAT* output0 = outputs[0];
    for (int i0 = 0; i0 < count; i0 = i0 + 1) {
        output0[i0] = ((FAUSTFLOAT)((fSlow0 * ((float)(input0[i0])))));
    }
}


#ifdef __cplusplus
}
#endif

#endif
EXIT=0
