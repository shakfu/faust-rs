# 2026-07-19

## Plan the transform crate cleanup, documentation, and factorization

Commit date: 2026-07-19
Source commit: this commit

Added `porting/transform-cleanup-documentation-factorization-plan-2026-07-19-en.md`,
a proposed nine-phase plan (R0–R9) for structural cleanup of
`crates/transform` with zero intended change to semantics, FIR shape,
generated code, or public compiler behavior. The diagnosis: the June scalar
and delay decompositions remain sound; the new complexity center is the July
checked vector pipeline, whose largest files each combine versioned artifact
models, producers, independent checkers, and large embedded test suites. The
sequence is freeze → current-state documentation → test relocation →
vocabulary-only sharing → per-stage producer/checker splits → import and
visibility cleanup → mechanical enforcement, with the trust-boundary rule
that checkers never call producers or reuse producer-derived results.

The plan was refined against the current HEAD before landing: metrics
re-measured at `c70f97d9` (crate 56,704 lines, vector 27,253; 385 transform
unit tests; 799 missing-docs diagnostics; vector coverage 96/132 in all 16
modes, 1,536 retained pairs), the frozen-contract list extended with the July
clocked-vector correctness semantics (ZeroPad fire gating, history-served
back-edges, `attach` ordering edges, `UnadoptedStatefulRead`), and a new
finding (§4.8) recorded: admission guards that live in shared verify
functions — `reject_unadopted_stateful_reads` is the concrete case — must
demonstrably remain on both the producer and checker paths when R6 splits
build from check, each covered by a checker-side rejection test. The
validation matrix now also carries the two recorded harness traps (purge
cached `.ir` before impulse runs and assert `filesCompare` counts; byte
identity via a frozen baseline worktree hands any emission difference to the
oracle).

Documentation only; no code changes.

## Drop redundant parentheses around C-family `if`/`while` conditions

Commit date: 2026-07-19
Source commit: this commit

The shared C/C++ statement emitter wrapped an already parenthesized rendered
condition in the statement's own parentheses, producing
`if ((dsp->fDSCounter_d0 == 0))` and tripping clang's
`-Wparentheses-equality` on every emitted guard. `unwrap_condition` in
`c_family.rs` now strips exactly one outer layer, and only when the leading
parenthesis closes at the final character — `((a == b) && c)` and
`(a) == (b)` are left untouched. Applied to the three shared statement heads
(`If`, `Control`, `WhileLoop`); the shared walker serves only the C and C++
backends. Unit tests cover the whole-wrapper/partial-wrapper distinction, and
the existing C++ core-statement test now asserts the single-parenthesis
forms.

Qualification: the purged full-corpus impulse matrix on the four C-family
lanes (`cpp`, `c-ss0`, `cpp-vec0-ss0`, `c-vec0-ss0`) matches the references
with zero differences, and the lane compile logs contain no
`-Wparentheses-equality` occurrence. The emission change is cosmetic by
construction (parentheses only); the oracle arbitrates the numerics.

## Gate ZeroPad on the fire index and fail-close unadopted clocked state

Commit date: 2026-07-19
Source commit: this commit

A user-run C++ impulse oracle caught `upsampling_01_accumulator` emitting 2
where the reference says 1 in `-vec` mode. Reproduced in the repository
harness: scalar passes, all eight `-lv 0/1 x -ss 0..3` vector modes fail
identically. Two distinct vector clock-domain bugs were behind the 
divergences, both invisible to the structural pipeline, which certified the
wrong emissions.

First, the pure vector lowerer treated `ZeroPad(x, h)` as a pass-through, so
an upsampled domain read the outer-rate input on every fire and, e.g.,
accumulated an impulse `factor` times. The lowering now mirrors the scalar
`generateZeroPad` gating: `((vclock_d<N>_fire == h - 1) ? x : 0)`, with the
island resolved from the `CountedUpsampling` clock plan by the ZeroPad's own
signal id, failing closed outside an upsampling island. This fixes
`upsampling_01_accumulator` and `upsampling_05_parallel_state` (8/8 oracle
modes each).

Second, `upsampling_02_feedback` fails differently: its recursion has only
constant inputs, so clock inference correctly types it rate-polymorphic
(bottom environment), and the plan hoists it into an outer-rate loop — its
state then advances once per outer sample instead of once per fire. The
analogous downsampling shape (`downsampling(1 : (+ ~ *(0.5)))`, no committed
fixture) was confirmed equally wrong against the scalar emission. Adopting
such groups into their wrapper's domain is a real design (sharing, nested
domains, storage geometry) and is deliberately not improvised here; instead
the clock plan producer and its independent checker both reject any
Immediate/Delayed dependency from an in-domain consumer to an audio-rate
stateful producer (`UnadoptedStatefulRead`), with one legal crossing: the
`ZeroPad` value operand, which is exactly where propagation zero-stuffs an
outer-rate producer into an upsampled domain. `upsampling_02_feedback` and
the downsampling shape now fall back to scalar and match the reference; the
guard demotes no other corpus DSP.

Qualification: the 16-mode sweep is 96 certified / 35 fallback / 1 error in
every mode (exactly the deliberate `upsampling_02_feedback` demotion; the
versioned baseline and certified list were regenerated). The full 8-mode C++
impulse matrix passes for all six clocked corpus DSPs, 48/48. Byte-identity
against the pre-fix build holds for 188 of 192 certified emissions; the four
differences are the two upsampling DSPs under both loop variants — the added
fire gating, oracle-arbitrated. New regressions:
`upsampling_zero_pad_gating_is_certified_and_bit_exact` (fails on the pre-fix
tree with 2 vs 1) and
`unadopted_stateful_recursion_under_upsampling_falls_back`. The complete
vector-retention gate, fmt, clippy with warnings denied, and the workspace
suite are green.

Lesson recorded: the six clocked DSPs promoted into the certified baseline
during X3 had scalar-side numeric validation only; certification is
structural and never a numeric proof. Promotion into the certified list must
require the vector-mode oracle matrix. Open follow-up: domain adoption for
rate-polymorphic stateful groups under clock wrappers (would recover
`upsampling_02_feedback` and the equivalent downsampling/ondemand shapes as
certified vector code).

## Complete vector-plan divergence qualification (X3)

Commit date: 2026-07-19
Source commit: this commit

Completed X3 from
`porting/vector-plan-contexts-divergence-plan-2026-07-18-en.md`. The expanded
132-DSP corpus remains exactly 97 certified, 34 fallback, and one error in
every `f32/f64 x -lv 0/1 x -ss 0..3` mode. The versioned coverage baseline was
stale relative to the already-committed 39-DSP clocked/multirate expansion; it
now includes that expansion and the six universally certified clocked cases
(`downsampling_01_accumulator`, `downsampling_04_delay`,
`downsampling_05_parallel_state`, `upsampling_01_accumulator`,
`upsampling_02_feedback`, and `upsampling_05_parallel_state`). X2b itself does
not newly certify a corpus DSP.

FIR comparison against pre-X2b commit `e8c49891` is byte-identical for 192 of
194 emissions (97 DSPs under both loop variants), with no compilation failure.
The two differences are `gate_compressor` lv0/lv1: four cross-loop histories
are now explicit. The old and new native 60,000-frame outputs compare exactly,
and the current output passes the genuine C++ reference under all eight
`-lv 0/1 x -ss 0..3` combinations.

The external WAC 197-DSP directory moves from 165 to 173 certified, closing
all eight class-B Karplus/chain cases. The final split is 173 certified, 20
fallback, and four front-end errors; fallback reasons are 11 assembly, six
pure foreign-function effects, two event-bound cases, and one cyclic plan.
The corpus scanner now runs on a 64 MiB worker stack and accepts a checked
`--shard=INDEX/COUNT` option. Deterministic sharding keeps the full source
directory visible, so local DSP imports remain valid during parallel external
qualification; argument validation has a focused regression.

Validation: the 16-mode sweep and complete vector-retention gate, the five-case
release compile budget, 40 optimized/unoptimized interpreter traces, the 8/8
`gate_compressor` native oracle, formatting, workspace clippy with warnings
denied, all-target workspace tests, and the Rust golden gate are green.

## Serve cross-group symbolic back-edges with previous-sample state (X2b)

Commit date: 2026-07-19
Source commit: this commit

Completed X2b from
`porting/vector-plan-contexts-divergence-plan-2026-07-18-en.md` without changing
the prepared scalar signal tree. The P6.1 state producer now combines the
checked `Delayed { amount }` dependency with P4.4 placements: a recursive
projection whose alias and selected body have distinct owned loops gives the
body an effective history obligation of at least `amount`. The P6.1 checker
independently derives and verifies the same transition coverage and geometry.

The pure vector lowerer consumes that accepted body history for the matching
cross-loop `SIGPROJ(..., SYMREF(...))` instead of asking the route for a
current-sample value. The cross-loop placement and accepted transition are
both required; same-loop recursion retains its previous lowering. This guard
was necessary to avoid duplicating delay state and disabling the existing
lockstep register bundles for ordinary explicit `delay1` recursions.

The structural regression
`delayed_dependency_without_occurrence_delay_still_requires_history` pins the
P4.3b/P4.4-to-P6.1 mapping. The real library-level
`karplus_pass_through_backedge_is_certified_and_bit_exact` regression uses
`pm.ks(200, 0.5)` and passes certification plus scalar/vector interpreter
bit-exactness under both loop variants, all four scheduling strategies, and
vector sizes 32 and 24. The test runs on an explicitly enlarged test-thread
stack because expanding `stdfaust.lib` exceeds libtest's default thread stack
on some hosts.

Validation: `cargo fmt --all -- --check`, workspace clippy with warnings
denied, all-target workspace tests, the 385-test transform suite, the complete
33-test vector-mode integration suite, and `cargo run -p xtask -- golden-check`
are green. X3 remains the external/corpus qualification step.
