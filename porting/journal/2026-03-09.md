# JOURNAL 2026-03-09

Entries originally recorded under this journal day.

- Sorted by Git commit recency (newest first).
- `Commit date` is the commit day for the entry addition.

---

### `transform` / `compiler` / `cranelift-ffi` / `xtask` / `porting` — Tighten the post-`de_bruijn_to_sym` FIR boundary and lock fixed-delay integration coverage

Commit date: 2026-03-09

Source commit: pending

#### Context

Once fixed-size `SIGDELAY` lowering was in place, two follow-up cleanups were
needed before the slice could be considered stable:

- `signal_fir/module.rs` still carried fallback `DEBRUIJN` / `DEBRUIJNREF`
  decoding even though the production path now always goes through
  `prepare_signals_for_fir(...)` and `de_bruijn_to_sym(...)`
- the generated state/delay names and integration coverage were still a bit too
  ad hoc for the new circular-buffer shape

During final workspace validation, the expanded `rep_38_sine_phasor.dsp`
fixture also exposed that Cranelift bring-up tests still reject DSP struct
array fields, so the selected Cranelift subset had to stay narrower than the
fast-lane C/C++ subset for now.

#### Changes

- `crates/transform/src/signal_fir/module.rs`
  - removed `DEBRUIJN` / `DEBRUIJNREF` handling from `lower_proj(...)`
  - tightened the recursion contract to `SYMREC` / `SYMREF` plus the remaining
    `SIGREC` adapter path
  - updated the RustDoc to document the strictly post-`de_bruijn_to_sym`
    boundary
  - renamed generated state carriers from `state_n*` to `fState*`
  - renamed generated delay-line carriers from `delay_n*` to `fDelay*`

- `crates/transform/src/signal_fir/mod.rs`
  - aligned structural tests with the new `fState*` / `fDelay*` naming

- `crates/compiler/tests/signal_fir_lane.rs`
  - added a dedicated integration test for
    [rep_04_delay_echo.dsp](/Users/letz/Developpements/RUST/faust-rs/tests/corpus/rep_04_delay_echo.dsp)
    asserting that the generated C and C++ contain:
    - `fIOTA`
    - the circular `fDelay*` buffer
    - masked read access
    - delay-line clearing in `instanceClear`

- `crates/compiler/tests/signal_pipeline.rs`
  - aligned the `rep_38_sine_phasor` signal-shape assertion with the current
    two-output fixture and its summed phasor branches

- `crates/cranelift-ffi/src/factory.rs`
  - kept `rep_38_sine_phasor.dsp` out of the selected Cranelift runtime subset
    because the current Cranelift bring-up contract still rejects DSP struct
    array fields such as the new fixed delay lines

- `tests/corpus/rep_38_sine_phasor.dsp`
  - kept the updated multi-output sine-phasor fixture

- `tests/golden/rust/rep_38_sine_phasor/compiler_stdout.txt`
  - refreshed the Rust golden snapshot for the current fixture/program output

- [signal-fir-preparation-plan-2026-03-09-en.md](/Users/letz/Developpements/RUST/faust-rs/porting/signal-fir-preparation-plan-2026-03-09-en.md)
  - updated the follow-up status to reflect that constant-delay lowering is now
    implemented and raw de Bruijn recursion is no longer part of the FIR input
    contract

#### Validation

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test -p transform --all-targets`
- `cargo test -p compiler --test signal_fir_lane`
- `cargo test -p compiler --test signal_pipeline`
- `cargo test -p cranelift-ffi --lib`
- `cargo test --workspace --all-targets`
- `cargo run -p xtask -- golden-check`

#### Outcome

The fast-lane FIR lowerer is now stricter about its recursion boundary,
explicitly post-`de_bruijn_to_sym`, and its fixed-delay lowering is covered by
both unit tests and C/C++ integration tests.

The remaining Cranelift gap is now isolated to its reduced FIR subset contract
instead of surfacing as a workspace-wide regression.

### `transform` / `compiler` / `xtask` / `porting` — Implement fixed-size fast-lane `SIGDELAY` lines and align the sine-phasor corpus snapshot

Commit date: 2026-03-09

Source commit: pending

#### Context

After the preparation plan was closed, the remaining `signalFIRCompiler`
parity gap in the fast-lane was general `SIGDELAY`.

The Rust lowerer still treated `SIGDELAY` as a restricted scalar-state case,
while the C++ reference allocates typed circular delay lines, indexes them with
`fIOTA`, and keeps a zero-delay fast path.

The agreed implementation scope for this slice stayed intentionally narrower
than C++:

- support constant integer delay amounts only
- reject variable delays explicitly until a real delay-bound analysis exists

During validation, the local `rep_38_sine_phasor.dsp` corpus fixture also
turned out to have been expanded to two outputs, so the signal-pipeline test
and Rust golden snapshot had to be aligned with that updated fixture state.

#### Changes

- `crates/transform/src/signal_fir/module.rs`
  - added a pre-scan that allocates delay-line resources before lowering
  - added typed fixed-size delay-line declarations keyed by the carried delayed
    signal
  - added persistent struct `fIOTA` state and per-sample increment
  - lowered constant `SIGDELAY` to masked circular read/write access
  - preserved the zero-delay fast path
  - kept `SIGDELAY1` / `SIGPREFIX` on scalar state slots
  - made variable-delay rejection explicit in the fast-lane error path
  - documented the new lowering boundary in RustDoc with C++ provenance notes

- `crates/transform/src/signal_fir/mod.rs`
  - updated fast-lane RustDoc status notes for fixed-size delay support
  - added structural regressions for:
    - delay-line array allocation
    - `fIOTA` declaration/reset/increment
    - masked circular indexing
    - zero-delay fast path
    - variable-delay rejection

- `crates/compiler/tests/signal_pipeline.rs`
  - relaxed the sine-phasor shape test so it validates every emitted output
    without assuming a fixed output count

- `tests/corpus/rep_38_sine_phasor.dsp`
  - kept the updated two-output fixture variant

- `tests/golden/rust/rep_38_sine_phasor/compiler_stdout.txt`
  - refreshed the Rust golden snapshot for the updated fixture

- [signal-fir-preparation-plan-2026-03-09-en.md](/Users/letz/Developpements/RUST/faust-rs/porting/signal-fir-preparation-plan-2026-03-09-en.md)
  - marked the fixed-size delay-line slice as implemented
  - kept variable delays explicitly deferred

#### Validation

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test -p transform --all-targets`
- `cargo test -p compiler --test signal_fir_lane`
- `cargo test -p compiler --test signal_pipeline`
- `cargo test --workspace --all-targets`
- `cargo run -p xtask -- golden-check`

#### Outcome

The fast-lane now implements the fixed-size portion of C++ `SIGDELAY` lowering:
typed circular buffers, persistent `fIOTA`, and masked read/write indices.

Variable delays are still rejected by design, but the supported constant-delay
subset is now explicit, tested, documented, and green across the workspace and
Rust golden corpus.

### `transform` / `porting` — Scope the next `SIGDELAY` parity slice to fixed-size delay lines only

Commit date: 2026-03-09

Source commit: pending

#### Context

The signal-to-FIR preparation plan was already closed, but a follow-up review
against C++ `signalFIRCompiler` showed that the remaining `SIGDELAY` gap had
been documented too narrowly in the Rust fast-lane.

The C++ reference does not treat general `SIGDELAY` as a scalar `delay1`
variant. It allocates a typed circular delay line, indexes it with a
persistent `fIOTA` counter, and sizes the buffer from a static delay bound.

Rust does not have the full interval-driven sizing contract yet, so the next
parity slice needed a sharper specification before code changes.

#### Changes

- [signal-fir-preparation-plan-2026-03-09-en.md](/Users/letz/Developpements/RUST/faust-rs/porting/signal-fir-preparation-plan-2026-03-09-en.md)
  - added a follow-up note describing the remaining `SIGDELAY` gap versus C++
  - fixed the scope of the next slice to constant integer delays only
  - documented the required FIR machinery:
    - typed circular delay-line arrays
    - persistent `fIOTA`
    - power-of-two masked indexing
    - zero-delay fast path
  - deferred variable delays explicitly until Rust grows a separate
    delay-bound analysis

#### Validation

- documentation-only change

#### Outcome

The plan now states the active parity target precisely: implement fixed-size
delay lines first, and reject variable delays explicitly instead of pretending
they already belong to the supported fast-lane subset.

### `transform` / `compiler` / `xtask` / `porting` — Close the signal-to-FIR preparation parity plan after workspace and corpus validation

Commit date: 2026-03-09

Source commit: pending

#### Context

After the four implementation slices were complete, the plan still required a
closing validation pass on the parity-critical DSP families before it could be
marked done.

The goal of this final step was to verify two things:

- the workspace still passes all local gates after the preparation/lowering
  refactor
- the observable corpus behavior used by CI remains unchanged

#### Changes

- [signal-fir-preparation-plan-2026-03-09-en.md](/Users/letz/Developpements/RUST/faust-rs/porting/signal-fir-preparation-plan-2026-03-09-en.md)
  - marked the plan as `completed`
  - marked the final validation step as complete

#### Validation

- `cargo test --workspace --all-targets`
- `cargo run -p xtask -- golden-check`

Both passed locally after the full preparation stack:

- staging-arena forest clone
- list-wide `de_bruijn_to_sym`
- reduced simple typing
- reduced promotion
- typed FIR lowering consumption

#### Outcome

The signal-to-FIR preparation plan is now closed cleanly.

The fast-lane preparation path is implemented end-to-end and remains compatible
with the current workspace corpus snapshots and CI-style local validation.

### `transform` / `porting` — Make `signal_fir` consume prepared reduced types for integer state and table lowering

Commit date: 2026-03-09

Source commit: pending

#### Context

After the prepared forest carried symbolic recursion, reduced types, and the
relevant promotion casts, the remaining algorithmic gap was in the FIR lowerer
itself: it still defaulted many carriers to `real_ty`.

That was especially visible on the parity-critical families called out by the
plan:

- delay/prefix state slots
- recursion projection state slots
- waveform and writable table element types
- integer result carriers such as promoted `select2` / arithmetic outputs

#### Changes

- `crates/transform/src/signal_fir/module.rs`
  - threaded the prepared `SimpleSigType` map through the FIR module builder
  - documented the reduced typed boundary in RustDoc
  - added typed FIR helpers so lowering can map:
    - `Int -> FirType::Int32`
    - `Real -> real_ty`
    - `Sound -> FirType::Sound`
  - switched delay and recursion state slots to the prepared FIR type instead
    of always using `real_ty`
  - switched waveform and writable-table declarations/loads to the prepared
    element type
  - switched typed zero initialization for state/table carriers
  - switched `SIGBINOP`, `SIGSELECT2`, and the `enable` helper path to use the
    prepared result type instead of the blanket internal real type

- `crates/transform/src/signal_fir/mod.rs`
  - updated RustDoc to document that the fast-lane now consumes the prepared
    reduced type map from `signal_prepare`

- `crates/transform/src/signal_fir/mod.rs` tests
  - added a regression proving that integer `delay1` allocates an `Int32`
    state slot
  - added a regression proving that integer waveforms declare `Int32` FIR table
    element types

- [signal-fir-preparation-plan-2026-03-09-en.md](/Users/letz/Developpements/RUST/faust-rs/porting/signal-fir-preparation-plan-2026-03-09-en.md)
  - marked typed FIR lowering consumption as complete

#### Validation

- `cargo fmt --all`
- `cargo test -p transform --all-targets`

#### Outcome

The fast-lane no longer forces delay/recursion/table carriers through
`real_ty`. The prepared reduced type contract is now active on both sides of
the pre-FIR boundary: preparation and FIR lowering.

### `transform` / `porting` — Port the reduced pre-FIR `SignalPromotion` cast subset on the prepared signal forest

Commit date: 2026-03-09

Source commit: pending

#### Context

With staging and reduced typing in place, the next fast-lane parity step was to
port the useful cast-insertion part of C++ `sigPromotion.cpp` before FIR
lowering.

The intended scope stayed deliberately reduced:

- no extra simplification
- no table-bounds safety promotion
- no second C++ type-annotation pass as a semantic phase

But the prepared forest needed the core numeric casts that make the later FIR
lowerer see correctly promoted signal trees.

#### Changes

- `crates/transform/src/signal_prepare.rs`
  - inserted a reduced promotion pass after simple typing and before the final
    prepared package is returned
  - added a memoized forest rewriter that preserves sharing while promoting:
    - `delay` amount to `SIGINTCAST`
    - `prefix` mixed numeric inputs to float
    - `select2` selector to int and mixed branches to float
    - arithmetic/comparison/bitwise binops following the reduced C++ rules
    - unary/binary math families to promoted real inputs
    - `rdtbl` / `wrtbl` index and write-signal casts
    - `soundfile_length/rate/buffer` integer arguments
    - bargraph input promotion to float
    - clock-carrying `ondemand` / `upsampling` / `downsampling`
  - preserved symbolic recursion and list structure while rewriting the forest
  - re-ran the reduced typer internally on the promoted forest so the exported
    `PreparedSignals.types` map describes the final IR consumed by `signal_fir`
    (implementation adaptation, not a new semantic normalization phase)

- [signal-fir-preparation-plan-2026-03-09-en.md](/Users/letz/Developpements/RUST/faust-rs/porting/signal-fir-preparation-plan-2026-03-09-en.md)
  - marked reduced signal promotion as complete

#### Validation

- `cargo fmt --all`
- `cargo test -p transform --all-targets`

#### Outcome

The prepared signal forest is now close to the C++ `deBruijn2Sym ->
typeAnnotation -> signalPromote` contract for the active fast-lane subset, and
the remaining work can focus on making `signal_fir` consume the prepared types
instead of defaulting to `real_ty`.

### `transform` / `porting` — Add reduced `Int / Real / Sound` typing to the pre-FIR signal preparation stage

Commit date: 2026-03-09

Source commit: pending

#### Context

After the staging-arena and list-wide `de_bruijn_to_sym` boundary was in
place, the next planned step was to add one reduced typing layer before signal
promotion and FIR lowering.

The explicit target for this stage was not the full C++ `sigtyperules` lattice.
It was a smaller parity-oriented contract that can already drive:

- numeric promotion decisions
- delay/prefix sample typing
- recursive group result typing
- waveform and table element typing
- soundfile handle versus numeric result distinctions

#### Changes

- `crates/transform/src/signal_prepare.rs`
  - added public `SimpleSigType` with the reduced domain:
    `Int`, `Real`, `Sound`
  - extended `PreparedSignals` with a `types` map and a `ty(...)` accessor
  - implemented the first simple typer over the prepared forest
  - covered the active fast-lane subset, including:
    - arithmetic/comparison/bitwise nodes
    - `delay` / `delay1` / `prefix`
    - `select2`
    - waveform / `rdtbl` / `wrtbl`
    - UI primitives, bargraphs, soundfile helpers
    - symbolic recursion groups and projections
  - used a small recursive-group fixpoint on symbolic groups and closed
    unresolved recursive sample placeholders to `Real` so the reduced typer
    stays compatible with the existing fast-lane default behavior
  - documented the reduced mapping against C++ `typeAnnotation(...)`

- [signal-fir-preparation-plan-2026-03-09-en.md](/Users/letz/Developpements/RUST/faust-rs/porting/signal-fir-preparation-plan-2026-03-09-en.md)
  - marked simple signal typing as complete

#### Validation

- `cargo fmt --all`
- `cargo test -p transform --all-targets`

#### Outcome

The prepared signal package now carries enough type information to port the
reduced `SignalPromotion` rules in the next step without importing the full C++
type lattice.

### `transform` / `tlib` / `porting` — Add staging-arena forest preparation and list-wide `de_bruijn_to_sym` before fast-lane FIR lowering

Commit date: 2026-03-09

Source commit: pending

#### Context

The signal-to-FIR preparation plan starts by making the fast-lane consume a
prepared forest instead of raw propagated outputs.

The critical parity constraint for this first slice is that recursive groups
must be converted from de Bruijn form to symbolic form at the **forest** level,
not output-by-output, so shared multi-output groups keep one coherent symbolic
identity.

#### Changes

- `crates/tlib/src/arena.rs`
  - added `TreeArena::clone_forest_from(...)` with one shared memo table so
    cross-root sharing survives staging-arena cloning

- `crates/tlib/tests/core_semantics.rs`
  - added a structural regression proving that shared source subtrees remain
    shared after cloning multiple roots together

- `crates/transform/src/signal_prepare.rs`
  - added the first preparation module and `PreparedSignals`
  - implemented `prepare_signals_for_fir(...)` as:
    - clone whole output forest into a private arena
    - rebuild one cons-list root
    - run `de_bruijn_to_sym(...)` on that list root
    - unpack the symbolic output list back to `Vec<SigId>`

- `crates/transform/src/signal_fir/mod.rs`
  - routed `compile_signals_to_fir_fastlane(...)` through the new preparation
    stage before FIR emission

- `crates/transform/src/signal_fir/module.rs`
  - taught recursion lowering to decode `SYMREC/SYMREF` groups produced by the
    preparation stage while preserving the C++ list-shaped recursive payload

- `crates/transform/src/lib.rs`
- `crates/tlib/src/lib.rs`
  - updated RustDoc to describe the new fast-lane staging/preparation contract

- [signal-fir-preparation-plan-2026-03-09-en.md](/Users/letz/Developpements/RUST/faust-rs/porting/signal-fir-preparation-plan-2026-03-09-en.md)
  - moved the plan from `proposed` to `in progress`
  - marked the staging-arena + list-wide `de_bruijn_to_sym` step as complete

#### Validation

- `cargo fmt --all`
- `cargo test -p tlib --all-targets`
- `cargo test -p transform --all-targets`

#### Outcome

The fast-lane now has the first real pre-FIR preparation boundary:

- outputs are cloned into a private mutable arena
- recursive groups are converted with forest-wide memoization
- FIR lowering can consume symbolic recursion without losing the original
  list-based group payload contract used by propagated signals

### `transform` / `compiler` / `tlib` / `porting` — Specify a typed preparation stage before fast-lane Signal -> FIR lowering

Commit date: 2026-03-09

Source commit: pending

#### Context

The current Rust fast-lane still consumes propagated signals directly, with:

- de Bruijn recursion payloads kept intact
- no dedicated signal typing pass
- no `SignalPromotion`-style cast insertion pass

That leaves an important semantic gap against the C++ `signalFIRCompiler`-style
path: integer-vs-real behavior, symbolic recursion handling, and delay/table
resource typing are still partially driven by ad hoc lowering choices instead of
by an explicit pre-FIR signal preparation contract.

The requested direction for the lane is:

- activate `de_bruijn_to_sym` after `propagate`
- add a simple signal typing phase
- port the useful subset of `sigPromotion.cpp`
- do this before FIR lowering, without adding full signal simplification yet
- keep delay, recursion, and table handling as parity-critical fast-lane families

#### Changes

- added [signal-fir-preparation-plan-2026-03-09-en.md](/Users/letz/Developpements/RUST/faust-rs/porting/signal-fir-preparation-plan-2026-03-09-en.md)
  - defines the target pipeline:
    `propagate -> de_bruijn_to_sym -> simple typing -> signal promotion -> signal_fir`
  - fixes the intended scope to the reduced preparation subset, not full
    `simplifyToNormalForm(...)`
  - makes delay/prefix, recursion, and tables explicit parity-critical families
  - recommends a mutable staging arena plus shared forest-clone helper
  - proposes a minimal `SimpleSigType` model (`Int` / `Real` / `Sound`)
  - separates the work into preparation, lowered-type consumption, and
    differential validation steps

#### Validation

- local source analysis only
- compared against:
  - `compiler/normalize/normalform.cpp`
  - `compiler/transform/sigPromotion.cpp`
  - `compiler/transform/signalFIRCompiler.cpp`
  - `compiler/box_signal_api.cpp`
  - current Rust `crates/transform/src/signal_fir/{mod.rs,module.rs}`

#### Outcome

There is now an explicit implementation plan for moving the fast-lane toward
C++ `signalFIRCompiler` preparation semantics without pulling in the whole
normalization pipeline at once.

### `compiler` / `tests` — Lock C/C++ `-double` semantics to internal `double` with unchanged `FAUSTFLOAT`

Commit date: 2026-03-09

Source commit: pending

#### Context

The C/C++ `--double` contract needed to stay aligned with Faust:

- `--double` changes the internal DSP real type to `double`
- `FAUSTFLOAT` remains controlled by the architecture layer and must not be
  rewritten by the Rust backends

The fast-lane implementation already behaved that way, but the contract was
not explicitly protected by RustDoc and regression tests.

#### Changes

- `crates/compiler/src/lib.rs`
  - clarified in RustDoc that `Compiler::with_real_type(Float64)` changes only
    the internal FIR real type
  - documented that backend interface types such as C/C++ `FAUSTFLOAT` remain
    architecture-controlled

- `crates/compiler/tests/signal_fir_lane.rs`
  - added a C++ fast-lane regression proving that `RealType::Float64` keeps
    `#define FAUSTFLOAT float` while switching the generated compute cast from
    `float` to `double`
  - added the matching C fast-lane regression

#### Validation

- `cargo test -p compiler fastlane_cpp_double_keeps_faustfloat_interface_and_uses_double_internal_ops -- --exact --nocapture`
- `cargo test -p compiler fastlane_c_double_keeps_faustfloat_interface_and_uses_double_internal_ops -- --exact --nocapture`
- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace --all-targets`

#### Outcome

The workspace now documents and tests the intended Faust-compatible contract:
C/C++ `--double` upgrades the generated DSP core to `double` without changing
the `FAUSTFLOAT` interface macro.

### `propagate` / `compiler` / `box-ffi` / `eval` — Make the typed FlatBox API the primary post-eval contract and close the plan

Commit date: 2026-03-09

Source commit: pending

#### Context

The FlatBox work was technically complete through stage 4, but the final plan
step was still open:

- make the typed API the primary documented contract
- keep or retire the raw `BoxId` wrappers explicitly rather than implicitly

At that point the gap was no longer algorithmic. It was about making the
post-`eval/a2sb` boundary visible in the public API and in the main workspace
call sites.

#### Changes

- `crates/propagate/src/lib.rs`
  - introduced `box_arity_typed(...)` as the primary typed arity entry point
  - kept `box_arity_flat(...)` as a hidden compatibility alias
  - updated RustDoc so:
    - `box_arity_typed(...)` / `propagate_typed(...)` are documented as the
      primary post-eval contract
    - raw `box_arity(...)` / `propagate(...)` are documented as compatibility
      wrappers over `FlatBoxId`

- `crates/compiler/src/lib.rs`
  - the main production compile flow now validates `process_box` into
    `FlatBoxId` once, then uses typed arity and typed propagation

- `crates/eval/src/lib.rs`
  - scalar-evaluation propagation now validates the lowered box into
    `FlatBoxId` before calling typed propagation

- `crates/box-ffi/src/lib.rs`
  - the box-to-signals and box-to-FIR FFI helpers now build `FlatBoxId`
    explicitly and route through typed arity/propagation
  - raw wrappers are still available, but no longer needed on the main FFI
    post-eval paths

- `crates/propagate/tests/core_api.rs`
  - renamed the typed arity regression to use `box_arity_typed(...)`

- `porting/flat-box-input-plan-2026-03-09-en.md`
  - marked the plan as `completed`
  - documented that stage 5 is considered closed with raw wrappers retained as
    compatibility entry points

#### Outcome

The FlatBox plan is now closed cleanly:

- the typed API is the primary documented and exercised post-eval contract
- the raw wrappers are explicitly compatibility adapters, not the conceptual
  core API
- the main workspace users already cross the boundary explicitly before
  propagation

### `signals` / `propagate` / `tests` — Fix gate-only issues after workspace fmt, clippy, and full test run

Commit date: 2026-03-09

Source commit: pending

#### Context

After the FlatBoxId stage-4 implementation was complete, the mandatory
workspace gates were run:

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace --all-targets`

The implementation was functionally correct, but two gate-only cleanup items
remained:

- one `clippy::collapsible_if` in `signals`
- one `clippy::too_many_arguments` in `propagate`

#### Changes

- `crates/signals/src/lib.rs`
  - collapsed the trivial nested `if` in `SigBuilder::clocked(...)`

- `crates/propagate/src/lib.rs`
  - replaced the long helper parameter list for clocked wrapper lowering with a
    small internal `ClockedWrapperCtx`
  - kept lowering behavior unchanged while satisfying the strict clippy budget

- `crates/propagate/tests/core_api.rs`
- `crates/signals/tests/core_api.rs`
  - accepted `rustfmt` normalization from the full workspace formatting pass

#### Validation

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace --all-targets`

#### Outcome

The full mandatory workspace gates are now green for the current branch, and
the FlatBoxId stage-4 work is in a clean CI-ready state.

### `propagate` / `signals` / `tests` — Port typed `soundfile` and clocked-wrapper lowering onto the new signal nodes

Commit date: 2026-03-09

Source commit: pending

#### Context

Once `signals` exposed the missing soundfile and clock-wrapper node families,
the remaining `FlatBoxId` stage-4 gaps were all inside `crates/propagate`:

- `soundfile`
- `ondemand`
- `upsampling`
- `downsampling`

These families are valid post-`eval/a2sb` input in C++, so leaving them as
`UnsupportedBox` would have kept stage 4 incomplete.

#### Changes

- `crates/propagate/src/lib.rs`
  - threaded an internal `clock_env` through the typed recursion
  - added adapted Rust clock-environment construction that preserves the C++
    field order while still leaving `slotenv` / `path` empty for now
  - implemented typed `soundfile` lowering to:
    - `soundfile`
    - `soundfile_length`
    - `soundfile_rate`
    - `soundfile_buffer`
    - bounded read-index computation via `max(0, min(ridx, length - 1))`
  - implemented typed lowering for:
    - `ondemand`
    - `upsampling`
    - `downsampling`
  - ported the C++ trivial clock cases:
    - clock `0` => zero outputs
    - clock `1` => direct body propagation
  - ported the structural wrapper construction:
    - `temp_var`
    - `double_clocked`
    - optional `zero_pad` for upsampling
    - `perm_var`
    - wrapper `OD/US/DS` node
    - final `seq(wrapper, output)` result

- `crates/propagate/tests/core_api.rs`
  - added `soundfile_box_lowers_to_length_rate_and_channel_buffers`
  - added `clocked_wrapper_boxes_port_trivial_and_structural_cases`

- `porting/flat-box-input-plan-2026-03-09-en.md`
  - updated stage 4 status to completed
  - documented the remaining internal adaptation around `clock_env`

#### Validation

- `cargo test -p propagate --all-targets`

#### Outcome

`FlatBoxId` stage 4 is now closed:

- no valid flat family remains in the “builder-valid but lowering-invalid”
  bucket,
- the remaining work is now stage-5 contract tightening and later cleanup of
  the adapted internal clock-environment representation.

### `signals` / `tests` — Add soundfile accessors and clocked wrapper nodes needed by FlatBoxId stage 4

Commit date: 2026-03-09

Source commit: pending

#### Context

The remaining `FlatBoxId` stage-4 families in `propagate` were blocked by
missing Rust signal nodes rather than by box-side structure:

- `soundfile` lowering needs:
  - `sigSoundfileLength`
  - `sigSoundfileRate`
  - `sigSoundfileBuffer`
- `ondemand` / `upsampling` / `downsampling` lowering needs:
  - `sigTempVar`
  - `sigPermVar`
  - `sigClocked`
  - the C++ nested representation of `sigDoubleClocked`

Without these nodes, any propagate-side port would have needed fake surrogate
encodings.

#### Changes

- `crates/signals/src/lib.rs`
  - added builder/matcher support for:
    - `soundfile_length`
    - `soundfile_rate`
    - `soundfile_buffer`
    - `temp_var`
    - `perm_var`
    - `clocked`
    - `double_clocked`
  - added `SigMatch` variants for the corresponding public decode shapes
  - documented the parity invariant that `double_clocked` remains represented as
    nested `clocked` nodes, matching C++
  - kept `clocked` canonical for the trivial duplicate-clock case:
    - `clocked(h, clocked(h, y)) -> clocked(h, y)`

- `crates/signals/tests/core_api.rs`
  - extended the UI/soundfile test to cover:
    - `SoundfileLength`
    - `SoundfileRate`
    - `SoundfileBuffer`
  - extended the stream-wrapper test to cover:
    - `TempVar`
    - `PermVar`
    - `Clocked`
    - nested `double_clocked`

#### Validation

- `cargo test -p signals --all-targets`

#### Outcome

Rust `signals` now exposes the missing public node families required to port
the remaining C++ `propagate` stage-4 paths without inventing alternate IR.

### `porting` / `signals` — Record that remaining FlatBoxId stage-4 work depends on signal-IR extensions

Commit date: 2026-03-09

Source commit: pending

#### Context

After landing the typed `FlatBoxId` boundary, typed arity, typed propagation,
and the `route`/`ffun` lowering paths, the remaining unsupported flat families
were no longer blocked by propagation structure itself.

The actual gap had become architectural:

- C++ `propagate.cpp` lowers `soundfile`, `ondemand`, `upsampling`, and
  `downsampling` through signal nodes that do not yet exist in Rust.
- Continuing directly in `crates/propagate` would have required either fake
  surrogate encodings or reduced semantics, both of which would break the
  parity goal.

#### Changes

- `porting/flat-box-input-plan-2026-03-09-en.md`
  - changed plan status from `proposed` to `in progress`
  - updated stage 4 to reflect completed substeps:
    - typed `Route`
    - typed `FFun`
  - documented that the remaining work now depends on extending
    `crates/signals` with the missing C++-parity families:
    - `sigSoundfileLength`
    - `sigSoundfileRate`
    - `sigSoundfileBuffer`
    - `sigTempVar`
    - `sigPermVar`
    - `sigClocked`
    - nested `sigClocked` shape for `sigDoubleClocked`

#### Outcome

The plan now describes the real next step accurately:

- finish parity by extending the Rust signal IR first,
- then re-enter `propagate` to port the remaining flat families on top of those
  new signal nodes.

### `propagate` / `tests` — Implement typed `route` and `ffun` lowering through `FlatBoxId`

Commit date: 2026-03-09

Source commit: pending

#### Context

After moving `propagate(...)` onto the typed flat boundary, stage 4 still had a
mixed bucket of builder-valid but lowering-invalid families. Two of them were
already directly representable with the existing Rust signal IR:

- `route`
- `ffun`

Unlike `soundfile`, `ondemand`, `upsampling`, and `downsampling`, these do not
require additional signal node families to preserve the current C++ semantics.

#### Changes

- `crates/propagate/src/lib.rs`
  - implemented `box_arity_flat(...)` support for `FFun` via C++-shaped
    `ffarity(signature) = len(signature) - 2`
  - implemented `propagate_inner(...)` support for `FFun` by lowering to
    `signals::SigBuilder::ffun(...)` with the propagated input list preserved in
    order
  - implemented typed `Route` lowering by:
    - flattening nested `par(...)` route specifications like C++
      `flattenRouteList(...)`
    - interpreting route endpoints with the same 1-based source/destination
      convention as C++
    - accumulating multiple sources per destination with additive merge
      semantics
  - updated crate RustDoc to record that `route` and `ffun` are now supported
    through the typed flat boundary

- `crates/propagate/tests/core_api.rs`
  - added `route_box_propagates_by_mixing_selected_inputs`
  - added `ffun_box_arity_and_propagation_follow_signature`

#### Validation

- `cargo test -p propagate --all-targets`

#### Outcome

The remaining stage-4 gap list is now narrower and better classified:

- `route` and `ffun` are no longer merely builder-valid placeholders
- `soundfile`, `ondemand`, `upsampling`, and `downsampling` remain blocked by
  missing Rust signal-node support rather than by flat-boundary refactoring

### `propagate` / `tests` — Route propagation through `FlatBoxId` and add typed entry point

Commit date: 2026-03-09

Source commit: pending

#### Context

With `FlatBoxId` and typed arity in place, the public propagation wrapper still
matched directly on unrestricted `BoxMatch` internally. That left the most
important runtime entry point only half migrated: the boundary was explicit for
validation and arity, but not yet for actual lowering.

#### Changes

- `crates/propagate/src/lib.rs`
  - added `propagate_typed(...)` as the flat-boundary entry point
  - changed the public `propagate(...)` wrapper to:
    - validate `BoxId` into `FlatBoxId`
    - delegate to `propagate_typed(...)`
  - ported `propagate_in_slot_env(...)` and `propagate_inner(...)` to accept
    `FlatBoxId`
  - switched internal recursion from unrestricted `BoxMatch` dispatch to
    `FlatNodeKind` dispatch
  - kept raw `match_box(...)` only where payload extraction is still needed
    from the underlying shared `TreeId`
  - preserved the distinction between:
    - boundary-invalid syntax rejected before propagation,
    - valid flat families still unimplemented by propagation (`route`,
      `ondemand`, `upsampling`, `downsampling`, `soundfile`, `ffun`)

- `crates/propagate/tests/core_api.rs`
  - added `propagate_typed_uses_flat_boundary_and_matches_wrapper`
  - verifies that:
    - typed propagation on a validated flat composition matches the legacy
      wrapper result,
    - raw `propagate(...)` rejects `case` before lowering starts

#### Validation

- `cargo test -p propagate --all-targets`

#### Outcome

The propagation runtime now shares the same typed boundary as the arity path:

- the flat subset is the actual internal input language,
- evaluator-only box syntax no longer appears in runtime lowering branches,
- remaining unsupported families are now genuine flat-propagation gaps rather
  than mixed boundary/semantic failures.

### `propagate` / `tests` — Route `box_arity(...)` through `FlatBoxId`

Commit date: 2026-03-09

Source commit: pending

#### Context

After introducing `FlatBoxId`, arity inference still matched directly on raw
`BoxMatch`. That meant the propagation boundary was explicit, but `box_arity`
itself still depended on the full box language internally.

The second step was to move arity inference onto the validated flat boundary
without changing the public wrapper API yet.

#### Changes

- `crates/propagate/src/lib.rs`
  - changed `ArityCache` to key results by `FlatBoxId`
  - added `box_arity_flat(...)` as the typed post-eval arity entry point
  - changed the public `box_arity(...)` wrapper to:
    - build `FlatBoxId` via `try_build_flat_box(...)`
    - delegate to `box_arity_flat(...)`
  - ported the arity implementation to `FlatNodeKind` instead of matching on
    unrestricted `BoxMatch`
  - added `From<FlatBoxBuildError> for PropagateError` so raw wrapper callers
    still observe the existing `UnsupportedBox` surface

- `crates/propagate/tests/core_api.rs`
  - added `box_arity_flat_uses_validated_flat_boundary`
  - verifies that:
    - typed arity inference succeeds on a validated flat composition,
    - raw `box_arity(...)` rejects `case` before entering arity inference

#### Validation

- `cargo test -p propagate --all-targets`

#### Outcome

Arity inference now treats the post-`eval/a2sb` flat subset as its actual input
language:

- dead evaluator-only forms are rejected at the boundary,
- arity logic no longer depends on the full box syntax universe,
- the public wrapper remains compatible while the internal contract is tighter.

### `propagate` / `tests` — Add `FlatBoxId` boundary validation for the post-`eval/a2sb` subset

Commit date: 2026-03-09

Source commit: pending

#### Context

The `FlatBoxId` plan established the intended propagation boundary, but the
crate still accepted raw `BoxId` everywhere. The first implementation slice was
to make that boundary executable without yet changing arity or lowering logic.

The goal of this step was therefore:

- introduce a typed handle rooted in `TreeId`,
- validate the post-eval flat-box subset structurally,
- reject evaluator-only syntax early,
- preserve the existing propagation behavior unchanged for now.

#### Changes

- `crates/propagate/src/lib.rs`
  - added `FlatBoxId(TreeId)` as the typed handle for the post-`eval/a2sb`
    propagation subset
  - added `FlatBoxBuildError::UnexpectedPostEvalBox { node, kind }`
  - added `try_build_flat_box(...)`
  - added internal `FlatNodeKind` decoding/validation over the admissible
    families
  - documented the new boundary in RustDoc, including the C++
    `evalprocess(...) -> a2sb(...) -> realPropagate(...)` provenance

- `crates/propagate/tests/core_api.rs`
  - added tests asserting that flat-box validation accepts valid post-eval
    families:
    - `symbolic`
    - `metadata`
    - `route`
    - `ondemand`
    - `upsampling`
    - `downsampling`
    - `soundfile`
    - `environment`
  - added tests asserting that flat-box validation rejects evaluator-only
    families:
    - `case`
    - `pattern_var`
    - `abstr`
    - `modulation`
    - local-definition shells
    - `component`
    - `library`
    - applicative/iterator forms
    - `ffunction`
  - added a nested regression test to ensure a non-flat subtree inside a larger
    flat-looking composition is still rejected

#### Validation

- `cargo test -p propagate --all-targets`

#### Outcome

This step makes the boundary check explicit without changing runtime lowering
yet:

- the crate now has a typed post-eval handle,
- the contract is enforced structurally,
- the `TreeArena` sharing model is preserved,
- later steps can move `box_arity(...)` and `propagate(...)` onto this boundary
  instead of matching against the full `BoxMatch` universe.

### `porting` / `propagate` — Specify a typed `FlatBoxId` boundary for post-eval propagation

Commit date: 2026-03-09

Source commit: pending

#### Context

The current Rust `propagate(...)` API accepts a generic `BoxId`, even though the
effective C++ production route is `evalprocess(...) -> a2sb(...) -> propagate`.
That means propagation conceptually does not consume arbitrary box syntax: it
consumes a stricter post-eval box language where lambda-calculus and pattern
matching have already disappeared.

This mismatch left the Rust contract too implicit:

- `propagate` still carried `UnsupportedBox` arms for evaluator-only forms that
  should never survive the eval boundary,
- valid C++ propagation families such as `route`, `ondemand`, `upsampling`,
  `downsampling`, and `soundfile` were mixed together with dead-by-contract
  syntax in the same unsupported bucket,
- the original plan draft proposed an owned structural `PropagateBox` IR, which
  would have weakened the existing `TreeArena` hash-consing and structural
  sharing model.

#### Changes

- Added [`porting/flat-box-input-plan-2026-03-09-en.md`](../flat-box-input-plan-2026-03-09-en.md)
  as the specification plan for a typed propagation boundary.

- The plan now defines the contract in terms of:
  - `FlatBoxId(TreeId)` as the typed post-eval propagation handle,
  - `FlatNodeKind` as the restricted internal decode view,
  - `FlatBoxBuildError` as the boundary error type.

- The representation choice was explicitly revised away from an owned copied IR
  toward a `TreeId`-based handle so the design keeps:
  - TreeArena hash-consing,
  - DAG structural sharing,
  - node identity parity with the rest of the compiler.

- The plan records the C++-derived family split:
  - valid after `eval/a2sb`: literals, slots/symbolic, primitives, widgets,
    groups, composition algebra, `Environment`, `Route`, `Inputs/Outputs`,
    `Ondemand`, `Upsampling`, `Downsampling`, `Soundfile`, waveform, foreign
    forms used by propagation,
  - forbidden after `eval/a2sb`: `Ident`, `Appl`, `Access`, `IPar`, `ISeq`,
    `ISum`, `IProd`, local-definition syntax, `Component`, `Library`, `Case`,
    `PatternVar`, `Abstr`, `Modulation`.

- The plan also keeps `Environment` in the flat contract to match the C++
  `realPropagate(...)` input language, while documenting it as operationally
  trivial (`(0,0)` arity and empty signal result).

#### Outcome

The resulting design is now aligned with the repo architecture:

- the typed boundary is explicit,
- the name `FlatBoxId` communicates that residual lambda/pattern syntax is gone,
- the representation remains rooted in `TreeId` instead of duplicating the box
  DAG,
- future propagation work can separate:
  - boundary violations from `eval/a2sb`,
  - true unimplemented propagation families.

#### Next steps

- implement `FlatBoxId`
- add `try_build_flat_box(...)`
- move dead-by-contract `UnsupportedBox` cases into the flat-box builder
- migrate `box_arity(...)` and `propagate(...)` internals toward `FlatBoxId`
  plus restricted decode views
