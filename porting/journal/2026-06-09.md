# 2026-06-09

## Add first Signal FFI slice

Commit date: 2026-06-09
Source commit: this commit

Added the first `signal-ffi` crate for the libfaust Signal C API parity plan.

Implementation updates:

- registered `crates/signal-ffi` in the workspace and exported it through
  `faust-ffi`;
- implemented constants, inputs, binary operators, shifts, comparisons,
  binary/unary math, delays, and casts as `Csig*` entry points;
- implemented basic `CisSig*` predicates for ints, reals, inputs, delays, and
  binary operators;
- reused the shared `tree-ffi` context so existing `CprintSignal` and
  `freeCMemory` symbols from `box-ffi` can decode and release Signal API data;
- regenerated `porting/generated/libfaust-signal-c-api-matrix.md`, which now
  reports 60 exact implementation candidates and 63 missing symbols.

Validation:

- `cargo fmt --all`
- `cargo test -p signal-ffi --all-targets`
- `cargo check -p signal-ffi -p faust-ffi`
- `cargo clippy -p signal-ffi -p faust-ffi --all-targets -- -D warnings`
- `cargo run -p xtask -- libfaust-api-matrix --cpp-root /Users/letz/Developpements/RUST/faust --out porting/generated`

## Share global tree FFI context for Signal API

Commit date: 2026-06-09
Source commit: this commit

Moved the process-global tree handle context into `tree-ffi` so future
`signal-ffi` constructors can share the same arena and opaque handle model as
the existing Box FFI surface.

Implementation updates:

- added `tree_ffi::global_context`, `with_global_context`, and
  `reset_global_context`;
- migrated `box-ffi` to use the shared `tree-ffi` context;
- preserved `createLibContext` and `destroyLibContext` behavior by resetting
  the shared context;
- kept Box API tests green while preparing `signal-ffi` handles to work with
  shared symbols such as `CprintSignal` and `freeCMemory`.

Validation:

- `cargo fmt --all`
- `cargo test -p tree-ffi --all-targets`
- `cargo test -p box-ffi --all-targets`
- `cargo check -p box-ffi -p faust-ffi`

## Test Box source generation FFI contract

Commit date: 2026-06-09
Source commit: this commit

Locked down the first `CcreateSourceFromBoxes` parity contract for supported
Rust backends and unsupported language diagnostics.

Implementation updates:

- added `box-ffi` tests for `c`, `cpp`, `fir`, and `interp` source generation
  from a simple wire box;
- verified returned source strings are non-empty and released with
  `freeCMemory`;
- added a negative test for unsupported languages with the stable diagnostic
  text.

Validation:

- `cargo fmt --all`
- `cargo test -p box-ffi --all-targets create_source_from_boxes`
- `cargo test -p box-ffi --all-targets`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace --all-targets`

## Test Box-to-Signal FFI array ownership

Commit date: 2026-06-09
Source commit: this commit

Locked down the `CboxesToSignals` and `CboxesToSignals2` ownership contract
from the Box API parity plan.

Implementation updates:

- added `box-ffi` tests for both conversion entry points;
- verified returned signal arrays are null terminated;
- verified arrays can be released with `freeCMemory`;
- verified signal handles remain printable after the returned array allocation
  is freed, as long as the libfaust context remains alive.

Validation:

- `cargo fmt --all`
- `cargo test -p box-ffi --all-targets boxes_to_signals`
- `cargo test -p box-ffi --all-targets`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace --all-targets`

## Cover fully-applied C++ soundfile box wrapper

Commit date: 2026-06-09
Source commit: this commit

Closed the Box API soundfile audit item without changing the C ABI.

Implementation updates:

- added local C++ header overloads for `boxSoundfile(const char*, Box, Box,
  Box)` and `boxSoundfile(const std::string&, ...)`;
- matched the C++ Faust reference composition exactly:
  `boxSeq(boxPar(part, ridx), boxSoundfile(label, chan))`;
- kept the C ABI surface unchanged because the reference C header exposes only
  `CboxSoundfile(label, chan)`;
- removed the matrix manual audit note for `CboxSoundfile` and
  `CisBoxSoundfile`;
- regenerated the Box/Signal API matrices.

Matrix snapshot:

- Box C API: 186 exact candidates, 6 nearest Rust IR mappings, 0 missing.
- Signal C API: 7 exact candidates, 116 missing.

Validation:

- `clang++ -std=c++17 -Icrates/box-ffi/include -x c++ -fsyntax-only -`
- `cargo fmt --all`
- `cargo test -p xtask --all-targets libfaust_api_matrix`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace --all-targets`
- `cargo run -p xtask -- libfaust-api-matrix --cpp-root /Users/letz/Developpements/RUST/faust --out porting/generated`

## Add dedicated exp10 Box/Signal path

Commit date: 2026-06-09
Source commit: this commit

Replaced the temporary `CboxExp10*` fallback-to-`exp` mapping with a dedicated
`exp10` representation through the Box, Signal, FIR, and backend pipeline.

Implementation updates:

- added `BOXEXP10`, `BoxBuilder::exp10`, and `BoxMatch::Exp10`;
- added `SIGEXP10`, `SigBuilder::exp10`, and `SigMatch::Exp10`;
- propagated `Exp10` through eval, flat propagation, signal typing,
  normalization, forward AD, reverse AD classification, signal preparation, and
  signal-to-FIR lowering;
- added interval math support for `exp10`;
- added `FirMathOp::Exp10` and backend handling for C++, Cranelift, interpreter,
  and Wasm code generation;
- changed `CboxExp10*` to construct the dedicated box primitive;
- added structural FFI tests for distinct `exp`/`exp10` tags and source
  generation preserving an `exp10` math call;
- serialized the `box-ffi` context tests around the process-global libfaust
  context so create/destroy lifecycle checks do not race;
- regenerated the Box/Signal API matrices.

Matrix snapshot:

- Box C API: 184 exact candidates, 8 nearest Rust IR mappings, 0 missing.
- Signal C API: 7 exact candidates, 116 missing.

Validation:

- `cargo fmt --all`
- `cargo test -p box-ffi --all-targets exp10_source_generation_keeps_exp10_math_call`
- `cargo check -p signals -p boxes -p interval -p sigtype -p propagate -p normalize -p transform -p codegen -p box-ffi -p compiler -p draw`
- `cargo test -p boxes --all-targets`
- `cargo test -p signals --all-targets`
- `cargo test -p box-ffi --all-targets`
- `cargo test -p transform --all-targets pow_min_max_and_unary_math_lower_to_fir_fun_calls`
- `cargo clippy -p boxes -p signals -p interval -p sigtype -p propagate -p normalize -p transform -p codegen -p box-ffi -p compiler -p draw -p xtask --all-targets -- -D warnings`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace --all-targets`
- `cargo run -p xtask -- libfaust-api-matrix --cpp-root /Users/letz/Developpements/RUST/faust --out porting/generated`

## Split Box logical and arithmetic right shift

Commit date: 2026-06-09
Source commit: this commit

Fixed the Box API representation gap where `CboxLRightShift*` and
`CboxARightShift*` both mapped through the same Rust `rsh` box primitive.

Implementation updates:

- added a distinct `BOXLRSH` box tag;
- added `BoxBuilder::lrsh` and `BoxMatch::LRsh`;
- preserved existing `BoxBuilder::rsh` / `BoxMatch::Rsh` as the arithmetic
  right-shift path;
- propagated `BoxMatch::LRsh` through flattening, complexity, eval, and signal
  propagation as `SigBuilder::lrsh`;
- changed `CboxLRightShift*` and `SOperator::kLRsh` to use the logical
  right-shift Box primitive;
- added a `box-ffi` structural non-regression test for distinct ARsh/LRsh tags;
- regenerated the Box/Signal API matrices.

Matrix snapshot:

- Box C API: 182 exact candidates, 10 nearest Rust IR mappings, 0 missing.
- Signal C API: 7 exact candidates, 116 missing.

Validation:

- `cargo fmt --all`
- `cargo test -p boxes --all-targets`
- `cargo check -p propagate`
- `cargo check -p eval`
- `cargo test -p box-ffi --all-targets logical_and_arithmetic_right_shift_use_distinct_box_tags`
- `cargo run -p xtask -- libfaust-api-matrix --cpp-root /Users/letz/Developpements/RUST/faust --out porting/generated`
- `cargo clippy -p boxes -p propagate -p eval -p box-ffi -p xtask --all-targets -- -D warnings`

## Extract shared tree FFI context support

Commit date: 2026-06-09
Source commit: this commit

Extracted the common Box/Signal FFI context mechanics into `crates/tree-ffi`.

Implementation updates:

- added `tree-ffi` as a workspace crate;
- moved shared opaque tree handle encoding/decoding into `FfiTreeContext`;
- moved context-owned C string pooling and null-terminated handle-array
  tracking into the shared crate;
- moved `SType`, `SOperator`, C string decoding, and null-safe out-pointer
  helpers into `tree-ffi`;
- migrated `box-ffi` to use the shared context support while keeping all public
  `Cbox*` and conversion symbols owned by `box-ffi`.

Architecture decision:

- chose the clean shared crate support layer over a new top-level `faust-ffi`
  context owner, so API crates can retain symbol ownership while sharing the
  low-level tree-handle contract.

Validation:

- `cargo fmt --all`
- `cargo check -p tree-ffi`
- `cargo check -p box-ffi`
- `cargo test -p tree-ffi --all-targets`
- `cargo clippy -p tree-ffi -p box-ffi --all-targets -- -D warnings`
- `cargo test -p box-ffi --all-targets`

## Generate libfaust Box/Signal API matrices

Commit date: 2026-06-09
Source commit: this commit

Added an `xtask` workflow that generates parity matrices for the public
libfaust Box and Signal C APIs.

Implementation updates:

- added `cargo run -p xtask -- libfaust-api-matrix`;
- parsed `LIBFAUST_API` prototypes from `libfaust-box-c.h` and
  `libfaust-signal-c.h`;
- scanned Rust `*-ffi` crates for explicit and macro-generated C exports;
- generated `porting/generated/libfaust-box-c-api-matrix.md` and
  `porting/generated/libfaust-signal-c-api-matrix.md`;
- documented known adapted Box mappings for right shifts, `exp10`, soundfiles,
  and primitive predicate identity.

Matrix snapshot:

- Box C API: 178 exact candidates, 14 nearest Rust IR mappings, 0 missing.
- Signal C API: 7 exact candidates, 116 missing.

Validation:

- `cargo fmt --all`
- `cargo check -p xtask`
- `cargo test -p xtask --all-targets libfaust_api_matrix`
- `cargo run -p xtask -- libfaust-api-matrix --cpp-root /Users/letz/Developpements/RUST/faust --out porting/generated`
- `cargo clippy -p xtask --all-targets -- -D warnings`

## Plan libfaust Box/Signal API parity

Commit date: 2026-06-09
Source commit: this commit

Added `porting/libfaust-box-signal-api-parity-plan-2026-06-09-en.md` to define
a parity plan for the public libfaust Box and Signal C/C++ APIs.

Planning updates:

- documented the C++ reference headers and the expected Box/Signal API surface;
- summarized the current Rust crates involved in Box, Signal, and FFI exposure;
- proposed a shared FFI support layer for tree handles, strings, arrays, and
  `freeCMemory` ownership;
- split the work into Box API completion, Signal API introduction, headers,
  packaging, and export verification phases;
- listed open compatibility questions around shared context ownership, C++
  wrappers, AD carriers, primitive function identity, and signal interval
  metadata.

Validation:

- Documentation-only change; no code validation required.
