# 2026-07-24

## State fused-group reads as escape rules, not presence rules

Commit date: 2026-07-24
Source commit: this change

Fixed the `vector-coverage-check` regression introduced by "Reject dead pure
FIR roots" (`4abb6d37`): `echo_bug.dsp` and `phaser_flanger.dsp` lost their
certified vector structure with `Fallback(FirAssembly)` / "fused serial group 0
has an invalid FIR shape", in every one of the 16 retention modes.

Cause. `remove_pure_drop_roots` runs on the loop-region statements before
`assemble_vector_fir`, so a region root whose only anchor was its seeding
`Drop` disappears from the assembled FIR. Two of the fused-group envelope
rules in `verify_assembled_fused_serial_groups` demanded that a recorded value
be *present* inside the physical sample loop: the delayed-read definition
(echo_bug loses a `vstate_s249` read at a constant delay whose branch nothing
consumes) and the load side of a fused scalar transport (phaser_flanger loses
`transport_s22657_l300_l0`). Dead-code removal therefore read as a shape
violation.

Fix. Both rules are now stated the way the envelope property actually reads:
no delayed read and no transport load may occur *outside* the selected body. A
value that survives anywhere in the assembly but not in the body is still
rejected — that is the escape the rule exists to catch — while a value absent
from the whole assembly is accepted, because an eliminated read cannot escape.
The whole-assembly node set is built on first use only, so the common path
pays nothing. Writes keep their presence rules: they are statements, and the
pure-Drop cleanup never removes them.

Emission impact is confined to dead code. Against the pre-`4abb6d37` output,
both DSPs are line-for-line identical except for deleted `(void)(...)`
statements (2 in echo_bug, 44 in phaser_flanger) — which is exactly what
`4abb6d37` set out to remove.

Public API mapping: internal **adapted** vector-checker behavior only. No CLI,
C, C++, ABI, or generated-code contract change.

Validation: `cargo run -p xtask -- vector-coverage-check` back to OK (1,552
retained certified mode/DSP pairs, 16 modes, 133 corpus DSPs, 97 per mode);
`golden-check` (196 cases), `vector-interp-opt-check` (40 traces),
`vector-compile-budget-check`; `cargo fmt --all -- --check`;
`cargo clippy --workspace --all-targets -- -D warnings`; `cargo test
--workspace`. A rejecting mutation in the assemble checker relocates the
delayed read out of the physical body with the read still reachable from the
top level and asserts `FusedGroupShape`, so the tolerance cannot degrade into
"never checked"; a compiler-level test locks both corpus shapes at
`Certified`/`CertifiedVector` and holds echo_bug to the scalar bit-exactness
oracle across all four scheduling strategies and both loop variants.

Known unrelated break on this branch: `xtask structure-check` fails because
`crates/transform/src/signal_fir/vector/lower/signal.rs` reached 2,100 lines
in `b93bc4cc`, over the 2,000-line review threshold.

## Preserve external-control root promotion during pure-Drop cleanup

Commit date: 2026-07-24
Source commit: this change

Fixed the vector external-control (`-ec`) path after `FIR-D01` exposed
temporary pure `Drop` roots in the final module. Control roots that are not
already materialized by CSE, including constants and other non-UI values, are
now promoted deterministically to DSP struct fields and emitted as real
`StoreVar(Struct)` operations in `control`. Temporary `Drop` loads remain only
long enough for the independent body-evidence checker, then final module
assembly removes them.

This preserves the documented external-control contract that every value
crossing from `control` to block compute is DSP-owned, without weakening the
new dead-expression diagnostic. A structural regression covers trivial
control-root promotion, and the three previously failing external-control
tests now remain certified.

Public API mapping: internal **adapted** vector-lowering behavior only. There
is no CLI, C, C++, ABI, or generated-code contract change beyond restoring the
already documented `-ec` behavior.

Validation: `cargo fmt --all -- --check`;
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test`; and `cargo run -p xtask -- golden-check` (196 cases).

## Reject dead pure FIR roots and clean vector CSE scaffolding

Commit date: 2026-07-24
Source commit: this change

Added the `FIR-D01` warning to the module verifier for `Drop` statements whose
value is side-effect-free. The conservative purity predicate now recognizes
canonical FIR math calls recursively but retains ordinary foreign calls, whose
evaluation may be observable. Final vector certification treats this specific
warning as a rejection while continuing to tolerate unrelated generic FIR
warnings.

Fixed checked vector lowering so temporary `Drop` roots remain present through
the independent body-evidence check, then are removed after output
materialization and before final module assembly. This preserves the
producer/checker boundary while preventing dead `pow`, `fmin`, `floor`, and
arithmetic evaluations from reaching generated code. The `karplus.dsp`
regression that emitted `(void)(std::pow(...))` now retains certified vector
lowering without any pure `Drop` in the final FIR.

Public API mapping: `is_obviously_side_effect_free_value` is **adapted** from
the compiler's existing FIR canonicalization policy rather than a C++ API
surface. It is public only to share one conservative classification across the
`fir` and `transform` crates; there is no CLI, C, C++, ABI, or generated-code
compatibility impact.

Validation covered focused FIR checker/canonicalization tests, vector final
materialization and certified-module tests, plus direct FIR and C++ generation
for `karplus.dsp` under `-vec -lv 1`.

## Add a reader-oriented porting history

Commit date: 2026-07-24
Source commit: this commit

Added `docs/faust-cpp-to-rust-port-history-overview-en.md` as a compact,
English-language entry point to the complete technical history. The overview
keeps the full source document as the detailed reference while presenting the
compiler pipeline, four conceptual crate groups, five architectural decisions,
the validation evidence, current limits, and a condensed timeline.

The final overview expands the initially targeted 2,500-word format where the
technical context benefits from more precision. Dedicated sections now explain
the module-wide FIR verifier and FIR-owned execution model, the interpreter's
role as an immediate in-process runtime oracle, and the different contracts and
maturity levels of the C, C++, Interpreter, Cranelift, WebAssembly/WAT,
AssemblyScript, Julia, Rust, and FIR outputs.

The overview also gives the native embedding work appropriate weight. It
documents the Interpreter and Cranelift factory/instance APIs, lifecycle and
cache ownership, FBC and experimental Cranelift serialization status, UI and
metadata callbacks, auxiliary JSON/SVG generation, and the unified
`libfaust-rs` static/dynamic distribution. A separate Box/Signal API section
covers the shared `tree-ffi` context, construction and inspection families,
Box-to-Signal conversion, normal form and source generation, C memory
contracts, C++ wrappers, and the 269-symbol export check.

The root README now links to the concise and detailed histories separately,
and the detailed history links readers back to the overview.

Validation was documentation-scoped: all local links in the overview resolve,
no trailing whitespace was found, and `git diff --check` passed.

## Execution-options port — Phase 2 complete; FIR backend live

Branch `external-control-one-sample` (5 commits today).

Phase 2 (FIR execution contract), per plan §4.3–§4.6:

- **P2a — execution-owned sections.** `ModuleSections.control_statements`
  entries are now tagged `Externalizable` vs `ComputePreamble`. One
  interleaved list deliberately kept (not two): classic emission must
  reproduce the exact lowering-encounter order byte-for-byte; the tag is
  what splits the section by semantics. CSE re-derives tags after
  `materialize_shared_values` (surviving statements keep theirs; inserted
  shared fSlow/iSlow declarations are externalizable).
- **P2b — typed contracts.** faust_api reserves `control(dsp)` and
  `frame(dsp, FAUSTFLOAT*, FAUSTFLOAT*)`; the FIR checker records function
  bodies and adds FIR-F08 (frame declared but canonical compute not empty)
  and FIR-F09 (control/frame references `count`). `control`/`frame` stay
  out of DSP_API_FUNCTIONS so M07 does not require them.
- **P2c — external control.** `materialize_in_bucket` promotes
  `Bucket::Control` values to DSP-owned storage under
  `ControlRateMode::External` by generalizing the existing
  `konst_escapes` escape promotion (§4.4): struct field + StoreVar(Struct)
  in the externalizable section. `build_module` emits the externalizable
  statements as the body of `control(dsp)`; block plumbing stays in the
  block entry point. CSE-shared slow declarations remain stack-local
  inside `control` (their uses never leave it — confirmed by the FIR
  scope rules).
- **P2d — one-sample.** Direct `inputs[channel]` loads and
  `outputs[channel]` stores (flat FunArgs arrays), slice bodies emitted
  straight into `frame` with no loop/count/i0, canonical `compute` kept
  but empty, block aliases skipped. `-ec -os` splits control work from
  the per-sample body, which loads the promoted state.
- **P2e — structural tests** (§7.1) over an input×slider fixture for all
  four shapes: entry-point presence, slow-value storage class, direct vs
  aliased I/O, loop/count/i0 absence in `frame`, empty `compute`, and no
  cross-function stack references (via verifier scope analysis + F08/F09).

Phase 3, FIR slice: the implementation gate is lifted for the `fir`
backend (its emission is the verified module dump). CLI-observable now:
`-lang fir -ec` → `control` + struct-promoted slow values;
`-lang fir -os` → `frame` + empty `compute`; `-ec -os` → all three.
c/cpp/rust still stop at FRS-EXEC-UNIMPLEMENTED until their emitters land.

Gates at every commit: fmt, clippy workspace 0, workspace tests, golden
196 OK (defaults byte-identical).

Next: Phase 3 C/C++ emitters (language signatures for control/frame,
architecture discoverability), then differentials against the local
pinned reference (../faust/build/bin/faust @ 8eebea429).

## Execution-options port — Phase 3: C and C++ backends live

- C++ emitter: `control`/`frame` join the DSP API method handling. The
  pinned reference emits `void control()` as a plain (non-virtual) method
  but `frame`/`compute` as virtual — mirrored exactly, with the
  `FAUSTFLOAT* RESTRICT` frame signature.
- C emitter: `void control{class}({class}* dsp)` and
  `void frame{class}({class}* dsp, FAUSTFLOAT* RESTRICT inputs, FAUSTFLOAT*
  RESTRICT outputs)`, emitted before the canonical compute.
- Gates lifted for c/cpp in SCALAR mode only. `-ec -vec` stays at
  FRS-EXEC-UNIMPLEMENTED for every backend until phase 5: the vector
  producer does not represent the control split yet, and letting it
  through would silently emit classic vector output.
- Differential validation (external harness, impulse+step input, fixed
  controls, 256 samples, g++ -O2):
  * faust-rs `control(); frame()×N` bit-identical to faust-rs
    `compute(N)` on a stateless DSP AND a recursive feedback DSP;
  * bit-identical to the pinned reference in classic and `-ec -os` modes
    on the stateless DSP;
  * **upstream finding**: at `8eebea429` the C++ reference's own
    `-ec -os` output does not compile on the recursive DSP
    (`process = +~(_*hslider(...)) : *(hslider(...))`): it declares
    `fVec0SEState` but emits `fVec0SE[1]`/`fVec0SE[0]` — a one-sample
    state-renaming bug for vec-state recursions in the dev branch. The
    internal block-vs-frame oracle arbitrates there (faust-rs passes).
    Worth reporting upstream.
- Integration tests (`crates/compiler/tests/execution_options.rs`) lock
  the emitted signatures for all four shapes on both backends, the empty
  canonical compute, and the vector/capability rejections.

Remaining: Phase 4 (Rust backend, D3 inherent methods), Phase 5
(vector external control), Phase 6 (hardening/docs).

## Upstream finding refined: reference `-ec -os` drops state-reload statements

Minimization of the Phase 3 upstream finding (reference C++ at
`8eebea429`, version 2.84.3), correcting the earlier "state-renaming"
description:

- **Minimal reproducer**: `process = +~_;` — no controls needed.
  `faust -lang cpp -ec -os t.dsp` (same with `-lang c`).
- **`-os` alone is CORRECT**: `frame()` opens with the local state reload
  `float fVec0SE[2]; fVec0SE[1] = fVec0SEState;` and the emitted code
  compiles and runs.
- **`-ec -os` combined is broken**: those two reload statements vanish
  entirely — they are in neither `control()` nor `frame()` — so `frame()`
  references the undeclared local `fVec0SE[...]` and the generated code
  does not compile. They appear to be classified as control-rate
  declarations and routed to the external-control path, which does not
  emit them (and `control()` would be the wrong home anyway: the reload
  must run once per frame, before the sample body, because the tail of
  `frame()` writes the updated `fVec0SE[1]` back to `fVec0SEState`).
- **Both cpp and c backends affected.** Stateless DSPs are unaffected
  (bit-exact faust-rs ↔ reference parity verified there).

Why faust-rs is structurally immune (no fix was needed on our side):

1. Execution ownership is decided **at statement creation time** via the
   tagged compute-preamble list (P2a); recursive state updates never
   enter that list at all — they live in the region sample phases, which
   become the whole `frame` body. There is no post-hoc routing step where
   a statement can land on the wrong side, which is where the reference
   appears to lose the reloads.
2. faust-rs represents recursive state as a directly read/written struct
   scalar (`fRec36`), not the reference's persistent-scalar +
   locally-reloaded-array pattern — less machinery to route.
3. Defense in depth: had such a bug been introduced, the FIR verifier's
   scope rule (FIR-SC01, load of an undeclared variable) and the P2e
   structural tests ("no stack local crosses a function boundary") would
   have failed before any emission. The reference has no equivalent
   check; its bug only surfaces when someone compiles a recursive DSP's
   generated `-ec -os` output.

Verified on the exact reproducer: faust-rs `-ec -os` emits
`control()` = promoted slow store only, `frame()` = recursion body with
direct `fRec` state read/write; compiles under `g++ -Wall -O2` and is
bit-exact against faust-rs `compute(N)`.

Reported upstream: https://github.com/grame-cncm/faust/issues/1277
(with the reproducer and both emitted-shape comparisons).

## Execution-options port — Phase 4: Rust backend live

- D3 honored: `control`/`frame` are public inherent methods on the
  generated DSP type; the host-facing FaustDsp trait is untouched (its
  impl still declares only the canonical block compute).
- `frame(&mut self, inputs: &[FaustFloat], outputs: &mut [FaustFloat])`
  uses flat channel slices — an `adapted` contract decision, consistent
  with this backend's existing AsRef/AsMut compute adaptation (the
  reference uses `&[&FaustFloat]`).
- One-sample inherent compute: kept, empty, parameters underscored so
  the generated source is warning-free.
- Scalar gate lifted for rust: fir/cpp/c/rust now accept all four scalar
  shapes; `-ec -vec` stays gated until phase 5.
- Validation: external runtime differential (impulserust host types,
  rustc -D warnings) — `control(); frame()×N` bit-identical to
  `compute(N)` on the recursive feedback DSP (max_diff = 0).
  Architecture-project source compatibility per AGENTS.md: block and
  `-ec -os` outputs both pass `cargo check` inside a real
  `faust2jackrust -source` project generated by the pinned reference.
  The final link step could not run locally: /usr/local/lib/libjack is a
  universal binary without an arm64 slice (environment limitation,
  recorded; not a generated-code failure).
- Possible second upstream finding (unverified): the pinned reference's
  own `-os` Rust output seems to emit no inherent `compute` while its
  FaustDsp impl delegates to `self.compute(count as usize, ...)`, which
  cannot resolve. To be confirmed with a real cargo build before
  reporting alongside grame-cncm/faust#1277.

Remaining: Phase 5 (vector external control), Phase 6 (hardening, CLI
docs, golden coverage for the new shapes, API-mapping entries).

## Execution-options port — Phase 5: vector external control

Producer (P5a):

- UI zone reads under `-ec` go through promoted snapshot fields
  (`fSlowN = cast(zone)` stored in `control`); compute never observes
  host-mutated zones (§2.4 semantics).
- Shared control-root temporaries (`fVecControlTemp*`) are
  struct-promoted through a new Struct mode of the CSE materializer (top
  scope only) — the konst-escape reasoning of §4.4 generalized: their
  stores move to `control` while vector transport fills in `compute`
  read them across the function boundary.
- The externalizable section (snapshots, promoted root stores,
  control-scope UI stores) becomes `control(dsp)`; input aliases stay in
  the compute preamble. Classic mode is byte-identical (single
  interleaved list, stack temporaries).

Checker (P5b):

- `verify_final_module` gains external-control rules: `control` must be
  absent in classic mode; under `-ec` it must contain EXACTLY the
  externalized statement list, and every promoted field must be declared
  in the struct, stored exactly once and only in `control`, never
  written by `compute`.
- BuiltVectorModule carries the evidence; the corruption test verifies
  the exact lists pass while a missing event, a duplicated event, and a
  misattributed field are each rejected. Non-UI control values
  (control-region constants) externalize too; a pure passthrough
  externalizes nothing and emits no `control`.
- Coherence guard in the wrapper: classic mode must not carry
  external-control evidence.

Gate lifted: `-ec -vec` accepted for fir/cpp/c/rust; `-os -vec` remains
a stable early error.

Reference note: the pinned dev-branch reference disables `-vec`
unconditionally ("'-vec' is not yet supported with 'ondemand' primitive"
even for programs without ondemand), so the recorded behavioral oracle
for `-ec -vec` is stable Faust 2.83.1. Runtime differential (recursive
feedback DSP, fixed controls, 256 samples): our `-ec -vec` output is
bit-identical to our classic vector, to our scalar, and to the stable
reference.

Gates: golden 196, vector_mode 36/36, workspace tests, clippy 0.
Remaining: phase 6 (hardening/docs/HANDOFF), then the follow-ups
(asc one-sample target design, upstream Rust `-os` finding check).

## Execution-options port — AssemblyScript one-sample target (plan §5.7)

The merged §5.7 amendment (PR #11) is delivered:

- asc emits `control(): void` and
  `frame(inputs: StaticArray<f32>, outputs: StaticArray<f32>): void` as
  class methods before the canonical compute; the block compute stays the
  default contract and is emitted empty under `-os` (adapted decision —
  no C++ reference exists for `-os` asc; recorded per the amendment).
- Capability table: asc → Explicit ×3; gate open.
- Runtime differential with the real toolchain: both generated classes
  compiled by `asc` into one wasm module, executed under node —
  `control(); frame()×N` bit-identical to `compute(N)` on the recursive
  feedback DSP (max_diff = 0). (tests/impulse-tests/node_modules
  reinstalled via the kept package.json after yesterday's cleanup.)
- Integration tests lock the asc shapes and the updated capability
  matrix.

This natively replaces the wasm-music block→one-sample transpiler layer
(`transpile-core.js` parseComputeBody): MidiVoice-style hosts consume
`control` + `frame` directly.

Phase 6 in progress: CLI guide + README + HANDOFF done; vector-coverage
non-regression run in background.

## Execution-options port — Phase 6 closed

- vector-coverage-check non-regression: OK — 1,552 retained certified
  mode/DSP pairs, 16 modes, 133 corpus DSPs (the corpus grew by one DSP
  on main since the transform-cleanup freeze; 97 certified per mode, no
  regression against the current baseline).
- CLI guide, README, and HANDOFF updated earlier today. Golden decision
  recorded: the golden harness snapshots default emission only, so the
  emitted -ec/-os shapes are locked by the execution_options integration
  tests and the transform structural tests (adapted).
- The port is complete end to end: phases 0-6 plus the §5.7
  AssemblyScript one-sample follow-up, all differentials green.

## faustwasm service: execution options in the argv (PR #13)

Consuming the merged `-ec`/`-os` port from the wasm compiler module exposed
one gap: the faustwasm surface (`generateAuxFiles`) hand-parses its raw argv
string and never consulted the execution options — `-lang asc -ec -os`
compiled fine but silently emitted the classic block class (the exact
silent-flag-ignore hazard the capability table guards against elsewhere).
Fixed by deriving a per-request `Compiler` (`Clone` added) with
`-ec/--ec/--external-control/--ext-control` and `-os/--os/--one-sample`
applied before dispatch, for the asc path and the flag-driven cpp/c/wasm/json
outputs alike; unsupported combinations now error through the existing
per-backend validation. Locked by an integration test over all three flag
spellings plus the classic no-flag shape. Verified end-to-end from Node
against the rebuilt wasm32 module (note: the module only embeds the standard
libraries when built with `FAUST_RS_EMBEDDED_LIB_ROOT`).

Downstream (wasm-music, PR petersalomonsen/javascriptmusic#175): the
1538-line block→one-sample reshape transpiler is replaced by 804 lines of
thin wrapper codegen around the native `control()`/`frame()` class — the
first production consumer of the AssemblyScript one-sample target.
