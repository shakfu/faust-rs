# 2026-03-27

## docs(codegen,compiler): realign WASM backend documentation with current APIs

Commit date: 2026-03-27
Source commit: working tree after `d336e17`

Updated crate-level documentation so the documented backend/compiler surfaces
match the current Rust WASM implementation rather than the earlier scaffold
state.

### Scope

- Updated
  [crates/codegen/README.md](/Users/letz/Developpements/RUST/faust-rs/crates/codegen/README.md)
  to:
  - mark `wasm` as a bring-up backend instead of scaffold-only
  - add explicit C++ provenance for `backends::wasm`
  - include `WASM binary + JSON` in the pipeline summary
  - document the `backends::wasm` public API (`WasmOptions`,
    `WasmJsonContext`, `WasmModule`, `generate_wasm_module`,
    `generate_wasm_module_with_context`, `WasmMemoryLayout`)
  - describe the matched `.wasm` / companion JSON runtime contract and CLI
    entry points
- Updated
  [crates/compiler/README.md](/Users/letz/Developpements/RUST/faust-rs/crates/compiler/README.md)
  to include the existing WASM and JSON facade entry points in the API table,
  pipeline summary, and facade responsibilities
- Updated public rustdoc in
  [crates/compiler/src/lib.rs](/Users/letz/Developpements/RUST/faust-rs/crates/compiler/src/lib.rs)
  so `compile_source_to_wasm` and `compile_file_to_wasm` no longer claim to
  emit a “WASM module scaffold”; they now describe the real output shape:
  a WASM module plus matched companion JSON

### Validation

- `cargo fmt --all --check`

## fix(wasm): restore soundfile runtime parity for faustwasm

Commit date: 2026-03-27
Source commit: working tree after `d2770a4`

Fixed the remaining Rust WASM backend gaps that made `soundfile.dsp` compile
successfully but produce silence inside `faustwasm`.

### Scope

- Canonicalized `soundfile.url` JSON payloads in
  [crates/codegen/src/json.rs](/Users/letz/Developpements/RUST/faust-rs/crates/codegen/src/json.rs)
  to the historical C++ form expected by `faustwasm`:
  - `{'foo'; 'bar'}` now renders as `{-foo-;-bar-}`
  - documented the exact runtime tokenization rule and failure mode in Rustdoc
- Added a JSON non-regression in
  [crates/codegen/src/json/tests.rs](/Users/letz/Developpements/RUST/faust-rs/crates/codegen/src/json/tests.rs)
  to lock the `soundfile.url` contract
- Updated the WASM linear-memory layout in
  [crates/codegen/src/backends/wasm/layout.rs](/Users/letz/Developpements/RUST/faust-rs/crates/codegen/src/backends/wasm/layout.rs)
  so `Soundfile*` struct fields are moved first, matching the C++ WASM backend
- Extended the WASM compute lowerer in
  [crates/codegen/src/backends/wasm/mod.rs](/Users/letz/Developpements/RUST/faust-rs/crates/codegen/src/backends/wasm/mod.rs)
  to support:
  - `LoadSoundfileLength`
  - `LoadSoundfileRate`
  - `LoadSoundfileBuffer`
- Aligned the WASM memory contract for soundfile DSPs:
  - soundfile modules now force imported/external memory
  - the backend now rejects unsupported `compute` bodies up front instead of
    silently emitting an empty `compute` stub
- Added focused WASM regressions in
  [crates/codegen/src/backends/wasm/tests.rs](/Users/letz/Developpements/RUST/faust-rs/crates/codegen/src/backends/wasm/tests.rs)
  covering:
  - external-memory import for soundfile modules
  - `soundfile` JSON offset at `index = 0`
  - non-empty lowered `compute` with real soundfile access instructions

### Validation

- `$HOME/.cargo/bin/cargo fmt --all`
- `$HOME/.cargo/bin/cargo test -p codegen wasm_soundfile_ -- --nocapture`
- `$HOME/.cargo/bin/cargo test -p codegen wasm_compute_lowers_soundfile_access_nodes -- --nocapture`
- `$HOME/.cargo/bin/cargo test -p codegen json_ -- --nocapture`
- `$HOME/.cargo/bin/cargo test -p compiler wasm -- --nocapture`
- `$HOME/.cargo/bin/cargo run -p xtask -- build-faustwasm-compiler-module`

### Result

The Rust `faustwasm` compiler-module now regenerates `soundfile.dsp` artifacts
with the three compatibility facts the web runtime depends on:

- soundfile names are tokenized correctly from JSON,
- the DSP field offset exposed as `ui[*].index` matches the installed
  `Soundfile*` pointer slot,
- the emitted WASM module uses imported memory and a real `compute` body rather
  than silently compiling to no-op audio.
