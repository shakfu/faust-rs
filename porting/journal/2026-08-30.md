# 2026-08-30

## Expand the README "Porting references" section

Commit date: 2026-08-30

Source commit: this commit

The section listed five links while `porting/` had accumulated a much larger
set of stable (non-dated) reference documents. Reorganized it into four
groups: status and scope (supported subset, differences from C++,
`missing.md`, the two assessment reports), architecture contracts and design
notes (backend lifecycle, FIR architecture, TreeArena IR, recursion model,
diagnostics model and error flow, memoization roadmap, Cranelift FFI parity
matrix), the four Lean formal specifications (with a pointer to
`docs/lean-usage-methodology-en.md`), and history/process (the original five
links plus a pointer to the dated `*-plan-YYYY-MM-DD-en.md` documents).
Deliberately excluded anything already covered by the README's
"Documentation" section (user guides, ondemand/FAD-RAD notes, porting
histories) and the internal session documents (`HANDOFF.md`, Codex prompts).
All links are repository-relative, per the 2026-07-23 convention.

## Bring the supported-subset status document up to date (0.5.0 → 0.8.0)

Commit date: 2026-08-30

Source commit: this commit

`porting/faust-rs-supported-faust-subset-en.md` had last been maintained on
2026-05-20 (version 0.5.0), with a single 2026-08-11 retouch for remote
imports — three months of boundary moves were missing. Per its own §9 update
policy, the two xtask reports were regenerated first
(`corpus-status-report`, `backend-full-corpus-diff-report`): 221 cases,
`OK/OK=103`, `ERR/ERR=40`, `OK/ERR=0`, `ERR/OK=78` (all classified expected —
`fad`/`rad`/clock-domain fixtures the default pinned C++ build rejects);
backend `OK=103`, `DIFF=0`, `UNSUPPORTED=118` on both the C and C++ routes.

Main content changes:

- header: last-updated 2026-08-30, version 0.8.0 (workspace release of
  2026-08-13),
- §4.2: the historical `rep_18_stream_wrappers.dsp` "single gap" paragraph
  replaced — it now compiles, and numerical parity is stated on the strength
  of the 2026-08-14 impulse sweep (133 DSPs × 8 backends, 133/133,
  0 mismatch),
- new executive-summary subsections: §4.5 clock-domain primitives
  (`ondemand`/`upsampling`/`downsampling`), §4.6 vector mode
  (`-vec`/`-vs`/`-lv`, certified selection, scalar fallback), §4.7 additional
  text backends (AssemblyScript, Rust, Cmajor, Codebox, with per-backend
  validation status), §4.8 CLI/diagnostics/tooling (CLI consolidation,
  diagnostics v2, `faustprobe`, remote imports, 0.8.0-era hardening),
- §5: clock domains added to the supported source subset and the fast-lane
  slice; the "non-trivial stream wrappers" exclusion rewritten as a closed
  boundary; the Julia maturity caveats replaced by the impulse-sweep result;
  a maturity entry added for the newer text backends,
- §6: C++ no longer credited with broader stream-wrapper lowering or a more
  mature Julia backend; Rust side gains impulse-level numerical parity,
  vector mode, clock domains (beyond public C++ releases), and
  wrapping-integer semantics (`faust_wrap_*`),
- §7: six new dated history sections (7.28–7.33) covering 2026-05-21 through
  2026-08-30 from the journal record: factorization campaign + AssemblyScript
  backend + impulse harness; clock domains; vector mode; the
  Cmajor/Codebox completion wave; remote imports + release 0.8.0 + the
  133-DSP sweep; August tooling and security hardening,
- §8: the practical reading rule no longer excludes stream wrappers and
  reflects the current backend validation levels.

The dated historical sections 7.1–7.27 were left untouched — they are
snapshots. Also fixed in this commit: `porting/journal/README.md` had not
been indexing 2026-08-28.
