# JOURNAL 2026-03-11

Entries originally recorded under this journal day.

- Sorted by Git commit recency (newest first).
- `Commit date` is the commit day for the entry addition.

---

### `porting` / `xtask` / `runtime_traces` — Normalize absolute faust-rs paths into portable repo-relative paths

Commit date: 2026-03-11

Source commit: pending

#### Context

Several versioned documents and generated runtime trace snapshots still embedded
absolute local paths under `/Users/letz/Developpements/RUST/faust-rs`.

That caused two portability problems:

- Markdown links under `porting/journal/` were broken once pushed to GitHub
- generated reports and runtime traces exposed host-specific absolute paths
  even though repo-relative paths were sufficient

#### Changes

- [2026-03-09.md](../../porting/journal/2026-03-09.md)
  - replaced absolute repository links with `../../...` relative links

- [2026-03-10.md](../../porting/journal/2026-03-10.md)
  - replaced absolute repository links with `../../...` relative links

- [2026-03-11.md](../../porting/journal/2026-03-11.md)
  - replaced absolute repository links with `../../...` relative links
  - added this journal entry

- [main.rs](../../crates/xtask/src/main.rs)
  - added `workspace_relative_path(...)` so generated runtime traces store
    repository-relative DSP paths when possible
  - switched both runtime trace generation paths to use that normalization

- [AGENTS.md](../../AGENTS.md)
  - documented the repository policy that versioned docs, generated reports,
    and stored test artifacts should prefer repository-relative paths over
    absolute local checkout paths

- `tests/runtime_traces/rust/*/*.json`
  - normalized stored `dsp` fields from absolute local paths to repo-relative
    paths

- [phase-3-parser-parity-report-en.md](../../porting/phases/phase-3-parser-parity-report-en.md)
  - converted Rust parser/lexer file references to repo-relative textual paths

- [phase-4-corpus-status-diff-report-en.md](../../porting/phases/phase-4-corpus-status-diff-report-en.md)
  - converted embedded `faust-rs` corpus paths in generated report text to
    repo-relative paths

- [phase-6-backend-full-corpus-diff-report-en.md](../../porting/phases/phase-6-backend-full-corpus-diff-report-en.md)
  - converted embedded `faust-rs` corpus paths in generated report text to
    repo-relative paths

#### Validation

- `rg -n '/Users/letz/Developpements/RUST/faust-rs' porting/journal porting/phases tests/runtime_traces`
- `cargo test -p xtask compare_runtime_traces`

#### Outcome

GitHub-visible documentation no longer depends on a local absolute checkout
path, and future runtime traces generated through `xtask` now keep portable
repo-relative DSP paths by default.

### `codegen` / `interp-ffi` / `cranelift-ffi` — Realign crate README files with current FFI artifacts and backend pipeline

Commit date: 2026-03-11

Source commit: pending

#### Context

Several crate README files had drifted from the current workspace structure:

- `interp-ffi` still documented itself as producing final `faust`
  `cdylib`/`staticlib` artifacts even though it now builds as an `rlib`
  consumed by `faust-ffi`
- `cranelift-ffi` still described the older crate outputs and an outdated
  internal module layout
- `codegen` still showed an inverted `fir`/`transform` pipeline order and a
  stale CLI example using `--lang fir` to generate `.fbc`

This made the crate-local documentation misleading for contributors trying to
understand the current backend and FFI packaging model.

#### Changes

- [README.md](../../crates/interp-ffi/README.md)
  - corrected crate outputs to `rlib`
  - corrected the Rust library target name to `faust_interp`
  - documented that final `cdylib` / `staticlib` distribution artifacts come
    from `crates/faust-ffi`
  - clarified that `build.rs` exists but does not currently generate headers

- [README.md](../../crates/cranelift-ffi/README.md)
  - corrected crate outputs to the current `rlib` packaging model
  - documented the `faust-ffi` aggregation role for final distribution
  - updated current-status notes to mention the native runtime descriptor and
    JIT-backed compute path already present in the implementation
  - replaced the outdated `src/ui.rs` mention with the current `runtime.rs`
    and `clif.rs` internal structure

- [README.md](../../crates/codegen/README.md)
  - fixed the pipeline diagram to the current
    `parse -> boxes -> eval -> propagate -> signals -> transform -> fir -> codegen`
    ordering
  - updated the interpreter backend section to reflect the current
    `compute_block` / `compute_dsp_block` split
  - corrected the CLI example so `.fbc` generation uses `--lang interp`
    instead of `--lang fir`

- [JOURNAL.md](../../JOURNAL.md)
  - added the `2026-03-11` daily journal entry to the top-level index

- [README.md](../../porting/journal/README.md)
  - added the `2026-03-11` day entry to the journal day index

#### Validation

- manual cross-check of updated README content against:
  - `Cargo.toml` crate-type and lib-name declarations
  - current module layout under `src/`
  - compiler CLI options in `crates/compiler/src/main.rs`
  - interpreter/cranelift backend implementation docs and entry points

#### Outcome

The affected crate README files now describe the current packaging and backend
behavior instead of the older intermediate state, which reduces confusion for
workspace contributors and keeps local crate documentation aligned with the
actual code.
