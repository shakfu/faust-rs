# 2026-08-13

## Release 0.8.0

Commit date: 2026-08-13

Source commit: this commit

Raised the workspace package version from `0.7.0` to `0.8.0`. All member
crates inherit this release version through `version.workspace = true`, and
`Compiler::version()` reports it via `CARGO_PKG_VERSION`.

Re-recorded `tests/cli-transcripts` so the 70 snapshots that embed the version
string agree with the new number; the diff is version-only. The unrelated
snapshot drift that had accumulated since the mem0 C-family work was recorded
separately, in the commit immediately before this one, so that this release
diff stays reviewable.

## Name the `-mem0` JSON `memory_layout` zones after the C/C++ class name, not the source stem

Commit date: 2026-08-13
Source commit: this correction commit

- User-reported: `tests/impulse-tests$ faust-rs -mem dsp/osc.dsp -json` emitted
  `"name": "oscSIG0"` in `memory_layout`, which does not match the C++ that
  `faust-rs -mem dsp/osc.dsp` actually generates — that names its identifiers
  `mydspSIG0` / `ftbl0mydspSIG0` / `mydsp`, from the default class name, since
  `-cn` was not passed.
- Root cause: `compile_file_to_json_with_compile_options_and_memory` and its
  source-text twin (`crates/compiler/src/emitters.rs`) lowered FIR through the
  generic `Compiler::lower_to_fir`, which derives the module name from
  `sanitize_cpp_ident(source_name_to_class(source_name))` — the source file stem
  — unconditionally. The real C and C++ backends instead go through
  `resolve_module_name(options.class_name.as_deref(), source_name)`
  (`crates/compiler/src/signal_lowering.rs`), which defaults to `"mydsp"` and
  honors `-cn`. Because `analyze_effective_mem0`'s zone names come straight from
  the FIR module's baked-in name (and the `SIG0` / `ftbl0` sub-module names
  generated during lowering), a `memory_layout` advertising `"backend": "cpp"`
  could never match the identifiers that backend emits. `-cn` was ignored by the
  JSON path entirely.
- Confirmed against the pinned C++ Faust oracle (`8eebea429`): its own
  `-lang cpp -mem0 -json` output names the zones `mydsp` / `mydspSIG0` /
  `ftbl0SImydspSIG0`, never the source stem — so this was a genuine divergence
  from the reference, not a faust-rs naming convention.
- Cranelift-flavored JSON is deliberately exempt and left unchanged: its module
  name is a JIT identity keyed to the source name with no `-cn` convention, as
  pinned by `compiler_cranelift_report_matches_the_cli_contract`.
- Fix, expressed as one naming-policy decision rather than a second lowering
  path: `lower_signals_to_fir` gained a `module_name: Option<String>` parameter
  (`None` reproduces the previous source-derived behavior), and a new
  `json_memory_layout_module_name(flavor, class_name, source_name)` next to
  `resolve_module_name` returns `Some(resolve_module_name(..))` only for the `C`
  and `Cpp` flavors. `Compiler::lower_to_fir` became a one-line wrapper over a
  new `lower_to_fir_with_name(.., Option<String>)`, leaving its nine existing
  callers untouched. The two JSON-with-memory entry points now call the helper
  and forward the result — one line each.
- Public API grew by exactly one method,
  `compile_file_to_json_with_compile_options_memory_and_class_name`, to carry
  `-cn` from the CLI; the pre-existing `_and_memory` variants delegate to it with
  `None` and stay byte-compatible. The CLI (`cli/runner.rs`, `cli/source_mode.rs`)
  threads `selected_class_name(cli)` through both the `-json` and the
  `-lang <x> -json` companion paths.
- Also collapsed a redundant `if cli.import_dir.is_empty()` branch in
  `emit_cli_json_companion_for_backend`: both arms made the identical call, since
  the `_default_` wrapper it selected only forwards `&[]`, which is what an empty
  `--import-dir` already is.
- Verified behavior on six shapes: default (`mydspSIG0`/`ftbl0mydspSIG0`/`mydsp`,
  matching the generated `.cpp` and the oracle), `-cn FooDsp`
  (`FooDspSIG0`/`ftbl0FooDspSIG0`/`FooDsp`), `-lang cpp -json` companion, the same
  with a non-empty `-I`, plain `-json` without `-mem` (unchanged: `"name": "osc"`,
  no `memory_layout`), and `-lang cranelift` (unchanged, still source-derived).
- Added `json_memory_layout_names_the_identifiers_the_generated_cpp_emits` to
  `crates/compiler/tests/mem0_cpp_differential.rs`: it asserts that every zone
  faust-rs's own `-mem0` C++ registers through `->info(...)` appears in its JSON
  `memory_layout` under the same name (containment, since the JSON additionally
  describes non-allocated scalars), anchored to the pinned reference's `"mydsp"`,
  and that no zone is named after the source stem. Checked that it fails without
  the fix by reverting only `crates/compiler/src` and re-running.
- Trap worth recording: `reference_json` in that test file builds its scratch
  directory from `pid + SystemTime nanos` only. A first version of the new test
  called it, and the resulting second concurrent caller made
  `create_dir(...).expect(...)` fail intermittently under the parallel test
  runner. The final test reuses `reference_cpp` / `parse_zones` instead (no temp
  directory), which is both stronger and race-free, but the latent collision
  remains for any future test that adds a second `reference_json` caller — it
  needs a real unique suffix (atomic counter) first.
- Validation: `cargo test -p compiler --release` — 634 passed, 0 failed, run five
  consecutive times with no flake; `cargo clippy -p compiler --release
  --all-targets`; `cargo fmt -p compiler -- --check`; `cargo doc -p compiler
  --no-deps --document-private-items` (0 warnings).

## Fix a pre-existing CI failure and document the Structure check gates in AGENTS.md

Commit date: 2026-08-13
Source commit: this correction commit

- Root-caused a red CI run (`Lint and test (ubuntu-latest)`, "Structure check"
  step): `cli-parser-check` flagged
  `crates/cranelift-ffi/src/bin/mem0_json_check.rs:15: direct process argument
  access is not classified`. Confirmed by `git diff` against the pre-session
  baseline commit that this file was byte-identical before and after this
  session's work — it last changed in `6c15283b`/`b8b883b7`, well before this
  session started; CI simply had never been run against exactly this state
  before, and the check itself predates none of my changes either.
- Fixed by converting `mem0_json_check`'s raw `env::args().skip(1)` to a small
  `#[derive(Parser)]` `CliArgs` struct, matching the style of its sibling
  `impulse_cranelift.rs` in the same crate (`cranelift-ffi` already depends on
  `clap`, so no new dependency). The existing `run()` function already took a
  generic `impl IntoIterator<Item = String>`, so the fix only touches `main()`
  — same error message on empty input, same `backend=path.json [...]`
  multi-argument shape, all 5 existing unit tests untouched and passing.
- Verified behavior identity directly: `--help` output, the exact "at least
  one backend=path argument is required" error on empty args, and a real
  three-backend invocation (`cpp=... c=... cranelift=...`) against JSON
  generated earlier this session for `phaser_flanger`. Replayed the CI job's
  "Structure check" step locally, all four commands: `cli-parser-check`,
  `error-model-check`, `ffi-boundary-check`, `structure-check` — all clean.
- `AGENTS.md` §3 ("Code Quality" — the pre-commit checklist) and §4 ("CI
  Expectations") listed `fmt`/`clippy`/`test`/`golden-check`/
  `compile-budget-check` but never mentioned these four `xtask` structure
  gates at all, despite CI enforcing them on every push. That gap is exactly
  why this violation went undetected locally and had to be found via a live
  CI failure instead. Added the four commands to §3's pre-commit list (noting
  they are OS-independent pure text/metadata analysis, so any one machine
  suffices), described the "Structure check" step in §4, and pointed §2's
  existing `clap`-policy rule at the command that mechanically enforces it
  (`cli-parser-check`) plus its exception-document
  (`porting/cli-parser-consolidation-analysis-and-porting-plan-2026-07-28-en.md`).
- Validation: `cargo build -p cranelift-ffi --bin mem0_json_check`; `cargo
  test -p cranelift-ffi --bin mem0_json_check` (5 passed); `cargo clippy
  --workspace --all-targets -- -D warnings`; `cargo fmt --all -- --check`;
  all four `xtask` structure-check commands run locally and clean.

## Add the missing `phaser_flanger` rounding-tolerance band for `make all-mem0`

Commit date: 2026-08-13
Source commit: this correction commit

- Root-caused a report that `tests/impulse-tests$ make all-mem0 -j 8` fails.
  `phaser_flanger.dsp`'s generated C++ diverges from the pinned C++ Faust
  oracle reference starting at frame 883 (delta 2e-6), growing through its
  LFO-modulated feedback delay chain to a few ×1e-4 within the mem0 lane's
  15000-frame prefix comparison — the same class of chaotic-feedback
  rounding sensitivity already covered for `vcf_wah_pedals`, `gate_compressor`,
  and `cubic_distortion` via `PRECISION_<dsp>` overrides in
  `tests/impulse-tests/known.mk`; `phaser_flanger` simply had no entry, so it
  ran under the default 2e-6 tolerance meant for bit-exact cases.
- Confirmed by bisection this is not a regression from any change made in
  this session: checked out the commit immediately before the session
  started (`f8d9f7b9`) in an isolated worktree, built its release binary,
  and reproduced byte-for-byte the identical divergence pattern.
- Confirmed by direct comparison that the divergence is entirely unrelated
  to `-mem0`: generated the ordinary (non-mem0) C++ output with the same
  reference-generating architecture (`impulsearch.cpp`) and compared it to
  the same reference file over the full 60000 frames. It diverges at the
  identical first sample (frame 883, output 1, delta 2e-6) as the mem0 lane,
  growing further from there to a true max of 1.419e-3 — `-mem0` only
  changes memory management, never arithmetic, so this is a general
  faust-rs/C++-Faust numeric characteristic of this DSP, not a mem0 defect.
- First fix attempt (`PRECISION_phaser_flanger := 1e-3`) was calibrated only
  from the mem0 lane's 15000-frame prefix (observed max 3.55e-4) and was
  too tight for the ordinary `cpp`/`c` lanes' full 60000-frame comparison
  (true max 1.419e-3, exceeding 1e-3) — those lanes share the same
  `known.mk` table and were failing independently of mem0. Recalibrated to
  `1e-2` from the larger, authoritative full-range figure.
- Validation: individually reran and confirmed passing —
  `ir/cpp/phaser_flanger.ir` and `ir/c/phaser_flanger.ir` (full 60000-frame
  ordinary lanes), `ir/cpp-mem0/`, `ir/c-mem0/`, and
  `ir/cranelift-mem0/phaser_flanger.ir` (15000-frame mem0 lanes) — then a
  full `make all-mem0 -j 8` end to end with zero errors across cpp/c/cranelift
  plus the mem0-smoke audit.

## Fix `cargo test --release` failing while `cargo test` passes

Commit date: 2026-08-13
Source commit: this correction commit

- Root-caused a report that `cargo test --release` fails while plain
  `cargo test` is clean. `normalize::tests::normalize_add_term_debug_rejects_incoherent_de_bruijn_input`
  is a `#[should_panic]` test for a coherence check in
  `crates/normalize/src/normalize.rs:50` that is itself gated behind
  `#[cfg(debug_assertions)]`. `debug_assertions` is `true` by default for the
  `dev` profile (`cargo test`) and `false` by default for `release`
  (`cargo test --release`), so in a release build the whole check — panic
  included — is compiled out of the binary entirely; the test then fails
  because nothing panics.
- Not a logic bug: the check is an intentional debug-only sanity assertion,
  same category as `debug_assert!`. The test itself just didn't account for
  that. `crates/signals/tests/core_api.rs` already has the correct pattern
  three times over for the same situation — each of its "rejects bad input
  in debug" tests is marked `#[cfg(debug_assertions)]` itself, so it's
  excluded from release builds rather than failing there. Grepped the whole
  workspace for every file combining `should_panic` with
  `cfg(debug_assertions)`; `crates/normalize/src/normalize.rs` was the only
  one missing the guard.
- Fixed by adding `#[cfg(debug_assertions)]` above the test, matching
  `core_api.rs`'s established pattern.
- While validating, `cargo test --release --workspace` surfaced a second,
  unrelated one-off failure in
  `compiler::tests::machine_applicable_fixes::applying_a_machine_applicable_fix_removes_the_diagnostic_it_targets`
  ("applying `insert `)`` left FRS-PARSE-0001 in place"). Investigated
  directly: manually reproduced the exact reported patched source
  (`process = (_ , _);`) against both the debug and release binaries and it
  parsed cleanly on both; reran the same test in isolation 11 times (single-
  and multi-threaded) with zero failures; reran the full workspace release
  suite a second time end to end and it passed with zero failures across all
  116 test groups. Concluded this was a transient one-off (most likely
  resource contention from running inside the full concurrent workspace
  build+test), not a deterministic bug — nothing was changed for it, since
  there was nothing reproducible to fix, and it would be dishonest to claim
  a fix for a symptom that never recurred.
- Validation: `cargo test -p normalize --lib` (78 passed, debug — the test
  runs and passes); `cargo test --release -p normalize --lib` (77 passed,
  release — the test is now excluded rather than failing);
  `cargo test --release --workspace` clean end to end;
  `cargo clippy -p normalize --all-targets [--release] -- -D warnings`;
  `cargo doc -p normalize --no-deps` with warnings denied; `cargo fmt --all
  -- --check`.

## Forward semantic warnings on the file-backed WASM artifact path

Commit date: 2026-08-13
Source commit: this correction commit

- Closed the gap flagged during the earlier doc-consistency pass:
  `Compiler::compile_file_to_wasm_artifact_with_lane` always returned an
  empty `warnings` bundle, regardless of `Compiler::with_semantic_warnings`,
  unlike its source-based sibling `compile_wasm_artifact`.
- Confirmed this was a genuine oversight, not an intentional limitation: the
  file-backed path's `compile_file_to_signals` returns the same
  `SignalCompileOutput` type, with the same `warnings` field populated by
  `self.semantic_warnings`, as the source-based path — the file-loading
  machinery already retains warnings correctly. The gap was purely
  structural: `compile_file_to_wasm_artifact_with_lane` delegated to
  `compile_file_to_wasm_with_lane`, which discards `signals` (and its
  warnings) before returning just the `WasmModule`.
- Fixed by inlining the WASM-generation pipeline directly into
  `compile_file_to_wasm_artifact_with_lane`, mirroring how
  `compile_wasm_artifact` already does this for the source-based path, so
  `signals.warnings` can be captured and forwarded into the returned
  `WasmArtifactBundle` instead of a hardcoded empty bundle. Updated the
  function's doc comment accordingly; the general `WasmArtifactBundle`
  field doc no longer needs any caveat since both producers now agree.
- Added `compiler_compile_file_to_wasm_artifact_forwards_semantic_warnings_when_enabled`,
  compiling a file with `process = sqrt;` (the same domain-warning trigger
  used by the existing source-based warning tests) both with and without
  `with_semantic_warnings(true)`.
- Validation: `cargo test -p compiler --lib` (75 passed); `cargo test -p
  compiler --test semantic_guidance` (13 passed); `cargo clippy --workspace
  --all-targets -- -D warnings`; `cargo doc -p compiler --no-deps` with
  warnings denied; `cargo fmt --all -- --check`.

## General README and Rustdoc consistency pass

Commit date: 2026-08-13
Source commit: this correction commit

- Scanned all 49 `README.md` files in the tree for broken relative markdown
  links with a small throwaway script: zero broken links found, a clean
  baseline.
- Cross-checked the root README's "Workspace crates" table against the
  actual `crates/` directory listing (31 crates) and found `libfaust-ffi`
  completely undocumented there — no row in the table, no other mention
  anywhere in the file, despite having its own README and an
  `xtask libfaust-export-check` verification target. Added it to the FFI
  adapters table.
- Ran `cargo doc --workspace --no-deps` with `RUSTDOCFLAGS="-D warnings"`
  (the project's standard Rustdoc gate) across every workspace crate and
  found one real broken intra-doc link: `crates/parser/src/lib.rs`'s public
  `lexerdef()` linked to the private `shared_lexerdef` function, which
  `rustdoc::private_intra_doc_links` correctly rejects under `-D warnings`.
  De-linked the reference to plain code text; the informational content
  (there is a cached variant, and where to find it) is unchanged.
- Did not attempt to reconcile the much larger archive of dated planning/
  analysis notes under `porting/*.md` and `porting/journal/*.md`: many
  reference the pinned upstream C++ Faust tree by relative-looking paths
  that were never meant to resolve inside this repo, and the rest are
  point-in-time snapshots of files that have since been legitimately
  refactored (e.g. `signal_prepare.rs` and `signal_fir/delay.rs` becoming
  module directories) — "fixing" those links would misrepresent history
  rather than correct a defect. The scope of this pass was `README.md` files
  and Rust source `///`/`//!` documentation, which is what stays load-bearing
  as the code changes.
- Followed up with a semantic pass beyond structural link-checking: six
  parallel read-only agents, one per crate group, each read module-level and
  item-level doc comments and cross-checked their claims against the actual
  implementation (not just link syntax). Found and fixed 12 genuine
  prose/code mismatches across 11 files:
  - `interval/ops/logic.rs`: `gt`'s doc claimed it delegates to `ge` swapped;
    it is an independent implementation, unlike the `le`/`lt` pair just below
    that genuinely do delegate.
  - `parser/lib.rs`: crate doc claimed integer-literal overflow is *clamped*;
    the implementation (and that function's own doc) *wraps*, matching the
    C++ parser's natural 32-bit `int` overflow.
  - `propagate/reverse_ad.rs`: doc pointed at
    `crates/transform/src/signal_fir/module.rs`, since split into a
    `signal_fir/module/` directory; repointed at `module/bra.rs`, the actual
    home of `BlockReverseAD` lowering.
  - `codegen/backends/cranelift/jit_data.rs`: a `# Why the name says \`stub\``
    doc section survived the function's rename from `declare_compute_stub`
    to `declare_jit_function`, leaving a heading about a name the function
    no longer has.
  - `compiler/service.rs`: `with_execution_options_from_argv`'s flag table
    omitted `--table-init`/`--table-init-sample-rate`, which the function
    does decode.
  - `compiler/json_naming.rs`: `json_meta_entries_from_snapshot`'s doc didn't
    mention the `author`→`contributor` fold applied to repeated values.
  - `compiler/emitters.rs`: `WasmArtifactBundle::warnings`' field doc claims
    it reflects `with_semantic_warnings` for every producer; the file-backed
    `compile_file_to_wasm_artifact_with_lane` always returns it empty
    regardless. Documented the gap on that function and flagged the
    underlying omission as a separate follow-up task rather than silently
    changing behavior during a doc-consistency pass.
  - `draw/lib.rs`: `DrawConfig::draw_route_frame`'s doc had the flag's effect
    inverted (claimed something is drawn without it, nothing with it — the
    opposite of `schemas/route.rs`'s own accurate doc and code); the
    module's "Architecture" diagram also named `generate_schema` as
    `draw_schema`'s first call when it actually calls
    `generate_folded_inside` (`generate_schema` is a separate non-folding
    path used only internally for group decoration), with a parameter list
    that didn't match the real signature either.
  - `wasm-ffi/lib.rs`: module doc called `expandDSP`/`generateAuxFiles`
    "explicit stubs" and claimed only `getInfos(version|help)` was
    implemented; both helpers are fully implemented against the real
    compiler service, and `getInfos` already supports five more keys
    (`libdir`, `includedir`, `archdir`, `dspdir`, `pathslist`).
  - `cranelift-ffi/instance.rs`: three function docs described reading
    "sidecar init/reset-ui/UI instruction lists" — leftover terminology from
    a superseded design the crate's own module docs say was removed
    (`"no interpreter sidecar is used ... anymore"`). Rewritten to describe
    what the functions actually do: run the JIT-compiled entry point or fall
    back to the native `RuntimeDescriptor`-driven initializers.
  - `interp-ffi/factory.rs`: `getCInterpreterDSPFactoryLibraryList`'s
    `# Safety` doc required a non-null factory pointer; the parameter is
    unused and never dereferenced, so any pointer (including null) is safe —
    matching the equivalent Cranelift function's already-correct wording.
- Validation: the link-check script; `cargo doc --workspace --no-deps` with
  warnings denied (previously failing on the parser crate, now clean for
  every crate, still clean after the semantic-pass edits);
  `cargo clippy --workspace --all-targets -- -D warnings`; `cargo build -p
  parser`; `cargo fmt --all -- --check`; `cargo test --lib` for every crate
  touched by the semantic pass (`interval`, `parser`, `propagate`,
  `compiler`, `codegen`, `draw`, `wasm-ffi`, `cranelift-ffi`, `interp-ffi`),
  all green.

## Correct the mem0 JSON schema doc and add a user guide for custom allocators

Commit date: 2026-08-13
Source commit: this documentation commit

- Fixed §6.8 of the `mem0` porting plan to match the schema actually shipped:
  the worked JSON example and rules list were missing `size_source` (the
  zone's `size_exact` provenance: `computed`/`compiler_expression`/`estimated`)
  and `abi.maximum_allocation_alignment` (a fixed per-target ceiling, not a
  per-DSP maximum, that every zone's `alignment` is validated against).
- Added `docs/user-mem0-guide-en.md`, a new user-facing guide for host/plugin
  developers writing a custom `-mem0` memory manager. It documents the C ABI
  and C++ interface side by side, a full field-by-field reference for the
  `memory_manager`/`memory_layout` JSON (verified against real generated
  output, not just the spec), the two-phase describe/allocate/release
  lifecycle contract (including which entry points terminate versus return a
  failure code — `create`/`createChecked` never terminate, unlike
  `memoryInfo`/`classInit`/`classDestroy`), and three worked allocator
  patterns: a real-time-safe arena sized from the description pass (no heap
  traffic after startup), a `(size_bytes, alignment)`-keyed free-list pool for
  polyphonic voice reuse, and a SIMD-aligned C++ manager using the
  alignment-aware overloads added earlier today. Linked from the root
  README's documentation index and its `-mem0` paragraph.
- Every command and code fragment quoting live behavior was checked against
  the actual compiler output before writing it down: generated `-json`
  payloads for two real DSPs (a waveform table, a fixed `@(10)` delay), the
  `jq` budgeting one-liner run end to end, and the exact C++/C call-site
  signatures cross-checked against `crates/codegen/src/backends/{c,cpp}/mod.rs`
  (in particular, that the C++ `info()` callback carries no per-zone
  alignment unlike the C ABI's, and that `create()`/`createChecked()` are
  both already fallible rather than one terminating).
- Validation: `faust-rs -mem0 --json` on two fixtures plus the `jq` pipeline
  from the guide, run manually; every relative link in the new doc resolves;
  no trailing whitespace. No Rust code changed.

## Add alignment-aware `dsp_memory_manager` overloads to generated C++ `mem0`

Commit date: 2026-08-13
Source commit: this enrichment commit

- The pinned reference `dsp_memory_manager` (`architecture/faust/dsp/dsp.h`)
  only declares `allocate(size_t)` and `destroy(void*)`, unlike the Rust-only
  C ABI (`faust_memory_manager`), which already asks for alignment on
  allocation and hands size/alignment back on release. Generated C++ under
  `-mem0` now additionally recognizes `allocate(size_t, size_t)` and
  `destroy(void*, size_t, size_t)` and prefers them when the linked header
  declares them, while remaining a strict superset of the legacy contract.
- Every allocation/destruction call site (instance buffers, class tables, the
  DSP object, and generated table subcontainers) now routes through a new
  `faust_mem0_detail::{allocate,destroy}` shim emitted once per `-mem0`
  translation unit. It uses the classic `int`/`long`-tagged SFINAE overload
  pair to probe, at compile time, whether the static type of
  `dsp_memory_manager` declares the additive overloads; if not, it falls back
  to the legacy one-argument calls unchanged. The probe is against
  `dsp_memory_manager`'s static type, not the concrete manager subclass, so a
  manager that only overrides the legacy pair keeps compiling unmodified.
- The legacy overloads are documented as obsolete directly in the generated
  code (a comment inside `faust_mem0_detail`), kept only for source
  compatibility with hosts still built against the unextended upstream
  header.
- Also deduplicated `classDestroyChecked()` to call the already-shared
  `classDestroyTables()` helper instead of carrying its own copy of the
  reverse-order release loop.
- Enriched `tests/impulse-tests/archs/faust_mem0.h`'s `AuditCppMemoryManager`
  with the additive overloads, auditing that the returned address satisfies
  the requested alignment and that `destroy`'s size argument matches what was
  recorded at allocation.
- Added `mem0_generated_cpp_compiles_against_the_unextended_legacy_manager_header`,
  a new compile-and-run test defining `dsp_memory_manager` exactly as
  documented upstream (nothing else), proving the SFINAE fallback keeps
  generated code compiling and running against a host that has never heard of
  the extension. Extended the existing
  `mem0_generated_cpp_compiles_and_clone_is_independent` with a second
  `aligned_manager` that overrides the additive overloads directly and
  asserts the legacy ones are never reached, proving the richer path is
  actually preferred when available.
- Manually regenerated the real 33-buffer `karplus32.dsp` `-mem0` C++ output
  with the release binary and compiled/ran it end to end against the
  enriched `AuditCppMemoryManager` (`-std=c++17 -Wall -Wextra`, exit 0),
  beyond the small synthetic unit-test fixture.
- Documented the difference as `DIFF-API-005` in
  `faust-rs-vs-faust-cpp-differences-en.md`.
- Validation: the full `codegen` test suite (364 tests, +1 new), including
  all three mem0 C++ compile-and-run smoke tests; the `compiler` crate's
  pinned `mem0` C++ differential tests; `cargo fmt --all`. The full 94-DSP
  `all-mem0` corpus gate (`make -f Make.mem0 all-mem0`) was not rerun in this
  session — the unit-level SFINAE dispatch tests plus the manual
  full-scale `karplus32.dsp` check are the evidence for this change; rerunning
  that gate is a reasonable follow-up before the next `mem0` milestone.

## Compact `mem0` allocation-failure unwinding from O(N²) to O(N)

Commit date: 2026-08-13
Source commit: this correction commit

- Fixed quadratic code generation in the C and C++ `-mem0` backends: each
  allocation-failure branch re-emitted an explicit `destroy` call for every
  previously allocated field, so `memoryCreate()`/`create<Class>()` and the
  class-table allocation prefix grew as O(N²) in the number of buffers/tables.
  `karplus32.dsp` alone (33 instance buffers) produced roughly 4,700 lines for
  `memoryCreate()`.
- Every not-yet-allocated field is already `nullptr`/`NULL` (zeroed by the
  constructors in C++, by `memset` in C), and the failing field already holds
  its own possibly null/misaligned result, so a single reverse, null-guarded
  sweep releases exactly what was allocated so far. C++ reuses the existing
  `memoryDestroy()` from `memoryCreate()`'s failure path and factors
  class-table release into a new shared `classDestroyTables()` method. C adds
  `releaseInstance<Class>()` and `classDestroyTables<Class>()` helpers, the
  latter also reused by the pre-existing `classDestroyChecked<Class>`.
  Cranelift was unaffected: its ownership already runs through
  `ExternalAllocations`'s `Drop`, not generated per-field unwind text.
- Result on `karplus32.dsp`: generated C++ `-mem0` output shrank from 5,951 to
  1,599 lines (-73%) with identical transactional semantics.
- Validation: the full `codegen` test suite (363 tests), including the C and
  C++ compile-and-run smoke tests that inject an allocation failure at every
  index and assert a leak-free unwind under `-Wall -Wextra -Werror -pedantic`
  (C) and `-Wall -Wextra -Werror` (C++); the full `compiler` test suite,
  including the pinned `mem0` C++ differential tests.

## Correct `mem0` impulse coverage to use the full DSP corpus

Commit date: 2026-08-13
Source commit: this correction commit

- Changed the public `cpp-mem0`, `c-mem0`, and `cranelift-mem0` targets from
  the three-fixture smoke set to the ordinary `dsp/` corpus, using the C++
  oracle manifest, backend exclusions, per-DSP tolerances, 15,000-frame
  reference prefixes, and standard include paths. `all-mem0` now combines the
  full three-backend/JSON-cost gate with a separate focused `mem0-smoke` audit;
  optimization and sanitizer matrices remain on the compact import-free set.
- The expanded gate exposed and fixed four defects hidden by the smoke set:
  C++ helper-object descriptions charged a nonexistent DSP vptr; helper arrays
  were incorrectly pointerized without their own allocations; temporary table
  subcontainers from `instanceConstants` were not destroyed; and strict-C
  soundfile code received only an opaque `Soundfile` declaration. Structural
  C/C++ tests now cover embedded helper arrays and both class/instance helper
  cleanup paths.
- Validation: all 94 DSPs accepted by the pinned C++ oracle pass generated C,
  generated C++, and strict-lowered Cranelift `-mem0` comparisons over 15,000
  frames; 39 oracle-unsupported inputs are explicitly classified. Per-DSP JSON
  checks, exact cross-backend `compute_cost` checks, and the focused allocation
  plus C/C++/Cranelift optimization audit all pass. A forced clean run also
  regenerated ordinary faust-rs C++ references and all managed artifacts for
  those 94 inputs, preventing incremental build products from hiding failures.
- The mandatory release compilation-cost gate was run three times without a
  baseline update. Each run reproduced only the pre-existing noisy
  `reverb_designer` vector case: two normalized measurements were about 1,022
  units against 953.982, and one absolute/ratio backstop measured 5,505 ms
  against 5,411 ms. All other cases passed; this emitter-only correction does
  not affect the non-`mem0` program measured by that case.

## M9: close `mem0` compatibility and documentation

Commit date: 2026-08-13
Source commit: this documentation commit

- Documented the scalar mode-zero CLI contract and version-2 JSON/cost output
  in the root and compiler guides. Updated the codegen guide for the generated
  C, C++, and Cranelift ownership models and linked the canonical layout/cost
  analysis.
- Updated Cranelift module Rustdoc and the C/C++ FFI guide for pointer-slot
  state, class/instance ownership, binding, cache identity, source-backed
  serialization, and mandatory fresh binding after restore. Removed stale
  statements that manager hooks and executable instance paths were deferred.
- Closed M9 in the authoritative plan with an explicit `1:1`, `adapted`, and
  `reference-fix` public-surface table and marked M0 through M9 complete.
  Updated the Cranelift backend plan and parity matrix for the implemented
  manager, JSON, persistence, and foreign-function behavior.
- Replaced the stale Cmajor handoff with the complete ten-commit `mem0` arc,
  exact decisions, validations, untouched local files, and follow-up boundary.
- Validation: CLI help exposes the exact scope; warnings-denied Rustdoc for
  `codegen`, `compiler`, and `cranelift-ffi`; README consistency tests;
  warnings-denied workspace Clippy; Rust golden parity; and the serialized
  full workspace all-target suite pass. A deliberately serialized rerun was
  needed after concurrent Cargo commands raced on the `xtask` test artifact.
  The release cost gate passed without a baseline update on its third run; the
  first two 4-ms-calibration runs produced one quantization outlier each
  (`cubic_distortion` vector by 0.535 units, then `APF` scalar), neither of
  which reproduced at the final 5-ms calibration.

## M8: harden `mem0` with pinned differentials and optimization parity

Commit date: 2026-08-13
Source commit: this implementation commit

- Added a live, optional differential against pinned Faust C++ `8eebea429`.
  Delay and static-table fixtures compare generated manager descriptions and
  every unaffected legacy field; only the documented object/table count,
  lifecycle, clone, ownership, failure, and alignment fixes are allowlisted.
  The delay fixture compares the complete legacy `compute_cost` object exactly.
- Fixed the cost/layout analysis to inspect the effective direct scalar compute
  loop instead of accidentally counting slow/control prelude statements. This
  restores exact pinned counts while retaining the versioned D6 corrections.
  Added exhaustive literal/bitcast/select/control/switch coverage and an
  asymmetric branch-order test for the component-wise maximum policy.
- Added C/C++ compiler `-O0`/`-O3` and Cranelift optimization-level 0/3 runtime
  parity for every mem0 impulse fixture. Cranelift JSON comparison ignores the
  intentional compile-option spelling difference while requiring identical
  layout and cost semantics.
- Added a Cranelift factory bitcode round-trip test proving that `mem0` and the
  optimization level survive serialization while manager callbacks do not;
  the restored factory must be freshly bound and releases all allocations.
- Added optional ASan/UBSan C/C++ impulse coverage and public `mem0-opt` and
  `mem0-sanitize` targets. Forced optimization and sanitizer matrices pass on
  the supported macOS toolchain, as do all three live pinned-C++ differential
  tests.
- Validation: `cargo fmt --all`; Rustdoc for `codegen`, `compiler`, and
  `cranelift-ffi`; warnings-denied workspace Clippy; Rust golden parity; the
  complete workspace all-target suite including its hermetic loopback test;
  forced `all-mem0`; and the release compilation-cost gate. The latter passed
  all five scalar/vector codegen cases and all eight normalized front-end
  cases without a baseline update.

## M7: integrate the self-contained `mem0` impulse gate

Commit date: 2026-08-13
Source commit: this implementation commit

- Added `Make.mem0` and the top-level `cpp-mem0`, `c-mem0`,
  `cranelift-mem0`, and `all-mem0` targets. The isolated import-free corpus
  covers scalar/UI state, delay buffers, and generated tables without a C++
  Faust oracle or installed standard libraries; ordinary faust-rs C++ output
  is its local numeric reference.
- Added self-contained C++ and strict-C11 manager architecture surfaces. Their
  audit allocators reconcile descriptions with requests, poison fresh bytes,
  reject unknown or repeated destruction, and require no live allocations.
  Generated C is compiled as C before linking to the C++ impulse driver.
- Extended the Cranelift impulse runner with all four mode-zero aliases, strict
  factory JSON validation, optional JSON output, and a versioned shared-ABI
  audit manager. It requires every logical allocation to match its description
  and all allocations to be destroyed in global reverse order after instance
  and factory release.
- The runtime audit exposed two real reverse-destruction defects left in the M5
  implementation: class allocations were cleared in forward order, and Rust
  field drop order released the main DSP block before its external instance
  buffers. Added explicit reverse-pop ownership for both paths. The existing M5
  failure-injection and deep-clone tests remain the owning regression coverage.
- Added a Rust semantic JSON checker for versioned headers, required zone
  fields, unique names, checked `compute_cost` totals, strict Cranelift lowering,
  and exact cross-backend cost equality. Added unit tests for the checker and
  all impulse-runner aliases, and documented the new lane and architecture
  contracts.
- Validation: Cranelift runner/checker unit tests; forced rebuild of
  `make -B -f Make.mem0 all`; warning-clean `-O2` C++17 and strict C11 builds;
  three ordinary-versus-managed impulse comparisons per backend; nine
  individual JSON validations and three C/C++/Cranelift cost comparisons. Make
  retains the ordinary reference artifacts for incremental runs. Rustdoc for
  `cranelift-ffi`, warnings-denied workspace Clippy, and the complete workspace
  all-target suite (including its permitted hermetic loopback server) pass. The
  release compilation-cost gate was run twice without updating its baseline;
  its sub-10 ms calibration produced unrelated, non-reproducible outliers:
  `virtual_analog_oscillators` front-end at 113.000 vs 92.950 permitted, then
  `spectral_level` scalar codegen at 177.166 vs 168.132 permitted, while the
  first outlier fell to 60.833 on the second run.

## M6: emit versioned `mem0` JSON and `compute_cost`

Commit date: 2026-08-13
Source commit: this implementation commit

- Extended the shared strict JSON model with an optional version-2 memory
  description derived from the retained `Mem0Analysis`. It identifies the
  effective C, C++, or Cranelift backend, distinguishes the legacy C++ manager
  ABI from the shared aligned C ABI, records target pointer facts and access
  metric, and serializes every canonical zone with legacy fields plus explicit
  scope, role, alignment, allocation phase/order, exactness, and provenance.
- Emitted the corrected version-2 `compute_cost` alongside every `mem0` layout,
  retaining the legacy one-element arrays and scalar key order. Binary and math
  operation maps come directly from deterministic `BTreeMap` analysis and
  carry checked totals. C, C++, and Cranelift use the same cost snapshot for
  the same effective FIR.
- Kept ordinary strict and Wasm JSON byte-stable by making all additions
  optional. Added explicit source/file compiler entry points for selecting the
  native memory-layout flavor and re-exported that typed flavor as documented
  facade API.
- Replaced the Cranelift factory's minimal hand-built status JSON with the
  shared strict FIR serializer. Factory JSON now carries normal DSP metadata,
  UI and arity fields while retaining `backend`, `jit_compiled`, and
  `compute_body_lowered`; a `mem0` factory additionally exposes its immutable
  native allocation/cost plan before a manager is bound.
- Added semantic `serde_json` tests for schema versions, backend ABI selection,
  deterministic rendering, zone metadata, one-element cost blocks, total/map
  agreement, cross-backend cost equality, and strict absence of all memory
  fields without `mem0`.
- Validation: `cargo fmt --all`; Rustdoc for `codegen`, `compiler`, and
  `cranelift-ffi`; warnings-denied workspace Clippy; and the full workspace
  all-target suite, including the hermetic loopback test with local-bind
  permission. The mandatory release cost gate was run twice without changing
  its baseline; only the known sub-millisecond-calibration noise triggered,
  first for `APF` vector (2.250 units vs 2.165) and then scalar (1.250 vs
  1.082), while all larger cases and front-end baskets remained within their
  ceilings.

## M5: implement Cranelift `mem0` JIT and factory ownership

Commit date: 2026-08-13
Source commit: this implementation commit

- Retained the canonical `Mem0Analysis` in every `-mem0` JIT result and added
  the typed `FRS-CGEN-CLIF-0005` layout diagnostic. Instance arrays now occupy
  pointer-sized `ExternalTable` slots linked to stable `MemoryZoneId` values;
  ordinary layouts retain inline payload sizes and behavior.
- Changed struct table load/store/shift lowering to resolve inline payloads or
  dereference managed pointer slots without changing finalized function ABIs.
  Writable generated static tables now use finalized JIT pointer slots backed
  by factory-owned class allocations; literal read-only data remains embedded.
- Added copied, version-checked manager bindings and transactional aligned
  allocation for the logical JIT object, every instance buffer, and generated
  class table. Partial failure unwinds safely, clone recreates and copies every
  instance allocation, and final cache release drops instances, class storage,
  then JIT code. No host callback executes while the global cache lock is held.
- Added `setCCraneliftMemoryManager` to the C API and mirrored the canonical
  header. Compilation/cache/serialization may remain unbound, while instance
  creation under `-mem0` fails until binding. Rebinding is idempotent for the
  same copied callbacks and rejected after allocation for a conflicting one.
- Replaced the C++ wrapper's no-op manager methods with factory-lived adapter
  glue and a real getter. The adapter documents the narrower legacy type and
  alignment surfaces; Rust validates every returned address before publishing
  it to JIT code.
- Extended `compute_cost` for no-op FIR labels and scalar delay-shift nodes,
  fixing a real fast-lane delay rejection found by the Cranelift runtime test.
  Added structural pointer-slot identity coverage and a managed runtime test
  with a generated class table, delay buffer, unbound creation failure,
  multiple allocations, deep clone, and complete destruction.
- Updated the authoritative plan, compatibility registry, and Cranelift FFI
  parity matrix. M6 remains responsible for strict shared JSON emission and M7
  for `tests/impulse-tests` integration.
- Validation: `cargo fmt --all`; Rustdoc for `codegen`, `cranelift-ffi`, and
  `ffi-common`; full workspace Clippy with warnings denied; and the full
  workspace all-target suite. Its loopback test passed with the required local
  bind permission; one parser temp-directory test affected by an accidentally
  overlapping diagnostic rerun passed immediately in isolation. The mandatory
  release cost gate again reports only the pre-existing noisy
  `reverb_designer` vector measurement (996.000 units against 953.982); all
  other baskets pass and no baseline changed.

## M4: implement generated C `mem0` and the shared C ABI

Commit date: 2026-08-13
Source commit: this implementation commit

- Published ABI v1 as the authoritative `#[repr(C)]` callback table in
  `ffi-common::abi` and as the guarded
  `crates/ffi-common/include/faust-memory-manager.h`. The table carries a
  version, byte size, opaque context, explicit alignment, and the complete
  append-only memory-type vocabulary shared with Cranelift.
- Embedded that exact header in self-contained generated C and reject missing,
  truncated, or version-mismatched callback tables before calling the host.
  Rust layout/compatibility tests and strict C11/C++17 header-smoke tests guard
  field order and enum discriminants.
- Retained one canonical `Mem0Analysis` in C emission. Eligible instance arrays
  and writable tables become pointers; literal static tables remain compiled
  constants. Effective single/double precision is reflected in manager
  allocation sizes while preserving the established host-supplied
  `FAUSTFLOAT` interface contract.
- Added class-namespaced `memoryInfo*`, `classInit*`, `classDestroy*`,
  `create*`, and `destroy*` functions, plus checked companions for operations
  with legacy void surfaces. Instance creation checks alignment, unwinds
  partial allocations in reverse, captures its manager/context, and returns
  `NULL` on failure. Destruction never consults a rebinding global manager.
- Preserved `init = classInit -> instanceInit`, idempotent same-manager/sample
  class initialization, live-instance protection, pointer clearing, and
  manager allocation for generated-table subcontainers. Ordinary C output
  retains its original `new*`/`delete*` ABI and inline fields.
- Added generated-C structural and runtime tests. Strict C11 compilation uses
  warnings as errors; failure is injected at each object/buffer allocation;
  two live DSPs prove independent contexts; the delay DSP runs identically in
  both contexts; early class destruction is rejected; and all allocations are
  released. Added single/double source-shape tests and the corresponding C++
  precision guard.
- Validation: `cargo fmt --all`; Rustdoc for `codegen` and `ffi-common`;
  warnings-denied Clippy for all touched crates; and the full workspace
  all-target suite (the loopback HTTP test was rerun with its required local
  bind permission). The mandatory release cost gate was run twice; its only
  failure is the already-recorded noisy `reverb_designer` vector case at
  964.000/966.250 units against 953.982, reproduced before M4 at M0–M2. M4's
  ordinary pipeline adds only a false `memory_manager_mode` branch at emission
  and all other baskets pass. No budget baseline changed.

## M3: implement generated C++ `mem0`

Commit date: 2026-08-13
Source commit: this implementation commit

- Made `CppOptions` carry the effective sample precision into canonical target
  analysis and added the typed `FRS-CGEN-CPP-0004` layout failure. Ordinary
  code generation remains free of manager fields/methods and retains inline
  arrays.
- Under `mem0`, every eligible DSP array is emitted as a pointer. Writable
  generated static tables become manager-owned class pointers; literal static
  tables stay compiled constants. Generated subcontainers use manager
  placement construction/destruction, including nested generators.
- Preserved the public `fManager`, `memoryInfo`, `classInit`/`classDestroy`,
  `create`/`destroy`, and `memoryCreate`/`memoryDestroy` names. Added checked
  companions for recoverable description, class initialization/destruction,
  and creation. Legacy void lifecycle entry points terminate deterministically
  on a violated contract rather than dereference null or publish partial state.
- Allocation is transactional, verifies returned alignment, unwinds in reverse
  order, nulls released pointers, and captures the manager that owns each DSP
  and class allocation set. Object destruction no longer consults a possibly
  rebound global manager, and class destruction rejects live instances.
- Kept `init = classInit -> instanceInit` and the established internal
  `instanceConstants -> instanceResetUserInterface -> instanceClear` order.
  Repeated class initialization is idempotent only for the same manager/sample
  rate. Semantic static-table filling is exception-contained with allocation
  rollback.
- Replaced the reference's shallow clone with independent manager allocation,
  named scalar copies, and per-buffer payload copies; approved class tables
  remain shared. Zero-length arrays remain null and produce no allocation.
- Added Rustdoc/provenance comments and structural tests for ordinary/mem0
  separation, pointer emission, descriptions, lifecycle, generated tables, and
  subcontainers. A self-contained C++17 test compiles with
  `-Wall -Wextra -Werror`, injects failure at each allocation, checks leak-free
  unwind, runs the delay DSP, proves clone buffer independence, and verifies
  final class cleanup.
- Validation: `cargo fmt --all`; `cargo doc -p codegen --no-deps`; focused
  workspace Clippy with warnings denied; all codegen targets/tests; and all
  compiler targets/tests except the sandbox-denied loopback bind pass. The sole
  hermetic HTTP test passes when rerun with loopback permission.
- The mandatory release `compile-budget-check` passes all five scalar/vector
  codegen cases and all eight normalized front-end cases. No baseline changed.

## M2: add canonical memory-layout and compute-cost analyses

Commit date: 2026-08-13
Source commit: this implementation commit

- Added the immutable `Mem0Analysis` snapshot shared by C, C++, Cranelift, and
  JSON. Its version-2 `MemoryLayout` co-locates stable zone identity, type,
  scope, role, allocation phase/order, exact checked sizes, alignment,
  exactness provenance, and scalar-frame read/write counts.
- Added an explicit `TargetAbi` instead of the pinned C++ compiler's virtual
  pointer and padding heuristic. C, C++, and Cranelift object models are
  distinguished; C++ numeric object layout is marked as a compiler-expression
  estimate, while generated `sizeof`/`alignof` remains authoritative in M3.
- Implemented append-only `Int64`/`Bool` memory types, zero-length arrays as
  described but non-allocated zones, one-byte pointer-aligned empty Cranelift
  state, exact static-table counts, and explicit class/instance/subcontainer
  roles. All multiplication, addition, alignment, and identity conversions are
  checked and unknown/unsupported constructs fail closed.
- Added `analyze_effective_mem0`: C/C++ analyze nested-container FIR directly;
  Cranelift uses its canonical `MergedStructFields` flattening first, ensuring
  the analysis describes the same effective snapshot as the JIT.
- Added the exhaustive version-2 scalar-FIR `ComputeCost` visitor with lexical
  `BTreeMap` breakdowns, exact function-call keys, bitcast counting, all scalar
  literal variants, occurrence traversal without hash-cons deduplication,
  synthesized loop-control accounting, and D6 component-wise branch maxima.
- Added Rustdoc provenance to C++ `createMemoryLayout`, `StructInstVisitor`,
  `ArrayToPointer`, and `InstComplexityVisitor`, plus structural tests for zone
  identity, access aggregation, empty allocations, target alignment, static
  tables/subcontainers, all standard scalar fixtures, D6 branch maps, loop
  normalization, and repeated shared occurrences.
- Validation: `cargo fmt --all`; `cargo doc -p codegen --no-deps`; full workspace
  Clippy with warnings denied; all workspace tests except one sandbox-denied
  loopback-bind test pass. That hermetic HTTP test was rerun with loopback
  permission and passed. The focused codegen analysis suites pass.
- The mandatory release compile-cost gate was run. As at M1, its only failure
  is the pre-existing `reverb_designer` vector measurement: 965.250 units
  versus the 953.982 ceiling; the unchanged M0 baseline had already reproduced
  the same failure at 995.250 units. M2 analysis is invoked only under future
  `mem0` consumers and does not alter the measured ordinary pipeline. No
  baseline was changed.

## M1: thread the typed `mem0` option

Commit date: 2026-08-13
Source commit: this implementation commit

- Added the shared `MemoryManagerMode::{None, Mem0}` codegen type with Rustdoc
  provenance to C++ `global.cpp`; it exposes one canonical `-mem0` spelling and
  deliberately has no speculative variants for `mem1`–`mem3`.
- Added the four CLI aliases and propagated the selected mode through
  `COptions`, `CppOptions`, `CraneliftOptions`, source mode, FIR fixture mode,
  and compilation-options metadata. Scalar C, C++, and Cranelift are the only
  accepted backend combinations; vector mode and non-codegen modes fail closed.
- Extended the dependency-light FFI argument parser with the same four aliases,
  explicit `mem1`–`mem3`, vector, and `-it` rejection. Cranelift source, file,
  signal, and box factory paths now pass the typed mode to JIT options.
- Canonicalized all aliases, including duplicates, to one `-mem0` token for
  Cranelift cache/serialization identity. Allocator pointers remain outside that
  identity.
- Recorded the newly reachable adapted CLI surface as `DIFF-CLI-010`. M1 only
  carries typed intent; allocation/layout behavior remains assigned to M2–M5.
- Validation: `cargo fmt --all`; all 31 `ffi-common` tests; the two new codegen
  option tests; three compiler CLI mem0 tests; the Cranelift cache-identity test;
  `cargo check -p compiler --all-targets`; and
  `cargo check -p cranelift-ffi --all-targets`; and focused Clippy with warnings
  denied all pass. One initial Cargo test invocation supplied several positional
  filters and was rejected by Cargo; the tests were rerun with valid per-filter
  commands and passed.
- `compile-budget-check` was run twice on M1 and found the same sole failure:
  `reverb_designer` vector at 984.750 then 1010.000 units versus the existing
  953.982 ceiling. A clean detached worktree at the unchanged M0 commit
  `fc8b5689` reproduced it at 995.250 units. M1 adds no lowering/emission work,
  so this is recorded as pre-existing measurement/baseline debt; the baseline
  was not changed.

## Complete the `-mem0` Phase 0 gate

Commit date: 2026-08-13
Source commit: this implementation commit

- Recorded the effective production path shared by generated C, generated C++,
  and Cranelift: the existing compiler facade and transform signal→FIR fast
  lane, followed by a backend-effective FIR snapshot and the canonical memory
  analysis. TreeArena, recursive signal representation, and the DSP algorithm
  are outside the change.
- Froze the capability matrix to scalar `mem0` for C, C++, and native
  Cranelift. The other memory modes, vector mode, `-it`, and unrelated backends
  fail closed until separately qualified.
- Approved the plan's recommended D1–D12 outcomes, including exact target ABI,
  captured managers, transactional allocation, corrected `compute_cost`,
  Cranelift pointer slots and factory-owned tables, unbound factory behavior,
  wrapper/state ownership, strict JIT validation, and the adapted class
  lifecycle.
- Added the public API mapping, differential protocol, Rustdoc provenance
  convention, ownership/failure rules, and completion gates. The decision is
  GO with no Phase 0 blocker or new stub debt.
- Validation: documentation whitespace check only; this phase changes no Rust
  code or runtime behavior.

## Plan the `-mem0` custom memory-manager port

Commit date: 2026-08-13
Source commit: this planning commit

- Analyzed the pinned Faust C++ `-mem`/`-mem0` contract, its generated C++
  lifecycle, memory-layout JSON, and `compute_cost` visitor. Recorded the
  reference weaknesses that the Rust port must not reproduce: heuristic object
  size, zero-sized static-table sentinels, empty `init`, shallow `clone`, global
  manager rebinding, unchecked allocation failure/alignment, incomplete type
  vocabulary, and defective conditional cost accounting.
- Defined one typed `Mem0Analysis` as the source of truth for generated C,
  generated C++, Cranelift JIT storage, manager descriptions, JSON
  `memory_layout`, and versioned `compute_cost`. Scope is deliberately limited
  to the four `mem0` aliases; `mem1`–`mem3` stay rejected.
- Specified an adapted, versioned plain-C manager ABI with alignment and opaque
  context, while preserving the legacy-compatible C++ manager surface. The
  plan includes transactional allocation, captured-manager destruction,
  poison/failure testing, exact target-ABI metadata, and deep clone behavior.
- Extended the design to Cranelift: external-buffer pointer slots in
  `StructLayoutPlan`, pointer-aware lowering, factory-owned writable static
  tables, managed class/instance allocation sets, functional C/C++ factory
  manager binding, cache and serialization rules, explicit wrapper-versus-JIT
  state ownership, and strict rejection of compute stubs in parity tests.
- Added a dedicated `tests/impulse-tests` strategy with `cpp-mem0`, `c-mem0`,
  `cranelift-mem0`, and `all-mem0` targets, self-contained DSP fixtures, a
  portable allocation trace, semantic JSON checks, and ordinary-versus-mem0
  plus optimized-versus-unoptimized runtime parity.
- This commit is planning/documentation only. No compiler behavior or public
  API is enabled yet; the document's whitespace check is clean.
