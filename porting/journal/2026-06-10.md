# 2026-06-10

## Make recursive FAD runtime tests self-contained

Commit date: 2026-06-10
Source commit: this commit

Removed an accidental dependency on external Faust libraries from the recursive
FAD runtime regression tests.

Implementation updates:

- replaced `import("stdfaust.lib")` in the affected temporary DSP snippets with
  local `bus2` definitions;
- replaced `ro.cross(2)` with a local two-channel cross routing definition so
  mandatory CI tests no longer depend on `FAUST_LIB_PATH` or a local
  `faustlibraries` checkout;
- kept the tested recursive/FAD structures unchanged: multi-output recursion,
  mutual recursion, nested mutual recursion, and multi-seed variants.

Validation:

- `cargo test -p compiler --test fad_recursive_runtime`
- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`

## Refresh Rust goldens for CI corpus

Commit date: 2026-06-10
Source commit: this commit

Refreshed Rust golden snapshots needed by the current CI corpus.

Implementation updates:

- updated the compact diagnostic snapshot metadata for
  `err_fad_rad_temporal` and `err_rad_delay_temporal_unsupported`;
- added missing Rust golden snapshots for tracked RAD/TBPTT corpus fixtures
  referenced by `golden-check`;
- left generated snapshots for locally untracked corpus files out of the commit.

Validation:

- `cargo run -p xtask -- golden-gen-rust`
- `cargo run -p xtask -- golden-check`
- `cargo run -p xtask -- backend-align-smoke --skip-golden`

## Fix clippy warnings on stable toolchain

Commit date: 2026-06-10
Source commit: this commit

Updated code patterns flagged by the current stable clippy run with
`-D warnings`.

Implementation updates:

- replaced manual guarded division patterns in propagate arity diagnostics with
  explicit `checked_div` calls after the non-zero guard;
- collapsed the block reverse AD match arm for `exp`/`exp10`/`sqrt` into a
  guarded pattern while preserving the `val[sig]` dependency;
- removed redundant `.into_iter()` calls in transform and codegen paths;
- switched parser source-bundle ordering to `sort_by_key`;
- replaced the wasm local-index counter loop with a zipped index range.

Validation:

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`

## Keep boxFad public seed parameter singular

Commit date: 2026-06-10
Source commit: this commit

Recorded the API naming decision for the public Box FAD wrapper after review.

Implementation updates:

- kept `CboxFad(Box exp, Box seed)` and `boxFad(Box exp, Box seed)` using the
  singular `seed` parameter name;
- changed `CboxRad(Box exp, Box seed)` / `boxRad(Box exp, Box seed)` to the same
  public singular parameter naming convention.

Validation:

- documentation-only update; no additional code validation needed.
