# 2026-07-16

Branch: `ondemand-vec-fad-synthesis`.

## Align the Wasm pure-drop test with backend semantics

Commit date: 2026-07-16
Source commit: this commit

Fixed the workspace Wasm failure exposed by the final lockstep gates. Commit
`a29954d8` made active backends omit `Drop` roots only when the shared
structural purity predicate proves that evaluation cannot call, write, or
allocate. The older Wasm control-flow test still required a `Drop` opcode for
an input load and its arithmetic negation, although both roots are pure and
are now intentionally removed.

The test now requires the optimized absence of `Drop` and documents why calls
and unknown FIR nodes remain conservatively retained. The isolated regression
passes, and all 285 active `codegen` library tests pass (one manual benchmark
remains ignored). The workspace run proceeds beyond Wasm and separately
reveals the pre-existing scalar scheduling assertion in `p3_shadow_mode`,
which observes three distinct APF C++ forms where its test expects four.

## Attribute mixed-DSP SIMD to the lockstep subgraph

Commit date: 2026-07-16
Source commit: this commit

Completed Step 3 of the lockstep SIMD remediation plan. The native gate now
locates the generated physical sample loop containing checked `vlock_*` state,
requires each mixed DSP to retain its second non-bundle loop, and compiles with
Clang line tables. Only four-wide floating-point instructions whose LLVM debug
location belongs to the lockstep source range satisfy the threshold.

Negative tests reject an unexpected physical-loop count and prove that vector
operations attributed to an unrelated source line are excluded. Under Clang 14
with `-O3 -ffp-contract=off`, all three cases have 14 lockstep-attributed
four-wide operations; complete-module totals are 14 for the recursive bank, 30
for the mixed reduction, and 22 for the mixed side branch. The four focused
xtask tests and the attributed native gate pass.

## Carry lockstep delay-one state in registers

Commit date: 2026-07-16
Source commit: this commit

Completed Step 2 of the lockstep SIMD remediation plan. State-plan schema v4
adds a checked bundle record that co-locates each ordered lane's loop,
recursion-group, delay carrier, local state, and persistent state identity.
Only complete top-rate bundles with one scalar recursion projection and fixed
delay one are promoted; partial, variable-delay, clocked, and longer-delay
forms retain the existing copy/ring representation.

FIR now loads persistent lane state once per chunk, carries it in scalar locals,
and stores it once after the physical sample loop. Assembly emits homologous
statement rows adjacently across lanes, preserving every lane's IEEE expression
order while exposing profitable SLP. The independent checker rejects crossed
lane records and missing final stores. All 367 transform tests pass, the
complex and partial corpus remains bit-exact for both loop variants and all
four schedulers, and the corrected certified-vector gate reports 14, 30, and
22 four-wide LLVM floating-point operations for the main, mixed-reduction, and
mixed-branch cases under Clang 14 with `-O3 -ffp-contract=off`.

## Compact repeated lockstep event evidence

Commit date: 2026-07-16
Source commit: this commit

Completed Step 1 of the lockstep SIMD remediation plan. Event certificates now
separate the logical chunk size from the concretely checked sample count. When
a verified lockstep plan exceeds the full event-table bound, a canonical
two-sample basis checks the repeated event templates and the adjacent
`n -> n + 1` carried-state boundary without lowering the requested `vec_size`.
Non-lockstep plans retain the previous full-expansion behavior.

The independent checker reconstructs the compact threshold from routed FIR,
requires identical per-loop templates at samples zero and one, and rejects
mutated basis length, event template, and carried recursion edge. The three
complex/partial corpus cases now report certified vector FIR at default
`-vs 32` for both loop variants and all four scheduling strategies. All 366
transform tests pass, relevant Clippy passes with warnings denied, and the
corrected native-SIMD gate now reaches LLVM before failing with the expected
zero vector operations. This is the Step-2 register-state baseline.

## Make the lockstep SIMD gate reject scalar fallback

Commit date: 2026-07-16
Source commit: this commit

Completed Step 0 of the lockstep SIMD remediation plan. The xtask workflow now
compiles each source to `FirCompileOutput`, requires
`VectorPipelineStatus::Certified`, `VectorEffectiveMode::CertifiedVector`, and
the absence of fallback detail, then generates C++ from that exact FIR module.
It can no longer count SIMD produced by a scalar fallback.

Two focused tests cover the trust boundary. The corrected command now fails on
the intended red baseline before invoking Clang:
`vector_lockstep_simd_quad.dsp` reports `Fallback(EventCertificate)`, effective
scalar mode, and 4243 required events against the 4096 limit. Formatting,
xtask Clippy with warnings denied, and both focused xtask tests pass.

## Plan remediation for the lockstep SIMD false positive

Commit date: 2026-07-16
Source commit: this commit

Audited the native-SIMD evidence after comparing the exact CLI output with
scalar C++. At default `-vs 32`, `vector_lockstep_simd_quad.dsp` requires 4243
expanded event-certificate entries against the 4096 limit, so `-vec` falls back
and emits byte-identical scalar C++. The 14 four-wide LLVM operations reported
by the previous gate came from that scalar fallback. At `-vs 24`, checked
vector FIR is retained, but its four independent `vstate_*_tmp` histories yield
zero four-wide LLVM operations; Clang reports SLP as possible but not
beneficial.

Added an English remediation plan that preserves the existing legality bundle
and planar ABI while specifying compact repeated event evidence,
register-carried delay-one lockstep state, later SoA support for longer delays,
and a corrected native-SIMD trust boundary. The corrected gate must establish
`VectorPipelineStatus::Certified` before generating C++, attribute vector IR to
the lockstep region, and reject scalar fallback as a negative test. The plan
defines phased commits, independent checker mutations, bit-exactness gates, and
default `-vs 32` acceptance criteria.

## Verify profitable lockstep C++ lowering produces native SIMD

Commit date: 2026-07-16
Source commit: this commit

Added `xtask lockstep-simd-check`, a target-independent native-code evidence
gate. It compiles the three complex lockstep corpus cases through checked
vector C++, invokes Clang with `-O3 -ffp-contract=off -S -emit-llvm`, and
requires at least ten four-wide floating-point operations in optimized LLVM IR.
The gate observes 14 operations for the recursive bank, 17 for the bank with a
downstream reduction, and 14 for the bank beside an unrelated branch.

The gate intentionally does not use fast-math, force the vectorizer, or infer
SIMD from source-loop fusion alone. The elementary pair and quad corpus cases
remain legality/structure tests: their bodies are too small for the local Clang
cost model to choose SLP, whereas the new profitability-oriented examples prove
that the emitted lockstep shape becomes real SIMD when enough work is present.
Rustdoc documents this boundary and the check remains independent of a specific
machine instruction syntax by inspecting optimized LLVM IR.

**Correction:** this entry's 14/17/14 counts are invalid as lockstep evidence.
The gate omitted the effective pipeline-status check and measured scalar
fallback. The remediation entry above and
`porting/lockstep-simd-remediation-plan-2026-07-16-en.md` supersede the claim.

## Add complex and partial lockstep corpus coverage

Commit date: 2026-07-16
Source commit: this commit

Three four-lane recursive corpus cases extend the original structural examples.
One contains a sufficiently rich recursive arithmetic chain, one feeds a
non-lane reduction, and one coexists with an unrelated feed-forward branch.
The two mixed cases each emit two physical sample loops, proving that lockstep
bundling can remain local to one subgraph instead of consuming the complete DSP.

All three cases are bit-exact between scalar and checked vector execution for
both loop variants and all four scheduling strategies. The elementary pair
continues to cover optimization levels zero and maximum because the larger
corpus bodies exceed the bounded event-certificate budget at the gate's default
vector size. Rust golden snapshots were added for the three new corpus sources.

## Complete the lockstep instance-vectorization extension

Commit date: 2026-07-16
Source commit: this commit

Section 8 of the signal-level vectorization plan is now marked implemented.
The maintained impulse harness was rebuilt in release mode, then its complete
interpreter corpus was compiled and compared under both `-vec -lv 0` and
`-vec -lv 1`. Each variant passes 92 expected DSPs; `subcontainer1` is the
single pre-existing documented exclusion from the 93-file corpus.

The completed feature is an adapted internal representation with no public
API, CLI, ABI, or planar-I/O compatibility impact. Exact independent recursive
instances are bundled automatically; unsupported shapes, dependencies, effect
conflicts, fused-delay slices, and near-isomorphic bodies retain their previous
loop forms.

## Add accepted and rejected lockstep corpus coverage

Commit date: 2026-07-16
Source commit: this commit

The committed corpus now contains exact two- and four-instance recursive
families plus a near-isomorphic add/sub pair that must remain unbundled. A
compiler integration test runs the accepted pair through scalar and checked
vector interpreter paths for both loop variants and all four scheduling
strategies, comparing every output bit. A C++ FIR-backend assertion confirms
that two logical recursive lanes produce exactly one physical `i0` sample
loop.

The accepted pair also joins the representative vector interpreter optimizer
gate, which compares `opt_level=0` with the maximum optimizer level under both
loop variants and every scheduling strategy. This guards the scalar-operation
and FMA-contraction policy through optimization.

Validation: all 25 `compiler/tests/vector_mode.rs` cases pass; the focused
lockstep test covers 16 scalar/vector configurations; `vector-interp-opt-check`
reports 40 matching optimized/unoptimized traces; Clippy passes with warnings
denied for `compiler`, `transform`, and `xtask`; and `golden-check` accepts the
three generated Rust snapshots together with the complete golden corpus.

## Emit one physical FIR sample loop per lockstep bundle

Commit date: 2026-07-16
Source commit: this commit

FIR assembly now preserves every logical lane record for routing and state
ownership while emitting one physical `i0` sample loop per accepted lockstep
bundle. Its body concatenates the unchanged scalar lane iterations in canonical
lane order. The external `compute` ABI and all chunk transports remain planar;
no reassociation, explicit SIMD operation, or new FMA contraction policy is
introduced.

The independent assembly checker rejects missing, split, reordered, or
duplicated top-rate lane loops. Bundles inside one clock island must form one
canonical contiguous lane sequence under the island's existing outer sample
loop. The bounded event certificate now models both fused-delay groups and
lockstep bundles as sample-interleaved execution units.

Validation: `cargo fmt --all`,
`cargo clippy -p transform --all-targets -- -D warnings`, and all 363
`transform` library tests pass. The structural mutation test confirms that two
separate physical sample loops are rejected for a two-lane bundle.

## Detect and schedule exact lockstep recursion bundles

Commit date: 2026-07-16
Source commit: this commit

Production signal-level vector planning now automatically invokes lockstep
detection under `-vec`. The deterministic producer considers independent
top-rate recursive loops that are not already part of a fused-delay slice,
requires matching root decorations, proves pairwise graph incomparability and
effect commutation, and confirms exact prepared-signal shape by parallel
traversal. Unsupported constructors and near-isomorphic bodies remain ordinary
recursive loops without changing compilation behavior.

Accepted lanes retain their stable logical loop ids and recursion-group ids.
The generic epoch scheduler projects each bundle to its representative as one
DAG node, verifies that unit schedule, then expands the member ids canonically
for downstream routing. State planning resolves recursion groups through the
bundle lane records. A structural regression test proves that a two-lane
bundle has one scheduler node while the public execution schedule still covers
both logical loops.

Validation: `cargo fmt --all`,
`cargo clippy -p transform --all-targets -- -D warnings`, and all 362
`transform` library tests pass, including the lockstep producer, rejection,
verifier, and scheduler cases.

## Add the lockstep schema-v2 trust boundary

Commit date: 2026-07-16
Source commit: this commit

The in-memory `VectorPlan` and its JSON boundary now use schema version 2.
`LoopKind::Lockstep { width }`, planar/interleaved transport layout,
`LockstepBundleRecord`, lane records, root witnesses, shape hashes, and explicit
leaf mappings are first-class certificate data. Version-1 plans fail closed at
the v2 verifier rather than silently acquiring defaults.

The plan-local checker independently validates canonical membership, width,
epoch and clock agreement, graph incomparability, effect commutation, witness
coverage, and transport-layout consistency. A second prepared-forest checker
confirms every hash candidate by constructor-for-constructor traversal and
reconstructs the exact leaf map; unsupported signal shapes remain ineligible.
Rejecting mutations cover schema version, bundle width, dependency-connected
lanes, leaf-map corruption, and shape-hash corruption.

Validation: `cargo fmt --all`, `cargo check -p transform --all-targets`, the 40
vector-plan verifier tests, and the two prepared-isomorphism tests pass. The v2
JSON schema parses successfully with `python3 -m json.tool`.

## Freeze the lockstep implementation gate

Commit date: 2026-07-16
Source commit: this commit

Section 8 of the signal-level vector-mode port plan now records the decisions
that precede implementation. Lockstep detection is automatic under `-vec` and
adds no public option. The Rust representation preserves logical lane loop
identities inside a canonical `LockstepBundleRecord`; scheduling and assembly
still expose one execution unit and one physical sample loop. This adaptation
keeps the original graph available to the independent checker for dependency,
epoch, and effect validation.

The section also freezes five pass gates covering schema-v2 mutations,
prepared-signal isomorphism, physical FIR fusion, differential corpus coverage,
the vector impulse compilation matrix, and the mandatory workspace quality
gates.

## Group vector FIR modules under a dedicated submodule

Commit date: 2026-07-16
Source commit: this commit

The `signal_fir` `vector_*.rs` files are now grouped under
`crates/transform/src/signal_fir/vector/`, with the prefix removed from their
names (`plan.rs`, `analysis.rs`, `lower.rs`, etc.). The parent module exposes
the new `signal_fir::vector::*` namespace while retaining the existing
internal/public aliases so that pipeline paths remain compatible during this
reorganization.

Validation: `cargo fmt --all` and `cargo check -p transform` pass.

## Validate state-aware scalar load CSE against impulse oracles

Commit date: 2026-07-16
Source commit: this commit

Completed Phase E of the state-aware scalar FIR load-CSE plan. The native C++
impulse matrix passed for all 92 applicable DSPs under every scalar scheduling
strategy (`-ss 0..3`); `subcontainer1` remains the pre-declared shared compile
gap. APF and Karplus were additionally compared successfully through C, the
interpreter, Cranelift, Wasm, and AssemblyScript. Julia has no impulse runner
in this harness, so its generated vector APF and structural Drop tests are the
applicable validation.

Current release scalar compilation measured 0.04 s for APF, 0.03 s for
Karplus, and 7.95 s for `reverb_designer`, which is in line with the earlier
frontend-cost audit. APF/Karplus now each have one materialized prior-state
read reused by the history copy; no cost regression was observed. A fresh
32-case vector interpreter opt-level matrix was started but produced no
incremental result within the session window and was stopped; it is explicitly
not claimed as validation. The vector scheduler/certifier itself is untouched
by this scalar pass and retains its pre-existing coverage gate.

Focused formatting, Clippy, transform/codegen/compiler tests, backend Drop
tests, and impulse checks passed. The broad `cargo test --workspace
--all-targets` run stalled silently in the pre-existing `box-ffi` batch and
was interrupted; a separate golden-check run likewise produced no progress
within the session window and was interrupted. Neither interrupted global run
is represented as a passing quality gate.

## Share conservative pure-Drop elision across all active backends

Commit date: 2026-07-16
Source commit: this commit

Audited every implemented `FirMatch::Drop` consumer after the C/C++ cleanup.
AssemblyScript and Julia were still emitting pure roots as an expression
statement or `_ = expression`; both now use the same shared conservative FIR
predicate as C/C++. The predicate has Rustdoc documenting C++ provenance and
the crucial safety asymmetry: unknown nodes and every foreign call are kept.

The interpreter, Cranelift, and Wasm lowering paths now also skip only values
accepted by that predicate. Cranelift's subset checker recognizes such drops
as no-ops, while effectful drops continue through the existing lowering path.
Focused backend tests prove pure arithmetic is elided and foreign calls remain
emitted/evaluated. Vector APF source generation succeeds for AssemblyScript
and Julia after the shared change.

## Preserve table-load cache across disjoint scalar state stores

Commit date: 2026-07-16
Source commit: this commit

Karplus exposed an overly broad conservative barrier: the independent scalar
commit `fRec205 = fRecCur205` cleared a cached `fRec214[1]` even though FIR
models table storage by an explicit distinct name. The effect summary now
keeps a table cache across an ordinary `StoreVar` while still treating a call
or tee nested in its value as a barrier.

The generic test proves reuse across an unrelated scalar store; a Karplus C++
structural witness now requires `fRec214[2] = fTemp0` while preserving both the
scalar `fRec205` commit and the recursive `fRec214[1]` commit. Its scalar
`-ss 0` 60,000-sample impulse comparison matches the C++ oracle.

## Reuse proven state loads in generic scalar table-store operands

Commit date: 2026-07-16
Source commit: this commit

Extended the Phase C scalar cache from redundant load declarations to direct
table-load operands in later straight-line statements. Consequently the APF
history shift now emits `fRec204[2] = fTemp0`, where `fTemp0` is the already
proven old `fRec204[1]` value. The distinct final store
`fRec204[1] = fRec204[0]` remains explicit and ordered.

This is not an APF rule: the generic proof requires a matching literal table
location and no intervening aliasing write. Dynamic read indices no longer
enter the cache at all, preventing unrelated `table[i]` and `table[j]` reads
from being conflated. Calls and tee-containing expressions remain opaque local
ordering barriers. Focused FIR and generated-C++ tests cover both properties.

## Integrate scalar load reuse after FIR materialization

Commit date: 2026-07-16
Source commit: this commit

Completed Phase D of the state-aware scalar FIR load-CSE plan. The pass now
runs after ordinary scalar `materialize_shared_values`, so it consumes the
scheduled `fTemp*` declarations as cache keys and cannot alter scheduling or
create a backend-specific form. All scalar FIR consumers receive the same
rewritten program; the earlier C/C++ pure-`Drop` cleanup remains an emitter-only
optimization.

A C++ integration regression compiles `tests/impulse-tests/dsp/APF.dsp` and
checks that the duplicate `fRec204[1]` materialization is absent while the
`fRec204[2] = fRec204[1]` and `fRec204[1] = fRec204[0]` history shift remains.
The test runs that library-heavy fixture on a 32 MiB test thread to avoid a
test-runner stack limit that is unrelated to generated code.

## Reuse straight-line scalar state-load materializations

Commit date: 2026-07-16
Source commit: this commit

Completed Phase C of the state-aware scalar FIR load-CSE plan. The new private
pass consumes stable stack temporaries already emitted by scalar scheduling;
it does not invent declarations or specialize a tested DSP. A repeated direct
table load is replaced by the first temporary only when every intervening
write is proven to target a different literal slot.

The implementation retains `state[2] = state[1]` and `state[1] = state[0]`
as distinct ordered writes. Exact aliases, dynamic indices, calls, and nested
execution scopes reject reuse. Regression tests additionally verify the use of
a removed temporary is rewritten to the first temporary, rather than merely
removing dead source text.

## Define conservative scalar table-effect summary

Commit date: 2026-07-16
Source commit: this commit

Completed Phase B of the state-aware scalar FIR load-CSE plan. The private
`transform` summary tracks table reads and writes by name, access class, and a
proof-carrying index (`Int32` literal or unknown). It classifies calls, tee
writes, local stores, and nested control as barriers. Array shifts invalidate
the named table conservatively.

The safety asymmetry is documented in Rustdoc and tested: a missed alias only
loses an optimization, whereas a false non-alias proof could change a recursive
DSP. This summary is not exposed as a general FIR effect API.

## Establish state-aware load-CSE FIR invariant fixture

Commit date: 2026-07-16
Source commit: this commit

Started Phase A of the state-aware scalar FIR load-CSE plan. A compiler-internal
fixture now expresses the generic recursive-history sequence: two reads of
`state[1]`, an update of `state[0]`, and the explicit `state[2] = state[1]`,
then `state[1] = state[0]` shift. Its structural test ensures the existing
expression CSE does not reorder or remove the history stores. This is not tied
to APF or another corpus program; APF remains only a later emitted-code witness.

The baseline remains the C++-matching scalar behavior observed under all four
`-ss` strategies. The next phase adds the conservative alias/effect matrix that
will decide which of these repeated reads may be reused.

## Require corpus impulse validation for state-aware scalar load CSE

Commit date: 2026-07-16
Source commit: this commit

The proposed state-aware scalar FIR load-CSE plan now requires validation with
the configured `tests/impulse-tests` DSP corpus and its C++ oracle. Scalar
coverage must run under all four `-ss` strategies; any checked-vector path
touched by the FIR change must also run the `-lv 0/1 × -ss 0..3` matrix.
Per-DSP tolerances and explicit unsupported limitations remain authoritative.

## Elide redundant pure C-family drops

Commit date: 2026-07-16
Source commit: this commit

The checked vector lowerer uses `Drop` statements as temporary FIR roots while
it materializes and verifies shared expression DAGs. The C and C++ emitters
were rendering every one as `(void)(...)`, so a vector APF build contained
dead constants, state reads, temporary loads, and pure arithmetic statements.

The shared C-family emitter now elides a `Drop` only when its FIR value is
structurally known to be side-effect-free. The proof is deliberately
conservative: foreign and math calls, tee writes, allocations, and unknown
nodes remain emitted. This removes the noisy `fSampleRate`, `6.283...`, and
`fVec*Temp` examples without changing FIR construction or the other backends.

Validated with the C-family unit suite and a conservative-purity regression
test, plus native C++ APF `-vec -lv 1` comparison against the 60,000-sample
reference oracle.

## Audit and correct scalar scheduling cost

Commit date: 2026-07-16
Source commit: this commit

Completed vector coverage recovery Phase 6. Release binaries built from the
parents of the authoritative `-ss` and vector-option commits established that
`reverb_designer` was already evaluation-bound (about 7.1 seconds of a 7.2
second compile) before either feature. The current release remains within 1.5%
of those historical totals, so the costly scalar frontend is not caused by
vectorization.

The audit did find and remove a genuine generic scalar scheduling overhead.
Scalar lowering had built vector occurrence/condition facts although it only
needs direct effects to orient conflicts. It now derives a compact direct-effect
map, and effect orientation groups nodes by resource instead of checking every
pair of stateful nodes. Unknown/impure foreign calls retain their complete
ordering barrier. On `reverb_designer`, scalar effect analysis and orientation
fell from approximately 140 ms and 157 ms to 2 ms and 8 ms respectively; the
whole signal-to-FIR phase is now about 136 ms.

The compiler's `-time` report now splits FIR planning, verified
preparation/normalization, hierarchical graph construction, scalar effects,
effect orientation, scheduling, vector certification, and lowering. Scalar
mode is structurally tested to retain `NotRequested` vector status and no
fallback detail. Direct scalar effects are compared against full vector facts,
and the foreign-barrier ordering test remains in place.

Validated with two 15,000-sample C++ impulse-oracle comparisons for
`reverb_designer.dsp` and `cubic_distortion.dsp` under scalar `-ss 0`, the
focused structural tests, formatting, clippy, workspace tests, and golden
snapshots. Full measurement method and remaining verified-preparation cost are
documented in `porting/scalar-compilation-history-and-cost-audit-2026-07-16-en.md`.

## Add checked vector-retention gates

Commit date: 2026-07-16
Source commit: this commit

Completed vector coverage recovery Phase 5. `count_vector_corpus` now reports
single/double precision, portable paths, and stable fallback reason codes. Its
complete 16-cell matrix (precision × `-lv` × `-ss`) is stored under
`tests/vector-coverage/`: every mode has 49 certified DSPs, 43 scalar
fallbacks, and the pre-existing SIGGEN error. The per-file report remains
reviewable, while a generated 49-file intersection is the only input to the
vector throughput benchmark.

The new `vector-coverage-check` rejects incomplete or duplicated report modes,
stale benchmark lists, count/reason inconsistencies, and fallback entries that
are not explicitly scalar. It recompiles every claimed certified pair and
requires checked-vector status, effective vector mode, no diagnostic detail,
and the final `vindex`/`vcount` FIR chunk-driver shape. The local run retained
all 784 claimed pairs. Ubuntu CI installs the Faust standard library and runs
this guard, followed by an interpreter metamorphic test.

`vector-interp-opt-check` exercises delayed recursion (`APF`), structural
recursion (`capture`), `prefix` state, and UI lifecycle (`UITester`) under both
vector layouts and all four strategies. All 32 optimized/unoptimized checked
vector traces matched.

Validated with:

- full 16-mode × 93-DSP float/double corpus matrix;
- `cargo run -p xtask -- vector-coverage-check` (784 retained pairs);
- `cargo run -p xtask -- vector-interp-opt-check` (32 matching traces);
- focused `xtask` unit tests, formatting, clippy, and benchmark-list dry run.

## Certify symbolic recursion carriers and state-mediated delay routing

Commit date: 2026-07-16
Source commit: this commit

Completed vector coverage recovery Phase 4. The decoration certificate now
identifies `SYMREC` and `SYMREF` as checked structural carriers, and the vector
plan preserves that fact explicitly. These tuple records keep authoritative
analysis facts but emit no standalone FIR value; separately planned scalar
projection bodies remain executable. Mutation tests reject a forged carrier
fact, and the independent plan/route checks continue to require definitions
for every non-structural signal. This avoids evaluating recursion bodies in a
carrier's unrelated loop and removes false cross-loop transports.

Variable delays whose certified amount interval can include zero now treat
their carrier dependency as state-mediated. The plan does not synthesize a
value transport that scalar lowering never performs. A crossing is accepted
only when both loops belong to one checked fused serial group; otherwise it
fails closed with an explicit `UnfusedImmediateDelayCrossing` diagnostic.

The 93-DSP corpus moved from 44 to 49 certified files. The additions are
`capture`, `echo`, `karplus`, `quadecho`, and `stereoecho`. The residual
waterfall is 26 event-bound fallbacks, 13 specific unfused-delay plan
fallbacks, three intentional pure-lowering fallbacks, one soundfile UI
fallback, and the existing SIGGEN error. `smoothdelay` now clears routing and
state verification and reaches the bounded-event rejection (4,967 events
versus the 4,096 limit).

All five new files passed the 60-case native C++ matrix: scalar `-ss 0..3` and
vector `-lv 0/1 x -ss 0..3`, each over 60,000 frames. The 120 focused vector
tests pass, including `BlockReverseAD` separation and colocated recursive
projection fixtures. `reverb_designer` profiling measured 60--61 seconds for
the scalar frontend, 5.83 seconds for the complete vector plan, and 67.0
seconds total in the reproducible timed vector run; no stage-level regression
from this slice was found.

The full workspace gate also exercised clocked recursion. Structural carriers
aggregate state effects but own no execution loop, so clock/state ownership is
verified on their executable projection bodies; the `clock-state` fixture is
certified under all four scheduling strategies.

Validated with:

- a complete `count_vector_corpus` sweep over all 93 DSPs;
- 60 native C++ impulse comparisons for the five newly certified files;
- `cargo fmt --all -- --check`;
- `cargo clippy --workspace --all-targets -- -D warnings`;
- `cargo test --workspace --all-targets`;
- `cargo run -p xtask -- golden-check`.

## Certify read-only vector tables and indirect recursive paths

Commit date: 2026-07-16
Source commit: this commit

Continued vector coverage recovery Phase 4 with checked read-only generated
tables. Finite waveform, literal, and pure computed generators are interpreted
at compile time and materialized as immutable typed declarations; `rdtable`
uses them with scalar-compatible bounds. Mutable table writes and unknown
foreign-function effects remain fail-closed. Generic routed-definition and CSE
identity handling was also corrected without consulting corpus filenames.

The 93-DSP counter moved from 27 to 44 certified paths. The 17 new files are
`BPF`, `math_simp`, `midi_tester`, `osc`, `phasor`, `pow`, `priority`,
`reverb_tester`, `select2`, `spat`, `switcher`, `table`, `vumeter`, `waveform2`,
`waveform3`, `waveform4`, and `waveform6`. The remaining waterfall is 31 event
fallbacks, 16 specific pure-lowering fallbacks, one soundfile UI fallback, and
the pre-existing SIGGEN error.

Native sample comparison found a generic three-loop delayed-recursion defect:
a delayed read could reach its recursive writer through an intermediate loop
that was outside the fused serial group. The producer now closes every
same-sample data path from delayed read to writer and records every internal
transport. The independent checker reconstructs data-only reachability and a
mutation test proves that removing the intermediate loop is rejected. Fused
groups are scheduling envelopes whose members inherit external dependencies;
the event certificate independently models their sample-major execution and
managed state transitions. A corpus-independent compiler fixture exercises the
indirect path under both loop variants and all four scheduling strategies.

Compile-cost profiling added a conservative event-count lower bound before
event materialization, reducing `spectral_level` from about 29.0 to 20.7
seconds. `reverb_designer` measured about 61.8 seconds scalar and 68.3 seconds
for a vector request. Stage timing attributes only about 0.3 seconds to the new
fused-group work; the complete pre-existing vector-plan stages cost about 5.8
seconds. Scheduling-envelope dependencies are precomputed once per epoch to
avoid a new repeated reconstruction cost. The dominant scalar frontend cost is
left visible for the planned Phase 6 historical audit.

Validated with:

- producer/checker tests for immutable generated tables and indirect fused
  paths, including forged-artifact mutation cases;
- scalar/vector interpreter bit-exactness over both loop variants and all four
  scheduling strategies;
- a complete `count_vector_corpus --json` sweep over all 93 DSPs;
- the 204-case native C++ impulse matrix for all 17 newly certified DSPs;
- `cargo fmt --all -- --check`;
- `cargo clippy --workspace --all-targets -- -D warnings`;
- `cargo test --workspace --all-targets`;
- `cargo run -p xtask -- golden-check`.
