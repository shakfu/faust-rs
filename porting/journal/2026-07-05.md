# 2026-07-05

## Propagate memoization plan review fixes

Commit date: 2026-07-05
Source commit: this commit

Tightened `porting/cpp-propagate-eval-memoization-port-plan-2026-07-04-en.md`
after review before implementation. The plan now:

- fixes the §2.1 wording to list three residual follow-ups, not two;
- makes the validation gate match repository policy explicitly:
  `cargo fmt --all`, workspace clippy with `-D warnings`,
  `cargo test --workspace --all-targets`, and `cargo run -p xtask -- golden-check`;
- adds a grouped-UI replay regression to prove memo keys include the normalized
  group path when the same widget node appears under different UI groups;
- clarifies that the P2 `liftn` aperture fast-path should also populate
  `memo.liftn`, and notes that the `< threshold` predicate matches the existing
  `tlib` substitution guard.

No code behavior changed; this is a documentation/planning correction only.

## Upstream re-analysis: tlib speedup commit 688c0a478 folded into the memoization plan

Re-analyzed `/Users/letz/faust` since `3c44e5cb` (7 commits: the 3 already
covered by the memoization plan, plus 4 new). Only `688c0a478` — "tlib:
faster version of the Tree library (array-based make, non-virtual
Garbageable)" — is relevant; `f607b2780`/`d80e2271b`/`29cda8807` are Windows
release/packaging fixes.

Updated `porting/cpp-propagate-eval-memoization-port-plan-2026-07-04-en.md`:
new §1.3 (array-based `make` overloads eliminating the temporary `tvec` on
intern lookups; non-virtual `Garbageable`; tlib decoupled from
`global`/`exception` into a standalone library — `-hlf` survives as a call to
the new `setHashLoadFactor()` API), three new mapping-table rows, and §2.1
item 3. Verdict repeats the pattern: upstream converges on faust-rs's design
(arities 0/1/2 already intern allocation-free through tuple-keyed maps; no
vtables; `crates/tlib` standalone from day one) — **except one real
residual**: `TreeArena::intern`'s arity-≥3 arm builds its `Arc<[TreeId]>` key
*before* the `interner_n` probe, so every wide-node intern — cache hit
included — pays a heap allocation plus refcount traffic, the exact pathology
upstream just removed from `CTree::make`. Fix shape documented: borrowed-key
probe (hashbrown raw-entry or `Equivalent`-style key), `Arc` materialized
only on insert; hot cons cells are arity 2 and unaffected, so measure with
the P0 profiler on UI-heavy and FFT-like files.

## C-family follow-ups: drifts 2 and 6 closed, asc NaN fix — all 7 catalogued drifts now resolved

Second pass on `porting/c-family-emitter-core-plan-2026-07-04-en.md` (see
"Follow-ups outcome (2026-07-05, second pass)" in §4), completing the drift
catalogue opened by the first pass below.

**DRIFT 2 — `Bitcast`, both backends.** Oracle work first: upstream
`generateBitCast` (`instructions_compiler.cpp:1248`) is only produced in
`-ftz 2` mode; verified live on `process = + ~ *(0.999);` that upstream cpp
emits `*reinterpret_cast<int*>(&fTemp0SE)` while upstream **c emits garbled,
uncompilable code** — the `// TODO : does not work` in `c_instructions.hh:376`
is accurate, so no valid upstream C oracle exists. Decisions: cpp now
byte-matches the upstream form; c emits `*((T*)&v)`, the C-cast transposition
of the C++ form (what the broken upstream visitor evidently intends); the
memcpy alternative was rejected (needs statement context) with rationale in
the plan doc. Placement: shared arm in `emit_value_common` via three new
`CFamilySyntax` leaves (`bitcast_open/mid/close`); cpp's former per-language
`bitcast<T>(v)` arm — which named a template nothing defines and could never
have compiled — deleted. No `.dsp` path reaches FIR `Bitcast` yet (no
`-ftz 2` port), so both backends are locked by regression tests.

**DRIFT 6 — reset-fallback replay.** `StructInit`/`TableInit` and both
collectors moved into `c_family` (generic over the backend error type)
instead of duplicating ~70 lines; c delegates through thin wrappers keeping
its stable error codes, and cpp's synthesized `instanceResetUserInterface()`
now replays declared state initializers like c does. Dormant-path fix: the
standard pipeline always supplies explicit reset bodies, so no pipeline
output changed. cpp's `emit_dsp_contract_methods` became fallible with a
`DspContractEmitInput` struct mirroring c's `CApiEmitInput`.

**asc `trim_float`.** `NaN`/`Infinity`/`-Infinity` (AssemblyScript globals)
replace the invalid `NaN.0`/`inf.0` literals; `-0.0 → 0.0` added.
Deliberately not unified with `c_family::trim_float` — C macros vs
AssemblyScript globals genuinely differ.

**Phase 6 skipped with rationale** (stub decision is six `any()` one-liners
in cpp vs `find()`s interleaved with emission in c; unifying the decision
without the LANG-heavy rendering churns c for near-zero absorbed lines).
**Julia note added** to plan §5: `string_literal` is verbatim-adoptable
today; the value/stmt seams and collectors are confirmed non-transferable.

Guardrails green: fmt, workspace clippy `-D warnings`, codegen 278 tests
(+4 regression), compiler tests, `golden-check` 190/190,
`cpp-backend-diff-report` 8/8 with 0 DIFF. Method: Sonnet 5 sub-agent
end-to-end (first pass without session-limit interruption), independently
re-verified in the main session.

## Shared C-family emitter core: c/cpp backends deduplicated, 4 drifts closed (Phases 1–5)

Executed Phases 1–5 of `porting/c-family-emitter-core-plan-2026-07-04-en.md`
on branch `main-dev`, addressing improvement #5 of the port quality review:
the `c` and `cpp` backends were near-parallel implementations (~1 700 lines,
42–44 functions each) tracking each other function for function, with seven
confirmed silent drifts (same function, different behavior) between them.

New module `crates/codegen/src/backends/c_family.rs` (1 032 lines, 9 unit
tests) owns the shared emission core, parameterized by plain data — no traits:

- `CFamilySyntax` — fixed token leaves (`bool`/`int`, `nullptr`/`NULL`,
  UI glue threading, `FAUSTFLOAT` cast spelling, switch `default` style).
- `CFamilyValueCtx` / `CFamilyStmtCtx` — per-language seams (fn pointers for
  capture-free rules, `&dyn Fn` for `Options`-aware rendering, `EmitNodeFn`
  recursion seams) consumed by `emit_value_common` / `emit_stmt_common`.
- Shared leaf functions: `emit_binop(_expr)`, `emit_type`, `trim_float`,
  `format_float32`, `string_literal`, `emit_static_tables`.
- The core owns only the **intersection** of both backends (`None`-contract:
  unmatched arms fall back to each backend's own language-only arms and
  error type), so unification cannot silently grant one backend arms the
  other had.

20 statement arms and all common value arms are now shared; per-language
remainders: `AddMetaDeclare`, `Label`, cpp-only `DeclareFun`, plus cpp's
`Quad`/array literals, `NewDsp`, `Bitcast`.

Drift outcomes (numbering from the plan §2):

| Drift | Status | How |
|---|---|---|
| 1 — cpp dropped `DeclareTable` initializer values | **closed** | shared arm, c as reference; regression test |
| 2 — c lacks `Bitcast`; cpp spelling matches no oracle | deferred | standalone fix against upstream `*reinterpret_cast<T*>(&v)`; unifying now would launder the wrong spelling |
| 3 — `-0.0` normalization missing in cpp | **closed** | shared `trim_float`; regression test |
| 4 — `\r`/`\t` escaping missing in cpp | **closed** | shared `string_literal`; regression test |
| 5 — `FAUSTFLOAT` cast missing on cpp UI widgets | **closed** | `CFamilySyntax` cast leaves; oracle = in-repo upstream golden `tests/golden/cpp/rep_09_ui_slider/` (`FAUSTFLOAT(0.5f)`) |
| 6 — cpp lacks `StructInit`/`TableInit` reset replay | open | Phase 6 follow-up |
| 7 — c lacked `Control`/`WhileLoop` | **closed** | shared arms; regression test |

Single-owner decision: the former cpp-only `DeclareStructType` /
`DeclareBufferIterators` / `ShiftArrayVar` / `IteratorForLoop` arms were
silent comment stubs (`IteratorForLoop` even unrolled its body once — wrong
code instead of an error); both backends now hard-error, with a rejection
regression test. These nodes only occur on interp/cranelift/asc paths today.

Guardrails green after every phase: `cargo fmt`, workspace clippy
`-D warnings`, `cargo test -p codegen` (273) and `-p compiler` (333),
`golden-check` 190/190, `cpp-backend-diff-report` 8/8 with 0 DIFF. The only
rust golden artifacts are stdout fingerprints, so the intentional DRIFT 5
output change is locked by the updated `cpp_fir_sine_phasor.rs` integration
test rather than a golden regeneration.

Result: `cpp/mod.rs` 1 756 → 1 430, `c/mod.rs` 1 676 → 1 326 — ~680 lines of
duplicated surface removed while *gaining* four bug fixes. The drift class
"fixed in one backend, forgotten in the other" is structurally impossible for
everything the core owns. Upstream C++ precedent: `Text.hh/.cpp` is already
shared across its ~12 backends.

Method: analysis, plan, and Phases 1–2 plus most of 3–5 by a **Sonnet 5
sub-agent** (twice interrupted by session limits); completion, stabilization,
guardrail verification, and documentation by Fable in the main session.

## Upstream optimization analysis + port quality review (docs)

Two analysis documents written on 2026-07-04 (recorded here with the work
they led to):

- `porting/cpp-propagate-eval-memoization-port-plan-2026-07-04-en.md` —
  analysis of upstream commits `cb6891ae` (growable CTree/Symbol hash
  tables), `2aec3bff0` (plain-struct propagate memo key, lazy per-node
  properties, propagation profiler), `0ef3eb314` (`property2<Tree>` binary
  memoization). Key finding: all three converge on designs faust-rs already
  has (side-table `AHashMap` memoization, no per-node property lists); the
  one genuinely portable item is propagate-result memoization itself
  (`propagate_in_slot_env` still has none — plan phases P0 profiler /
  P1 memo / P2 `liftn` aperture fast-path, with FAD-seed bypass and
  per-top-level-call lifetime as the safety conditions).
- `porting/rust-port-quality-review-2026-07-04-en.md` — whole-workspace
  quality review (30 crates, ~141 k LOC): clippy/fmt/test/CI posture,
  unsafe/panic/expect discipline, four-level differential validation as the
  port's strongest asset, and a 10-item prioritized improvement list. Item
  #5 (near-parallel textual backends) is what the C-family emitter core
  above executes.
