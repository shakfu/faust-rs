# JOURNAL 2026-02-28

Entries originally recorded under this journal day.

- Sorted by Git commit recency (newest first).
- `Commit date` is the commit day for the entry addition.

### A5 executed: parser API now exposes deterministic import-used file list
Commit date: 2026-02-28

Executed ticket `A5` from
`porting/phases/phase-3-parser-parity-status-2026-02-28-en.md`.

Changes:

- `crates/parser/src/lib.rs`
  - extended `ParseOutput` with `used_files: Vec<PathBuf>`.
  - `parse_program(...)` now returns empty `used_files` for pure in-memory parse.
  - `parse_file_with_imports(...)` now propagates `SourceReader::used_files()`
    into parser output.
- `crates/parser/tests/api_bridge.rs`
  - added deterministic-order test:
    `parse_file_with_imports_exposes_deterministic_used_files_order`.
  - strengthened import parsing bridge test to assert `used_files` content.

Validation:

- `cargo test -p parser --all-targets` ✅
- `cargo test -p compiler --all-targets` ✅

### A4 executed: structural parser differential gate expanded in production parser tests
Commit date: 2026-02-28

Executed ticket `A4` from
`porting/phases/phase-3-parser-parity-status-2026-02-28-en.md`.

Changes:

- added `crates/parser/tests/structural_cpp_differential.rs`:
  - `production_parser_structural_shapes_align_with_cpp_acceptance`:
    validates representative parser tree shapes for precedence, unary, and
    application forms.
  - `production_parser_import_heavy_fixture_matches_cpp_acceptance`:
    validates import-heavy parsing path through `parse_file_with_imports`.
  - both tests optionally cross-check acceptance against C++ reference parser
    when `FAUST_CPP_BIN` (or `/usr/local/bin/faust`) is available.

Validation:

- `cargo test -p parser --all-targets` ✅
- `cargo test -p compiler --all-targets` ✅

### A3 executed: parser diagnostic codes now use explicit class mapping in parse loop
Commit date: 2026-02-28

Executed ticket `A3` from
`porting/phases/phase-3-parser-parity-status-2026-02-28-en.md`.

Changes:

- `crates/parser/src/context.rs`
  - extended `ParserDiagnostic` with optional explicit stable code payload.
  - added `error_with_code(...)` helper to record parser errors with known code.
- `crates/parser/src/lib.rs`
  - parse loop now assigns codes directly by parser error class:
    - `LexError` -> `FRS-LEX-0001` (`LEX_INVALID_TOKEN`)
    - `ParseError` -> `FRS-PARSE-0001` (`PARSE_UNEXPECTED_TOKEN`)
  - bundle conversion now uses explicit parser-local code when present, then
    falls back to severity/message mapping only for uncoded diagnostics.
  - added `parser_code_for_lex_parse_error(...)` helper.

Validation:

- `cargo fmt --all` ✅
- `cargo test -p parser --all-targets` ✅
- `cargo test -p compiler --all-targets` ✅

### A2 executed: import-expanded parser diagnostics now preserve imported file origin
Commit date: 2026-02-28

Executed ticket `A2` from
`porting/phases/phase-3-parser-parity-status-2026-02-28-en.md`.

Changes:

- `crates/parser/src/source_reader.rs`
  - introduced `ExpandedSource` and `SourceLineOrigin` payloads.
  - added `read_file_with_origins(...)` to return expanded text plus per-line
    origin mapping.
  - kept `read_file(...)` compatibility helper.
- `crates/parser/src/lib.rs`
  - parser state now optionally carries source-line origins.
  - parser cursor mapping (`update_cursor_from_span` and parse-error path) now
    resolves location through origin map before recording diagnostics/props.
  - `parse_file_with_imports(...)` now uses `read_file_with_origins(...)`.
- `crates/parser/tests/api_bridge.rs`
  - added regression test:
    `parse_file_with_imports_preserves_imported_file_diagnostic_origin`.

Validation:

- `cargo fmt --all` ✅
- `cargo test -p parser --all-targets` ✅
- `cargo test -p compiler --all-targets` ✅

### A1 executed: production parser ownership moved from facade delegation to in-crate implementation
Commit date: 2026-02-28

Executed ticket `A1` from
`porting/phases/phase-3-parser-parity-status-2026-02-28-en.md`.

Changes:

- `crates/parser`
  - migrated parser implementation files into production crate:
    - `build.rs`
    - `src/context.rs`
    - `src/source_reader.rs`
    - `src/grammar/faustlexer.l`
    - `src/grammar/faustparser.y`
    - `src/lib.rs` now hosts parser implementation directly (no forwarding facade).
  - updated crate dependencies to own parser stack directly (`lrlex/lrpar/cfgrammar`,
    `boxes`, `errors`, `tlib`) in:
    - `crates/parser/Cargo.toml`
- removed runtime delegation model:
  - `crates/parser` no longer imports/forwards `parser_proto::*` for production APIs.
- documentation alignment:
  - updated parser crate/module comments from prototype wording to production ownership wording.

Validation:

- `cargo fmt --all` ✅
- `cargo test -p parser --all-targets` ✅
- `cargo test -p compiler --all-targets` ✅
- `cargo check --workspace --all-targets` ⚠️ blocked by unrelated local untracked file
  `crates/codegen/examples/interp_baseline.rs` (pre-existing workspace condition).

### Parser parity audit + executable closure checklist (A1..A6) and pattern-rewrite plan refinement
Commit date: 2026-02-28

Completed two documentation tracks in `porting/`:

1. Parser parity status (Rust vs C++) with concrete closure tickets.
2. Pattern/rewrite engine plan refinement (shared `crates/patterns`, macro-first examples).

Changes:

- `porting/phases/phase-3-parser-parity-status-2026-02-28-en.md` (new)
  - added audited parser parity status snapshot.
  - documented precise remaining gaps G1..G6 with evidence/impact.
  - added executable checklist tickets `A1..A6` with:
    - tasks,
    - validations,
    - exit criteria,
    - suggested execution sequence.
- `porting/phases/phase-3-parser-en.md`
  - updated status line to reflect validation through slices `1..12`.
  - linked to the new parity status snapshot document.
- `porting/fir-pattern-rewrite-engine-plan-en.md`
  - updated architecture naming to `crates/patterns`.
  - clarified signals-first / FIR-second rollout.
  - expanded macro-based authoring examples (`pattern!`, `rewrite_rule!`, `analysis_rule!`).
  - added explicit "recognize vs rewrite" and pass-integration code examples.

Validation:

- `cargo test -p parser --all-targets` ✅
- `cargo test -p parser-proto --all-targets` ✅
- `cargo run -p xtask -- parser-parity-report` ✅

### FIR checker Rustdoc pass: document remaining internal structures and helpers
Commit date: 2026-02-28

Completed a focused Rustdoc documentation pass in `crates/fir/src/checker.rs`
to cover remaining internal data structures and helper functions.

Changes:

- `crates/fir/src/checker.rs`
  - documented internal struct fields:
    - `VarEntry` (`access`, `typ`, `init`, `is_table`)
    - `ScopeFrame` (`vars`)
    - `ScopeStack` (`frames`)
    - `VerifyCtx` core fields (`store`, `module_id`, `diags`, `symbols`)
  - documented previously uncovered internal functions:
    - `check_compute_io_arity_contract`
    - `check_compute_body_io_access`
    - `register_function_signature`
    - `input_alias_index`
    - `output_alias_index`
    - `funargs_constant_index`

Validation:

- `cargo fmt --all` ✅
- `cargo check -p fir` ✅
