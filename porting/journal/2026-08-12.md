# 2026-08-12

## Emit `declare` values verbatim, matching the Faust lexer

Commit date: 2026-08-12
Source commit: this implementation commit

- The Windows CI job failed `expansion_settles_after_the_second_pass` on the
  one fixture that imports `stdfaust.lib`. Cause: `declare` values were written
  with backslashes escaped, but the Faust lexer's string rule is `"[^"]*"` and
  performs **no** escape processing. Reading an expansion back therefore saw
  the escape itself as data and escaped it again, so a Windows
  `library_path` like `D:\a\faust-rs\faustlibraries\stdfaust.lib` doubled its
  backslashes on every pass and the document never converged.
- The lexer is not at fault: raw strings are a deliberate, self-consistent
  language choice. The writer was, and in two ways — it escaped `\` (which
  corrupts the value, since nothing unescapes it) and `"` (which cannot work
  either, because `\"` still ends the string). Both emission sites now share
  one `declare_string` helper that emits verbatim and refuses a value carrying
  a quote, a case unreachable from source for exactly the same lexer reason.
- The two sites had also drifted apart: `library_path` lines were already
  emitted raw while the metadata header escaped. That asymmetry is what made
  the corruption grow rather than merely be wrong once.
- Added `declare_values_containing_backslashes_survive_re_expansion`, which
  drives a backslash-bearing value through ordinary metadata so any platform
  catches a relapse. No Unix path contains a backslash, which is precisely why
  only the Windows runner saw it; the test removes that dependency.

## Accept `-lang` alongside `-e`

Commit date: 2026-08-12
Source commit: this implementation commit

- `faust-rs -lang cpp -e prog.dsp` was rejected with the usage block. C++
  accepts the same command line and simply expands, so this was a
  compatibility regression introduced by phase E4, not a deliberate
  divergence — and the rationale recorded then ("rejected rather than letting
  one silently win") conflated two different situations.
- `-e --dump-cpp` *is* a conflict: two emitters, one would have to win.
  `-e -lang cpp` is not: `-lang` selects a backend the expansion never uses.
  The CLI already had this exact distinction for `--json --lang`
  (`json_plus_lang_only`), so `-e` now shares that exemption rather than
  introducing a second rule.
- `-lang` is recorded in the expansion's `compile_options` instead of being
  dropped silently, so the document still states the command line that
  produced it.
- Transcript matrix updated accordingly: `err_export_with_lang` became
  `export_with_lang` under the option runs, and `err_export_with_dump_cpp`
  now covers the case that genuinely is an error. Regeneration touched only
  those entries — no other transcript moved.
- Note for readers comparing against C++: `-lang` still has no effect on the
  emitted numerals here. C++ derives the literal suffix from the backend, and
  `-lang julia -e` there produces `3.1415927f0`, which its own parser rejects
  (see `DIFF-BEH-006`).

## Make generated recursion-carrier names program-local

Commit date: 2026-08-12
Source commit: this implementation commit

- Recursion carriers were named `fRec{node.as_u32()}` — after the arena node
  id — so the generated variable depended on how many nodes happened to be
  interned before it. Compiling `020_library_import.dsp` directly produced
  `fRec157`; compiling its `-e` expansion, the same DSP with fewer nodes
  interned on the way, produced `fRec161`.
- Added `RecursionState::carrier_ordinal`, a dense first-request-order
  numbering shared by every site that names a carrier: the struct array
  (`recursion.rs`), its current-sample binding and the two-element state slot
  (`module/state.rs`). All three ask about the same node, so `fRec<n>` and
  `fRecCur<n>` still agree, and distinct nodes still get distinct names — the
  invariant the node id provided for free.
- One correction to the task as filed: the 199 Rust goldens do **not** encode
  generated variable names. They are source hashes (`fnv1a64`), and
  `golden-check` passed unchanged; only `tests/cli-transcripts` records
  generated code. No golden regeneration was needed.
- Removed the renumbering workaround in `expand_corpus.rs::algorithm_only`,
  which now compares raw text, and added
  `generated_names_do_not_depend_on_what_was_evaluated_first`.

## Fix `-e` expansion of `fad`/`rad` programs

Commit date: 2026-08-12
Source commit: this implementation commit

- Investigating the naming drift over the whole DSP corpus — rather than the
  35-fixture expansion corpus — exposed a defect shipped in the `-e` work
  earlier today: **13 `tests/corpus` programs expanded into text that does not
  re-parse**. `fad`/`rad` operands were printed at top priority, so an operand
  whose own top-level operator is `,` merged with the argument separator and
  the call re-parsed with the wrong arity: `rad(a, b : *, seeds)` reads as
  three arguments.
- Added `PRIORITY_ARGUMENT`, above every operator priority, and used it for
  both AD operands. This only bites inside a binder, where sharing is
  suppressed and operands print inline — which is why the one simple `fad`
  fixture in `tests/expand/` never showed it.
- The real lesson is coverage shape, not the missing parentheses.
  `tests/expand/` is organized by construct family, which makes it a good
  differential against the reference and a narrow test of the printer: it held
  one `fad` program and no `rad` at all. Added
  `every_corpus_program_expands_into_something_that_compiles`, which requires
  that every one of the 180 corpus programs that compiles also expands into
  something that compiles with no import search path.
- Two corpus programs surfaced by that test turned out to be correct behavior,
  not defects: `err_fad_rad_temporal` and `rep_74_soundfile_basic` do not
  compile directly either, and their expansions faithfully reproduce programs
  the compiler rejects. The test's subject is now explicitly the programs that
  compile.
- Both fixes were verified by mutation: reverting the parenthesization fails
  14 corpus expansions, and restoring the node-id numbering fails the naming
  and round-trip tests.
- Regenerated all 152 CLI transcripts. The diff has three parts, each checked
  line by line: the `fRec` renumbering from this change, the `--export-dsp`
  help text from today's earlier phases, and pre-existing drift recorded
  before this session (`--allow-network-imports`, `--table-init` help text,
  JSON `version`, `compile_options` spelling, banner text, default class
  names, absolute paths in error prefixes). `cli-transcript-check` now passes
  for the first time in a while: 152 modes identical.

## Phase E7: documentation, registry, and repository gates

Commit date: 2026-08-12
Source commit: this implementation commit

- Rewrote `DIFF-BEH-006`, which had recorded the stub ("returns the original
  source string"). It now states the parity result — 33 of 33 fixtures with a
  recorded oracle expand byte-identically — and enumerates every residual
  divergence: the three values that cannot match by construction, the four
  deliberate deviations, the convergence-at-the-second-pass behavior both
  compilers share, and the recursion-index artifact only this port has.
- Added `DIFF-CLI-009` for the `-e` option and `DIFF-API-004` for the
  `libfaust.h` inline-wrapper strategy, which is forced by the ABI rather than
  chosen.
- Documented `-e` in the user CLI guide, including the two deliberate
  differences a user would otherwise discover by surprise (stdout instead of
  nothing when `-o` is absent; version, options and library paths differing).
- Updated the plan document to `implemented`, marking the three claims the
  work corrected so the plan is not left asserting things the implementation
  disproved.
- Two repository gates failed on the new code and were fixed rather than
  suppressed:
  - `ffi-boundary-check` classified `libfaust-ffi` as a core crate depending
    rightward on an adapter. It *is* an adapter — it exports C symbols over the
    compiler facade and owns no compiler logic — so it joins the adapter and
    unsafe-opt-in lists.
  - `cli-parser-check` rejected the `-e` branch for reading `std::env::args`
    directly. The recorded options are now rebuilt from the parsed `CliArgs`,
    which is both policy-clean and better: C++ records its raw argv, so its
    expansions carry the input and output file names, which say nothing about
    how the program was compiled.
- Full sweep green: workspace tests, workspace Clippy, 199 golden snapshots,
  `structure-check`, `error-model-check`, `cli-parser-check`,
  `ffi-boundary-check`, `libfaust-export-check`, and `expand-oracle --check`.

## Phase E6: the `libfaust.h` FFI surface

Commit date: 2026-08-12
Source commit: this implementation commit

- Added `crates/libfaust-ffi`, exporting the seven backend-agnostic entry
  points the reference C API documents: `generateCSHA1`,
  `expandCDSPFrom{File,String}`, and `generateCAuxFilesFrom{File,String}[2]`.
  Until now those operations were only reachable through backend-specific
  spellings (`expandCInterpreterDSPFromString`, `expandCCraneliftDSPFromString`)
  even though neither expansion nor auxiliary-file generation involves a
  backend.
- Shipped both headers. `include/libfaust-c.h` is the exported C ABI;
  `include/libfaust.h` is the header the request named, and it cannot declare
  the reference API's functions directly — their symbols are mangled and
  `std::string` has no stable ABI. It is therefore header-only inline wrappers
  over the C ABI, the shape `libfaust-box.h` already uses, each wrapper
  adopting its returned `const char*` and releasing it through `freeCMemory`
  so no allocation crosses back over the boundary.
- `freeCMemory` is deliberately not redefined here: `interp-ffi` exports it
  unconditionally for the whole distribution.
- Resolved the one place the C API and the Rust facade disagree. The facade
  returns auxiliary outputs as in-memory artifacts so hosts without a writable
  filesystem can consume them; the `*2` entry points return a single string.
  A request selecting no output, several outputs, or a binary one has no
  well-defined single string, so each is reported rather than answered with
  whichever artifact came first.
- Strengthened the gate rather than reusing it as-is. The existing header
  checks are syntax-only, which proves declarations parse and nothing about the
  contract behind them — and `libfaust.h` is *entirely* wrappers, including the
  `freeCMemory` hand-off, so a client that only compiles leaves the part most
  likely to be wrong untested. `xtask libfaust-export-check` now also links and
  runs a C++ client that calls `expandDSPFromString` and checks
  `generateSHA1("abc")` against the RFC vector in libfaust's uppercase form.
- Verified the gate rejects its failure modes: declaring a symbol the library
  does not export fails with `missing header-declared exports`, and defining a
  second `freeCMemory` in this crate fails the link with a duplicate symbol.
- Re-blessed the export baseline: 395 exports, 353 header declarations.

## Phase E5: real SHA-1 for libfaust cache keys

Commit date: 2026-08-12
Source commit: this implementation commit

- Added `crates/ffi-common/src/sha1.rs` and replaced the stand-in in both
  `interp-ffi` and `cranelift-ffi`, which had been returning an FNV-1a 64-bit
  value repeated four times to fill a 64-character hex string. That satisfied
  the buffer contract while answering "different program" for identical
  sources, and the key is a cache identity that hosts compare across
  processes — including one C++ and one Rust.
- Implemented in-crate rather than adding a dependency: `ffi-common` documents
  itself as dependency-light, and SHA-1 is a fully specified 100-line
  algorithm whose correctness is settled by published vectors.
- Reading `sha_key.hh:302` caught a parity detail that a from-memory
  implementation would have gotten wrong: C++ formats the digest with the
  alphabet `"0123456789ABCDEF"`, so libfaust keys are **uppercase** hex,
  unlike almost every other SHA-1 rendering. A key differing only in case is
  still a different string to a host comparing two keys.
- Tested against the four RFC 3174 vectors, the empty input, and the three
  padding branches at 55/56/63/64/119/120 bytes, with reference digests taken
  from `hashlib` rather than written by hand. Truncating the message schedule
  by one round fails all three tests.
- Documented that SHA-1 is used here only because the reference defines the
  key format, and that nothing may treat it as a security primitive.

## Phase E4: the `-e` command-line mode

Commit date: 2026-08-12
Source commit: this implementation commit

- Added `-e` / `--export-dsp` to the CLI, spelled as in C++ Faust, with the
  branch placed before every backend so it is reached the same way C++ reaches
  its `gExportDSP` test. The options recorded in the document are the ones the
  process was invoked with, so an expansion documents how it was produced.
- Registered the flag in the mode-exclusion count: `-e` emits Faust source, not
  generated code, so pairing it with `-lang` is rejected rather than letting
  one of the two silently win.
- Deliberate deviation, recorded rather than inherited: C++ writes `-e` output
  to `ofstream(gOutpath)` and, with no `-o`, opens the empty path — so
  `faust -e prog.dsp` produces nothing and exits 0. faust-rs prints to stdout,
  like every other faust-rs dump mode.
- Added the round-trip check, which is the property that makes expansion
  useful and is stronger than matching the reference text: every fixture's
  expansion must compile to C++ **with no import search path at all**, and
  produce the same algorithm as compiling the original. All 35 fixtures pass.
- That check found a faust-rs-specific artifact C++ does not have. Recursion
  state variables are numbered from a counter that advances with the number of
  recursion groups seen while evaluating, so `020_library_import` yields
  `fRec157` compiled directly and `fRec161` compiled from its expansion — the
  same variable, a different index. C++ round-trips the same fixture with
  byte-identical code. The comparison renumbers indices by first appearance and
  documents why; making the counter program-local is filed as separate work,
  since 199 goldens encode the current names.
- Recorded three CLI transcripts for the new mode (`simple`, `rec`, `bad`) plus
  the `-e --lang cpp` rejection. The other 99 transcripts were left untouched
  although the current binary no longer reproduces them: that drift predates
  this work (`--allow-network-imports`, `--table-init` help text, JSON
  `version`, `compile_options` spelling), and re-recording them here would
  launder unrelated changes into this commit — the exact failure mode the
  harness's own documentation warns about.

## Phase E3: expansion document assembly

Commit date: 2026-08-12
Source commit: this implementation commit

- Added `crates/compiler/src/expand.rs`: `Compiler::expand_source_to_dsp` and
  `expand_file_to_dsp` build the whole `-e` document — version line, normalized
  `compile_options`, `library_path<i>` declarations, the `declare` metadata
  header, then the serialized box program.
- Ported `reorganizeCompilationOptionsAux` including its two subtleties: a
  following argument counts as an option value only when it does not itself
  start with `-`, and options the normalizer does not know keep their original
  relative order at the end.
- Rewrote `Compiler::expand_dsp`, which had returned its input verbatim since
  the facade was created, on the real expansion. Its `compile_options`
  short-circuit is reproduced with the C++ quirk intact and documented: the
  test never matches a real expansion, because the document starts with
  `declare version`.
- Two parity defects surfaced only by diffing against the recorded corpus, both
  invisible to unit tests written from the sources:
  - the unshared printer uses different composition-operator spellings from the
    shared one (`,` and `~` unpadded, `compiler/boxes/ppbox.cpp:311-319` against
    `:592-600`), so an abstraction body printed `\(x1).(x1, x1 : +)` where C++
    prints `\(x1).(x1,x1 : +)`;
  - library-scoped metadata keys are recorded with canonical absolute paths in
    this port, so the header read `_usr_local_share_faust_maths_lib_name`
    instead of `maths_lib_name`. Reusing the JSON path's `metadata_source_path`
    relativization fixes it and keeps the two headers consistent.
- Added `FRS-COMP-0007` for expansion failures, with its entries in the
  diagnostics reference and the error-model document. The frozen-code gate
  caught the omission before the commit, which is what it is for.
- Result: **33 of 33 fixtures with a recorded oracle expand byte-identically to
  the C++ compiler**, modulo the three lines that cannot match by construction
  (version, option spelling, absolute library paths). The two extension
  fixtures expand as expected.
- Corrected a claim from the plan while verifying it. Expansion is *not*
  idempotent after one pass, and neither is the reference — checked against it
  on the same corpus. The second pass grows the header (an expansion declares
  its own `version` and `compile_options`, which the next pass reads as
  metadata) and can shrink the body (re-evaluating `(65536 : int)` folds it to
  `65536`). The property that actually holds, and that the test now asserts, is
  convergence: the third pass equals the second.
- Fixture `033_deep_seq` was reduced from 2000 to 200 stages: faust-rs rejects
  the deeper program with a 1024-frame evaluator depth budget where C++ accepts
  it. That is a pre-existing evaluator limit unrelated to expansion, and it is
  now visible in the corpus rather than hidden.

## Phase E2: split the pipeline at the evaluation boundary

Commit date: 2026-08-12
Source commit: this implementation commit

- Split `pipeline_to_signals` into `pipeline_to_boxes` and
  `pipeline_boxes_to_signals`, joined by the new `BoxCompileOutput`. The cut is
  where C++ `libcode.cpp:1378` branches for `-e`: after the process box is
  evaluated and its arity inferred, before any signal is propagated.
- Kept every evaluation-stage rule on the prefix side — evaluator source
  loading, top-level metadata aggregation, diagnostic enrichment, owner
  attribution, eval guidance — so a caller that stops at the box boundary
  reports evaluation failures exactly as a full compilation does, rather than
  growing a second, weaker error path.
- Carried the flattened box, the arity memo and the evaluator definition names
  as private fields so completing the pipeline repeats no work. `process_flat`
  is deliberately not the tree a serializer should print; `process_box` is, and
  the field documents that.
- `SignalCompileOutput` keeps its public shape, so no caller changed.
- Checked independently rather than by inspection: the 199-case golden
  snapshot suite and the full compiler test suite both pass unchanged. A
  mutation that drops the evaluator metadata snapshot in the new prefix is
  invisible to the golden corpus but fails two compiler tests
  (`compiler_compile_file_to_signals_aggregates_component_metadata` and
  `c_family_callbacks_replay_master_and_component_metadata_with_relative_keys`),
  which is the coverage that matters for the header the expansion path will
  build from this snapshot.

## Phase E1: Faust-syntax box printers

Commit date: 2026-08-12
Source commit: this implementation commit

- Added `crates/boxes/src/print.rs` with `box_pp` (unshared expansion),
  `box_pp_shared` (the `ID_`-sharing printer behind `-e`), and `format_real`.
  Both printers are built on the existing `match_box` vocabulary, so they cover
  every box shape the evaluator can produce rather than a diagnostics subset.
- Three deliberate deviations from `compiler/boxes/ppbox.cpp`, each documented
  at its site: the memo table, definition trace and identifier counter are
  session-local instead of living in `gGlobal`, so two expansions in one
  process cannot interleave their numbering; the traversal is an explicit
  worklist rather than `operator<<` recursion, because evaluated diagrams reach
  thousands of levels; and an unprintable node returns `BoxPrintError` instead
  of throwing from inside a stream operator.
- Refused rather than approximated the shapes with no source syntax:
  `with { ... }`, `letrec`, closures, and partially-applied pattern matchers
  carry payloads that live in an evaluator side-table or an environment tree.
  C++ prints `closure[...]`, `PM[...]` and a raw tree dump inside `with { }`,
  none of which re-parse. None of these can occur in a successfully evaluated
  `process`, so refusing costs nothing and keeps unparseable text out of the
  output.
- Ported `T(double)` faithfully, including the detail that decides the digits:
  C++ narrows to the target precision *before* formatting, so 2*pi prints as
  `6.2831855f` and not the `6.2831853f` that formatting the unnarrowed `f64`
  produces. Rust has no `%g`, so the exponent-versus-fixed rule, trailing-zero
  stripping, and two-digit signed exponent are reproduced explicitly.
- Fixed the upstream `downsampling` defect in passing: the Rust printer emits
  `downsampling(...)`, where `faust -e` throws.
- Added 11 tests. Beyond the literal tables captured from the C++ binary, the
  structural ones assert properties rather than expected text: definitions
  precede their first use, no identifier is defined twice or left unused,
  24 doubling levels produce 24 definitions rather than 16 million
  occurrences, a 50000-level chain does not exhaust the stack, and nothing
  inside a binder is hoisted.
- Verified the checks reject their failure modes: disabling the memo lookup
  fails the sharing and linearity tests, reversing the definition order fails
  the definition-before-use test, and letting binder bodies share fails the
  hoisting test.

## Phase E0: capture the C++ `-e` reference corpus

Commit date: 2026-08-12
Source commit: this implementation commit

- Added `tests/expand/` with 34 fixtures, one per construct family:
  composition operators, iterative constructions, every UI widget and group,
  tables and waveforms, foreign functions/constants/variables, `select2`/
  `select3`, delays, routes, pattern matching, `with{}`, shared sub-diagrams,
  library imports, `ondemand`/`upsampling`/`downsampling`, `fad`, and a
  2000-stage `seq` for the traversal-depth case.
- Added the `xtask expand-oracle` workflow, which captures each fixture's
  `faust -e` output into `tests/expand/oracle/` and verifies it with `--check`.
  Every capture is fed back through the reference compiler, so a fixture whose
  expansion does not re-compile fails at capture time with the compiler's own
  error instead of surfacing later as an apparent port defect.
- Normalized three host-dependent lines to placeholders (`declare version`,
  `declare compile_options`, `declare library_path<i>`), keeping their presence
  and position asserted while eliding values that would tie the corpus to the
  capturing machine.
- Established the printing rules by observation rather than from the sources
  alone. The parenthesization of a memoized definition is fixed by the operator
  priority at its *first* visit (`streambinopShared`), which is why the same
  `*(0.99)` prints as `ID_1 = (ID_0 : *)` under `~` but as `ID_1 = ID_0 : *` at
  the top level. Real literals are `%g`-shaped (`4.4e+02f`, `3.1415927f`),
  `ffunction` prints `gFloatSize` names separated by `|`, and `route`/
  `ffunction` use comma separators without a following space while `fconstant`
  and `fvariable` use `", "`.
- Found an upstream C++ bug while capturing: `boxppShared::print()` tests
  `isBoxUpsampling` twice (`compiler/boxes/ppbox.cpp:615-617`) and never
  reaches a `BoxDownsampling` branch, so `faust -e` on any program containing
  `downsampling(...)` fails with
  `boxppShared::print() : BoxDownsampling[...] is not a valid box`. The
  non-shared `boxpp` printer handles the node correctly at
  `compiler/boxes/ppbox.cpp:467`, so the defect is reachable only through `-e`.
  The fixture is kept and reported as skipped; faust-rs will print
  `downsampling(...)`.

## Analyze the `-e` export-DSP option and plan the `libfaust.h` FFI surface

Commit date: 2026-08-12
Source commit: this documentation commit

- Added `porting/export-dsp-e-option-and-libfaust-api-port-plan-2026-08-12-en.md`,
  covering both the `-e` / `--export-dsp` option and the backend-agnostic
  `libfaust.h` / `libfaust-c.h` header pair, since `expandDSP*` is the header's
  central entry point and is exactly the `-e` code path.
- Established the C++ reference contract from `master-dev-ocpp-od-fir-2-FIR19`
  at `8eebea429`: the option branches in `libcode.cpp:1378` after parse, eval
  and the `numOutputs == 0` rejection but before propagation, and
  `expandDSPInternalAux` emits `declare version`, the normalized
  `declare compile_options`, one `declare library_path<i>` per imported source
  except the DSP file itself, the whole metadata set through
  `printDeclareHeader`, then the `boxppShared` serialization.
- Captured ground truth by running the installed `faust 2.87.4` binary rather
  than reading the sources alone. Two measured facts became the acceptance
  criteria: the expanded file re-compiles, and its generated C++ is identical
  to compiling the original except for the metadata block (mangled keys, added
  `library_path*` and `version`, different `compile_options`).
- Documented the properties of `boxppShared` that a port must preserve: DAG
  sharing through the `ID_` memo table, definition-before-use ordering, and the
  deliberate fallback to the non-shared printer for `BoxAbstr` and
  `BoxSymbolic`, whose bound variables would become free if hoisted.
- Recorded two C++ quirks instead of silently correcting them: the
  `expandDSPFromString` `startWith("declare compile_options ")` short-circuit
  never fires on real `-e` output because `declare version` is printed first,
  contradicting the invariant stated at `libcode.cpp:1194`; and
  `faust -e prog.dsp` without `-o` opens an empty output path, producing no
  output and exiting 0.
- Surveyed the Rust boundary: `BoxMatch` already covers every box shape and is
  a ready substrate, but there is no `boxpp`/`boxppShared` port
  (`render_human_box_expr` is a lossy, depth-capped, non-sharing diagnostics
  renderer and must not be reused), no eval-only pipeline, no
  `reorganizeCompilationOptions`, no `%g`-shaped real formatter, a stub
  `Compiler::expand_dsp` returning its input, and a fake SHA-1 that is an
  FNV-1a value repeated four times to fill 64 hex characters.
- Chose the header strategy imposed by the ABI: C++ `std::string` signatures
  cannot be exported from Rust, so `libfaust.h` follows the existing
  `libfaust-box.h` pattern as a header-only inline wrapper over a new
  `libfaust-c.h`, each wrapper releasing its `const char*` through the
  `freeCMemory` already exported unconditionally by `interp-ffi`. Both headers
  live in a new `crates/libfaust-ffi/` rlib linked into `faust-ffi` and enter
  the `xtask libfaust-export-check` gate.
- Structured the work as phases E0-E7, each with a producer, an independent
  checker, and mutations that must break that checker. The central check
  re-parses and re-evaluates the emitted `ID_` program and asserts structural
  DAG equality with the input, which is what catches a dropped memo table
  (exponential tree re-expansion) and an incorrectly hoisted abstraction body.
- Stated the parity contract explicitly: byte parity with C++ output is not
  promised (version string, argv spelling, installation-dependent library
  paths, `ID_` numbering), while self-containedness, re-compilability,
  idempotence, and codegen equality after round-trip are.
