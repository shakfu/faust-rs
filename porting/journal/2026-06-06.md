# 2026-06-06

## Align README documentation with current crate behavior

Commit date: 2026-06-06
Source commit: this commit

Checked the top-level and crate README files for drift against the current
workspace layout, CLI surface, and implemented crate APIs.

Documentation updates:

- added the recommended local validation sequence to the top-level
  `README.md`, including formatting, clippy, tests, and `xtask golden-check`;
- updated `crates/draw/README.md` from a stale scaffold description to the
  implemented SVG block-diagram renderer, including C++ provenance, public
  APIs, CLI integration, and pipeline position;
- clarified `crates/wasm-ffi/README.md` so `generateAuxFiles(...)` documents
  the currently supported `-cpp`, `-c`, `-wasm`, `-json`, and `-svg` outputs.

Validation:

- `cargo test -p draw --lib`
