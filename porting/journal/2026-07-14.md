# 2026-07-14 - Verified vector FIR routing

Branch: `ondemand-vec-fad-synthesis`.

## Assemble verified vector state phases and clock islands

Commit date: 2026-07-14
Source commit: this commit

Implemented P6.3b as a finite producer/checker boundary in
`signal_fir::vector_assemble`. The producer composes accepted P5 routed FIR
with optional P6.1 and P6.2 artifacts and emits C++-compatible copy/ring delay
storage, `pre/exec/post` words, simultaneous recursive-projection captures,
single-fire island loop bodies, and nested OD/US/DS guards. Certified reverse
AD fallback evidence refuses vector assembly.

P6.2 transport lifetime is now materialized in FIR: outer-chunk values retain
their indexed stack arrays, island scalars are declared below the matching
domain guard, and held outputs become cleared struct state. The independent
checker verifies exact loop/action/island coverage, storage and guard shapes,
and island-local declaration ownership. Focused tests cover copy and ring
words, recursive capture, counted clocks, both guarded transport lifetimes,
and rejection of a substituted state word.

The change remains additive: final output/lifecycle module placement,
`build_module` selection, differential execution, canonical serialization,
Lean acceptance, and backend activation remain open.

Validation is fully green: `cargo fmt --all -- --check`; 2 focused P6.3b tests;
11 routed-FIR tests; `cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`; and all 190
`cargo run -p xtask -- golden-check` snapshots unchanged.

## Route recursive tuple definitions through scalar projections

Commit date: 2026-07-14
Source commit: this commit

Implemented the P6.3a routed-FIR boundary for recursive tuples. Tuple
definitions now use a deterministic structural FIR type and canonical nested
`ValueArray` constructors. The independent route checker recursively validates
arity and component types, including the case where a malformed value forges
the expected outer type.

The representation follows C++ `sigProj` lowering: tuple groups are retained
as simultaneous-recursion evidence, but inter-loop tuple transports remain
rejected and values cross loop boundaries through scalar projections. A real
prepared graph with two recursive projections now closes routed-FIR
verification under all four scheduling strategies. The change remains
additive and does not alter CLI, ABI, `build_module`, or backend behavior.

P6.3b remains open: lower the actual stateful projection bodies, emit P6.1
`pre/exec/post` actions, nest them below P6.2 islands, rematerialize
island-local and held transports, and independently verify the final FIR
assembly before module integration.

Validation is fully green: `cargo fmt --all -- --check`; 10 focused routed-FIR
tests; `cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`; and all 190
`cargo run -p xtask -- golden-check` snapshots unchanged.

## Add verified P6.2 clock islands and AD policy

Commit date: 2026-07-14
Source commit: this commit

Added `signal_fir::vector_clock_ad`, a production producer/checker boundary
that composes verified prepared signals and `VectorPlan` facts with a freshly
recomputed `ClkEnvMap` and the propagation-owned clock-domain hierarchy. The
canonical artifact records one serial island per OD/US/DS wrapper, exact parent
links, scalar-compatible guard shapes, domain members, clock-state owners, and
nested P4 loops. P5 transports are exhaustively partitioned: only top-rate
values retain outer-chunk indexing, domain-rate temporaries are marked for
island-scalar rematerialization, and exported `PermVar` values retain explicit
persistent hold lifetime.

Added executable `ClockStep` evidence for boolean, counted, and downsample
modulo guards. Zero fires preserve state and held outputs; counted transitions
advance in fire time; nested-domain tests preserve parentage. Guard selection
uses the same interval/nature rule as scalar FIR lowering. A real compiler
fixture confirms propagated FAD is an ordinary signal graph. Reverse-time and
`BlockReverseAD` carriers receive an explicit `FRS-VEC-RAD-SCALAR` policy with
fixed `Forward < Reverse` epochs, and the reference executor makes that order
structural rather than scheduler-dependent.

The slice remains additive and does not change CLI, ABI, `build_module`, or
backend behavior. Final tuple routing, island/state FIR emission,
island-local transport replacement, output/module assembly, differential
execution, JSON/Lean acceptance, and backend activation remain open.

Validation is fully green: `cargo fmt --all -- --check`; 7 focused P6.2
transform tests; 2 compiler-level clock/FAD tests;
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`; and all 190
`cargo run -p xtask -- golden-check` snapshots unchanged.

## Add verified P6.1 delay and recursion transitions

Commit date: 2026-07-14
Source commit: this commit

Added `signal_fir::vector_state`, a production producer/checker boundary that
derives delay and recursion state transitions only from accepted P4.3b
decorations and the accepted P4.4 vector plan. Short delays use the C++
four-sample-rounded `_tmp`/`_perm` geometry; long delays use the exact
power-of-two ring, mask, index, and saved-advance geometry. Both are routed to
canonical loop `pre/exec/post` actions. Recursive projection aliases are grouped
by symbolic index and all projections advance simultaneously in the unique
serial recursion loop. Unsupported clock and AD resources fail closed for
P6.2.

Extended the bounded P5.3 event checker with a state-refined entry point.
Managed delay/recursion effects become explicit loop-pre, per-sample exec, and
loop-post events; phase barriers and carried recursion-step dependencies are
checked in both sample-major scalar and scheduled loop-major vector orders.
Tests cover all four scheduling strategies and reject plan/geometry/phase
substitution.

Added executable `DelaySim` evidence: the concrete copy and ring models are
compared through newest-first abstraction against `history_step` and
`delay_read` for 32,805 bounded input sequences under three full/partial chunk
partitions. The slice remains additive: clock/AD transitions, tuple-valued
routed FIR for real recursion groups, final FIR phase emission, module
assembly, Lean certificate ingestion, and backend activation remain open.

Validation is fully green: `cargo fmt --all -- --check`; 77 focused vector
tests; `cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`; and all 190
`cargo run -p xtask -- golden-check` snapshots unchanged.

## Add the bounded P5.3 FissionSafe event checker

Commit date: 2026-07-14
Source commit: this commit

Implemented P5.3 as an additive producer/checker boundary in
`signal_fir::vector_events`. The producer expands the complete routed vector
chunk into stable definition, use, transport store/load, exact effect, and
epoch-boundary events. It builds the sample-major scalar order, the selected
`-ss` loop-major vector order, and a conservative dynamic dependence relation.
The explicit caller bound fails closed when the full chunk is too large; no
truncated prefix can be accepted.

The independent checker does not call the producer reconstruction. It rebuilds
the event table with a separate traversal, derives both orders from execution
keys, checks event/order coverage, verifies each local, epoch, loop-edge,
route, and effect-conflict dependency family, and then exhaustively checks
`FissionSafe`. The routed artifact now retains the exact `VectorPlan` that owns
its identities so effect evidence cannot be checked against a different
same-shape plan.

Focused tests accept pure typed transport under all four scheduling strategies,
reject order and dependency mutations, reject an undersized bound, and expose
the key counterexample: two state or output effects in separately fissioned
loops reverse a cross-sample scalar dependence even when a static effect edge
orders the loop DAG. Co-locating the state effects in one serial loop is
accepted. The porting and certified-porting plans document the finite model,
its Rust/Lean relationship, and the remaining P6 transition-semantics gap.

Validation is fully green: `cargo fmt --all -- --check`; 70 focused vector
tests; `cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`; and all 190
`cargo run -p xtask -- golden-check` snapshots unchanged.

## Lower actual pure signal closures into P5 regions

Commit date: 2026-07-14
Source commit: this commit

Implemented P5.2 as an additive pure signal-to-routed-FIR artifact. The new
`signal_fir::vector_lower::lower_pure_vector_program` accepts only verified
prepared signals and an opaque verified vector plan. It cross-checks plan ids
and scalar types against the prepared forest, shares binary-operator mapping
with scalar lowering, and handles actual constants, inputs, output wrappers,
casts, selects, typed arithmetic/comparisons, min/max/abs, and pure math nodes.
Effectful records and stateful, recursive, table, UI, foreign, clock, and AD
nodes fail closed for P6.

Lowering uses provisional control/loop-local caches and delegates every sibling
read to `VectorRouteSession`; it never introduces transport storage. Each
region is CSE-materialized independently with a loop-id-derived namespace
before owned values and transport stores are sealed. The resulting opaque
program carries fixed control statements, canonical transport declarations,
scheduled region bodies, helper requirements, and accepted P5.1 routing
evidence. Added an independent final-body checker that reconnects definitions,
producer stores, consumer loads, and routed uses to the CSE-rewritten bodies.

Focused tests lower a real prepared two-loop pure forest, verify local CSE and
typed transport resolution for both real precisions and all four `-ss`
strategies, reject a removed consumer body, and reject a stateful delay. The
slice remains detached from `build_module`; effect-event checking, state
transitions, output/module assembly, vectorization retention, and backend
activation remain open.

Validation is fully green: `cargo fmt --all -- --check`; 64 focused vector
tests; `cargo clippy --workspace --all-targets -- -D warnings`; all workspace
and all-target tests; and all 190 `cargo run -p xtask -- golden-check`
snapshots unchanged.

## Add the P5.1 routed-FIR boundary

Commit date: 2026-07-14
Source commit: this commit

Implemented the additive P5.1 routing boundary in
`signal_fir::vector_route`. `VectorRouteSession` accepts only an opaque
`VerifiedVectorPlan`, applies the selected common `-ss` schedule per epoch,
materializes loop regions, and replaces the former global value-cache model
with separate control, owned-loop, and loop-local inline scopes. All typed
transport arrays are declared from the frozen plan before routing. Owned
cross-loop values can only emit and resolve the exact planned store/load pair;
no lookup can invent a buffer or stable name.

Added independent routed-FIR verification for definition coverage, legal region
visibility, FIR value types, exact transport identities and declarations,
producer/store and consumer/load linkage, and the canonical `i0 - vindex`
chunk index. Focused tests cover successful two-loop routing, all four `-ss`
strategies, control and inline scope rules, sibling-definition rejection,
missing loads, mutated load FIR, and substituted direct-use values. The public internal API is adapted from
C++ `DAGInstructionsCompiler`: plan construction owns allocation while routing
only resolves already-certified identities. It has no CLI or ABI effect.

The new tests exposed a P4.4 panic when an output root had already been visited
inline from an earlier root. Production construction now promotes that signal
to the shared root loop and revisits it to record both contexts; a real-signal
non-regression test covers the case. The route remains intentionally detached
from `build_module` until P6 supplies complete delay, recursion, clock, and AD
state transitions. Per-region CSE, bounded effect-event checking, actual
signal-expression routing, vectorization retention, and backend activation
remain later P5/P6 work.

Validation is fully green: `cargo fmt --all -- --check`;
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`; both focused route/plan test modules;
and all 190 `cargo run -p xtask -- golden-check` snapshots unchanged.
