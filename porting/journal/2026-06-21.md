# 2026-06-21

## Extract BraState from SignalToFirLower (W9 pilot)

Commit date: 2026-06-21
Source commit: this commit

First commit of the `SignalToFirLower` god-struct decomposition plan
(`signal-to-fir-lower-struct-decomposition-plan-2026-06-20-en.md`, cluster 1 of 7).

Extracted the five BRA-specific fields from `SignalToFirLower` into a new
`BraState` sub-struct defined in `bra.rs`:

- `bra_state_scheduled` → `bra.scheduled`
- `bra_grad_cache` → `bra.grad_cache`
- `bra_delay1_carry_vars` → `bra.delay1_carry_vars`
- `bra_delay_array_carry_vars` → `bra.delay_array_carry_vars`
- `bra_tape_store_var` → `bra.tape_store_var`

`BraState` derives `Default`; `setup.rs::new` now initialises with
`bra: BraState::default()` in place of the five individual `HashMap::new()` /
`HashSet::new()` calls.  All original field doc comments were moved verbatim into
the struct definition.

Validation:

- `cargo test -p transform` — 103/103
- `cargo test -p compiler` — 2/2
- `cargo clippy -p transform --all-targets` — clean
- `cargo fmt -p transform -- --check` — clean

## Analysis doc: mark W6, W9, I4, I12 resolved; partially close I2

Updated `signal-to-fir-transform-analysis-2026-06-20-en.md` to reflect work
completed since the analysis was written:

- **W6** (dead/unused analysis surface): all four items confirmed gone from the
  code (`rec_group_max_delay`, per-signal `delay_analysis` map,
  `SignalFirOptions::strict_mode`, stale `#[allow(dead_code)]` on
  `get_delay_line`). Marked resolved.
- **W9** (`SignalToFirLower` god object): marked resolved — 7 sub-structs
  extracted by the decomposition plan (2026-06-21, 7 commits). `*Ctx` bundles
  remain but are documented as inherent to the `store + sub-manager`
  split-borrow pattern.
- **I4** (decompose `SignalToFirLower`): marked resolved, references W9 update.
- **I12** (stale annotations and dead config): marked resolved, references W6
  update.
- **I2** (unify delay passes + delete dead state): W6 part (dead state deleted)
  marked resolved; W7 part (merging `analyze_signals` + `scan_signals`) remains
  open.

## Documentation pass: transform sub-struct modules + workspace-wide audit

Commit date: 2026-06-21

Two-part documentation cleanup following the W9 god-struct decomposition.

**Part 1 — `crates/transform/src/signal_fir/module/` (10 files)**

Updated every module-level doc and stale inline comment to reflect the new
sub-struct organisation:

- `mod.rs`: fixed reference to non-existent `placement.rs` → `PlacementInfo`
  in `setup.rs`; added a sub-struct organisation table to the
  `SignalToFirLower` struct doc listing all 7 sub-structs with their types and
  home files.
- `core_lowering.rs`: fixed `Self::struct_declarations` → `Self::sections` in
  `lower_signal` doc.
- `rad_formula_builder.rs`: fixed `used_math_ops` → `used_protos.math_ops` in
  module doc and `math_call` method doc.
- `state.rs`, `ui_lowering.rs`, `arithmetic.rs`, `setup.rs`, `build.rs`,
  `bra.rs`: each module doc now opens by naming the sub-state struct it defines.

**Part 2 — workspace-wide audit (29 crates)**

Full read-only audit of all crates except `transform`; 4 issues found and
fixed:

- `box-ffi/src/lib.rs`: orphaned doc "Lowers a propagated signal root list…"
  was above `control_label_and_metadata`; moved to the correct function
  `lower_signal_roots_to_fir`.
- `errors/src/lib.rs`: duplicate doc line on `DiagnosticBundle` — removed one.
- `normalize/src/simplify.rs`: "Phase 2" label was stale; simplify is wired
  into Phase 1. Fixed.
- `propagate/src/api.rs`: `PropagateError::UnsupportedBox` wrong for `rad()`;
  fixed to `PropagateError::RadUnsupportedNode`.

Validation: `cargo clippy --workspace --all-targets` clean.

## W9 complete — all 7 clusters extracted from SignalToFirLower

Commit date: 2026-06-21
Source commits: this session (7 commits)

Completed the full `signal-to-fir-lower-struct-decomposition-plan-2026-06-20-en.md`.
All seven clusters landed as independent, bisectable commits:

| Cluster | Sub-struct | Fields | File |
|---|---|---|---|
| 1 | `BraState` | 5 bra_* fields | `bra.rs` |
| 2 | `PlacementInfo` | sig_ref_counts, sig_at_boundary, konst_escapes | `setup.rs` |
| 3 | `RadReverseState` | forward_output_by_sig{,_key}, lowering_reverse_loop | `build.rs` |
| 4 | `UsedPrototypes` | used_math_ops, used_int_fun_names, used_foreign_{fun_protos,vars} | `arithmetic.rs` |
| 5 | `NameGen` | next_loop_var_id, {f,i}const_counter, {f,i}slow_counter | `setup.rs` |
| 6 | `UiLoweringState` | ui_controls, soundfiles, waveform_tables{,_len}, table_access_by_sig, ui_statements | `ui_lowering.rs` |
| 7 | `ModuleSections` | 7 statement vecs + 3 dedup guards | `state.rs` |

Result: `SignalToFirLower` went from ~51 fields to 14 fields (arena, ui_program,
types, sig_types, num_inputs, real_ty, store, cache, sample_phases,
state_name_by_node, recursion, scheduled_state_updates, delay, uses_iota,
input_ptr_aliases, sections, ui, used_protos, name_gen, placement,
rad_reverse, bra).

Validation: 103 transform + 2 compiler tests green after every commit;
clippy and fmt clean throughout.
