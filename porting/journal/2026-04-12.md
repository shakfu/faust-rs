# 2026-04-12

## docs: refresh README and workflow docs after transform/compiler CLI changes

Commit date: 2026-04-12

Source commit: pending

### Context

Several documentation files had drifted behind the current `transform`,
`compiler`, and `xtask` surfaces.

In particular:

- `crates/transform/README.md` still documented the older fast-lane entrypoint
  signature and under-described the explicit `signal_prepare` verified staging
  boundary
- `crates/compiler/README.md` did not reflect newer file-backed JSON helpers or
  the current lane defaults across C/C++ vs interp/WASM/JSON entry points
- `crates/xtask/README.md` omitted newer maintenance commands such as
  `fir-dump-scan`, `backend-align-smoke`, and `backend-align-nightly`
- `docs/user-cli-guide-en.md` and `docs/developer-workflows-en.md` still
  described a smaller CLI/backend surface than the one now exposed by
  `crates/compiler/src/main.rs`
- `porting/faust-rs-supported-faust-subset-en.md` needed a small status refresh
  after the explicit `signal_prepare` boundary/encapsulation work and the newer
  documentation updates

### Documentation update

Updated:

- `crates/transform/README.md`
- `crates/compiler/README.md`
- `crates/xtask/README.md`
- `docs/user-cli-guide-en.md`
- `docs/developer-workflows-en.md`
- `porting/faust-rs-supported-faust-subset-en.md`

Documented:

- the current `compile_signals_to_fir_fastlane_with_ui(...)` signature and the
  verified `signal_prepare -> signal_fir` staging contract
- the public `compiler` helpers added since the earlier README snapshot,
  including file-backed JSON/default wrappers and the current lane-default
  behavior
- the newer `xtask` maintenance/alignment commands now present in the CLI
- the actual current user/developer CLI surface:
  `--dump-fir-verify`, `--dump-cpp-from-fbc`, `--dump-cranelift`, `--json`,
  `-lang interp|cranelift|wasm|wast`, FIR fixtures, precision/delay flags, and
  runtime/alignment workflows
- the supported-subset snapshot wording so it matches the current transform
  preparation boundary and variable-delay description

### Validation

- documentation-only change
- checked updated docs against:
  - `crates/transform/src/lib.rs`
  - `crates/transform/src/signal_prepare.rs`
  - `crates/transform/src/signal_fir/mod.rs`
  - `crates/compiler/src/lib.rs`
  - `crates/compiler/src/main.rs`
  - `crates/xtask/src/main.rs`
- no code or tests changed in this step

## fix(cranelift-ffi): make runtime descriptor sample-rate test self-contained in CI

Commit date: 2026-04-12

Source commit: pending

### Context

The GitHub Actions run `24253067210` failed in
`runtime::tests::runtime_descriptor_tracks_sample_rate_fields` on the
`cranelift-ffi` crate.

The test compiled `tests/corpus/rep_61_fmin_sr.dsp`, which imports
`stdfaust.lib`. That dependency happened to resolve on the local development
machine but not on the CI runner, where the test failed with
`Import(UnresolvedImport { name: "stdfaust.lib", ... })`.

This made the test environment-sensitive even though its actual assertion only
checks that FIR lowering exposes the runtime descriptor sample-rate field.

### Implementation

Updated:

- `crates/cranelift-ffi/src/runtime.rs`

Implemented:

- replaced the file-backed `rep_61_fmin_sr.dsp` fixture usage in
  `runtime_descriptor_tracks_sample_rate_fields` with an inline DSP snippet
  that defines `SR` via `fconstant(int fSamplingFreq, <math.h>)`
- kept the same semantic intent by still exercising sample-rate-dependent FIR
  lowering before building the runtime descriptor
- removed the accidental dependency on an installed `stdfaust.lib` tree so the
  test now behaves the same on local machines and GitHub runners

### Validation

- `cargo test -p cranelift-ffi --lib runtime_descriptor_tracks_sample_rate_fields`
- `cargo test -p cranelift-ffi --lib`
- `cargo clippy -p cranelift-ffi --all-targets -- -D warnings`
- `cargo fmt --all -- --check`
