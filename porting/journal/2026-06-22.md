# 2026-06-22

## Legibility experiment executed: signal_prepare.rs → signal_prepare/ (Proposals B, C, A)

Executed the B→C→A plan from
`signal-prepare-simplification-experiment-2026-06-22-en.md` on the staging phase,
on branch `main-dev`. Behavior-preserving throughout: the prepared forest (arena,
output roots, `types`/`sig_types` maps) is unchanged, so downstream FIR is
identical. `cargo test -p transform` stayed at **103 passed** after every commit;
clippy, fmt, and the `-D rustdoc::broken_intra_doc_links` doc gate all clean.

Method: each slice implemented by a **Sonnet 4.6 sub-agent** (foreground, main
tree) under the test + doc gates, then independently verified here (re-ran the
suite, read the diff, checked the schedule fidelity for A).

Commits on `main-dev` (oldest first):

| Commit | Slice | What |
|---|---|---|
| `2aa126dc` | move | `git mv signal_prepare.rs → signal_prepare/mod.rs` (Opus prep; `mod tests;` still resolves) |
| `7b8d118e` | B (F4) | Extract the ~700-line verification boundary into `signal_prepare/verify.rs` (`PreparedSignals::verify` + `verify_prepared_signal` / `verify_control_exists` / `verify_promotion_invariant`); a child module reaches the parent's private `PreparedSignals` fields, so no field-visibility changes. mod.rs 1475→741. `verify.rs` `//!` header documents the certified postconditions |
| `e18d20f9` | C (F4) | Extract the two canonicalization rewrites (`canonicalize_unary_rec_projections` + helpers, `canonicalize_one_sample_delays` + helper) into `signal_prepare/rewrites.rs`; the "Explicit Limitation" caveat (not a 1:1 port of C++ `inlineDegenerateRecursions`) relocated to the rewrites header. mod.rs 741→489 |
| `9841595b` | A (F1/F2/F5) | Replace the hand-threaded five `sig_types_*` snapshots with a typed `Staging { arena, outputs, sig_types }` driver: one method per pass, an explicit `retype()` reproducing the **exact** five-infer schedule (steps 2.4/2.6/2.9/2.12/2.14), and the W4 contracts as named `assert_*` steps. `prepare_signals_for_fir_unverified` now reads as a flat sequence of `s.method()?` calls. mod.rs +132 lines (the abstraction; the win is conceptual, not line count) |

Result: a 1475-line monolith became a `signal_prepare/` module — producer
(`mod.rs` 621), checker (`verify.rs` 789), rewrites (`rewrites.rs` 283), tests
unchanged — with the pipeline's type schedule made structural instead of
conventional. Four reading frictions removed (F1 hand-threaded snapshots, F2
convention-only re-typing, F4 producer+checker in one file, F5 no uniform pass
contract).

Validation: 103 transform tests green after every commit; clippy + fmt clean;
`RUSTDOCFLAGS="-D rustdoc::broken_intra_doc_links" cargo doc -p transform
--no-deps` clean. Same playbook as the 2026-06-21 `signal_fir/delay` split.

## Legibility experiment: signal_prepare.rs simplification analysis (EN)

A design/analysis doc (no code change) applying the same method as the
2026-06-21 `signal_fir/delay` experiment to the staging phase
`crates/transform/src/signal_prepare.rs` (1475 lines). Written as a single
English file in `porting/`:

- `signal-prepare-simplification-experiment-2026-06-22-en.md`

Contents:

- **Context** — `signal_prepare` is stage 2 (staging) of `transform`, between
  propagation and `signal_fir`. Entry `prepare_signals_for_fir_verified`
  (called from `signal_fir/mod.rs:214`); the heart is a fixed linear pipeline of
  ~16 full-forest traversals, **5 of them `infer_full_types`**, threading 5
  hand-named `sig_types_*` snapshots. Boundary contract: de-Bruijn recursion /
  mixed numerics / non-canonical delays in → symbolic `SYMREC`, casts inserted,
  canonical `Delay1`, reduced `SimpleSigType` + full `sig_types` out, in a
  private arena.
- **Five reading frictions** (F1–F5), each anchored to `file:line`: the five
  hand-threaded type snapshots (F1), the "re-type after a structural change"
  rule as convention not structure (F2), the inline W4 inter-pass `debug_assert`
  clutter (F3), producer and ~700-line verifier sharing one file (F4), and the
  absence of a uniform pass contract (F5).
- **Three independent, orthogonal restructurings:** A — a typed staging pipeline
  (the pass sequence as data over a `Staging` value, the driver owning the
  re-type schedule); B — split the verification boundary into
  `signal_prepare/verify.rs` (producer vs. checker, a pure move mirroring the
  delay split); C — extract the two canonicalization rewrites into
  `signal_prepare/rewrites.rs` as testable units. Comparison table + recommended
  order **B → C → A**.
- **Safe/testable migration** anchored on a FIR-output (or structural prepared)
  snapshot plus the existing **32 `signal_prepare` tests**, the 103 transform
  tests, the impulse oracle, and the built-in `PreparedSignals::verify`
  postcondition checker; per-proposal recipe with differential testing for A.
  Performance argument: A reproduces the exact 5-infer schedule (same traversal
  count); B/C are pure moves. Making the schedule explicit later *enables*
  auditing redundant re-types (out of scope for "same perf").
- **Worked before/after** of the pipeline body: the five hand-threaded
  `sig_types_*` snapshots vs. a flat `PIPELINE` list of named `Pass` entries.

Validation: documentation only — no code changed, no tests affected. Companion
doc links verified to resolve; `file:line` references checked against current
`signal_prepare.rs`.
