# 2026-07-27

## Harden reference-counted FFI factory lifecycles (F4)

Commit date: 2026-07-27

Source commit: `35d7799d`

Implemented phase F4 of
`porting/ffi-crate-organization-analysis-2026-07-27-en.md` after resolving the
parity-sensitive decision gate against Faust C++ commit `8eebea429`. The
accepted contract is recorded in
`porting/ffi-factory-cache-lifecycle-contract-2026-07-27-en.md`.

Replaced integer-erased raw-pointer indexing with an owned
`FactoryCache<T, I>`. Each SHA entry now co-locates a stable boxed factory,
explicit external reference count, and its boxed DSP instances.
`FactoryHandle<T>` carries typed pointer identity without exposing safe
dereference. Instances are dropped before their parent factory.

Repeated creation and SHA lookup now coalesce to the same pointer and acquire a
releasable reference. Factory deletion returns `false` while references remain
and `true` only on final removal; final removal deletes remaining instances.
Manual instance deletion unregisters and drops that instance, while
`deleteAll*DSPFactories` invalidates all references and instances regardless of
their counts. Cache lifecycle operations remain mutex-protected in both public
MT compatibility modes, so lookup acquisition is atomic with respect to
deletion.

Added shared ownership, destruction-order, clear, and repeated concurrent
lookup/release tests, plus Interpreter- and Cranelift-specific public C API
conformance tests. Updated existing Cranelift factory coalescing tests and the
maintained C/C++ headers and crate READMEs to state the acquired-reference and
invalidation rules.

Public API mapping: Interpreter external lifecycle is 1:1 with the maintained
C++ API. Cranelift adopts the generic Faust lifecycle as an adapted backend
contract. Internal Rust representation is adapted. No C signature or symbol
changed; the frozen unified surface remains exactly 388 exported symbols and
346 maintained-header declarations.

Validation: `cargo fmt --all --check`;
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`;
`cargo run -q -p xtask -- ffi-boundary-check`;
`cargo run -q -p xtask -- golden-check`;
`cargo run -q -p xtask -- libfaust-export-check`; `git diff --check`.

## Own decoded C strings and verify callback-table layouts (F3)

Commit date: 2026-07-27

Source commit: `283c2cae`

Implemented phase F3 of
`porting/ffi-crate-organization-analysis-2026-07-27-en.md`. Replaced
`required_c_str_arg` and `optional_c_str_arg`, whose returned lifetimes were
not tied to C storage, with `required_c_string_arg` and
`optional_c_string_arg`. The new helpers copy inputs immediately into
`String`/`Option<String>`, and all Box, Signal, Interpreter, and Cranelift call
sites now borrow only from those owned values.

Added focused marshalling tests for ownership after the source `CString` is
dropped, null required pointers, invalid UTF-8 in required and optional
arguments, embedded-NUL escaping in returned strings, and 4095-byte error
buffer truncation plus termination.

Strengthened the shared `UIGlue`/`MetaGlue` contract with Rust size, alignment,
and field-offset assertions. The maintained Interpreter and Cranelift C11 and
C++17 smoke translation units now initialize every callback-table field and
independently assert the 14-slot `UIGlue`, two-slot `MetaGlue`, and default
`FAUSTFLOAT = float` layouts.

Public Rust helper mapping: adapted for explicit ownership. External C API
mapping: 1:1, with the frozen unified surface still matching all 388 exported
symbols and 346 maintained-header declarations.

Validation: `cargo fmt --all --check`;
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`;
`cargo run -q -p xtask -- ffi-boundary-check`;
`cargo run -q -p xtask -- golden-check`;
`cargo run -q -p xtask -- libfaust-export-check`; `git diff --check`.

## Enforce FFI dependency and unsafe-code boundaries (F2)

Commit date: 2026-07-27

Source commit: `96b8ca97`

Implemented phase F2 of
`porting/ffi-crate-organization-analysis-2026-07-27-en.md`. Added
`xtask ffi-boundary-check`, backed by Cargo workspace metadata, to enforce the
machine-checkable dependency direction
`compiler core <- FFI adapters <- distribution`. Core crates cannot depend on
adapter or distribution crates, adapters cannot depend on distribution
crates, and `ffi-common` cannot acquire any workspace dependency.

The check also requires every package ending in `-ffi` to have an explicit
layer classification and confines `unsafe_code = "allow"` to the six adapter
crates, `wasm-ffi`, and the documented `foreign-call` runtime exception. Each
override must carry an inline foreign-boundary reason. `faust-ffi` remains a
safe distribution facade.

The root workspace inventory now presents compiler core, FFI adapters, and
distribution/tooling separately. CI runs the new check on Ubuntu alongside the
existing structural checks. This is a repository architecture constraint only;
there is no compiler behavior or external C ABI change. Public API mapping:
1:1 for the existing exported interfaces.

Validation: `cargo fmt --all --check`;
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`;
`cargo run -q -p xtask -- ffi-boundary-check`;
`cargo run -q -p xtask -- golden-check`;
`cargo run -q -p xtask -- libfaust-export-check`; `git diff --check`.

## Rename and modularize shared FFI support (F1)

Commit date: 2026-07-27

Source commit: `b7179f21`

Implemented phase F1 of
`porting/ffi-crate-organization-analysis-2026-07-27-en.md`. Renamed the
misleading `utils` package and directory to `ffi-common`, updated its six
direct consumers, and split the previous monolithic module into `abi`, `args`,
`factory_cache`, `memory`, and `strings`. Flat re-exports preserve the existing
internal call shape while making ownership and unsafe-code review boundaries
explicit. Removed the unused `CRATE_NAME`/`crate_id` scaffold.

The `faust-ffi` distribution facade now inherits the workspace
`unsafe_code = "forbid"` lint instead of opting the whole crate into unsafe
code. Unsafe operations remain localized in the concrete adapter and
`ffi-common` crates that implement C pointer and callback contracts.

This is an adapted internal Rust package/module API: dependent manifests now
name `ffi-common` and Rust paths use `ffi_common`, with no intended external C
ABI change. The frozen unified library baseline remains exactly 388 exported
symbols, of which 346 are declared by the maintained headers.

Validation: `cargo fmt --all --check`;
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`;
`cargo run -q -p xtask -- golden-check`;
`cargo run -q -p xtask -- libfaust-export-check`; `git diff --check`.

## Inventory and freeze the unified FFI export surface (F0)

Commit date: 2026-07-27

Source commit: `8a426267`

Implemented phase F0 of
`porting/ffi-crate-organization-analysis-2026-07-27-en.md`.
The new
`porting/ffi-crate-map-and-export-baseline-2026-07-27-en.md` records every
consumer of the misleadingly named `utils` crate, the ownership role of each
FFI crate, the shared callback-table definitions, the maintained native
headers, and the current factory-cache limitations.

Extended `xtask libfaust-export-check` from the Box/Signal subset to all four
maintained C API tiers: Box, Signal, Interpreter, and Cranelift. The header
parser now removes C comments and ignores callback typedefs, and the smoke
check compiles the two backend C headers in separate translation units so their
intentional `UIGlue`/`MetaGlue` names do not collide.

Added the checked-in
`porting/generated/libfaust-rs-exported-symbols.txt` baseline and an explicit
`--bless` refresh mode. The unified macOS debug library currently exports 388
Faust C symbols: 346 are declared by the maintained headers and 42 are observed
extensions (39 Box `Aux` helpers, two Cranelift Box/Signal constructors, and
the Interpreter instance-version query). The normal check now rejects both
additions and removals, giving the following rename/split phases an exact ABI
guard.

No compiler behavior, Rust crate layout, generated code, or external ABI
change. Public API mapping: 1:1 observation of the existing C surface.
Validation: `cargo fmt --all --check`; `cargo test -p xtask`;
`cargo run -p xtask -- libfaust-export-check`; `git diff --check`.

## Analyze and clarify the FFI crate organization

Commit date: 2026-07-27

Source commit: `f51d5000`

Added
`porting/ffi-crate-organization-analysis-2026-07-27-en.md` in response to the
review concern that the workspace contains many `*-ffi` crates while the
non-FFI-named `utils` crate contains foreign-interface code.

The analysis distinguishes useful API/backend boundaries from naming debt.
`tree-ffi`, Box, Signal, Interpreter, Cranelift, WASM, and the final
`faust-ffi` aggregation crate have separate handle, compatibility, runtime,
target, or packaging responsibilities and should not be merged merely to
reduce the Cargo member count. The actual inconsistency is `crates/utils`: all
of its consumers are FFI adapters, and its implemented surface consists of ABI
callback tables, C string and pointer ownership, argument decoding, compile
options, and a raw-pointer factory cache.

The proposed migration renames that crate to `ffi-common`, splits it into
`abi`, `strings`, `memory`, `args`, and `factory_cache` modules, documents a
one-way `compiler core <- FFI adapters <- distribution` dependency rule, and
makes the `faust-ffi` packaging facade inherit the workspace unsafe-code
prohibition. Follow-up phases separately replace unconstrained borrowed C
string lifetimes with owned values, add ABI layout checks, and harden the
factory cache only after its parity-sensitive ownership and concurrency
contract is explicitly decided.

No compiler behavior, Rust crate layout, generated code, or external C ABI
changes in this documentation commit. Public API mapping is deferred to the
implementation phases; the proposed `utils` rename is an internal adapted Rust
API with no intended C ABI impact. Validation: relative-path review and
`git diff --check`.

## Analyze BRA tape safety, adjoint liveness, and seed-aware routing

Commit date: 2026-07-27

Source commit: `de7667be`

Documented the post-`Attach` C++ analysis in
`porting/rad-bra-tape-safety-liveness-analysis-2026-07-27-en.md`.
The analysis confirms that the `i0 & 8191` tape mask removes out-of-bounds
access but silently aliases gradients for `count > 8192`, and that structural
tape collection can leave write-only tape arrays and hot-loop stores.

It also corrects the claim that public `rad` always uses `BlockReverseAD`:
the symbolic sweep is attempted first, but currently descends into
seed-independent temporal subgraphs. For `w * no.noise`, this reaches the
noise recursion and triggers a conservative BRA fallback even though the
useful adjoint slice for seed `w` is feed-forward.

The proposed remediation is split into explicit gates:

1. choose and expose a safe BRA window contract (fixed chunked TBPTT is
   recommended; dynamic host-sized scratch remains the exact whole-block
   alternative);
2. compute seed-dependency masks, including a recursion-slot fixpoint, before
   symbolic/BRA dispatch;
3. produce a live `BraAdjointPlan` whose tape requirements are checked by an
   independent structural checker before FIR emission.

No compiler behavior or public API changes in this documentation commit.
Public API mapping is deferred until the fixed-window versus dynamic-scratch
decision is made. Validation: relative-link inspection and `git diff --check`.

## Preserve `Attach` semantics in the BlockReverseAD backward sweep

`SigBlockReverseAD` FIR lowering now treats `Attach(value, effect)` as an
identity in the adjoint graph: the full cotangent flows to `value`, while the
attached branch remains a forward-only effect. The BRA postorder follows only
the value child, so metering is neither differentiated nor replayed during the
reverse sweep. This closes the `FRS-SFIR-0004` failure reproduced by
`rad_attach_repro2.dsp`.

Added a structural BRA regression and an interpreter test whose attached
bargraph branch depends on the differentiated seed; the expected TBPTT gradient
proves that no spurious adjoint flows through that branch. Also aligned the
WASM JSON scaffold test with the canonical JSON contract that emits an empty
top-level `"meta": []` array.

Validation: `cargo run -q -p compiler --bin faust-rs -- -double
rad_attach_repro2.dsp`; `cargo fmt --all --check`; `cargo clippy --workspace
--all-targets -- -D warnings`; `cargo test --workspace --all-targets`; `cargo
run -p xtask -- golden-check`.

Public API mapping: 1:1. `Attach` still returns its value operand and forces the
attached operand in the forward pass; reverse-mode differentiation now applies
the matching identity adjoint without changing the generated DSP interface.

## Restore the `wasm-ffi` release build after Cranelift target gating

`cargo build -p wasm-ffi --target wasm32-unknown-unknown --release` failed
because `codegen` correctly omits its native Cranelift JIT module on `wasm32`,
while `backend_error` and the compiler facade still named Cranelift types
unconditionally.

The corresponding error implementation, facade imports, report entry points,
error variant, lowering envelope, and error mapper now carry the same native
target condition. This keeps the native Cranelift API unchanged and removes it
only from a compiler built for `wasm32`, where a native JIT is unavailable.

Validation: `cargo fmt --all --check`; `cargo build -p wasm-ffi --target
wasm32-unknown-unknown --release`; `cargo clippy -p wasm-ffi --target
wasm32-unknown-unknown --release -- -D warnings`; `cargo test -p compiler -p
wasm-ffi`.

Public API mapping: adapted. The Cranelift report API remains unchanged on
native targets and is intentionally unavailable on `wasm32`; this matches the
backend's existing target constraint.
