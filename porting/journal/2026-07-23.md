# 2026-07-23

## Correct stale figures and over-broad claims in the port history

Commit date: 2026-07-23
Source commit: this commit

A review of `docs/faust-cpp-to-rust-port-history-en.md` against the repository
found one stale measurement and three imprecise claims. All were corrected in
place; the historical figures were kept where they are dated as history.

Vector admission was the real error. Sections 25 and 29 reported "49 of 93
corpus DSPs admitted" from the July 16 implementation review, and section 29 is
explicitly the state of the port at the July 23 cutoff. The checked-in baseline
`tests/vector-coverage/corpus-baseline.json`, regenerated on July 22 by
`0a2d9d8a`, measures 97 of 133 certified identically in all 16 vector modes,
with 35 explicit fallbacks (13 plan, 11 clock/AD, 7 event, 4 pure) and 1 error.
Both places now report the July 22 baseline and link to the artifact, so the
number has a regenerable source rather than a prose claim.

The 93 and 133 denominators were being used interchangeably. Section 22 now
states the distinction once: 93 is the imported C++ impulse population and the
denominator for oracle comparison and per-backend gates, while 133 is the
corpus after Rust-extension and clock-domain additions and the denominator for
Rust-only measurements such as vector admission. Section 27 gained the matching
July 23 corpus snapshot.

The libfaust source-generation surface was described more broadly than it is.
`CcreateSourceFromBoxes` and `CcreateSourceFromSignals` accept `c`, `cpp`,
`fir`, and `interp` and reject anything else with a typed error, as asserted by
the `box-ffi` and `signal-ffi` tests. Section 20 now says so, and section 26
says "compiler facade" rather than "library facade" so that `8acde236` and
`fa073fdc` are not read as widening the Box/Signal entry points.

The condensed timeline stopped observing three days before the cutoff. It
gained a July 20 row (R5–R9 vector decomposition, byte-deterministic scalar
emission, independent `clock_ad` checker, LGPL-2.1-or-later license alignment,
README marked experimental) and a July 22 row (baseline regeneration,
project-local Faust libraries, published compiler Wasm artifact); the July 23
row now also records the `-ec`/`-os` porting plan. Finally, the conclusion's
restatement of the four-day spine carries the same "limited subset, not yet
good code" qualifier that section 6 already applied to it.

Validation was documentation-scoped: the new relative link resolves, the
baseline counts were read back from the JSON for all 16 modes, and the FFI
language sets were confirmed in the crate tests.

## Welcome human and AI-assisted contributions

Commit date: 2026-07-23
Source commit: this commit

Added a `Contributing` section to the root README. It welcomes pull requests,
including AI-assisted work, while making contributors responsible for the
correctness, maintainability, tests, and documentation of their submissions.

The section links directly to `AGENTS.md` §11 and requires every pull request
to follow its Commit and Documentation Hygiene rules: linear rebased history,
small coherent commits, appropriate README and journal updates, and concise,
factual, implementation-oriented documentation.

Validation was documentation-scoped: the local section link resolves and
`git diff --check` passed.

## Document the history of the Faust C++ to Rust port

Commit date: 2026-07-23
Source commit: this commit

Added an English history of the port from the initial C++ inventory and phased
plan through TLIB, Box/Signal evaluation, canonical FIR, the first executable
C/C++ chain, verification, runtime backends, optimization, delays and
recursion, public APIs, WebAssembly, JSON/SVG, FAD/RAD, Julia, AssemblyScript,
impulse tests, clock domains, vector compilation, and the Rust source backend.
The root README now links to the document.

The account also records the project as a concrete experiment in AI-assisted
compiler engineering. It distinguishes the implementation work accelerated by
agents from the semantic and architectural authority retained by the
maintainer, and documents the rule against DSP-specific patches in favor of
generic corrections at the owning compiler layer.

The final review cross-checked dates, corpus and impulse counts, backend
maturity, public contracts, and remaining gaps against the daily journal, Git
history, dated status reports, and implementation reviews. It explicitly
qualifies scalar equivalence to the tracked portable corpus, distinguishes
certified vector admission (49/93 DSPs at the July 16 review) from green
requested-mode matrices that may use scalar fallback, and presents Lean as an
experimental structural assurance investigation rather than a proof of the
compiler or its semantics.

Validation was documentation-scoped: all relative links resolve, the 30
numbered sections are continuous, code fences are balanced, no trailing
whitespace was found, and `git diff --check` passed.

## Plan external control and one-sample execution

Commit date: 2026-07-23
Source commit: this commit

Analyzed the C++ Faust `-ec` external-control and `-os` one-sample modes at
reference commit `8eebea429` and documented a phased faust-rs port. The plan
separates control scheduling and processing API from scalar/vector scheduling,
defines the required FIR `control` and `frame` contracts, covers slow-value
state promotion and vector producer/checker evidence, and records a capability
policy for every active and scaffolded backend.

Approved the three compatibility decisions needed before implementation:
`-ec -lang fir` is a documented faust-rs diagnostic extension, `-os` is
rejected for operations that require block boundaries, and the generated Rust
backend keeps the `FaustDsp` trait unchanged while exposing `control` and
`frame` as public inherent methods.

Validation was documentation-scoped: the plan was cross-checked against the
pinned C++ sources and current Rust FIR/backend entry points, and
`git diff --check` passed.

## Document frame-rate FFT and spectral processing

Commit date: 2026-07-23
Source commit: this commit

Expanded the English and French clock-domain notes with a dedicated spectral
processing section. It explains how `interleave.lib` serializes audio frames,
uses a boolean frame clock to run FFT effects through `ondemand`, and
reconstructs streams with non-overlapping or overlap-add framing. Added complete
analysis-only and spectral-low-pass examples plus links to the checked corpus
covering reconstruction, spectral masks, fast convolution, robotization,
phase-vocoder frequency shifting, and a 1024-point denoiser.

Documented the current scalability boundary: `ondemand` amortizes the transform
to one execution per hop, but the compiler still expands the FFT into a
specialized scalar butterfly graph. Large transforms therefore increase
compilation time, evaluator depth, code size, instruction-cache pressure,
register pressure, and worst-case frame-tick cost relative to a tuned looped or
library-backed FFT. Added a concise FFT entry point to the root README.

Validated the documented analysis FFT at 128 points and the 16-point spectral
low-pass through the interpreter backend, and checked all Markdown links and
whitespace.

## Document domain-local `ma.SR` and the current library clamp

Commit date: 2026-07-23
Source commit: this commit

Expanded the root README clock-domain overview to state that `ma.SR` is
automatically adapted to the effective local rate: `SR * H` inside `US(H)` and
`SR / H` inside `DS(H)`. Algorithms deriving coefficients from `ma.SR` therefore
do not need a manual rate correction inside these blocks.

Documented the current `platform.lib` limitation: its
`min(192000, max(1, fSamplingFreq))` definition keeps the adapted expression
clamped in generated code. As a result, large upsampling factors cannot expose
their full local rate—for example, `US(8)` at 48 kHz observes 192 kHz instead
of 384 kHz—until the library definition is relaxed or redesigned.

## Publish `libfaust-rs` for C/C++ and retain the Wasm module name

Commit date: 2026-07-23
Source commit: this commit

Renamed the public C/C++ distribution to `libfaust-rs`. Rust library target
names cannot contain hyphens, so `faust-ffi` builds an internal `faust_rs`
target and `cargo run -p xtask -- build-libfaust` publishes the stable native
artifacts: `libfaust-rs.a` and the platform dynamic equivalent
(`libfaust-rs.dylib`, `libfaust-rs.so`, or `faust-rs.dll`). The export checker
now validates the published dynamic library.

The separate WebAssembly compiler module intentionally retains its established
`libfaust-rs.wasm` filename. Its Cargo-internal source artifact remains
`faust_wasm_ffi.wasm`, which the `xtask` packaging step renames and validates.
