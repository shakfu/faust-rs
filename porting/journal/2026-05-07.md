# 2026-05-07

## Plan RAD BlockReverseAD signal model

Commit date: 2026-05-07

Source commit: pending

Added `porting/rad-block-reverse-ad-signal-ir-plan-2026-05-07-en.md`.
The plan changes the RAD foundation from strict-LTI detection to a
signal-level block reverse-mode AD fallback that targets the existing
FAD-supported primitive surface first.

The new plan keeps LTI detection, `SigIIR`, `StateSpace`, and `ReverseTimeRec`
as phase-2 optimizations after a correct block-local tape/replay model exists.

Validation:

- documentation-only change

## Document affine seed impact on FAD

Commit date: 2026-05-07

Source commit: 38324dc

Expanded the LTI filter intermediate-form plan to explain why simple affine
seed provenance is less urgent for FAD while it traverses raw expressions, but
becomes mandatory if a shared LTI canonicalization hides affine expressions
such as `-seed` or `const - seed` behind `SigFIR`/`SigIIR` carriers.

The plan now records FAD-specific L4b pass criteria so future shared
RAD/FAD LTI preparation cannot regress FAD seed identity.

Validation:

- documentation-only change

## Document RAD-LTI propagate placement constraint

Commit date: 2026-05-07

Source commit: dfa79e3

Updated `porting/lti-filter-intermediate-form-plan-2026-05-06-en.md` to record
the architectural consequence of expanding `rad(...)` inside `propagate`: LTI
reconstruction needed by RAD must run before or during `reverse_ad`, not only
in downstream `transform::signal_prepare` or FIR lowering.

The plan now documents the RAD-local preparation boundary, the coupling with
simple affine seed provenance, the distinction from FAD, and the remaining
runtime gate for `ReverseTimeRec` backend semantics.

Validation:

- documentation-only change

## Route SigIIR carriers through reverse AD

Commit date: 2026-05-07

Source commit: 3aeef27

Wired `SigMatch::Iir` into the internal `reverse_ad` sweep for first-order and
second-order structured filters. The RAD path now lowers typed IIR carriers
through the state-space bridge, builds a reverse-time adjoint recursion, and
routes sample-wise gradients to the independent input and feedback
coefficients.

Direct third-order IIR carriers continue to return the explicit
`iir-state-space` unsupported diagnostic until section factorization exists.

Validation:

- `cargo test -p propagate reverse_ad::tests::iir_carrier`

## Bridge typed IIR filters into RAD E1 transpose

Commit date: 2026-05-07

Source commit: 03b61a8

Added an internal `propagate::transpose_ad` bridge from typed scalar
`IirFilter` sections to the existing phase-E1 `DEBRUIJNREC` transpose
scaffold. The bridge lowers first-order/second-order `A/B/C/D` state-space
views into canonical recursive state-update groups, then reuses the current
cotangent-driven transpose path.

Direct third-order and higher IIR sections remain rejected with a structured
`UnsupportedIirOrder` diagnostic, preserving the biquad/first-order stability
constraint from the LTI filter intermediate-form plan.

Validation:

- `cargo test -p propagate transpose_ad::tests::iir_state_space_bridge`

## Add typed LTI filter extraction views

Commit date: 2026-05-07

Source commit: 39b8bd8

Added typed `FirFilter`, `IirFilter`, and private `StateSpace`/`A-B-C-D`
views in `crates/signals/src/filter_algebra.rs` as the first implementation
step for `porting/lti-filter-intermediate-form-plan-2026-05-06-en.md`.

The new API extracts validated typed views from C++-parity `sigFIR`/`sigIIR`
carriers and converts first-order/second-order scalar IIR sections to sparse
state-space rows. Direct order-3 and higher IIR conversion is rejected
explicitly to preserve the plan's numerical-stability constraint.

Validation:

- `cargo test -p signals filter_algebra`
