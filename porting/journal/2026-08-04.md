# 2026-08-04

## Share precedence-aware textual expression layout

Commit date: 2026-08-04

Source commit: this commit

Added a backend-neutral textual-expression helper that decides operand
parentheses from target precedence, associativity, conservative-operator
policy, and operand side. Its C-like policy ports the pinned C++
`TextInstVisitor`/`gBinOpTable` contract, while the generic descriptor permits
future textual backends to supply different language rules. This is an
internal `adapted` API: no generated-code ABI or public Rust API changed.

The scalar Cmajor emitter now uses that helper rather than wrapping every FIR
binary operation. Required grouping such as `a - (b - c)` and `(a + b) * c`
is preserved, while the right-nested associative addition in `bells` is
rendered as a flat chain like the pinned C++ backend. Structural tests cover
the precedence boundary, non-associative right operands, conservative
operators, and a 128-term addition chain.

After rebuilding the release compiler, Cmajor 1.0.3175 accepted `bells`, its
maximum rendered parenthesis depth fell from 111 to 3, and the generated C++
runtime trace matched `reference/bells.ir`. `bells` was removed from
`KNOWN_FAIL_cmajor`, increasing the supported impulse lane from 125 to 126
programs and leaving seven explicit exclusions.

Validation: formatting; workspace clippy with warnings denied; all workspace
and all-target tests; 17 Cmajor compiler integration tests; Rustdoc with
warnings denied; Rust golden check; the direct external `bells` impulse target;
and the release compile-budget gate without baseline changes. The first budget
sample reported an isolated `bells` front-end `--check` outlier even though the
changed backend is absent from that path; an immediate full rerun passed at
13.800 units against the unchanged 18.318-unit ceiling.

## Record the Cmajor impulse-gate handoff

Commit date: 2026-08-04

Source commit: this commit

Updated the session handoff at implementation HEAD `274bc934`. It records the
complete scalar Cmajor commit series, the 125/125 supported impulse result,
the eight named exclusions, the external-tool and pinned-checkout contract,
all validation gates, and the ordered C6 follow-ups for output events,
expression materialization, and large table initialization. Pre-existing
untracked user files and generated scratch data remain untouched.

## Integrate and run the full scalar Cmajor impulse gate

Commit date: 2026-08-04

Source commit: this commit

Integrated the external `make cmajor` target into the impulse-test driver and
documented its Cmajor/C++ requirements. It remains outside default `make all`
and the vector/scheduling matrix because the backend is intrinsically scalar
and the `cmaj` tool is optional. The target refreshes the ordinary C++ oracle,
then delegates to the parallel-safe Cmajor lane.

Running the 133-program oracle corpus produced 125 matching Cmajor traces. The
passing set includes controls, bargraphs, stateful filters, tables, waveforms,
and all current `upsampling_*` and `downsampling_*` fixtures. Eight exclusions
are explicit: shared `subcontainer1`; one-sample-incompatible `bs`; unsupported
soundfile `sound`; a fully parenthesized associative expression in `bells`; and
generated oscillator tables expanded into 65,536/65,537-element literal
initializers in `modulations`, `osci`, `tester`, and `tester2`. Depending on the
surrounding source, Cmajor diagnoses the latter as excessive nesting or an
oversized initializer. These are compile-time gaps, never relaxed numeric
tolerances. The pinned C++ backend instead applies precedence-aware expression
printing and preserves each table generator as a compact `SIG0` subprocessor
with a `fill..._<size>` loop.

The corpus exposed and fixed two additional emitter defects. Bargraph storage
in lifecycle bodies no longer attempts forbidden Cmajor endpoint writes during
`init()`, while the runtime sample body still emits its rate-limited event.
Repeated UI display addresses are now accepted like C++ Faust and share a short
name; uniqueness remains enforced on the actual zone-derived endpoints.

Validation: 125/125 supported impulse traces matched with Cmajor 1.0.3175 under
`make -j4`; top-level help exposes `cmajor`, `CMAJ_BIN`, and `CMAJ_CXX`;
formatting; workspace clippy with warnings denied; all workspace/all-target
tests; Rust golden check; and release compile-budget check without baseline
changes. The upstream adapter's known lifecycle warnings remain on stderr for
some DSPs but do not alter traces; direct lifecycle conformance tests remain
authoritative.

## Add the parallel-safe Cmajor impulse harness

Commit date: 2026-08-04

Source commit: this commit

Added `tests/impulse-tests/Make.cmajor`, which sends faust-rs scalar-double
Cmajor output through `cmaj generate --target=cpp`, the pinned C++
`cmajor_cpp_dsp` adapter, and the upstream impulse runner before comparing each
trace with the canonical C++ reference. Unlike upstream's shared scratch names,
each DSP owns a private build directory, and a two-case `make -j2` smoke run
proved the rules do not collide. `control.dsp` is excluded explicitly for the
same reason as in the pinned C++ lane.

The first Cmajor frontend run exposed a target-language mismatch in the emitter:
Faust FIR comparisons have `int32` result type, while Cmajor comparisons produce
`bool` and reject implicit stores to integers. Comparison expressions are now
wrapped in `int32(...)` when required by canonical FIR, with a structural unit
test. The `constant` and `delays` impulse traces then matched their references
both sequentially and in parallel. The upstream adapter prints lifecycle
`checkDefaults` warnings for `delays` because its reset methods are no-ops; this
does not change the matching trace and does not supersede the backend's direct
lifecycle conformance tests.

Validation: codegen Cmajor unit tests; release compiler build; sequential and
parallel two-DSP Cmajor impulse smoke tests with Cmajor 1.0.3175; formatting;
workspace clippy with warnings denied; all workspace/all-target tests; Rust
golden check; and release compile-budget check without baseline changes.

## Specify the Cmajor impulse-test lane

Commit date: 2026-08-04

Source commit: this commit

Specified the C6 integration with `tests/impulse-tests` after auditing the
pinned C++ `Make.cmajor`, `impulsecmajor.cpp`, and `cmajor-cpp-dsp.h`. The lane
will compile faust-rs Cmajor source through `cmaj generate --target=cpp`, wrap
the generated class as a Faust `dsp`, run the established impulse architecture,
and compare the resulting trace with the canonical C++ references.

The plan records four constraints: the external lane is opt-in rather than a
default workspace dependency; every DSP receives a private build directory so
parallel Make execution is safe; the pinned C++ impulse architecture and Cmajor
adapter are referenced in place rather than copied; and the first gate is
scalar double precision, with `control.dsp` excluded like upstream. The audit
also records that this route validates audio and input controls but cannot
replace the dedicated output-event/bargraph cadence runtime test.

## Qualify the scalar Cmajor backend

Commit date: 2026-08-04

Source commit: this commit

Added the first external qualification layer for the scalar backend. Five
compact Faust programs compare the generated Rust and pinned-C++ Cmajor
observable contracts: stream endpoints, annotated UI events, event handlers,
tick advancement, and bargraph scheduling. The comparison intentionally
excludes formatting, the documented adapted lifecycle, and table declarations.
For the table fixture the pinned C++ optimizer folds the constant lookup while
canonical Rust FIR retains a concrete array and `.at` access; this is classified
as a semantic-preserving optimizer difference rather than source-contract drift.

Added a Cmajor-to-C++ runtime harness and executed a recursive DSP through
Cmajor 1.0.3175 at both `-O0` and `-O4`. Both traces agree and match the expected
first sixteen recurrence samples. Frontend validation now also covers streams,
recurrence, delay, and scalar math. The execution-capability invariant test now
recognizes Cmajor's intrinsic per-sample `main()` contract.

Validation: 17 Cmajor integration tests with `CMAJ_BIN=/usr/local/bin/cmaj` and
the pinned `FAUST_CPP_BIN`; `cargo fmt --all -- --check`; workspace clippy with
warnings denied; all workspace/all-target tests; Rustdoc for compiler/codegen
with warnings denied; Rust golden check; release compile-budget check without a
baseline update. The long-run C++ golden guardrail still reports 34 unrelated,
pre-existing metadata-name differences (`mydsp` versus filename stem); no
snapshot was changed. Remaining C6 work is runtime UI/bargraph cadence and the
full impulse/numeric matrix. C7/C8 polyphonic, DSP wrapper, hybrid, and SDK
application layers remain deferred.

## Activate the scalar Cmajor facade and CLI

Commit date: 2026-08-04

Source commit: this commit

Activated Cmajor as a first-class scalar backend across the compiler facade,
typed diagnostic mapping, execution-capability table, FIR lowering dispatcher,
and `faust-rs -lang cmajor`. Public Rustdoc covers all six source/file/default
facade entry points and the intrinsic execution contract inherited from C++
`compileCmajor`: external control and one-sample processing are forced, their
flags are output-invariant aliases, and vector mode fails explicitly.
This records the C0 Go decision for the pinned scalar scope; polyphonic,
hybrid, DSP-event, and SDK application layers remain No-Go until C7/C8.

The CLI now wires default/specified processor names, single/double precision,
stdout/`-o`, JSON companions, fixture mode, and language-neutral architecture
wrapping. A repository-relative `.cmajor` architecture fixture protects
`<<includeclass>>` insertion. User-facing compiler, codegen, and workspace
documentation now lists the scalar target and its deferred polyphonic/hybrid
SDK layers.

Validation: facade/CLI byte parity; four intrinsic flag shapes; `-cn`,
`-double`, `-o`, `--json`, and `-vec` rejection; Cmajor architecture wrapper;
fourteen Cmajor integration tests; Cmajor 1.0.3175 frontend acceptance; full
compiler library tests; focused all-target clippy; compiler/codegen Rustdoc
with warnings denied; release `compile-budget-check` green without baseline
changes.

## Validate concrete Cmajor table lowering

Commit date: 2026-08-04

Source commit: this commit

Resolved the C4 table-specialization decision against production Rust FIR.
Unlike the C++ instruction hierarchy, canonical FIR already owns concrete
array element types and lengths at the Cmajor boundary, so read-only,
writable, waveform, and generated tables can be emitted directly without a
name-indexed `CmajorTablePlan`. Module Rustdoc and the porting plan document
this representation-level `adapted` mapping and why no detached side table is
introduced.

Structural tests cover runtime stores and dynamic `.at` access, waveform
initializers, generated tables at two sizes, and repeat compilation in one
process to detect leaked specialization state. All table fixtures are
self-contained and use the production signal-to-FIR lane.

Validation: eleven Cmajor integration tests; read-only, writable, waveform,
and generated-table sources accepted by Cmajor 1.0.3175 with
`generate --target=syntaxtree`; deterministic repeated emission confirmed.

## Add Cmajor UI events and rate-limited bargraphs

Commit date: 2026-08-04

Source commit: this commit

Added the second scalar Cmajor slice: a request-local two-pass UI plan now
derives shortest unique widget names, emits annotated input/output event
endpoints, preserves sequential Faust metadata, and generates dirty-control
event handlers. Bargraph stores publish output events at the upstream 50 Hz
cadence through `fControlSlice`. The same path covers `float32` and `float64`
streams, control endpoints, annotations, handlers, and literals.

The implementation records its C++ visitor provenance in Rustdoc and rejects
duplicate addresses, duplicate endpoints, invalid annotation names, and
soundfiles with typed errors. Cmajor's FIR `Static` storage is emitted as
per-processor mutable state, matching the upstream backend's table and
lifecycle model rather than incorrectly becoming `const`.

Validation: seven production signal-to-FIR Cmajor integration tests, including
controls, metadata, bargraphs, lifecycle, delays, typed soundfile rejection,
and double precision; all three generated UI/double fixtures accepted by
Cmajor 1.0.3175 with `generate --target=syntaxtree`; complete `codegen` library
tests; focused clippy with warnings denied; Rustdoc with warnings denied.

## Add the scalar Cmajor emitter core

Commit date: 2026-08-04

Source commit: this commit

Replaced the Cmajor scaffold with a typed FIR-to-Cmajor source emitter. The
first slice emits scalar streams, processor fields, single/double-aware types
and literals, scalar expressions and control flow, delay-array access, math
mapping, helper functions, separated `control`, and the forever-running
one-sample `main` loop. Unsupported FIR types and nodes return stable
`FRS-CGEN-CMAJ-0001..0005` errors rather than assertions or partial source.

The generated lifecycle is intentionally adapted from the upstream Cmajor
exception: `init` calls `classInit` before `instanceInit`, while direct
`instanceInit` calls only `instanceConstants`,
`instanceResetUserInterface`, and `instanceClear`. Module and public API
Rustdoc record the pinned C++ sources, semantic invariants, supported subset,
and compatibility status. The codegen README now classifies Cmajor as scalar
bring-up instead of a scaffold.

Validation: Cmajor codegen unit tests; production signal-to-FIR integration
tests for one-sample I/O, recurrence, fixed delay, lifecycle, and typed
soundfile rejection; complete `codegen` library tests; focused clippy with
warnings denied; `RUSTDOCFLAGS=-Dwarnings cargo doc -p codegen --no-deps`.

## Plan the scalar Cmajor backend port

Commit date: 2026-08-04

Source commit: this commit

Added the Cmajor backend port and test plan against the pinned C++ Faust
reference `8eebea429`. The plan inventories the source container, instruction
visitor, UI/event contract, table specialization, architecture wrappers,
application tooling, and upstream impulse path. It freezes scalar
`-lang cmajor` with single/double precision as the first completion target and
defers polyphonic, `cmajor-dsp`, hybrid, and SDK tooling to separately gated
milestones.

The lifecycle mapping is explicitly `adapted`: Cmajor tables remain processor
instance fields, while generated `init` follows the repository contract
`classInit -> instanceInit` and direct `instanceInit` does not call
`classInit`. The test plan layers pure Rust structural and numeric checks,
pinned C++ differential output, current Cmajor frontend validation, generated
C++ impulse execution, and `-O0`/`-O4` parity.

Validation: C++ Faust 2.84.3 generated scalar noise, UI, table, and double
fixtures at `8eebea429`; Cmajor 1.0.3175 accepted all four with
`generate --target=syntaxtree`; Markdown whitespace and diff check. The pinned
custom branch asserts in `backendDelayType` for the deferred `cmajor-poly`
probe, which is recorded as a C7 rebaseline/repair requirement.
