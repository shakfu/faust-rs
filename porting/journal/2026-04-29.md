# 2026-04-29

## perf(normalize): share simplify cache across output forest

Commit date: 2026-04-29

Source commit: this commit

The macOS sample of `rad_fxlms1.dsp` showed the active compiler worker
dominated by `normalize::simplify::sig_map`, `Aterm::add_sig`,
`normalize_add_term`, `Aterm::greatest_divisor`, and `mterm::gcd`.
The Rust `sig_map` traversal already memoized one simplification walk,
but `simplify_signals_fastlane(...)` created a fresh cache for every
output root. This missed an important C++ behavior: `simplify(Tree sig)`
stores results on tree nodes with the global `SIMPLIFIED` property, so
later roots in the same shared signal forest can reuse earlier
simplification results.

Added an explicit `SimplifyCache` wrapper around the existing
`HashMap<SigId, Option<SigId>>`, kept `simplify(...)` as the standalone
fresh-cache entry point, and added `simplify_with_cache(...)` for callers
that own a pass boundary. `simplify_signals_fastlane(...)` now allocates
one cache for the whole prepared output forest and clears it if a caught
simplification panic falls back to the original root.

The memoization boundary is documented in `porting/MEMOIZATION.md`: cache
entries are keyed by canonical `SigId`, are valid only for the same
`TreeArena`, `SigType` map, and simplification pass, and typed/untyped
simplification must not share one cache.

### Validation

- `cargo fmt -p normalize -- --check`
- `cargo test -p normalize`
- `cargo clippy -p normalize --all-targets -- -D warnings`
- `cargo build -p compiler --release`
- `./target/release/faust-rs --timeout 5000 rad_fxlms1.dsp > /tmp/rad_fxlms1.cpp`

The release repro completed in about 1.6 seconds after the cache was
shared across output roots; the earlier unpatched run had to be killed
after more than a minute.

## fix(wasm-ffi): unblock rust-branch faustwasm compiler module

Commit date: 2026-04-29

Source commit: this commit

The raw `faustwasm` compiler-module path was trapping under Node before
parsing user DSP code. The panic came from `std::time::Instant::now()`
inside the compiler facade timing helper: `wasm32-unknown-unknown`
does not provide a host clock for this raw ABI, and `Instant::now()`
lowers to a panic in that target.

The compiler facade now skips `Instant` entirely when no timing sink is
installed. Native CLI `-time` behavior is unchanged because timing still
uses `Instant` when a sink exists; the `wasm-ffi` path uses the no-sink
mode and can compile without touching unsupported clock APIs.

The same validation exposed the next rust-branch integration issue:
project-local imports such as `ad.lib` cannot be passed with `-I` because
the raw compiler module does not expose an Emscripten-style filesystem.
The `wasm-ffi` build script now supports embedding multiple `.lib` roots
from `FAUST_LIB_PATH`, preserving search order and first-root-wins
semantics. This allows a compiler module to bundle both a local project
library root and the standard Faust libraries.

### Validation

- `cargo test -p compiler timing_helper_without_sink_runs_without_measuring`
- `cargo test -p wasm-ffi`
- `cargo clippy -p compiler -p wasm-ffi --all-targets -- -D warnings`
- `FAUST_LIB_PATH=/Users/letz/Developpements/RUST/faust-rs:/Users/letz/faust/libraries cargo run -p xtask -- build-faustwasm-compiler-module`
- `node scripts/faust2wasm.js /Users/letz/Developpements/RUST/faust-rs/fad_pendulum_cello4.dsp test/fad_pendulum_cello4 -pwa -rust-compiler /Users/letz/Developpements/RUST/faust-rs/target/wasm32-unknown-unknown/release/faust_wasm_ffi.wasm`

## docs(propagate): expand stateful RAD Rustdoc

Commit date: 2026-04-29

Source commit: this commit

Expanded Rustdoc for the stateful RAD preparatory modules:

- `crates/propagate/src/stateful_rad.rs` is now self-contained for the
  phase-E0 recursive classifier. The module docs spell out the
  De Bruijn input representation, the three-fact abstract expression
  model, the monotonic `LinearLti < LinearTimeVarying < Nonlinear`
  lattice, conservative boundaries, temporal-delay caveats, and
  memoization complexity. Public API docs now describe the exact
  contracts for `RadRecLinearity`, `RecRadMode`,
  `classify_de_bruijn_rec_group`,
  `classify_de_bruijn_rec_rad_mode`, and
  `classify_recursive_projection_rad_mode`.
- `crates/propagate/src/transpose_ad.rs` now documents the phase-E1
  LTI transpose scaffold in detail: accepted De Bruijn input shape,
  matrix-term extraction, emitted adjoint recurrence form, current
  conservative limits, error semantics, and why temporal LTI terms are
  still blocked until the reverse-block convention exists.

Private helper docs were also filled in for
`--document-private-items`, including the coefficient variation model,
local arithmetic/branch rules, `LinearTerm` matrix-entry semantics,
the affine extractor, and recursive-reference detection.

### Validation

- `cargo test -p propagate stateful_rad --lib`
- `cargo test -p propagate transpose_ad --lib`
- `cargo doc -p propagate --no-deps --document-private-items`

The doc build still reports the pre-existing warnings in
`crates/propagate/src/lib.rs` and `crates/propagate/src/reverse_ad.rs`;
no warnings were introduced from `stateful_rad.rs` or `transpose_ad.rs`.

## test(corpus): RAD demonstrative fixtures for trainable feed-forward shapes

Commit date: 2026-04-29

Source commit: 6548c6b

Four corpus fixtures showing what phase-1 RAD covers end-to-end today,
sized to drive both the gradient-descent example and the
RAD-vs-FAD performance comparison:

- `rad_gain_bias_train.dsp` — minimal `gain·x + bias` map.
- `rad_waveshaper_polynomial_coefs.dsp` — 3rd-order polynomial
  waveshaper with coefficients `(c0, c1, c2, c3)` as seeds. The host
  pre-computes `(x, x², x³)` so the body uses each `_` exactly once.
- `rad_static_softclip_drive.dsp` — stateless `tanh(drive·x + bias) /
  drive`, exercises the unary FFun chain rule.
- `rad_fir_taps_external_delays.dsp` — 4-tap FIR with `c0..c3` as seeds.
  Phase-1 RAD refuses temporal feedback inside `rad(...)`, so the host
  buffers `x[n-k]` and feeds the four taps as four audio channels.

Goldens regenerated.

## feat(codegen,compiler): rad_gradient_descent example

Commit date: 2026-04-29

Source commit: 658909e

Adds two minimal public APIs on `FbcDspInstance` for host-driven
training loops:

- `ui_instructions()` exposes the UI-instruction list collected at
  compile time so the host can discover slider labels and the bound
  real-heap offsets.
- `get_real_zone(offset)` / `set_real_zone(offset, value)` provide
  bounds-checked R/W on the real heap.

Then ships `crates/compiler/examples/rad_gradient_descent.rs`, a
self-contained host loop that fits the `rad_gain_bias_train` fixture
against a synthetic target via plain SGD over 512-sample batches.
The example recovers `gain` and `bias` to within ~1e-6 of the true
values in 400 iterations.

This is the concrete answer to "what is RAD good for in `faust-rs`
today": any feed-forward DSP whose parameters live in `hslider` /
`vslider` / `numentry` controls is fittable by a few lines of host
Rust + the existing `rad(...)` surface.

Run with: `cargo run -p compiler --example rad_gradient_descent`.

## bench(compiler): rad_vs_fad_perf comparison example

Commit date: 2026-04-29

Source commit: f9f775b

Side-by-side RAD-vs-FAD harness covering five trainable shapes:

- `gain·x + bias` (1 in, 2 seeds),
- polynomial waveshaper (3 in, 4 seeds),
- static softclip with `tanh` (1 in, 2 seeds),
- FIR taps with host-fed delays (4 in, 4 seeds),
- deep multiplicative chain (1 in, 6 seeds — adjoint-sum stress).

For each pair the binary reports compile time, interp bytecode size,
and per-frame compute averaged over 200 reps on a 4096-sample buffer.

Concrete result on the current pipeline (release builds): bytecode
size and per-frame compute are within ~5 % between RAD and FAD on
every shape, **including** the deep multiplicative chain. The
signal-prepare + CSE pipeline already absorbs the symbolic-growth
cost of reverse-mode accumulation that plan §17 risk #2 warned
about. At the scales explored here, RAD is essentially free relative
to FAD for feed-forward seeds.

Run with: `cargo run --release -p compiler --example rad_vs_fad_perf`.

## docs(propagate): RAD usage guide

Commit date: 2026-04-29

Source commit: this commit

`docs/rad-usage-en.md` — host-facing usage guide that walks through
the four corpus fixtures and the SGD loop pattern, plus a section on
host-managed delay lines, a performance note pinning the RAD-vs-FAD
parity result, and a "limits to keep in mind" recap covering the
phase-1 boundary (no temporal feedback, implicit all-ones cotangent,
no auto seed discovery, no backend-side reverse-time evaluation).

The doc cross-links the existing `docs/rad-note-en.md` (algorithm and
rule table) and the implementation plan §19 (feasibility analysis for
stateful RAD), so a reader landing on either node has a clear path to
the others.

### Validation

`cargo run -p compiler --example rad_gradient_descent` and
`cargo run --release -p compiler --example rad_vs_fad_perf` both run
to completion locally. `cargo test --workspace` remains green.

## feat(corpus,compiler): rad_adaptive_notch demo (LMS on FIR notch)

Commit date: 2026-04-29

Source commit: this commit

A recognisable adaptive-filtering use case demonstrating that phase-1
RAD already covers a real DSP problem end-to-end.

### Implementation

- New corpus fixture
  `tests/corpus/rad_adaptive_notch_omega.dsp` — a 3-tap FIR notch
  with the radian frequency `omega` exposed as the single `rad(...)`
  seed:

  ```faust
  notch(xn, xn1, xn2) = xn - 2.0 * cos(omega) * xn1 + xn2;
  process = rad(notch, omega);
  ```

  The transfer function `H(z) = 1 - 2·cos(ω)·z⁻¹ + z⁻²` places zeros
  on the unit circle at `e^(±j·ω)`. The host buffers
  `x[n], x[n-1], x[n-2]` and feeds them as three audio channels so
  the differentiated body stays strictly feed-forward.

- New example
  `crates/compiler/examples/rad_adaptive_notch.rs` — synthesises a
  noisy sinusoid at `ω* = 1.3 rad/sample`, runs the rad-compiled DSP
  block-by-block, accumulates `loss = E[y²]` and
  `grad = E[2·y·∂y/∂ω]`, and SGDs `ω`. Starting from `ω = 0.4` the
  loop converges to `|ω − ω*| ≈ 2e-4` in under 50 iterations of
  512-sample blocks; the residual loss matches the additive noise
  floor `σ²` as an LMS adaptive notch should.

- Updated `docs/rad-usage-en.md` with a "Pattern: LMS adaptive
  filtering (host-managed delay line)" section that walks through the
  loop and links the example.

### Why it matters

This is the recognisable adaptive-filtering demonstration recommended
in the previous step's recap. It transforms the RAD chantier from
"works on a 2-parameter linear map" to "drives a real DSP adaptation
problem to the noise floor in <50 iterations". Importantly, it pins
the practical scope of phase-1 RAD: any FIR or memoryless nonlinear
filter parameterised by one or more sliders is fittable today, with
the host buffering whatever state the differentiated body cannot
carry.

The lasting friction point this exposes is not in the RAD pass
itself — it is the manual buffering of the delay line in the host.
The phase-E1 transposition path (plan §19.2) addresses this directly
by accepting LTI feedback inside `rad(...)` once the block/tape
convention is wired into the FIR backend.

### Validation

- `cargo run --release -p compiler --example rad_adaptive_notch`
  converges and asserts `|Δω| < 1e-2`.
- `cargo test --workspace` remains green.

## docs(porting): RAD plan §20 — LTI transposition route, decoupled from BPTT

Commit date: 2026-04-29

Source commit: this commit

Extends the RAD implementation plan with an engineering-focused §20
that turns §19.2's "system transposition" route into a concrete plan
which can be activated **independently of phase F (BPTT)**.

### Context

User question: "is it possible to handle LTI forms in RAD already,
independently of BPTT usage?". The answer is yes — phase E1 needs
only block-local reverse iteration plus structural transposition
(both already in scope), while phase F additionally needs a tape of
primal intermediates. The two routes share no critical-path work.

### Implementation

The new §20 (`Engineering plan for the LTI transposition path
(route B), independent of BPTT`) covers:

- §20.1 what is already in place (E0 classifier + E1 scaffold +
  numeric oracle + diagnostic kind),
- §20.2 the block-local boundary contract: compute block size is the
  reverse horizon, terminal adjoint at frame `count` is zero, no
  cross-block adjoint state. This pins the user-visible semantics
  ahead of any wiring,
- §20.3 a concrete signal-IR node (`ReverseTimeRec`) that lowers to
  a backward iteration on a single compute block,
- §20.4 the FIR lowering (rotating-IOTA index flip, terminal-zero
  pre-loop, sample I/O alignment),
- §20.5 mixed forward/reverse compute schedules (interleaved vs.
  split entry points), with a recommendation,
- §20.6 backend coverage notes for interp / Cranelift / C/C++,
- §20.7 the test surface (RAD vs. FAD parity on canonical LTI
  recursions, block-boundary effect, perf bench extension, golden
  regen),
- §20.8 diagnostic and migration plan (`recursive-linear-transpose`
  flips from refusal to lowering; `recursive-block-linear-time-varying`
  and `recursive-bptt-required` remain diagnostic-only),
- §20.9 the phase boundary with F: explicit confirmation that E1
  does not depend on F, and that the design is forward-compatible
  with the §19.4 hybrid once F lands a tape mechanism,
- §20.10 cost estimate (~6 working days for interp-only, ~9 days
  with all three backends),
- §20.11 what this concretely delivers to users (training recursive
  linear filter coefficients, biquad adaptive notch with delay line
  inside `rad(...)`, asymptotic `O(M+N)` advantage over FAD on
  high-order IIR with many adjustable coefficients).

No code changes — this is a planning-document extension that gives a
clear go/no-go for the next chantier.

### Validation

The plan document still renders cleanly in the markdown viewer; no
existing sections were modified.
