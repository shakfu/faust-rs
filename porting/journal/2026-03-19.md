# JOURNAL 2026-03-19

Entries originally recorded under this journal day.

- Sorted by Git commit recency (newest first).
- `Commit date` is the commit day for the entry addition.

---

### `eval`: memoize `eval(expr, env)` like C++ and bound matcher-state caching

Commit date: 2026-03-19

Source commit: `pending (recorded with this journal update)`

Source: `crates/eval/src/lib.rs`, `crates/eval/tests/core_eval.rs`, `crates/propagate/src/lib.rs`, `crates/sigtype/src/rules.rs`, `porting/MEMOIZATION.md`

#### Context

`jprev_demo_test` no longer failed structurally after the waveform fix, but
Rust still spent tens of seconds inside `eval` while the C++ compiler finished
in well under one second.

Profiling reduced the hotspot to evaluator reduction itself:
- `parse` completed quickly,
- `eval` kept revisiting the same higher-order box subgraphs in `jpverb`,
- C++ `eval.cpp` caches `eval(exp, env)` through `getEvalProperty(...)` /
  `setEvalProperty(...)`,
- Rust had no equivalent cache and only memoized `a2sb(...)` and case-rule
  automata.

Porting that cache directly exposed one representational difference:
- C++ stores immutable tree values in the cache,
- Rust stores host-side `EvalValue`,
- `EvalValue::PatternMatcher` still carries mutable per-rule environments,
  so caching it verbatim caused incorrect `PatternMatchFailed` reuse.

#### Changes

1. Added `LoopDetector.eval_cache` keyed by `(TreeId, EnvFrameKey)` and taught
   `EnvFrameKey` to include the source-context identity alongside the lexical
   environment/store identity.
2. Split `eval_value(...)` into a memoized wrapper and an uncached core,
   mirroring C++ `eval(...)` vs `realeval(...)`.
3. Restricted cacheable results to `EvalValue::Box` and `EvalValue::Closure`;
   `EvalValue::PatternMatcher` is intentionally excluded until its state can be
   represented immutably enough for C++-style property caching.
4. Documented this new cache and its current pattern-matcher limitation in
   `porting/MEMOIZATION.md`.
5. Kept the flat-box validation optimization in `propagate`:
   `try_build_flat_box(...)` now walks one shared DAG with a visitation set
   instead of recursively re-validating identical subtrees.
6. Adjusted one stale `eval` arity regression expectation and fixed two
   `clippy` issues in recursive `sigtype` convergence code so the workspace
   gates remain green.

#### Validation

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test`
- `cargo test -p eval --test core_eval -- --nocapture`
- `cargo test -p propagate try_build_flat_box_accepts_deep_shared_box_dag -- --nocapture`
- `cargo run -p compiler -- -pn jprev_demo_test -I /Users/letz/Developpements/faustlibraries /Users/letz/Developpements/faustlibraries/tests/demos_tests.dsp -o /tmp/jprev_rust.cpp`
- `/usr/bin/time -p target/debug/faust-rs -pn jprev_demo_test -I /Users/letz/Developpements/faustlibraries /Users/letz/Developpements/faustlibraries/tests/demos_tests.dsp -o /tmp/jprev_rust.cpp`

### `eval`: treat `BOXWAVEFORM` as a leaf in arity, eval, and `a2sb`

Commit date: 2026-03-19

Source commit: `pending (recorded with this journal update)`

Source: `crates/eval/src/lib.rs`, `crates/eval/tests/core_eval.rs`

#### Context

`jprev_demo_test` first failed in `propagate` with:
- sequential composition mismatch `4 != 3`
- left side built from `waveform{...}`
- right side `rdtable`

The root cause was in `eval`, not `propagate`:
- local arity inference did not recognize `BoxMatch::Waveform(_)`, so
  `rdtable(waveform{...}, x)` under-counted the waveform argument and inserted
  one extra implicit wire during non-closure application lowering,
- `eval_value(...)` and `a2sb(...)` still treated `BOXWAVEFORM` like a generic
  tree node and recursively traversed its internal `cons(...)` payload list,
  which caused stack overflows on large compile-time tables.

The C++ evaluator treats waveform boxes as already-normal first-order values:
- `isBoxWaveform(box) => box` in `eval.cpp`,
- `a2sb` also leaves them unchanged.

#### Changes

1. Added local evaluator arity support for `BoxMatch::Waveform(_)` as `(0, 2)`
   so `rdtable(waveform{...}, x)` is no longer rewritten with a spurious extra
   wire during `apply_list(...)`.
2. Updated `eval_value(...)` to return waveform boxes as leaf normal forms,
   matching the C++ evaluator.
3. Updated `a2sb(...)` to leave waveform boxes unchanged instead of recursively
   lowering their `cons` payload children.
4. Added regressions for:
   - a minimal `rdtable(waveform{2,3,5,7}, 1)` application case,
   - a large inline 2048-element waveform that previously overflowed the stack.

#### Validation

- `cargo fmt --all`
- `cargo test -p eval eval_process_applies_rdtable_waveform_argument_without_extra_wire -- --nocapture`
- `cargo test -p eval eval_process_keeps_large_waveform_nodes_as_leaf_values -- --nocapture`
- `cargo run -p compiler -- /tmp/wave2048.dsp`
- `cargo run -p compiler -- --dump-sig /tmp/wave2048.dsp`

### `eval`: resolve iterator counts through `a2sb(eval(...))` for `inputs/outputs`

Commit date: 2026-03-19

Source commit: `pending (recorded with this journal update)`

Source: `crates/eval/src/lib.rs`, `crates/eval/tests/core_eval.rs`

#### Context

`reverbTank_demo_test` failed with:
- `iteration count is not an int node`
- `expr=BOXINPUTS(BOXCLOSURE(...))`

The C++ evaluator handles iterative combinator counts through `eval2int(...)`.
For `inputs(expr)` / `outputs(expr)`, it evaluates the inner expression and
then computes arity on `a2sb(eval(expr,...))`, not on the raw residual closure.

Rust still computed `infer_box_arity(...)` directly on `eval(expr, ...)`, which
left `BOXINPUTS(BOXCLOSURE(...))` unresolved in some residual-closure cases and
caused the iterator-count path to reject a value that C++ accepts.

#### Changes

1. Updated the `BoxMatch::Inputs` and `BoxMatch::Outputs` evaluator branches so
   they now call `a2sb(...)` on the evaluated inner box before computing arity,
   matching the C++ `eval.cpp` logic.
2. Kept the existing fallback behavior for cases where arity still cannot be
   inferred after symbolic lowering.
3. Added a regression test proving that `inputs(f(1))` can be used as an
   iterative combinator count when `f(1)` is a residual closure, matching the
   C++ evaluator contract.

#### Validation

- `cargo test -p eval --test core_eval -- --nocapture`
- `cargo test`

### `eval`: align label placeholder integer interpolation with C++ `tree2int`

Commit date: 2026-03-19

Source commit: `pending (recorded with this journal update)`

Source: `crates/eval/src/lib.rs`, `crates/eval/tests/core_eval.rs`

#### Context

`debug.lib` uses label placeholders such as `%att` and `%rel` for probe
metadata. In the C++ compiler, these placeholders are resolved through
`evalLabel(...) -> eval2int(...) -> tree2int(...)`, and `tree2int(...)`
accepts both integer atoms and floating-point atoms, truncating real constants
toward zero.

The Rust evaluator still required the placeholder expression to reduce to a
literal integer atom only. This created a parity bug on
`probe_env_lin_test` / `debug_tests.dsp`, where `att = 0.001` should render as
`attack=0` in the UI metadata.

#### Changes

1. Updated `eval_ident_to_constant_int(...)` so label placeholders now accept:
   - integer constant scalar signals,
   - floating-point constant scalar signals, truncated toward zero like C++
     `tree2int(...)`.
2. Documented the C++ provenance and truncation rule directly in Rustdoc.
3. Added a regression test proving `%att` with a real constant placeholder
   renders as `0`, matching the C++ evaluator.

#### Validation

- `cargo test -p eval eval_process_widget_label_truncates_real_placeholder_like_cpp -- --nocapture`
- `cargo run -p compiler -- -pn probe_env_lin_test /Users/letz/Developpements/faustlibraries/tests/debug_tests.dsp`

### `sigtype`/`interval`: stabilize recursive canonical typing and restore `cubic_distortion.dsp`

Commit date: 2026-03-19

Source commit: `pending (recorded with this journal update)`

Source: `crates/sigtype/src/types.rs`, `crates/sigtype/src/rules.rs`, `crates/interval/src/lib.rs`, `crates/interval/src/ops/arithmetic.rs`, `crates/interval/src/ops/logic.rs`, `crates/interval/src/ops/trig.rs`

#### Context

After the first structural `sigtype` refactor steps, the remaining regressions
were no longer promotion bugs but failures in the canonical typing core:
- interval precision arithmetic could overflow on large recursive products and
  shifts,
- recursive typing could fail to converge because Rust `SigType` equality was
  still stricter than the C++ `operator==(Type, Type)` semantics,
- and even after recursive convergence, the canonical annotation map could
  remain incomplete for reachable projection nodes, which then broke promotion
  consumers such as `cubic_distortion.dsp`.

This step finishes the parity-critical part of the refactor by making the
recursive solver stable enough for the current fast-lane and by aligning the
type equality model with the C++ compiler.

#### Changes

1. Added saturating helpers for interval precision arithmetic and used them in
   arithmetic, logic, and trig interval operations so recursive typing no
   longer panics on precision overflow in Rust.
2. Fixed a parity bug in interval right-shift precision tracking:
   Rust now follows the C++ rule and uses the upper shift bound for `rsh`
   precision updates.
3. Realigned `SigType` equality with the C++ `operator==(Type, Type)` rules:
   - `SimpleType` equality now ignores `res` and interval `lsb`,
   - `TableType` equality compares only `content`,
   - `TupletType` equality compares only `components`.
4. Updated the recursive solve loop so convergence is tested on the widened
   approximation that is fed back into the next iteration, instead of the raw
   body re-inference value. This is a Rust value-semantics adaptation of the
   C++ recursive driver, not a 1:1 control-flow port.
5. Added a reachable-node population pass after recursive solving so the final
   canonical `SigId -> SigType` map is complete for all reachable nodes,
   including projections that can otherwise be skipped by memoized recursive
   inference.
6. Added focused regression tests covering:
   - saturating interval multiplication precision,
   - `rsh` precision parity,
   - C++-style `SigType` equality semantics,
   - recursive multiplication convergence and projection-type population.

#### Limitation

The Rust recursive typing driver is now substantially closer to the C++
semantics, but it is still not a structural 1:1 port of
`typeAnnotation(...)` / `updateRecTypes(...)`.

Current adapted mechanism:
- explicit recursive-group discovery and global solve phase,
- value-based `SigType` maps instead of pointer-attached mutable type cells,
- convergence judged on widened approximations re-injected into the next
  iteration,
- explicit reachable-type population after solving to restore a complete
  canonical map.

This limitation remains documented in Rustdoc because it matters for future
maintenance and for remaining parity work.

#### Validation

- `cargo fmt --all`
- `cargo test -p interval --lib -- --nocapture`
- `cargo test -p sigtype --lib -- --nocapture`
- `cargo test -p compiler --test signal_fir_lane -- --nocapture`
- `cargo run -p compiler -- --dump-fir-verify /Users/letz/Developpements/faust/tests/impulse-tests/dsp/cubic_distortion.dsp`
- `cargo test`

### `sigtype`: switch `annotate()` to a global recursive-group driver

Commit date: 2026-03-19

Source commit: `pending (recorded with this journal update)`

Source: `crates/sigtype/src/rules.rs`

#### Context

The recursive parity plan cannot be reached while recursion is still solved
locally inside `infer_sym_rec(...)`. The next step was therefore to move the
active path toward the C++ control structure:
- solve recursive groups globally first,
- then type bodies and outputs under those solved approximations.

#### Changes

1. Switched `TypeAnnotator::annotate(...)` to:
   - discover symbolic recursive groups first,
   - build a `RecTypingState`,
   - solve recursive approximations before ordinary output typing,
   - then seed the solved recursive groups back into the main annotation pass.
2. Added an explicit `update_rec_types(...)` helper in the Rust driver, mirroring
   the C++ `updateRecTypes(...)` structure:
   - seed all recursive groups up front,
   - infer each recursive body list under that seed,
   - merge intervals by intersection or reunion depending on the phase.
3. Replaced the old local recursive solver in `infer_sym_rec(...)` with a
   seeded lookup / conservative fallback.
4. Moved widening to a dedicated `apply_recursive_widening(...)` helper so the
   global recursive driver owns the recursive interval update policy.

#### Current Status

This is a major structural step toward parity:
- the active path is no longer driven by subtree invalidation inside
  `infer_sym_rec(...)`,
- recursive groups are now solved as an explicit set before final annotation.

Known remaining blocker after this step:
- `cubic_distortion.dsp` no longer fails with missing canonical recursive
  types, but now exposes an overflow in interval arithmetic
  (`crates/interval/src/ops/arithmetic.rs`) during deeper canonical typing.
- That is a new lower-level blocker and should be handled next; it is not
  committed away in promotion.

#### Validation

- `cargo fmt --all`
- `cargo test -p sigtype --lib -- --nocapture`
- `cargo test -p transform --lib signal_prepare::tests::recursive_fixpoint_recomputes_body_types_after_real_widening -- --nocapture`
- `cargo run -p compiler -- tests/corpus/rep_38_sine_phasor.dsp`

### `sigtype`: scaffold explicit recursive-group typing state for `updateRecTypes` parity

Commit date: 2026-03-19

Source commit: `pending (recorded with this journal update)`

Source: `crates/sigtype/src/rules.rs`

#### Context

The new recursive parity plan requires moving away from the current purely
node-driven recursive typing loop. Before switching the active algorithm, Rust
needs the same kind of explicit recursive-group state that C++
`typeAnnotation(...)` prepares before calling `updateRecTypes(...)`.

#### Changes

1. Added a private `RecGroup` scaffold in `sigtype` to record:
   - the `SYMREC` node,
   - its recursive variable,
   - its body list,
   - and its arity.
2. Added a private `RecTypingState` scaffold that mirrors the main C++ driver
   vectors:
   - `groups`,
   - `current`,
   - `upper`,
   - `age_min`,
   - `age_max`.
3. Added deterministic recursive-group discovery helpers:
   - DFS over the output forest,
   - DAG-aware deduplication,
   - explicit body-list arity validation.
4. Documented the new scaffolding in Rustdoc as a preparation step for the
   future structural port of `typeAnnotation/updateRecTypes`.

No behavioural switch was made yet: the active recursive typing path still uses
the existing `infer_sym_rec(...)` logic until the explicit driver is ported in
the next step.

#### Validation

- `cargo fmt --all`
- `cargo test -p sigtype discover_recursive_groups_keeps_dfs_order_and_arity -- --nocapture`
- `cargo test -p sigtype rec_typing_state_scaffold_matches_discovered_group_shapes -- --nocapture`
- `cargo test -p sigtype --lib -- --nocapture`

### `porting`: add a dedicated recursive `sigtype` parity refactor plan

Commit date: 2026-03-19

Source commit: `pending (recorded with this journal update)`

Source: `porting/recursive-sigtype-parity-plan-2026-03-19-en.md`

#### Context

The recent fast-lane regressions made the remaining gap clearer: the current
Rust recursive typing path still differs structurally from the C++
`typeAnnotation(...)` / `updateRecTypes(...)` algorithm.

Local fixes improved behaviour, but they did not remove the architectural
weakness:
- recursive convergence is still encoded in a node-driven memoized typer,
- canonical type maps can still become incomplete under recursive retyping,
- promotion then ends up compensating for typing instability instead of simply
  consuming a stable canonical annotation.

Before continuing with more local fixes, the port needs an explicit parity plan
for the recursive typing algorithm itself.

#### Changes

Added a dedicated porting plan:
- `porting/recursive-sigtype-parity-plan-2026-03-19-en.md`

The plan documents:
- the exact structural difference between the current Rust implementation and
  the C++ recursive typing driver,
- the target architecture for a global recursive-group typing state,
- the intended Rust equivalent of `updateRecTypes(...)`,
- the migration phases,
- the validation matrix and exit criteria,
- and the temporary nature of promotion-side fallbacks introduced only to
  survive incomplete canonical type maps.

#### Validation

- documentation-only change

### `sigtype`/`normalize`/`transform`: make canonical typing the single fast-lane source and realign recursive inference

Commit date: 2026-03-19

Source commit: `pending (recorded with this journal update)`

Source: `crates/sigtype/src/rules.rs`, `crates/normalize/src/normalform.rs`, `crates/transform/src/signal_prepare.rs`, `crates/fir/src/checker.rs`

#### Context

`goertzelOpt_test` exposed two coupled issues in the fast-lane:
- promotion lived in `transform::signal_prepare` instead of the canonical
  normalization/type pipeline,
- `signal_prepare` still ran its own reduced recursive typer/fixpoint, which
  diverged from the newer `sigtype`-based path and caused contradictory
  integer/real recursion decisions.

Subsequent regressions on `signal_fir_lane` showed that simply switching to
canonical `SigType` was not enough: the Rust `sigtype` recursion port still
used an oversimplified recursive algorithm that did not match the C++ `TREC`
seed semantics and did not re-evaluate memoised body subtrees under updated
recursive approximations.

#### Changes

1. Moved the detailed fast-lane promotion pass from `transform` into
   `normalize`, so the active preparation path now consumes canonical
   `sigtype::TypeAnnotator` results before lowering.
2. Simplified `transform::signal_prepare` so it derives its reduced
   `SimpleSigType` view from canonical `SigType` instead of running a second
   local recursive typer.
3. Realigned recursive `sigtype` inference with the C++ intent:
   - recursive groups are now seeded from the C++-style `TREC` approximation
     instead of a single maximal scalar,
   - recursive symbol references read the current approximation attached to the
     recursion variable,
   - memoised recursive body subtrees are invalidated between fixpoint steps so
     the next iteration is re-inferred under the updated approximation,
   - recursive interval updates now mirror the default C++ widening behaviour:
     after recomputing one recursive body, the new interval is first reunited
     with the previous approximation and any drifting bound is widened
     immediately to the `TRECMAX` carrier bound.
4. Realigned `Abs` typing with the C++ primitive semantics: it now preserves
   the input nature and only updates the interval, instead of being forced to
   `Real`.
5. Kept one explicit fast-lane-only adaptation in `transform`: a completely
   unconstrained self-recursion still closes to `Real` in the reduced
   `SimpleSigType` view so FIR lowering preserves the historical fallback used
   by the existing fast-lane tests.
6. Updated FIR verification expectations so comparison binops declared as
   `Int32` (“boolean int”) match the fast-lane/C++ signal typing model.
7. Restored canonical typing for `SIGFFUN` so the shared `sigtype` pass now
   uses the declared foreign return type and the argument-dependent
   variability/computability instead of falling back to a maximal real type.
8. Updated the affected `propagate` flat-box regression test to build
   `with_rec_def` using the current parser-shape definition lists and to expect
   the eager `withlocaldef` lowering introduced earlier in the day.

#### Limitation

The Rust recursive typing path is now aligned with C++ on the initial
approximation (`TREC`) and on iterative re-evaluation intent, but it is still
not a structural port of `typeAnnotation(...)` / `updateRecTypes(...)`.

Current Rust mechanism:
- memoised `SigId -> SigType` environment,
- local invalidation of the recursive body sub-DAG between fixpoint steps,
- re-inference under the updated recursive approximation,
- C++-style interval widening on recursive bounds.

Current C++ mechanism:
- global recursive-group discovery,
- explicit vectors of recursive group types,
- repeated `updateRecTypes(...)` passes over those vectors.

This limitation is now documented in Rustdoc in `sigtype::rules` and in the
fast-lane preparation docs in `transform::signal_prepare`.

#### Validation

- `cargo fmt --all`
- `cargo test -p normalize promote_signals_no_change_for_same_type_binop -- --nocapture`
- `cargo test -p transform prepare_signals_for_fir_promotes_real_mul_operands_before_binop -- --nocapture`
- `cargo test -p transform comparison_binops_lower_to_int32_boolean_values -- --nocapture`
- `cargo test -p transform prepare_signals_for_fir_keeps_integer_recursive_min_feedback_int -- --nocapture`
- `cargo test -p transform prepare_signals_for_fir_keeps_integer_recursive_abs_feedback_int -- --nocapture`
- `cargo test -p transform prepare_signals_for_fir_closes_unresolved_recursive_types_to_real -- --nocapture`
- `cargo test -p compiler --test signal_fir_lane fastlane_cpp_compiles_noise_smoo_slider_fixture -- --nocapture`
- `cargo test -p compiler --test signal_fir_lane fastlane_cpp_keeps_integer_recursive_min_feedback_in_int_state -- --nocapture`
- `cargo test -p compiler --test signal_fir_lane fastlane_cpp_keeps_integer_recursive_abs_feedback_in_int_state -- --nocapture`
- `cargo test -p compiler --test signal_fir_lane legacy_and_fastlane_both_compile_lowpass_feedback_fixture -- --nocapture`
- `cargo test -p compiler --test signal_fir_lane -- --nocapture`
- `cargo test -p compiler --test zita_pipeline -- --nocapture`
- `cargo test -p transform --lib -- --nocapture`
- `cargo test -p propagate --test core_api flat_box_builder_rejects_evaluator_only_families -- --nocapture`
- `cargo run -p compiler -- -pn goertzelOpt_test --dump-fir-verify /Users/letz/Developpements/faustlibraries/tests/analyzers_tests.dsp`
- `cargo run -p compiler -- tests/corpus/rep_38_sine_phasor.dsp`
- `cargo test`

### `boxes`/`eval`/`parser`: make `letrec` parity eager and remove the eval fallback

Commit date: 2026-03-19

Source commit: `pending (recorded with this journal update)`

Source: `crates/boxes/src/lib.rs`, `crates/boxes/tests/core_api.rs`, `crates/eval/src/lib.rs`, `crates/eval/tests/core_eval.rs`, `crates/parser/tests/structural_cpp_differential.rs`

#### Context

`spectralCentroid_test` in `faustlibraries/tests/analyzers_tests.dsp` exposed a
remaining Rust/C++ divergence around `letrec`.

C++ lowers `letrec` eagerly in `boxWithRecDef(...)` from
`compiler/boxes/boxes.cpp` into a `with_local_def(...)` carrying:
- one synthetic `LETRECBODY = rec(...)` definition,
- one projection definition per recursive binding.

Rust still preserved an adapted `BOXWITHRECDEF` node on the parser/boxes
production path and relied on `eval` to expand or interpret it later. That was
not C++-equivalent and led to evaluation failures on real library code.

#### Changes

1. Moved `letrec` parity to the correct layer: `boxes::node_with_rec_def(...)`
   now performs eager C++-style lowering instead of emitting a persistent
   `BOXWITHRECDEF` node for normal parser output.
2. Added dedicated helpers in `boxes` for the `LETRECBODY` expansion and
   documented their parser-shape invariant:
   definitions must arrive as normalized parser cells
   `cons(name, cons(args, expr))`.
3. Corrected the expression extraction step of the lowering to respect the Rust
   parser’s normalized definition payload instead of treating the whole payload
   as the expression.
4. Removed the active `eval` fallback for `WithRecDef`: reaching
   `BOXWITHRECDEF` in `eval` now raises an explicit internal invariant error.
5. Added structural regressions proving:
   - parser production no longer leaves `BOXWITHRECDEF` in the output tree,
   - parser-lowered `letrec` evaluates successfully without any `eval`
     fallback,
   - manually injected legacy `BOXWITHRECDEF` nodes are rejected explicitly,
   - `BoxBuilder::with_rec_def(...)` now expands eagerly to `WithLocalDef(...)`
     and exposes the synthetic `LETRECBODY` binding.
6. Expanded Rustdoc/comments to make the new parity point and invariant
   explicit: `boxes` owns `letrec` lowering; `eval` must never repair it.

#### Validation

- `cargo fmt --all`
- `cargo test -p boxes --test core_api builder_matches_iterative_scope_and_module_families -- --nocapture`
- `cargo test -p parser production_parser_expands_letrec_to_with_local_def_like_cpp -- --nocapture`
- `cargo test -p eval eval_process_accepts_parser_lowered_letrec_without_eval_fallback -- --nocapture`
- `cargo test -p eval eval_process_rejects_legacy_with_rec_def_nodes -- --nocapture`
- `cargo run -p compiler -- -pn spectralCentroid_test /Users/letz/Developpements/faustlibraries/tests/analyzers_tests.dsp`

### `transform`/`codegen`: lower foreign math calls through fast-lane FIR and backend runtimes

Commit date: 2026-03-19

Source commit: `pending (recorded with this journal update)`

Source: `crates/transform/src/signal_prepare.rs`, `crates/transform/src/signal_fir/module.rs`, `crates/transform/src/signal_fir/mod.rs`, `crates/codegen/src/backends/cranelift/mod.rs`

#### Context

`dsp/math.dsp` failed in the fast-lane with an unsupported `SIGFFUN` node for
`isnanf(...)`. The prepared signal forest already preserved foreign function
calls, but the FIR lowerer still rejected them in Step 2C.

Once `SIGFFUN` lowering was added, `-lang interp` worked immediately because the
interpreter backend already mapped `isnanf`/`isinf`/`copysign`-style calls to
FBC opcodes. `-lang cranelift` still fell back to the stub because its subset
matcher and lowering path did not accept these direct foreign math calls.

#### Changes

1. Corrected `signal_prepare` reduced typing for `SIGFFUN`: the result type now
   follows the `FFUN(signature, ...)` return code instead of defaulting every
   foreign call to `Real`.
2. Added fast-lane `SIGFFUN` lowering in Step 2C by decoding raw `FFUN`
   descriptors from `TreeArena`, selecting the precision-appropriate symbol
   name (`isnanf` vs `isnan`), and emitting FIR `FunCall` + extern prototypes.
3. Added transform regressions covering:
   - foreign-function return typing in `prepare_signals_for_fir`,
   - `SIGFFUN -> Fir::FunCall` lowering and prototype emission.
4. Extended Cranelift subset lowering/import registration for the foreign math
   calls exercised by `math.dsp`:
   - `isnanf` / `isnan`
   - `isinff` / `isinf`
   - `copysignf` / `copysign`
   - `acoshf` / `acosh`
   - `asinhf` / `asinh`
   - `atanhf` / `atanh`
   - `coshf` / `cosh`
   - `sinhf` / `sinh`
   - `tanhf` / `tanh`
5. Added a Cranelift regression proving that one supported foreign subset call
   lowers all the way to JIT codegen instead of triggering subset-gap fallback.

#### Validation

- `cargo test -p transform prepare_signals_for_fir_uses_foreign_function_return_type -- --nocapture`
- `cargo test -p transform foreign_function_calls_lower_to_fir_fun_calls_and_prototypes -- --nocapture`
- `cargo test -p codegen compile_module_lowers_supported_foreign_fun_subset_call -- --nocapture`
- `cargo run -p compiler -- /Users/letz/Developpements/faust/tests/impulse-tests/dsp/math.dsp`
- `cargo run -p compiler -- -lang interp /Users/letz/Developpements/faust/tests/impulse-tests/dsp/math.dsp`
- `cargo run -p compiler -- -lang cranelift /Users/letz/Developpements/faust/tests/impulse-tests/dsp/math.dsp`

---

### `transform`: memoize unary symbolic recursion group discovery on shared DAGs

Commit date: 2026-03-19

Source commit: `pending (recorded with this journal update)`

Source: `crates/transform/src/signal_prepare.rs`, `porting/MEMOIZATION.md`

#### Context

The unary recursive projection canonicalization added for `Birds.dsp` introduced
an unbounded traversal cost on large shared signal DAGs. On
`dsp/cubic_distortion.dsp`, `faust-rs` appeared to hang in
`prepare_signals_for_fir(...)`.

Sampling confirmed the hot path was entirely inside
`collect_unary_sym_groups(...)`, recursively revisiting the same shared
subgraphs while discovering unary symbolic recursion groups.

#### Changes

1. Added a `HashSet<SigId>` visited set to `collect_unary_sym_groups(...)` so
   each signal node is analyzed at most once per prepared forest.
2. Kept the existing structural list expansion semantics while ensuring shared
   DAGs no longer trigger exponential traversal during unary-group discovery.
3. Added a focused regression test that builds a deep shared "diamond DAG"
   around a unary recursive projection and verifies preparation completes.
4. Documented the new traversal memoization explicitly in `MEMOIZATION.md`.

#### Validation

- `cargo test -p transform prepare_signals_for_fir_handles_shared_unary_recursion_dag_linearly -- --nocapture`
- `cargo test -p transform prepare_signals_for_fir_canonicalizes_unary_recursive_projection_indices -- --nocapture`
- `cargo test -p compiler --test zita_pipeline -- --nocapture`
- `cargo run -p compiler -- /Users/letz/Developpements/faust/tests/impulse-tests/dsp/cubic_distortion.dsp`
