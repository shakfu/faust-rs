# 2026-05-20

## Update supported subset with Julia backend and RAD rule sharing

Commit date: 2026-05-20
Source commit: this commit (`Update supported subset with Julia backend and RAD rule sharing`)

Updated `porting/faust-rs-supported-faust-subset-en.md` to reflect the latest
backend and AD status:

- the initial Julia backend selected by `-lang julia`, including `-double`,
  architecture wrapping, Julia UI symbol zones, `faust_wrap_int32`, and the
  `fmod`/`atan2` helper aliases;
- the current Julia caveat that it is a functional first slice over the shared
  FIR fast-lane, not full C++ `-lang julia` parity;
- the 2026-05-17 RAD local-rule factorization through `signals::ad_rules`,
  where Signal symbolic RAD and FIR/BRA lowering share backend-neutral local
  rule classification and formulas while keeping tape loading, real storage,
  loop scheduling, and FIR typing local to `transform`.

Regenerated the linked corpus/backend reports so the document's summary counts
match the current `tests/corpus` snapshot. The refreshed reports now cover 190
corpus files, with 94 `OK/OK`, 18 `ERR/ERR`, 0 `OK/ERR`, and 78 intentional
Rust-extension `ERR/OK` front-end cases. The fast-lane backend report records
93 portable C/C++ parity cases, 0 diffs, and the same remaining portable valid
gap, `rep_18_stream_wrappers.dsp`.

The generated report paths were normalized before commit so checked-in
diagnostics use repository-relative paths such as `tests/corpus/fad_delay.dsp`
instead of local checkout paths.

Workspace README files were also checked and updated where the current status
had drifted: the root README, compiler facade README, codegen README, and the
signals/propagate/transform crate READMEs now mention Julia, WASM/WAST/SVG, and
the shared RAD/BRA rule boundary where relevant.

Validation:

- `cargo run -p xtask -- corpus-status-report`
- `cargo run -p xtask -- backend-full-corpus-diff-report`
- `git diff --check -- porting/faust-rs-supported-faust-subset-en.md porting/phases/phase-4-corpus-status-diff-report-en.md porting/phases/phase-6-backend-full-corpus-diff-report-en.md`
- `rg -n "julia|Julia|WASM|wasm|wast|SVG|svg|FAD|RAD|BlockReverseAD|scaffolded|Scaffolded|0\\.6\\.0" -g 'README.md'`
