# JOURNAL 2026-02-27

Entries originally recorded under this journal day.

- Sorted by Git commit recency (newest first).
- `Commit date` is the commit day for the entry addition.

### Documented interpreter hardening refactor plan: inline `BlockStore*` payload ownership
Commit date: 2026-02-27

Added a dedicated refactor plan in:

- `porting/phases/faust-rust-interpreter-porting-en.md`

Goal:

- replace the current `FbcBlock` side-table model
  (`block_store_data: Vec<(instruction_index, payload)>`)
  with instruction-owned payload for `BlockStoreReal/BlockStoreInt`, closer to
  C++ object semantics and structurally safer during optimizer rewrites.

Why:

- side-table indexing is rewrite-sensitive and previously allowed payload-loss
  regressions when blocks were rebuilt.
- instruction-owned payload removes this class of bug by construction.

Plan content added:

- target representation sketch (`FbcInstr` style enum)
- 6-step incremental migration sequence (serializer -> executor -> optimizer ->
  side-table removal)
- explicit validation gates:
  - unit tests across serialization/optimizer/executor
  - optimization invariants per level (`1..=6`)
  - runtime differential checks (`opt_level=0` vs `opt_level=6`) on table-heavy `.fbc` cases
- acceptance criteria for completion.

### Fix Interp regression: preserve `BlockStore*` payloads during optimizer rewrites
Commit date: 2026-02-27

Fixed a runtime regression in the interpreter backend where optimized `.fbc`
programs could lose static table initialization data.

Root cause:

- `optimizer::apply_rewriter` rebuilt `FbcBlock` instructions but dropped
  `block_store_data` metadata attached to `BlockStoreReal`/`BlockStoreInt`.
- this made table writes in `static_init_block` no-op at runtime, yielding
  incorrect all-zero audio on some programs (for example waveform tables).

Changes:

- `crates/codegen/src/backends/interp/optimizer.rs`
  - preserve `block_store_data` on copied instructions during rewrite passes
  - preserve payload on safe 1:1 emits that keep `BlockStore*` opcode
- added non-regression test:
  - `test_block_store_payload_preserved_across_optimization`
  - asserts `BlockStoreReal` payload survives optimization passes

Validation:

- `cargo test -p codegen test_block_store_payload_preserved_across_optimization -- --nocapture` ✅
- manual runtime repro with external `waveform.fbc`:
  - before fix (`opt_level >= 1`): output all zeros
  - after fix (`opt_level = 6` from file): output matches expected sequence
    (`0.25, 0.30, 0.35, 0.40, 0.45, ...`)

### Step E1 completed: parity matrix/documentation closure aligned with implemented Cranelift FFI/runtime state
Commit date: 2026-02-27

Closed documentation tracking for current Cranelift backend state:

- updated `porting/cranelift-dsp-ffi-parity-matrix-en.md`:
  - switched from pure Phase-0 draft wording to active implementation tracking
  - added explicit closure-status table using:
    - `done`
    - `adapted`
    - `deferred-v1`
  - documented real status of constructor families, bitcode contract,
    instance/UI/meta runtime, and deferred LLVM-specific families
- updated `porting/cranelift-backend-plan-en.md` Section 10 with an execution
  status snapshot (`A1`..`E1`) matching current code behavior

Result:

- plan and parity matrix are now aligned with exported symbols and actual
  runtime behavior, with remaining divergences explicitly labeled.

### Step D2 completed: `xtask` orchestration + PR smoke gate integration for Cranelift runtime checks
Commit date: 2026-02-27

Integrated Cranelift runtime validation in `xtask` smoke/nightly orchestration:

- `backend-align-smoke` now runs, in order:
  1. `cranelift_subset_strict_check_cases` (strict subset gate)
  2. `run_cranelift_ffi_runtime_diff_smoke` (executes `cranelift-ffi` differential + UI/meta smoke tests)
  3. existing interp trace checks/diff/fir scan
- `backend-align-nightly` now includes the same two Cranelift gates before
  the interpreter trace checks.

Implementation details:

- added helper `run_cranelift_ffi_runtime_diff_smoke()` in `crates/xtask/src/main.rs`
  which runs:
  - `cargo test -p cranelift-ffi cranelift_interp_runtime_diff_smoke_corpus -- --nocapture`
  - `cargo test -p cranelift-ffi cranelift_ui_meta_callback_smoke_path -- --nocapture`
- backend-align status messages now explicitly report the Cranelift strict/runtime gates.

Validation:

- `cargo fmt --all` ✅
- `cargo test -p xtask -- --nocapture` ✅
- `cargo clippy -p xtask --all-targets -- -D warnings` ✅
- `cargo run -p xtask -- backend-align-smoke --case tests/runtime_corpus/trace_01_passthrough.dsp --skip-golden --skip-diff-lanes --skip-fir-dump-scan`:
  - Cranelift strict subset check ✅
  - Cranelift runtime diff smoke ✅
  - command then fails on an existing `interp-trace-check` snapshot `dsp` field mismatch

### Step D1 completed: differential runtime smoke (`interp` vs `cranelift`) + UI/meta callback smoke
Commit date: 2026-02-27

Added automated runtime differential checks in `cranelift-ffi` test suite:

- new module: `crates/cranelift-ffi/src/diff.rs`
- new tests:
  - `cranelift_interp_runtime_diff_smoke_corpus`
  - `cranelift_ui_meta_callback_smoke_path`

Coverage:

- output differential checks with numeric tolerance (`interp` reference vs
  `cranelift`) on smoke corpus cases:
  - `tests/corpus/rep_01_passthrough.dsp`
  - `tests/corpus/rep_07_nonlinear_clip.dsp`
- explicit callback smoke for UI/meta path:
  - `tests/runtime_corpus/trace_09_ui_slider.dsp`
- failure messages include DSP case path + backend mismatch context
  (channel/sample/value/tolerance)

Stability improvements:

- introduced shared crate-level test serialization guard
  (`crate::test_serial_guard`) and reused it in factory/instance tests to avoid
  cache/global-state races during parallel test execution.

Validation:

- `cargo fmt --all` ✅
- `cargo test -p cranelift-ffi cranelift_interp_runtime_diff_smoke_corpus -- --nocapture` ✅
- `cargo test -p cranelift-ffi cranelift_ui_meta_callback_smoke_path -- --nocapture` ✅
- `cargo test -p cranelift-ffi -- --nocapture` ✅
- `cargo clippy -p cranelift-ffi --all-targets -- -D warnings` ✅

### Step C2 completed: strict subset mode + CI/validation gate wiring
Commit date: 2026-02-27

Hardened Cranelift fallback policy and wired strict checks into backend
validation workflows.

What changed:

- `codegen` Cranelift backend:
  - added `CraneliftOptions::fail_on_subset_gap`
  - strict mode now errors on subset-gap fallback (`UnsupportedModuleShape`)
  - default mode keeps permissive fallback behavior (`compute_body_lowered=false`)
  - added tests:
    - `compile_module_falls_back_to_stub_without_strict_subset_mode`
    - `compile_module_fails_on_subset_gap_with_strict_mode`
- `xtask` integration:
  - added strict subset check runner for runtime corpus cases
  - `backend-align-smoke` now runs Cranelift strict subset check before interp traces
  - `backend-align-nightly` now runs Cranelift strict subset check for full runtime corpus

Validation:

- `cargo fmt --all` ✅
- `cargo test -p codegen compile_module_falls_back_to_stub_without_strict_subset_mode -- --nocapture` ✅
- `cargo test -p codegen compile_module_fails_on_subset_gap_with_strict_mode -- --nocapture` ✅
- `cargo clippy -p codegen --all-targets -- -D warnings` ✅
- `cargo test -p xtask -- --nocapture` ✅
- `cargo clippy -p xtask --all-targets -- -D warnings` ✅
- `cargo run -p xtask -- backend-align-smoke --case tests/runtime_corpus/trace_01_passthrough.dsp --skip-golden --skip-diff-lanes --skip-fir-dump-scan`:
  - Cranelift strict subset check passes
  - command then fails on a pre-existing `interp-trace-check` snapshot mismatch (`field: "dsp"`)

### Step B2 completed: source-backed bitcode contract with runnable factory rebuild
Commit date: 2026-02-27

Replaced the temporary Cranelift bitcode payload with a real source/options
contract that rebuilds runnable factories on read.

What changed:

- introduced `CRANELIFT_FFI_V2_SOURCE` payload format in `cranelift-ffi`
  (`factory.rs`):
  - stores source name, source text, argv list, opt level
  - stores expected `sha_key` and `compile_options`
- read path now recompiles through the real compiler + Cranelift backend and
  validates rebuilt identity fields (`sha_key`, `compile_options`)
- write path now returns null/false for non-source-backed factories (for example
  constructors from signal/box handles) with explicit documented behavior
- extended `CraneliftDspFactory` runtime fields in `types.rs` to persist
  source/options metadata needed for deterministic rebuild
- upgraded bitcode tests to assert runnable rebuild
  (`compiled_jit.is_some()`) and identity preservation across round-trip

Validation:

- `cargo fmt --all` ✅
- `cargo test -p cranelift-ffi -- --nocapture` ✅
- `cargo clippy -p cranelift-ffi --all-targets -- -D warnings` ✅

### Step B1 completed: implement Cranelift factory constructors from Signals/Boxes with shared FIR lowering
Commit date: 2026-02-27

Implemented mandatory constructor families in `cranelift-ffi`:

- `createCCraneliftDSPFactoryFromSignals`
- `createCCraneliftDSPFactoryFromBoxes`

Implementation details:

- added shared Rust bridge helpers in `box-ffi`:
  - `export_fir_from_box_handle`
  - `export_fir_from_signal_array_handle`
  - both lower through the existing box/signal -> FIR fast-lane path
- `cranelift-ffi` now:
  - compiles the exported FIR to Cranelift JIT
  - compiles an interpreter sidecar from the same FIR for UI/meta parity
  - builds factories with a semantic FIR fingerprint in SHA keys, making cache
    identity consistent across string/boxes/signals constructors for identical DSP/options
- added regression test:
  - `boxes_and_signals_constructor_match_string_constructor_sha`

Validation:

- `cargo fmt --all` ✅
- `cargo test -p cranelift-ffi -- --nocapture` ✅
- `cargo clippy -p cranelift-ffi --all-targets -- -D warnings` ✅
- `cargo test -p box-ffi -- --nocapture` ✅
- `cargo clippy -p box-ffi --all-targets -- -D warnings` ✅

### Step C1 completed: corpus-level lowering guardrails for selected runtime subset
Commit date: 2026-02-27

Added an explicit Cranelift lowering guardrail for selected runtime corpus
cases in `cranelift-ffi` tests:

- new test:
  - `factory::tests::selected_runtime_corpus_cases_lower_compute_body`
- scope:
  - `rep_01_passthrough`
  - `rep_02_gain_bias`
  - `rep_03_stereo_mix`
  - `rep_07_nonlinear_clip`
  - `rep_38_sine_phasor`
- assertion:
  - factory creation succeeds
  - `compute_body_lowered == true` (no Cranelift stub fallback)

This turns the selected subset into an enforced backend regression gate.

Validation:

- `cargo fmt --all` ✅
- `cargo test -p cranelift-ffi selected_runtime_corpus_cases_lower_compute_body -- --nocapture` ✅
- `cargo clippy -p cranelift-ffi --all-targets -- -D warnings` ✅

### Step A2 completed: remove scaffold semantics from active Cranelift runtime/factory paths
Commit date: 2026-02-27

Implemented Step A2 cleanup on top of A1:

- Removed active-path `"scaffold"` markers in factory identity/status payloads:
  - JSON now exposes runtime-oriented keys (`jit_compiled`, `compute_body_lowered`)
  - SHA key prefix switched from `cranelift-scaffold:*` to `cranelift:*`
  - factory status string updated to runtime wording
- Updated module/function Rustdoc from scaffold wording to runtime wording in:
  - `crates/cranelift-ffi/src/factory.rs`
  - `crates/cranelift-ffi/src/instance.rs`
  - `crates/cranelift-ffi/src/types.rs`
  - `crates/cranelift-ffi/src/lib.rs`
- Kept temporary/deferred families explicit (bitcode temp format and
  unimplemented signals/boxes constructors), with clear documentation.

Validation:

- `cargo fmt --all` ✅
- `cargo check -p cranelift-ffi` ✅
- `cargo clippy -p cranelift-ffi --all-targets -- -D warnings` ✅
- `cargo test -p cranelift-ffi -- --nocapture` ✅

### Step A1 completed: real Cranelift instance runtime (`compute` + UI/meta sidecar dispatch)
Commit date: 2026-02-27

Implemented Step A1 from `porting/cranelift-backend-plan-en.md` in
`cranelift-ffi`:

- Replaced no-op instance runtime with a real JIT compute invocation path.
- Added owned aligned `dsp*` state allocation per instance (zero-initialized),
  clone support, and deterministic cleanup.
- Wired instance `compute` to the finalized Cranelift symbol address.
- Added interpreter sidecar wiring in factories for callback semantics:
  - compile-side sidecar generation from file/string paths
  - instance-side sidecar executor lifecycle (`static_init`, `init`, `reset_ui`, `clear`)
  - real UI/meta callback dispatch through shared instruction dispatch helpers.

Touched files:

- `crates/cranelift-ffi/src/types.rs`
- `crates/cranelift-ffi/src/factory.rs`
- `crates/cranelift-ffi/src/instance.rs`
- `crates/cranelift-ffi/src/ui.rs`

Rustdoc/documentation updates included:

- added full Rustdoc for new runtime buffer type and invariants
- added explicit `# Safety` context on new runtime dispatch boundaries
- updated module docs from scaffold wording to runtime-helper/runtime-execution wording

Validation:

- `cargo fmt --all` ✅
- `cargo check -p cranelift-ffi` ✅
- `cargo clippy -p cranelift-ffi --all-targets -- -D warnings` ✅
- `cargo test -p cranelift-ffi -- --nocapture` ✅

### Cranelift backend finish plan: update with concrete execution backlog from current implementation state
Commit date: 2026-02-27

Updated `porting/cranelift-backend-plan-en.md` to move from a pure draft
roadmap to an actionable completion plan aligned with the current codebase.

What changed:

- Updated header status/date to reflect active partial implementation.
- Added a "Current implementation snapshot" section:
  - real JIT/subset lowering already present in `codegen`
  - experimental CLI already wired
  - `cranelift-ffi` still scaffolded on instance runtime paths
- Added a new execution section:
  - `## 10. Finish Backlog from Current State (Execution Order)`
  - commit-sized steps `A1`..`E1`
  - explicit scope + pass criteria per step
  - immediate recommended PR sequence (`PR-1`..`PR-4`)
- Clarified section precedence:
  - legacy phase roadmap kept as baseline
  - new Section 10 is now the authoritative finish order
- Renumbered subsequent sections for consistency.

Why:

- reduce ambiguity on "what remains to finish Cranelift"
- make implementation progress measurable with objective gates
- align execution order with the actual current backend/FFI state

### Box FFI pass: new `box-ffi` crate + shared tree helpers + FIR/source export wiring
Commit date: 2026-02-27

Implemented the full `boxes` FFI pass as a dedicated crate and aligned related
infrastructure so the C/C++ API can be exercised with consistent memory and
conversion behavior.

Main deliverables:

- Added new crate `crates/box-ffi` (workspace member) with:
  - C export surface (`libfaust-box-c.h` style) and C++ convenience wrapper
    (`libfaust-box.h`)
  - global context lifecycle (`createLibContext` / `destroyLibContext`)
  - large `Cbox*` constructor/matcher surface
  - conversion pipeline exports:
    - `CDSPToBoxes`
    - `CboxesToSignals`
    - `CboxesToSignals2` (symbolic path via `de_bruijn_to_sym`)
    - `CcreateSourceFromBoxes` for `c|cpp|fir|interp`
  - `freeCMemory` ownership handling for strings and tracked pointer arrays
- Added `box-ffi` dependencies and workspace wiring:
  - `Cargo.toml`
  - `Cargo.lock`
- Introduced safe shared helpers in `tlib`:
  - `tree_to_string`
  - `tree_to_int`
  - `tree_to_double`
- Reused the new safe helpers in existing crates to reduce duplicated
  node-decoding logic:
  - `crates/compiler/src/lib.rs`
  - `crates/fir/src/lib.rs`
  - `crates/propagate/src/lib.rs`
  - `crates/tlib/src/recursion.rs`
  - `crates/transform/src/signal_fir/module.rs`

Documentation:

- Added Rustdoc and per-symbol `# Safety` sections across the `box-ffi` exports.
- Added crate-level `box-ffi/README.md`.
- Added header smoke tests under
  `crates/box-ffi/tests/header-smoke/`.

Validation:

- `cargo fmt --all` ✅
- targeted compile/clippy checks run during the pass on touched crates ✅

### Complete per-symbol Rustdoc `# Safety` coverage for FFI exports in `interp-ffi` and `cranelift-ffi`
Commit date: 2026-02-27

Performed a second Rustdoc pass focused on exported C ABI symbols to ensure
each public `extern "C"` function has an individual Rustdoc block with an
explicit `# Safety` section.

Touched files:

- `crates/interp-ffi/src/factory.rs`
- `crates/interp-ffi/src/instance.rs`
- `crates/cranelift-ffi/src/factory.rs`

What was added:

- missing `# Safety` sections on non-`unsafe` exported symbols
  (`getCLibFaustVersion`, cache/global MT toggles, array-return helpers, version re-export)
- clarified ownership/lifetime contract for returned static pointers and
  array/string deallocation responsibilities
- kept ABI and runtime behavior unchanged (documentation-only update)

Validation:

- `cargo fmt --all` ✅
- `cargo check -p interp-ffi -p cranelift-ffi` ✅
- `cargo clippy -p interp-ffi -p cranelift-ffi --all-targets -- -D warnings` ✅
