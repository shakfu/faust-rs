# 2026-05-13

## Add Julia backend plan and CLI wiring

Commit date: 2026-05-13
Source commit: this commit (`Add Julia backend and CLI language selection`)

Added `porting/julia-backend-plan-2026-05-13-en.md` to define the initial
Julia backend scope against the C++ Faust reference command:

```sh
faust -lang julia foo.dsp -o foo.julia
```

Implemented a module-first Julia FIR emitter in
`crates/codegen/src/backends/julia/mod.rs`, including typed `FRS-CGEN-JULIA-*`
diagnostics, Faust-style lifecycle methods, `mutable struct mydsp{T} <: dsp`,
UI/metadata calls, one-based Julia table indexing, and `compute!` over
`Matrix{FAUSTFLOAT}` input/output views.

Wired the backend through the compiler facade and CLI so `faust-rs -lang julia
foo.dsp -o foo.julia` generates Julia source. Added targeted backend, CLI, and
fast-lane integration coverage.

Validation:

- `cargo fmt --all`
- `cargo test -p codegen julia --lib`
- `cargo test -p compiler fastlane_julia_emits_faust_style_shell`
- `cargo run -p compiler -- -lang julia /private/tmp/faust-rs-julia-smoke.dsp -o /private/tmp/faust-rs-julia-smoke.julia`

## Document BRA tape type boundary

Commit date: 2026-05-13
Source commit: this commit (`Document BRA tape type boundary`)

Expanded `porting/rad-disable-reverse-time-rec-fast-path-plan-2026-05-10-en.md`
with the implementation boundary between promoted Signal-IR values and
backend-local BRA tape storage.

The new section explains why `SigBlockReverseAD` does not create `fBraTapeN`
nodes during RAD propagation: the carrier records the semantic need for a
block-local reverse sweep, while `signal_fir` owns the backend scheduling
decision about which values must be stored, where stores occur relative to
delay/state updates, and which concrete `real_ty` tape layout is used.

It also documents the 2026-05-12 cast fix: integer-typed Signal-IR values can
legitimately be selected for the BRA real tape after normal promotion, so the
FIR boundary must cast `Int32` lowered values to `real_ty` before
`store_table(fBraTapeN, ...)`.

Validation:

- `git diff --check -- porting/rad-disable-reverse-time-rec-fast-path-plan-2026-05-10-en.md`
