# 2026-08-25

## E3: one leaf grammar, two schedulers — the shared leaf_emit module

Commit date: 2026-08-25

Source commit: this commit

The campaign's last experiment. The scalar and vector lowerers each
carried their own copy of the stateless leaf emission rules; family by
family the two were proven to emit identical FIR (same `map_binop`
mapping, same operand typing contract, same integer-vs-real `min`/`max`/
`abs` selection, same internal-precision constant emission), and the
copies now live once in `signal_fir/leaf_emit.rs`: `emit_binop` (with the
full fast-lane typing contract and a `LeafBinopError` each path maps back
to its exact current diagnostics), `emit_math_call1`/`emit_math_call2`,
`emit_minmax`, `emit_abs`, `emit_real_const`, and `map_binop` itself
(moved from `module/arithmetic.rs`, where the vector lowerer had been
importing it — the sharing this module completes). A `LeafPrototypes`
trait lets each lowerer keep its own prototype containers
(`UsedPrototypes` on the scalar side, a two-field adapter on the vector
side) while the emitters just note usage; everything stays statically
dispatched. Out of scope, per the plan: state, placement, caching,
regions, UI, tables, delays, recursion, and `select2` until its sharing
is proven the same way.

Doctrine: the module is producer-side vocabulary, like `FirBuilder`
itself; it is consumed only by `module/arithmetic.rs`, `module/bra.rs`,
and `vector/lower/signal.rs`. structure-check now also flags any
checker file referencing `leaf_emit`, validated by a rejecting mutation.

Gates: transform 404 green, codegen 370 green, compiler green except the
4 known mem0 corpus failures, golden-check 199/199 byte-identical,
structure-check green, clippy/fmt clean. A stateless-leaf fix can no
longer land on one lowering path only — the drift risk the analysis
called L2 is closed.

## E2 complete: the function-length ratchet list is empty

Commit date: 2026-08-25

Source commit: this commit

The last three decompositions, and the milestone: `VectorPlanError`'s
`Display` impl (238 lines) delegates its two large variant families to
`fmt_lockstep` and `fmt_fused_group` (identical messages; the main match
keeps a guarded wildcard naming the family formatters).
`build_verified_vector_module_with_evidence` (230 → ~115) hands its
second half to `finish_vector_module` (output materialization, FIR
assembly, UI lifecycle, final verification, packaging) with the exact
`trace_stage` labels passed through. `assemble_module` (209 → ~75)
delegates to `declare_instance_functions` and `declare_compute_function`.

`OVERSIZED_FUNCTIONS` is now `[(&str, &str, usize); 0] = []`: every
production function in `crates/transform` is at most 200 lines, enforced
by `structure-check` check 8, and the gate's docs now say the list has
been empty since today — a new oversized function is decomposed, not
listed. E2 started the day with 21 functions over the threshold and
`build_module` at 757 lines.

Gates: transform 404 green, codegen 370 green, compiler green except the
4 known mem0 corpus failures, golden-check 199/199, structure-check
green, clippy/doc clean.

## E2 tail: lower_proj, verify_assembled_fused_serial_groups, signal_dependencies

Commit date: 2026-08-25

Source commit: this commit

`lower_proj` (258 → ~130): the once-per-clock-context simultaneous body
pass becomes `schedule_recursion_group_bodies` (multi-output snapshotting
and carrier stores included) and the carrier read becomes
`load_projection_result`. `verify_assembled_fused_serial_groups`
(252 → ~35): per-group phases as functions — `locate_physical_group_body`
(top-rate `i0` loop vs clock-island lanes), `check_group_delayed_reads`,
`check_group_state_writers`, `check_group_internal_transports` — sharing
the lazily built assembled-node set. `signal_dependencies` (249 → 193):
the SYMREC preamble, the interval-classified `Delay` rule, the `RdTbl`
rule, and the clock-wrapper rule are named functions; the remaining
dispatch is the one-arm-per-rule table it always wanted to be.
Ratchet 6 → 3.

Gates: transform 404 green, golden-check 199/199, structure-check green.

## E2 tail: lower_signal, infer_uncached, materialize_action

Commit date: 2026-08-25

Source commit: this commit

Three more. `lower_signal` (274 → 184): the clocked-machinery rejection
arm becomes `clocked_machinery_rejection` (diagnostic strings unchanged)
and the variability-driven placement tail becomes `place_by_variability`,
carrying its full Phase-1 design comment as the method's doc.
`infer_uncached` (261 → 197): the `R_PROJ` SYMREC seeding/join and the
`R_CD` wrapper rule are named methods (`infer_sym_rec_group`,
`infer_wrapper_domain`) with the clock-calculus rule in each header; the
remaining dispatch is one rule arm per line. `materialize_action`
(260 → 168): the two large state actions — the recursion step and the
four-shape delay write — are `materialize_recursion_step` and
`materialize_delay_write`. Ratchet 9 → 6.

Gates: transform 404 green, golden-check 199/199, structure-check green.

## E2 tail: propagate_bra_adj, compile_fastlane_inner, ensure_guarded_block

Commit date: 2026-08-25

Source commit: this commit

Three more recipe decompositions. `propagate_bra_adj` (300 → 152): the
four stateful adjoint arms — `Delay1` carry, `Delay(c)` circular carry
array, `Prefix` with its frame-0 init contribution, and `Proj` over
symbolic recursion carriers — are named methods with the adjoint equation
in each contract header. `compile_fastlane_inner` (282 → 138): the
clock-analysis closure becomes `analyze_clocks` + `build_scalar_gate`,
and the extraction also removed a genuine ~55-line duplication (the
hgraph/effect-orientation/schedule sequence appeared verbatim in both the
with-domains and empty-table branches). `ensure_guarded_block`
(277 → ~60): decode wrapper, select guard shape, emit guard precondition,
declare hold fields, lower the guarded body — one method each, with the
final guard wrap staying in the orchestrator. Ratchet 12 → 9.

Gates: transform 404 green, `p3_shadow_mode` 3 green, golden-check
199/199, structure-check green.

## E2: decompose build_module into a recipe of eight phase functions

Commit date: 2026-08-25

Source commit: this commit

The campaign's flagship: `build_module` (757 lines at the start of E2,
the crate's largest function) is now a ~140-line orchestrator whose body
reads as the module-assembly recipe — placement analysis and lowerer
construction, `lower_sample_slices` (scheduled graphs, forward slice,
reverse-AD slice), `emit_compute_prologue` (output aliases, reverse and
BRA carry resets), `run_bucket_cse`, `emit_lifecycle_functions`
(`metadata`/`staticInit`/`instanceConstants`/`buildUserInterface`/
`instanceResetUserInterface`/`instanceClear`, returned as a named
`LifecycleFunctions` bundle), `split_control_statements`,
`assemble_compute_statements`, `emit_entry_points`
(`compute`/`frame`/`control`), `collect_prototype_declarations`, and
`assemble_module_output` (fill-mode `SubModule` or the complete scalar
`Module`). Bodies verbatim; every phase carries a contract header; the
largest extracted phase is 164 lines. Ratchet 13 → 12.

Gates: transform 404 green, codegen 370 green, compiler green except the
4 known mem0 corpus failures, golden-check 199/199 (byte-identical
emitted modules), structure-check green.

## E2: LowerCursor bundling and program/dispatch decomposition in the vector lowerer

Commit date: 2026-08-25

Source commit: this commit

The crate's largest file (`vector/lower/signal.rs`) threaded the
`(cache, active)` pair as two separate `&mut` arguments through 16 method
signatures and 83 call sites. They are now one `LowerCursor` — the
analysis doc's prescribed bundling — so every recursive lowering call
reads `self.lower_dep(scope, x, cur)`. On top of that, `lower_raw`
(345 lines) delegates its three large inline arms to named methods
(`lower_delay`, `lower_projection`, `lower_clock_wrapper`) and lands at
194 lines of one-line dispatch arms; `lower_vector_program_impl`
(306 lines) delegates its phases to `lower_control_roots`,
`materialize_control_section`, and `lower_region_body` and lands at 183,
keeping the exact `trace_stage` points. Bodies verbatim modulo the cursor
rename. Ratchet 15 → 13.

Gates: transform 404 green, golden-check 199/199, structure-check green.

## E2: decompose build_fused_serial_groups into a FusionContext

Commit date: 2026-08-25

Source commit: this commit

`build_fused_serial_groups` (536 lines) becomes a 15-line orchestrator
over a `FusionContext`: the constructor derives the shared closure rows,
per-loop effects, and carrier-by-owner index once; the three seed-discovery
routes (delayed recursion paths, immediate state-mediated crossings,
transported delayed reads) are named methods each returning candidates;
`close_components` merges seeds and runs the fixpoint whose per-component
step is `absorb_into_component` (171 lines, the largest piece); and
`emit_groups` produces the canonical records. The `Candidate` struct is
hoisted to module level with field docs. Bodies verbatim modulo one
borrow-form fix. Ratchet 16 → 15.

Gates: transform 404 green, golden-check 199/199, structure-check green.

## E2: decompose build_vector_plan into staged builder functions

Commit date: 2026-08-25

Source commit: this commit

`build_vector_plan` (585 lines) already narrated itself through
`trace_stage` markers; the stages are now functions and the orchestrator
keeps the exact same trace points: `CertificateFacts::derive` (delayed
sets, record index, recursion groups, sample-required closure, separation
verdicts), `allocate_loops` (root/recursion/vectorizable loop identities
and pre-seeded owners), `place_signals` + the extracted
`place_disconnected_effect_components` (root visits, orphan effect
components, pre-seeded visits, the fail-closed unplaced check),
`derive_dependency_edges`, `close_delayed_edges`, then
`build_signal_records` / `build_loop_records` / `build_transports`, and
`check_immediate_delay_coverage`. Bodies verbatim except three borrow-form
adjustments and one rename to the state-owned `sample_required` clone
(identical set — cloned before any mutation, never mutated after).
The orchestrator is ~60 lines; the largest stage is 162. Ratchet 17 → 16.

Gates: transform 404 green, golden-check 199/199 (byte-identical plans and
FIR), structure-check green.

## E2: decompose verify_fused_serial_groups_after_plan into per-obligation methods

Commit date: 2026-08-25

Source commit: this commit

The 447-line fused-group checker is now a 10-line orchestrator: a
`FusedGroupCheckContext` derives the shared state once (decoration records,
plan signal/loop maps, reachability, delayed-occurrence pairs), and each
per-group obligation is a named method with a contract header — carriers
are genuinely delayed in-group state, one clock domain per group, delayed
reads cover exactly the carrier set with complete member paths, effect
isolation from outside loops, recursive writers, internal transports —
plus the global reconstruction of dangerous immediate crossings. Bodies
verbatim; largest method is 146 lines. Ratchet 18 → 17.

Gates: transform 404 green, golden-check 199/199, structure-check green.

## E2: verify_prepared_signal becomes a bundled walker with named arm methods

Commit date: 2026-08-25

Source commit: this commit

`verify_prepared_signal` (558 lines) threaded six arguments through every
one of ~25 recursive call sites — seven lines of ceremony per operand. The
traversal state now lives in a `PreparedSignalWalk` struct (arena, UI
program, visited memo, symbolic-group arities, reachable-node trace), so
each recursion is one `self.verify(child)?` line, and the four large node
families moved to named methods with contract headers: `verify_sym_rec_group`,
`verify_wrtbl`, `verify_projection`, `verify_block_reverse_ad`, plus
`reject_illegal_shapes` for the staging-eliminated forms. The dispatch
method is 189 lines; bodies are verbatim modulo the `self.` renames. Also
folds `cargo fmt` reflow of this campaign's earlier comment edits.

Gates: transform 404 green, golden-check 199/199, structure-check green
after removing the resolved ratchet entry (19 → 18).

## E2: decompose verify_vector_plan into a recipe of per-obligation checks

Commit date: 2026-08-25

Source commit: this commit

`verify_vector_plan` (643 lines, the crate's largest checker function) is
now a 13-line orchestrator reading as a table of contents: schema/canonical
orders, lockstep bundles, fused-group shape, epoch coverage, placement and
roots, edges and barriers, per-epoch acyclicity, effect-conflict ordering,
transports, `VecSafe` witnesses — one named function per obligation family,
each with a contract header. The shared derived state (id sets, record
maps, reachability, per-loop effect summaries) moved into a `PlanIndex`
built once, *after* the root/edge-endpoint pre-checks that make its
construction panic-free on a hostile DTO — the one previously implicit
ordering constraint, now stated on the struct. The only non-move change:
the effect-conflict section no longer rebuilds `effects_by_loop` and
`CheckedReachability` a second time; both are pure functions of the plan,
so reusing the index instances is observationally identical. Bodies are
otherwise verbatim; largest phase function is 157 lines.

Gates: transform 404 tests green, golden-check 199/199, structure-check
first *flagged the now-stale ratchet entry* ("13 lines, no longer over the
threshold — remove its entry") and passes after its removal — the E2
ratchet shrinking exactly as designed (20 → 19 entries).

## E2: function-length ratchet gate in structure-check

Commit date: 2026-08-25

Source commit: this commit

First landing of experiment E2 (recipe decomposition,
`porting/transform-legibility-analysis-2026-08-25-en.md`). `xtask
structure-check` gains check 8: no production function in
`crates/transform` may exceed 200 lines unless named in
`OVERSIZED_FUNCTIONS` with a ceiling. The list starts with the 20 current
offenders (`build_module` 757 down to `assemble_module` 209) and is
cross-checked both ways — an entry whose function no longer exists, no
longer exceeds the threshold, or grew past its ceiling is a finding, so
the list can only shrink and must end empty. Function spans are measured
by the same textual brace balance the file threshold uses, the inline
`#[cfg(test)] mod` tail is excluded, and trait-impl name collisions
(`fmt`) are resolved to the longest span per name. Validated by three
rejecting mutations: a delisted `build_module` is flagged as over the
threshold, a lowered `verify_vector_plan` ceiling is flagged as growth,
and a phantom entry is flagged as stale.

## Delete the unreachable in-scalar `-vec` chunk-driver machinery

Commit date: 2026-08-25

Source commit: this commit

Executes the follow-up flagged by E1's first landing (`9fa67e0e`). The
reachability proof was re-confirmed before deleting: `build_module` has
exactly two callers — `signal_fir/mod.rs`, where an accepted `-vec` compile
returns early from the checked vector pipeline and a rejected one forces
the fallback compute mode to `Scalar`, and `module/subcontainer_compile.rs`,
which hardcoded `Scalar` — so `emit_sample_loop`'s `ComputeMode::Vector`
branch and everything behind it executed only under in-file tests.

Deleted, net −1,214 lines across four files: in `module/build.rs`,
`emit_sample_loop` with its chunk-driver documentation, `split_bodies`,
`build_chunk_driver`, `chunk_inner_loops`, `build_simple_driver`, and
`build_fastest_driver` (the emission loop now pushes `plain_sample_loop`
directly), plus `build_module`'s now-meaningless `compute_mode` parameter;
in `loop_graph.rs`, the chunk-buffer/partition half (`ChunkBuffer`,
`RecursivePartition`, `partition_recursive_body`, `rewrite_var_loads`,
`collect_var_loads`, `stmt_reads_writes`), `slice_has_persistent_state`
with its `Vectorizable`/`Recursive` classification (its only consumer was
the deleted branch), the whole `LoopKind` enum and `LoopNode.kind` field,
and the eight tests that exercised them — `LoopNode` is now
`is_reverse` + `pre`/`exec`/`post` + `deps`, and `add_loop` takes only
`is_reverse`. `signal_fir/mod.rs` drops the `fallback_compute_mode`
computation (it was provably always `Scalar`) and passes the scalar
schedule unconditionally. Both file headers now describe the scalar-only
reality and point at git history for the deleted half.

FIR-neutral by construction and by measurement: `xtask golden-check` is
199/199 OK before and after (committed goldens are the byte diff),
`cargo test -p transform` 404 green (412 − the 8 deleted dead-code
tests), `-p codegen` 370 green, `-p compiler` green except the 4 known
`mem0_cpp_differential` failures caused by the working tree's pending
deletions of `tests/impulse-tests/dsp-mem0/*.dsp` (re-verified green with
the files restored), `structure-check` OK, clippy and `cargo doc` clean.
`loop_graph.rs` is now 593 lines (1,792 before the E1 campaign).

## E1: present-tense doc comments, a codename gate, and a crate reading order

Commit date: 2026-08-25

Source commit: this commit

Third E1 landing, the large one: ~260 comment lines across ~60 files of
`crates/transform` explained present-tense behavior through historical plan
codenames ("P4.3b", "the P5 verifier", "roadmap P3 slice 4", "§4.8", "R1's
bullet", "S7"). Every such line now names the behavior itself — decoration
certificate, vector plan, routing evidence, state plan, clock/AD plan,
event-order certificate, assembly gate, schedule certificate, guarded-block
lowering, fire-time maintenance — and provenance survives only in
`Plan provenance:` mentions, `Development history:` lines naming their
`porting/` document, and the stage-map glossary of `vector/mod.rs`.

The sweep surfaced four more stale claims, all truth-fixed in passing:
`schedule/mod.rs` still said "nothing in the production compile path calls
`schedule` yet; hgraph keeps its own hand-rolled walk" (contradicted by
`hgraph`'s own header — the generic scheduler is production-authoritative
on both paths); `core_lowering.rs` and `error.rs`/`coverage.rs`/
`contract.rs` said clocked lowering "has not landed" where the rejection
actually fires when no clocked-lowering state is active for the run; and
`region.rs` said guarded child regions "arrive with P3" while documenting
them two paragraphs later.

The gate: `xtask structure-check` gains check 6 — a bare plan codename
(`P4.3b`, `R1`, `V5b`, `S7`, `§4.8`) in a transform comment block is a
finding unless the block names a `porting/` document or contains a
provenance/history marker — and check 7 — no file-level
`#![allow(dead_code)]` in the crate. Both validated by rejecting mutations
(an appended bare-codename comment and an inserted file-level allowance
each fail the gate; both reverted). `lib.rs` gains a "How to read this
crate" section giving the pipeline reading order and pointing first-time
readers away from `diagnostics/`.

Verification: `structure-check` OK (274 files), `cargo test -p transform`
(412 green), `cargo test -p xtask` (116 green), `cargo doc -p transform`
clean, clippy clean for the touched crates. Comment-only changes plus the
xtask gate: FIR-neutral by construction.

## E1: quarantine the observation-only surfaces under signal_fir/diagnostics/

Commit date: 2026-08-25

Source commit: this commit

Second E1 landing. `pv_slice` (the frozen PV vector pre-slice prototype) and
`shadow` (schedule-conformance reports) lived undistinguished next to
production modules, so a reader walking the production path had to open each
header to learn they are load-bearing nowhere. Both moved to a new
`signal_fir/diagnostics/` tree whose module header states the contract in
one place: observation only, never on the production path, consumed solely
by workspace integration tests and explicit diagnostic entry points.
`pub use diagnostics::{pv_slice, shadow}` keeps every existing import path
(`transform::signal_fir::pv_slice::...`, `...::shadow::...`) compiling
unchanged, so the `compiler` integration tests needed no edits; the
`signal_fir` module map now points at `diagnostics` instead of listing the
two modules as production neighbors. No production code changed.

Verification: `cargo test -p transform` (412 green), the two consumers
`pv_vector_slice` (3 green) and `p3_shadow_mode` (2 green), clippy clean.

## E1: tell the truth in loop_graph.rs and drop its dead half

Commit date: 2026-08-25

Source commit: this commit

First landing of experiment E1 from
`porting/transform-legibility-analysis-2026-08-25-en.md` (present-tense
documentation and truth maintenance), on branch `transform-legibility-e1`.

`signal_fir/loop_graph.rs` still opened with "Nothing here is wired into
scalar codegen yet, so it cannot affect existing output" while
`module/build.rs` routes every scalar per-sample slice through `LoopGraph`,
and its file-level `#![allow(dead_code)]` hid an 11-warning dead cluster.
The header now states who uses what today: the scalar slice routing is live;
the `-vec` chunk-driver half (`ChunkBuffer`, `partition_recursive_body`,
`rewrite_var_loads`, `emit_sample_loop`'s split emission) is currently
unreachable in production — the checked vector pipeline owns every accepted
`-vec` compile and a rejected one falls back to scalar, so no production
caller passes a vector compute mode into `build_module` anymore (verified:
`mod.rs` forces `fallback_compute_mode` to `Scalar` on every path that
reaches `build_module`, and `subcontainer_compile.rs` hardcodes `Scalar`).
Removing that half is recorded as a separate, behavior-neutral decision,
not taken here.

Deleted outright: the superseded signal-level loop-assignment section
(`LoopAssignment`, `assign_loops`, `assign_one`, `signal_value_children`,
`loop_of`) and its five tests — `vector/plan` owns signal-to-loop placement
now — plus the `LoopKind::Island` variant, never constructed in production
(its only consumer was one unreachable `emit_sample_loop` arm; islands are
handled by the checked pipeline's `clock_ad` stage). Helpers that only
tests call (`add_dep`, `len`, `is_empty`, `is_vectorizable`, `loop_kind`,
`ChunkBuffer::name`) are now `#[cfg(test)]` with a sentence saying so —
notably `add_dep`: the scalar path builds edgeless graphs, and the tests
keep pinning `topological_order`'s deterministic tie-breaking through it.
The file-level allow is gone; `pv_slice`'s two stale doc references to
`signal_value_children` now name the real walk
(`vector::analysis::signal_dependencies`).

Verification: `cargo check`/`clippy`/`test -p transform` (412 green),
`cargo test -p codegen` (goldens, 368 green), `cargo test -p compiler` —
the 4 `mem0_cpp_differential` failures observed there reproduce with and
without this change and are caused by the working tree's pending
deletions of `tests/impulse-tests/dsp-mem0/*.dsp` (restoring the files
makes all 4 pass with this change applied); they are unrelated to E1.

## Crate-wide legibility analysis of `crates/transform`

Commit date: 2026-08-25

Source commit: this commit

New twin analysis documents
`porting/transform-legibility-analysis-2026-08-25-{en,fr}.md` (EN canonical):
why `crates/transform` is still costly for a human to read after the June 2026
decompositions and the July R0–R9 cleanup, and three independent,
FIR-neutral restructuring experiments to fix it.

Measured state (all commands recorded in the doc so the tables can be
re-derived): `transform` is now the largest crate of the workspace (34,628
effective + 15,681 test LOC via `scripts/loc_report.py`; 62,399 raw lines over
150 files, 48% of them under `signal_fir/vector/`). A brace-balance scan
counts 1,589 functions of which 69 exceed 100 lines and 21 exceed 200 —
`build_module` alone is 767 lines. Both production lowerers dispatch on the
typed `SigMatch` view but duplicate every stateless leaf-emission arm
(only `map_binop` is shared); ≈200 comment lines explain present-tense code
through historical plan codenames; and one measured truth-drift instance:
`loop_graph.rs` still claims "nothing here is wired into scalar codegen yet"
while `module/build.rs:991` routes every scalar per-sample slice through it,
and its file-level `#![allow(dead_code)]` hides an 11-warning dead cluster
(probed by removing the allow under `cargo check`, then restoring it).

The diagnosis names five costs (L1 function-level scale, L2 twin-lowerer
duplication, L3 codename indirection, L4 truth drift, L5
production/diagnostic mixing) and maps them to three experiments: E1
present-tense documentation + truth fixes + `diagnostics/` quarantine for
`pv_slice`/`shadow` (zero FIR risk), E2 recipe decomposition of the 21
oversized functions behind a ratcheting max-length gate, E3 a shared
stateless `leaf_emit` module for the two lowerers — the same pattern that
closed the C-family emitter drifts at backend level. The doc also freezes
what must not be simplified (producer/checker duplication, deterministic
ordering, fail-closed fallback taxonomy) and the shared migration protocol
(clean rebuild, golden-FIR byte diff across the mode matrix, impulse oracle,
rejecting-mutation validation for every new gate).
