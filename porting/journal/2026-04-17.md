# 2026-04-17

## Add host-driven stereo chorus adaptation example for current FAD subset

Commit date: 2026-04-17
Source commit: pending

Added [auto_chorus_stereo_fad_host.dsp](/Users/letz/Developpements/RUST/faust-rs/tests/corpus/auto_chorus_stereo_fad_host.dsp) as a
compileable replacement for the original `auto_chorus_stereo.dsp` idea under the
current forward-AD support boundary.

- Kept the stereo chorus topology and the loss/gradient monitors.
- Replaced the in-DSP gradient-descent state update with explicit `Delay L` /
  `Delay R` controls so `fad(lossExpr, delayL)` and `fad(lossExpr, delayR)`
  stay in the currently supported non-recursive explicit-seed subset.
- Documented in the DSP source that host code is expected to feed the emitted
  gradients back into the delay controls externally.
- Verified with `cargo run -q -p compiler -- auto_chorus_stereo_fad_host.dsp`.

## Reconcile signal pipeline arity expectations with workspace validation

Commit date: 2026-04-17
Source commit: pending

Followed the full workspace validation request and fixed three stale
`signal_pipeline` expectations that still claimed zero process inputs for
fixtures using the implicit input wire.

- Updated `rep_01_passthrough.dsp`, `rep_02_gain_bias.dsp`, and
  `rep_23_feedback_simple.dsp` expectations in
  `crates/compiler/tests/signal_pipeline.rs` from `inputs == 0` to
  `inputs == 1`, matching the actual `process = _`, `process = _ * ...`, and
  `process = + ~ _` source arities.
- Folded in the pending formatting-only edits in `crates/eval/src/lib.rs` and
  `crates/parser/tests/api_bridge.rs` so the worktree returns to a consistent
  validated state.
- Re-ran:
  - `cargo test --workspace --all-targets`
  - `cargo clippy --workspace --all-targets -- -D warnings`

## Add targeted recursive FAD regression families

Commit date: 2026-04-17
Source commit: pending

Expanded the recursive forward-AD regression surface with focused tests in
`crates/compiler/tests/signal_pipeline.rs` plus new shared fixtures in
`tests/corpus/`.

- Added new corpus fixtures for locally consumed recursive AD outputs:
  - `fad_recursive_local_projection.dsp`
  - `fad_recursive_pair_reduction.dsp`
  - `fad_recursive_multi_local.dsp`
  - `fad_recursive_multilane_local.dsp`
- Added signal-pipeline regressions for these families:
  - alternate local projection shape inside `Rec`,
  - reducing `[primal, tangent]` locally before the recursive output,
  - multiple local `fad(...)` consumers in one recursive branch,
  - local FAD under a transparent `vgroup(...)` wrapper,
  - explicit-seed FAD with a seed derived from a recursive projection,
  - multi-lane recursion with a local FAD consumer on one lane.
- Re-ran targeted validation on:
  - the recursive FAD `signal_pipeline` subset,
  - the new inline recursive FAD tests,
  - `parser` `production_corpus`,
  - the exact multi-`fad` and multi-lane regressions.

## Revalidate recursive FAD corpus after augmented-state Rec

Commit date: 2026-04-17
Source commit: 6641fc6

Ran a broader validation pass after enabling augmented-state recursive
propagation for local FAD consumers.

- Re-ran the recursive FAD subset in `crates/compiler/tests/signal_pipeline.rs`
  with the `fad_recursive` filter: 9 tests passed.
- Re-ran `inline_fad_seq_with_identity_pair_compiles` to confirm ordinary
  non-recursive local consumption of `[primal, tangent]` remains unchanged.
- Re-ran the concrete `auto_pan5.dsp` program through the compiler CLI and
  confirmed it now code-generates successfully.

## Enable augmented-state Rec propagation for local FAD consumers

Commit date: 2026-04-17
Source commit: aa5fb7f

Implemented the second code slice of the augmented-state recursion plan in
`crates/propagate/src/lib.rs`.

- `FlatNodeKind::Rec` now switches on `RecFadMode`:
  - `ExpandAfterRec` keeps the existing deferred tangent expansion protocol,
  - `AugmentedState` builds the recursive group directly on the expanded AD
    lanes.
- In augmented-state mode, `ForwardAD` is no longer suppressed during recursive
  branch propagation, so local consumers such as `fad(loss, prev) : !, _` can
  see the real `[primal, tangent]` outputs during recursive wiring.
- Added a new signal-pipeline regression test covering a local tangent
  projection in a feedback function.
- Verified that the concrete `auto_pan5.dsp` case now compiles end-to-end
  through the compiler CLI.
- Rechecked representative legacy recursive FAD fixtures to keep the old
  `ExpandAfterRec` path stable.

## Add RecFadMode classification infrastructure

Commit date: 2026-04-17
Source commit: d59cb97

Implemented the first code slice of the augmented-state recursion plan in
`crates/propagate/src/lib.rs`.

- Added `RecFadMode` with the three intended cases:
  `None`, `ExpandAfterRec`, and `AugmentedState`.
- Added the first structural classifier for `boxRec(...)`, distinguishing
  ordinary recursive FAD placement from recursive branches that consume FAD
  outputs locally through a non-transparent parent operator.
- Updated the `Rec` arity path so recursive validation and output-count
  prediction now depend on the selected mode:
  - `ExpandAfterRec` keeps the historical FAD-transparent wiring arity,
  - `AugmentedState` switches recursive validation to the real expanded AD
    arity.
- Updated Rustdoc around recursive FAD handling so the distinction between the
  deferred-expansion mode and the augmented-state mode is explicit.

## Plan augmented-state recursion for local FAD consumers

Commit date: 2026-04-17
Source commit: pending

Added `porting/rec-augmented-primal-tangent-state-plan-2026-04-17-en.md`, a
new plan for replacing the current "expand-after-Rec" handling of `fad(exp, x)`
with a recursion model that can carry primal and tangent lanes directly when a
recursive branch consumes AD outputs locally.

The plan is motivated by the `auto_pan5.dsp` family:

- `fad(loss, prev)` itself is fine,
- the failure comes from `fad(loss, prev) : !, _` inside the feedback function,
- which requires the tangent to be available *inside* the recursive branch,
  not only after the enclosing `Rec` is built.

The proposed direction is:

- keep the current deferred expansion mode for existing recursive FAD corpus
  cases that do not consume AD outputs locally,
- add an augmented-state `Rec` mode when local AD consumers require the real
  `[primal, tangent]` outputs during recursive wiring.
