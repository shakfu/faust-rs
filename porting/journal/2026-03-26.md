# 2026-03-26

## fix(xtask): handle WASM codegen errors in corpus status path

Commit date: 2026-03-26
Source commit: working tree after `db25b79`

Fixed the build break introduced by the newer `CompilerError::CodegenWasm`
variant by updating `xtask`'s corpus-status classifier to handle WASM codegen
failures explicitly.

### Scope

- Added a `CompilerError::CodegenWasm { .. }` arm in
  `crates/xtask/src/main.rs` so the status summarizer remains exhaustive.
- Added targeted `#[allow(clippy::too_many_arguments)]` annotations on two WASM
  backend helpers whose current bring-up signatures are intentionally wide:
  - `scaffold_function_body`
  - `ComputeSubsetLowerer::lower_for_loop`

### Why this step

- `cargo build` was failing because `xtask` still matched the older
  `CompilerError` set.
- After fixing the match, workspace `clippy -D warnings` still failed on two
  known wide helper signatures in the WASM backend, so the quality gate had to
  be restored before committing.

### Validation

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo build`

## feat(codegen): lower static table loads in WASM `compute`

Commit date: 2026-03-26
Source commit: working tree after `e3f7705`

Removed the next broad-corpus residual after the math pass by teaching the
WASM backend to read FIR static tables from linear memory and to emit their
initial contents in the data section.

### Scope

- Added `LoadTable(access: Static)` lowering in the WASM compute subset.
- Static table loads now use the absolute linear-memory offset assigned by
  `WasmMemoryLayout`, plus the lowered element index.
- Added static table data-segment emission for `DeclareTable(AccessType::Static)`
  entries so those loads read initialized bytes instead of zero-filled memory.
- Reused the existing layout offsets already derived for static tables.

### Adaptation note

- The current backend now emits multiple active data segments:
  - JSON metadata at offset `0`
  - one segment per static table at its derived table offset
- This keeps the current scaffold shape simple while making static table reads
  executable on the covered subset.

### Regression coverage

Added two targeted tests in `crates/codegen/src/backends/wasm/tests.rs`:

- `wasm_compute_loads_static_tables`
- `wasm_module_emits_static_table_data_segments`

The first asserts that `compute` contains:
- the static table base offset as `i32.const`
- `f32.load`
- `f32.store`

The second parses the data section and checks that:
- the JSON segment is still present at offset `0`
- the static table bytes are emitted at the expected derived offset

### Validation

- `cargo fmt --all`
- `cargo test -p codegen wasm_`
- `cargo test -p compiler wasm`
- corpus re-check of static-table cases:
  - `tests/corpus/rep_20_environment_waveform.dsp`
  - `tests/corpus/rep_34_table_rdtable_readonly_const.dsp`
  - `tests/corpus/rep_36_table_rdtable_negative_index.dsp`
  - `tests/corpus/rep_63_store_load_table.dsp`
- `xxd -l 8 /tmp/rep_20_environment_waveform.wasm` → `0061 736d 0100 0000`

## feat(codegen): lower native WASM `rint` / nearest

Commit date: 2026-03-26
Source commit: working tree after `1912796`

Extended the native WASM math subset with `rint`, lowering it directly to the
WebAssembly `nearest` opcodes instead of leaving it in the imported-math
fallback path.

### Scope

- Added `FirMathOp::Rint` lowering in the WASM compute subset for:
  - `f32` → `f32.nearest`
  - `f64` → `f64.nearest`
- Marked `Rint` as part of the native WASM math family in the subset matcher.
- Updated the subset documentation comment to keep the native math catalog
  explicit.

### Why this step

- A broader corpus scan showed the next math-related residual fallback as:
  `unsupported imported WASM math subset call: Rint / Float32 / argc=1`
- `rint` is a direct WASM opcode mapping in the C++ backend family, so it is a
  clean native extension of the current subset.

### Regression coverage

Extended the existing native-math regression in
`crates/codegen/src/backends/wasm/tests.rs`:

- `wasm_compute_lowers_native_math_fun_calls`

The fixture now also emits `FirMathOp::Rint`, and the test asserts presence of:
- `f64.abs`
- `f64.nearest`
- `f64.min`
- `f64.max`

### Validation

- `cargo fmt --all`
- `cargo test -p codegen wasm_`
- `cargo test -p compiler wasm`
- representative corpus re-check for math-heavy cases:
  - `tests/corpus/rep_31_extended_primitives.dsp`
  - `tests/corpus/rep_19_primitive_family.dsp`
  - `tests/corpus/rep_31_variant_filters.dsp`
- `xxd -l 8 /tmp/rep_31_extended_primitives.wasm` → `0061 736d 0100 0000`

### Remaining next blocker after this step

- The next broad-corpus residual seen after the math pass is not another math
  intrinsic but static table reads in `compute`:
  `LoadTable { ..., access: Static, ... }`

## feat(codegen): lower integer `abs` in WASM `compute`

Commit date: 2026-03-26
Source commit: working tree after `72e1011`

Removed the next residual `compute` fallback seen on the WASM corpus basket by
teaching the backend to lower integer `abs` calls directly in the subset.

### Scope

- Added explicit handling for `FunCall("abs", ..., Int32)` in the WASM compute
  subset.
- Lowered integer absolute value inline as a WASM `select` pattern rather than
  introducing a new host import dependency.
- Kept the existing floating-point `Abs` lowering unchanged (`f32.abs`,
  `f64.abs`).
- Updated the subset documentation comment to make the integer-`abs` support
  explicit.

### Adaptation note

- C++ WASM classifies integer `abs` as an external math call.
- The Rust WASM backend intentionally lowers it inline for this bring-up step
  so representative DSPs no longer depend on an extra host-provided `_abs`
  import.
- This is an `adapted` mapping for now, chosen to improve standalone
  operability while preserving semantics on the covered subset.

### Regression coverage

Added a targeted test in `crates/codegen/src/backends/wasm/tests.rs`:

- `wasm_compute_lowers_int_abs_inline`

The fixture forces `abs(-i0)` inside `compute` and asserts that the emitted
WASM body now contains:
- `i32.sub`
- `i32.lt_s`
- `select`

### Validation

- `cargo fmt --all`
- `cargo test -p codegen wasm_`
- `cargo test -p compiler wasm`
- representative corpus basket compiled with `-lang wasm`, including:
  - `tests/corpus/rep_31_extended_primitives.dsp`
  - `tests/corpus/rep_33_modulation_chain.dsp`
  - `tests/corpus/rep_56_noise_smoo_slider.dsp`
  - `tests/corpus/rep_70_route_arithmetic_params.dsp`
- no `lower_compute_subset fallback` diagnostic remained on that basket after
  this step
- `xxd -l 8 /tmp/rep_31_extended_primitives.wasm` → `0061 736d 0100 0000`

## feat(codegen): implement WASM param load/store accessors

Commit date: 2026-03-26
Source commit: working tree after `f44e9a0`

Implemented the next "useful v1" API step for the WASM backend by replacing
the `getParamValue` / `setParamValue` stubs with the same ad hoc memory-access
scheme used by the C++ WASM backend.

### Scope

- Implemented `getParamValue(dsp, index)` as a direct load from `dsp + index`.
- Implemented `setParamValue(dsp, index, value)` as a direct store to
  `dsp + index`.
- Kept the access typed to the backend real type:
  - `f32.load` / `f32.store` in single-precision mode
  - `f64.load` / `f64.store` in double-precision mode
- Documented the C++ parity contract in the backend: `index` is a byte offset
  in the DSP struct, not a UI ordinal decoded inside the WASM module.

### Why this step now

- These functions are part of the exported DSP ABI and were still returning
  zero / doing nothing.
- The C++ reference backend already treats them as low-level offset-based
  accessors, so the contract is explicit enough to implement without waiting
  for a higher-level UI-index map in FIR.

### Regression coverage

Added two targeted tests in `crates/codegen/src/backends/wasm/tests.rs`:

- `wasm_get_param_value_loads_real_from_dsp_plus_index`
- `wasm_set_param_value_stores_real_to_dsp_plus_index`

They assert the exact emitted instruction shapes for the default `f32` ABI:
- `local.get dsp`
- `local.get index`
- `i32.add`
- `f32.load` or `f32.store`

### Validation

- `cargo fmt --all`
- `cargo test -p codegen wasm_`
- `cargo test -p compiler wasm`
- `cargo run -p compiler --bin faust-rs -- -lang wasm tests/corpus/rep_09_ui_slider.dsp -o /tmp/faust_rs_step14_rep09.wasm`
- `xxd -l 8 /tmp/faust_rs_step14_rep09.wasm` → `0061 736d 0100 0000`

## feat(codegen): lower FIR-backed WASM `instanceClear`

Commit date: 2026-03-26
Source commit: working tree after `8824769`

Implemented the next "useful v1" lifecycle step for the WASM backend by
teaching it to lower a real FIR `instanceClear` body instead of always
emitting an empty stub.

### Scope

- Added function-body discovery for non-`compute` FIR declarations in the WASM
  backend.
- Added `instanceClear` lowering through the same statement/value subset used
  for `compute`, with the correct one-argument ABI (`instanceClear(dsp)`).
- Refactored the local-index allocation so the shared lowering path can start
  stack locals at the appropriate parameter count:
  - `compute`: first stack local at index 4
  - `instanceClear`: first stack local at index 1
- Kept the existing fallback policy for unsupported `instanceClear` bodies, but
  real FIR clear loops/stores now emit executable WASM when they stay in the
  supported subset.

### Why this step now

- `instanceClear` is part of the core DSP lifecycle already called by `init`
  and `instanceInit`.
- Until now, even stateful DSPs got an empty clear function in WASM, which
  made the backend structurally valid but not operational for runtime state
  reset.

### Regression coverage

Added a targeted test in `crates/codegen/src/backends/wasm/tests.rs`:

- `wasm_instance_clear_lowers_struct_state_reset_body`

The fixture builds a module with:
- one scalar struct field reset via `StoreVar(kStruct)`
- one array struct field reset via a `SimpleForLoop` of `StoreTable(kStruct)`

The test asserts that the emitted `instanceClear` body now contains:
- `loop`
- `br_if`
- `i32.store`
- `f32.store`

### Validation

- `cargo fmt --all`
- `cargo test -p codegen wasm_`
- `cargo test -p compiler wasm`
- `cargo run -p compiler --bin faust-rs -- -lang wasm tests/corpus/rep_04_delay_echo.dsp -o /tmp/faust_rs_step13_rep04.wasm`
- `xxd -l 8 /tmp/faust_rs_step13_rep04.wasm` → `0061 736d 0100 0000`

## feat(codegen): accept labels and int helpers in WASM `compute`

Commit date: 2026-03-26
Source commit: working tree after `26fdc84`

Started the first "useful v1" tranche for the WASM backend by removing two
high-frequency causes of `compute` fallback on the current corpus basket:
FIR `Label` markers emitted by the signal fast-lane path, and internal integer
helper calls to `max_i` / `min_i`.

### Scope

- Treated `Label(...)` as a structural no-op in the WASM `compute` subset:
  - accepted by the local collector
  - accepted by statement lowering
- Added lowering for internal helper calls:
  - `max_i`
  - `min_i`
- Reused the already-emitted module-local WASM helper bodies for those calls,
  instead of classifying them as imported math.
- Updated the backend subset documentation comment so these newly supported
  nodes are explicit.

### Why this step first

- The first corpus scan after Step 11 showed that `Label(...)` prevented any
  lowering at all on multiple representative DSPs.
- Once labels were accepted, the next blocker on the same basket was the
  internal helper `max_i`, notably in table/runtime-write style programs.
- Fixing these two cases removes fallback on a broader practical slice before
  touching the DSP API stubs (`instanceClear`, `getParamValue`,
  `setParamValue`).

### Regression coverage

Added two targeted tests in `crates/codegen/src/backends/wasm/tests.rs`:

- `wasm_compute_ignores_label_markers`
- `wasm_compute_calls_internal_max_i_helper`

The first asserts that a labeled passthrough FIR body still lowers to real
WASM load/store/loop instructions. The second asserts that `compute` now emits
a direct `call` to the internal `max_i` helper body rather than falling back.

### Validation

- `cargo fmt --all`
- `cargo test -p codegen wasm_`
- `cargo test -p compiler wasm`
- `cargo run -p compiler --bin faust-rs -- -lang wasm tests/corpus/rep_35_table_rwtable_runtime_write.dsp -o /tmp/faust_rs_step12_rep35.wasm`
- `xxd -l 8 /tmp/faust_rs_step12_rep35.wasm` → `0061 736d 0100 0000`
- Representative fallback scan:
  - `tests/corpus/rep_01_passthrough.dsp`
  - `tests/corpus/rep_05_one_pole_lowpass.dsp`
  - `tests/corpus/rep_04_delay_echo.dsp`
  - `tests/corpus/rep_35_table_rwtable_runtime_write.dsp`
  - `tests/corpus/rep_55_sine_phasor_echo_feedback.dsp`
  - `tests/corpus/rep_68_variable_delay_audio_rate.dsp`
  - no `lower_compute_subset fallback` diagnostic remained on this basket

## feat(codegen): lower WASM `ForLoop` and `WhileLoop` statements

Commit date: 2026-03-26
Source commit: working tree after `51e38ff`

Extended the Step-11 WASM `compute` subset with the remaining basic loop
statements so the backend can now execute FIR bodies that use explicit
`ForLoop` and `WhileLoop`, instead of falling back to the scaffold no-op body.

### Scope

- Added local-collector support for:
  - `ForLoop`
  - `WhileLoop`
- Added statement lowering for:
  - forward and reverse `ForLoop`
  - `WhileLoop`
- Added mutable local updates for loop variables through the existing
  `StoreVar(kLoop | kStack)` path.
- Added boolean literal lowering in the expression subset because `WhileLoop`
  conditions can legally be plain FIR `Bool` constants.
- Updated the subset documentation comment in the WASM backend to keep the
  newly supported loop/control-flow surface explicit.

### Deliberate limitation

- This step covers statement loops only; `IteratorForLoop` is still outside the
  executable WASM subset.
- The current `ForLoop` lowering uses a straightforward compare-and-branch
  shape and does not yet try to canonicalize richer C++ loop idioms.

### Regression coverage

Added a new targeted test in
`crates/codegen/src/backends/wasm/tests.rs`:

- `wasm_compute_lowers_for_and_while_loops`

The fixture `build_loop_statement_module()` asserts that the emitted WASM
`compute` body now contains:
- multiple `loop` blocks
- `i32.eqz` for `WhileLoop` exit
- `i32.ge_s` for forward `ForLoop` bounds
- `i32.add` for loop stepping

### Validation

- `cargo fmt --all`
- `cargo test -p codegen wasm_`
- `cargo test -p compiler wasm`
- `cargo run -p compiler --bin faust-rs -- -lang wasm tests/corpus/rep_25_with_local_defs.dsp -o /tmp/faust_rs_step11_rep25.wasm`
- `xxd -l 8 /tmp/faust_rs_step11_rep25.wasm` → `0061 736d 0100 0000`

## feat(codegen): lower WASM control-flow statements in `compute`

Commit date: 2026-03-26
Source commit: working tree after `2cb6d92`

Extended the Step-10 WASM `compute` subset with the first statement-level
control-flow lowering, so the backend can now execute the shared FIR
`control_flow` fixture instead of falling back to the no-op body.

### Scope

- Added statement lowering for:
  - `If`
  - `Control`
  - `Switch`
  - `Drop`
- Added local-state assignment lowering for `StoreVar(kStack | kLoop)`.
- Implemented `Switch` as a deterministic nested comparison chain using
  `if/else` plus integer equality checks, which keeps the bring-up path simple
  and explicit.
- Extended local-collection traversal so these control-flow nodes no longer
  abort subset discovery.

### Deliberate limitation

- This step still uses an explicit branch chain for `Switch`; it does not try
  to emit jump-table style lowering yet.
- It does not yet cover `WhileLoop`, `ForLoop`, or iterator loops in the WASM
  subset.

### Regression coverage

Added a new targeted test in
`crates/codegen/src/backends/wasm/tests.rs`:

- `wasm_compute_lowers_control_flow_statements`

The test uses `build_control_flow_test_module()` and asserts that the emitted
WASM `compute` body now contains:
- multiple `if`
- `else`
- `drop`
- `i32.eq`

### Validation

- `cargo fmt --all`
- `cargo test -p codegen wasm_`
- `cargo test -p compiler wasm`
- `cargo run -p compiler --bin faust-rs -- -lang wasm tests/corpus/rep_25_with_local_defs.dsp -o /tmp/faust_rs_step10_rep25.wasm`
- `xxd -l 8 /tmp/faust_rs_step10_rep25.wasm` → `0061 736d 0100 0000`

## feat(codegen): import external WASM math functions for `compute`

Commit date: 2026-03-26
Source commit: working tree after `d4acde4`

Extended the Step-9 WASM backend so `compute` can now lower a first subset of
math calls through the module import section instead of falling back to the
no-op body when the FIR requires non-native WASM math.

### Scope

- Added collection of imported math functions from the `compute` FIR body.
- Added type-section entries for imported math signatures.
- Added import-section emission for math imports from the `"env"` module.
- Added imported-call lowering in `compute` for a first subset including:
  - unary: `sin`, `cos`, `exp`, `log`, `log10`, `tan`, `atan`, `asin`, `acos`, `round`
  - binary: `pow`, `atan2`, `fmod`, `remainder`
- Fixed internal function index offsets so they now depend on imported
  functions only, not on imported memory.
- Added `Neg` lowering so the shared math-intrinsics fixture stays inside the
  executable subset.

### Deliberate limitation

- This step still does not cover the full C++ math import catalog
  (`sinh/cosh/tanh`, `copysign`, `isinf`, etc.).
- Type/import ordering is now stable for the current subset, but the full
  C++ parity ordering rules still need a later dedicated pass.

### Regression coverage

Added two new tests in `crates/codegen/src/backends/wasm/tests.rs`:

- `wasm_module_imports_external_math_functions_for_compute`
- `wasm_compute_calls_imported_math_functions`

These assert that the `math_intrinsics` fixture now emits:
- import names `_atan2`, `_cos`, `_pow`, `_sin`
- real `call` instructions to imported function indices from `compute`

### Validation

- `cargo fmt --all`
- `cargo test -p codegen wasm_`
- `cargo test -p compiler wasm`
- `cargo run -p compiler --bin faust-rs -- -lang wasm tests/corpus/rep_25_with_local_defs.dsp -o /tmp/faust_rs_step9_rep25.wasm`
- `xxd -l 8 /tmp/faust_rs_step9_rep25.wasm` → `0061 736d 0100 0000`

## feat(codegen): lower native WASM math calls in `compute`

Commit date: 2026-03-26
Source commit: working tree after `32c7df1`

Extended the Step-8 WASM `compute` subset with a first `FunCall` lowering path
for math operations that map directly to native WASM opcodes, without needing
host imports yet.

### Scope

- Added `FunCall` lowering for the native-opcode math subset:
  - unary: `fabs`, `sqrt`, `floor`, `ceil`
  - binary: `fmin`, `fmax`
- Covered both `f32` and `f64` result types.
- Kept the current subset boundary explicit: unsupported function calls still
  trigger the normal subset fallback rather than failing all WASM emission.

### Deliberate limitation

- This step does not yet lower imported math calls such as `sin`, `cos`,
  `pow`, `atan2`, `exp`, `log`, etc.
- It also does not yet populate the WASM import section with math functions.

### Regression coverage

Added a new targeted test in
`crates/codegen/src/backends/wasm/tests.rs`:

- `wasm_compute_lowers_native_math_fun_calls`

The test builds a minimal mono FIR module using:
- `fabs`
- `fmin`
- `fmax`

It then decodes the emitted WASM `compute` body and asserts the presence of:
- `f64.abs`
- `f64.min`
- `f64.max`

### Validation

- `cargo fmt --all`
- `cargo test -p codegen wasm_`
- `cargo test -p compiler wasm`
- `cargo run -p compiler --bin faust-rs -- -lang wasm tests/corpus/rep_25_with_local_defs.dsp -o /tmp/faust_rs_step8_rep25.wasm`
- `xxd -l 8 /tmp/faust_rs_step8_rep25.wasm` → `0061 736d 0100 0000`

## feat(codegen): lower WASM struct tables and `Select2` in `compute`

Commit date: 2026-03-26
Source commit: working tree after `5e8a462`

Extended the Step-7 WASM `compute` subset so the backend can now lower a small
stateful delay-style DSP using inline `Struct` tables and `Select2`-based index
wrapping.

### Scope

- Added `LoadTable(kStruct)` lowering against the derived linear-memory layout.
- Added `StoreTable(kStruct)` lowering for inline state tables.
- Added `Select2` lowering using the core WASM `select` instruction.
- Kept the existing partial-subset strategy: unsupported `compute` shapes still
  fall back to the no-op body rather than failing whole-module emission.

### Covered FIR shape

This step is enough for the existing `table_state_delay` fixture shape:

- `fDelay[idx]` struct-table reads and writes
- `fWriteIdx` scalar struct state
- integer compare (`Ge`) for wrap detection
- `Select2(ge_wrap, 0, idx_plus)` for modulo-like wrap without introducing
  generic control-flow lowering yet

### Regression coverage

Added a new targeted code-body test in
`crates/codegen/src/backends/wasm/tests.rs`:

- `wasm_compute_lowers_struct_tables_and_select2`

The test uses the shared `build_table_state_delay_test_module()` fixture and
asserts that the emitted WASM `compute` body contains:
- `select`
- `i32.ge_s`
- multiple `f32.load`
- multiple `f32.store`

### Validation

- `cargo fmt --all`
- `cargo test -p codegen wasm_`
- `cargo test -p compiler wasm`
- `cargo run -p compiler --bin faust-rs -- -lang wasm tests/corpus/rep_25_with_local_defs.dsp -o /tmp/faust_rs_step7_rep25.wasm`
- `xxd -l 8 /tmp/faust_rs_step7_rep25.wasm` → `0061 736d 0100 0000`

## feat(codegen): lower struct scalar state and casts in WASM `compute`

Commit date: 2026-03-26
Source commit: working tree after `24f0762`

Extended the Step-6 WASM `compute` subset so the backend can now read and
write scalar `Struct` state from linear memory and combine it with basic FIR
arithmetic/cast nodes inside the sample loop.

### Scope

- Wired the `compute` subset lowerer to the derived `WasmMemoryLayout` so
  `Struct` fields are resolved through the same offsets used by lifecycle code.
- Added `LoadVar(kStruct)` lowering for scalar fields stored in linear memory.
- Added `StoreVar(kStruct)` lowering for scalar field writes.
- Added a first arithmetic expression subset:
  - integer ops: add/sub/mul/div/rem/bitwise/shifts/comparisons
  - float ops: add/sub/mul/div/comparisons for `f32` and `f64`
- Added basic numeric `Cast` lowering covering the current bring-up cases:
  - `f32 <-> f64`
  - `i32 -> f32/f64`
  - `f32/f64 -> i32`
  - `i32 <-> i64`

### Deliberate limitation

- This step still does not cover `Select2`, math imports/opcodes, struct table
  accesses in the WASM subset, or generic UI/metadata lowering.
- Unsupported `compute` shapes still keep the no-op fallback instead of
  failing the whole module emission.

### Regression coverage

Added a new targeted code-body test in
`crates/codegen/src/backends/wasm/tests.rs`:

- `wasm_compute_lowers_struct_state_and_casts`

The test builds a minimal FIR module with:
- `LoadVar(kStruct)` on `fBias`
- `StoreVar(kStruct)` on `fBias` and `fLevel`
- `BinOp(Add)` on `FAUSTFLOAT`
- `Cast(Float64, ...)`

It then inspects the decoded WASM body and asserts the presence of `f32.add`,
`f64.promote_f32`, `f64.store`, and multiple `f32.load` operations.

### Validation

- `cargo fmt --all`
- `cargo test -p codegen wasm_`
- `cargo test -p compiler wasm`
- `cargo run -p compiler --bin faust-rs -- -lang wasm tests/corpus/rep_25_with_local_defs.dsp -o /tmp/faust_rs_step6_rep25.wasm`
- `xxd -l 8 /tmp/faust_rs_step6_rep25.wasm` → `0061 736d 0100 0000`

## feat(codegen): lower mono passthrough `compute` in the WASM subset

Commit date: 2026-03-26
Source commit: working tree after `fcd6bb5`

Implemented the next Step-5 slice of the WASM backend by replacing the no-op
`compute` body with a real lowering path for the smallest useful FIR subset:
mono passthrough-style DSPs built from I/O aliases and a forward sample loop.

### Scope

- Added a dedicated partial `compute` subset lowerer in
  `crates/codegen/src/backends/wasm/mod.rs`.
- The lowered subset now handles:
  - `Block`
  - `DeclareVar(kStack)`
  - forward `SimpleForLoop`
  - `LoadVar(kFunArgs=count | kLoop | kStack)`
  - `LoadTable(kFunArgs=inputs/outputs | kStack aliases)`
  - `StoreTable(kStack aliases)`
- The lowerer pre-collects local stack and loop variables, assigns WASM locals,
  and emits a real `block/loop/br_if` structure.
- Mono `input0` / `output0` aliases created from `inputs[0]` / `outputs[0]`
  now lower to pointer locals, then to real sample `f32.load` / `f32.store`
  operations inside `compute`.

### Deliberate limitation

- This is still a strict subset bring-up, not generic FIR-to-WASM lowering.
- Unsupported `compute` shapes still fall back to the valid no-op body instead
  of aborting the whole module emission.

### Regression coverage

Added a code-body inspection test in
`crates/codegen/src/backends/wasm/tests.rs`:

- `wasm_compute_passthrough_lowers_loop_and_sample_io`

The test decodes the generated WASM and asserts that `compute` now contains a
real loop plus `f32.load` / `f32.store` sample traffic for the passthrough
fixture.

### Validation

- `cargo fmt --all`
- `cargo test -p codegen wasm_`
- `cargo test -p compiler wasm`
- `cargo run -p compiler --bin faust-rs -- -lang wasm tests/corpus/rep_25_with_local_defs.dsp -o /tmp/faust_rs_step5_rep25.wasm`
- `xxd -l 8 /tmp/faust_rs_step5_rep25.wasm` → `0061 736d 0100 0000`

## feat(codegen): start real WASM lifecycle bodies for sample-rate state

Commit date: 2026-03-26
Source commit: working tree after `7127ad1`

Started the Step-4 function-body work for the WASM backend by replacing some
placeholder lifecycle bodies with real memory-based code using the new layout
engine.

### Scope

- `getSampleRate` now loads the `fSampleRate` field from linear memory when the
  field exists in the FIR-derived layout.
- `instanceConstants` now stores the incoming `sample_rate` parameter into
  `fSampleRate` when the field exists.
- `instanceInit` now calls:
  - `instanceConstants`
  - `instanceResetUserInterface`
  - `instanceClear`
- `init` now calls:
  - `classInit`
  - `instanceInit`
- `max_i` / `min_i` now emit the expected `i32.lt_s + select` shape instead of
  the previous placeholder return.

### Notes

- Memory accesses currently use the fixed alignment hint `2` (4-byte) required
  by the WASM plan.
- This is still not full FIR lowering: `compute`, UI reset bodies, and generic
  state/table initialization remain for later steps.

### Regression coverage

Added two new code-body tests in
`crates/codegen/src/backends/wasm/tests.rs`:

- `wasm_get_sample_rate_loads_struct_field_when_present`
- `wasm_instance_constants_stores_sample_rate_when_field_exists`

These inspect decoded WASM operators and assert the expected load/store bodies.

### Validation

- `cargo fmt --all`
- `cargo test -p codegen wasm_`
- `cargo test -p compiler wasm`
- `cargo run -p compiler --bin faust-rs -- -lang wasm tests/corpus/rep_25_with_local_defs.dsp -o /tmp/faust_rs_step4_rep25.wasm`
- `xxd -l 8 /tmp/faust_rs_step4_rep25.wasm` → `0061 736d 0100 0000`

## feat(codegen): implement initial WASM memory layout engine

Commit date: 2026-03-26
Source commit: working tree after `905d1d9`

Implemented the Step-2 memory layout engine for the WASM backend so the
scaffold no longer reports a placeholder memory description.

### Scope

- Replaced the dummy `WasmMemoryLayout::scaffold(...)` usage with a real
  `WasmMemoryLayout::from_module(...)` derivation.
- The layout now walks FIR module sections and computes:
  - struct-backed field offsets,
  - static-table offsets,
  - `tables_offset`,
  - `io_zone_offset`,
  - raw `total_bytes`,
  - required page count.
- The engine currently accepts:
  - `DeclareVar { access: Struct }`
  - `DeclareVar { access: Struct, typ: Array(..) }`
  - `DeclareTable { access: Struct }`
  - `DeclareTable { access: Static }`
  - helper `DeclareFun { body: None }` entries, which are ignored

### Layout rule implemented

The current layout follows the WASM plan’s C++-parity rule that each field slot
is at least one audio-sample slot wide:

- single precision: minimum slot size = 4 bytes
- double precision: minimum slot size = 8 bytes

For Rust FIR compatibility, `f64`-typed storage in single-precision mode is
still widened to 8 bytes instead of being truncated to a 4-byte slot.

### Regression coverage

Added three new layout-focused tests in
`crates/codegen/src/backends/wasm/tests.rs`:

- `wasm_layout_tracks_struct_offsets_for_sine_fixture`
- `wasm_layout_pads_i32_fields_to_audio_slot_in_double_mode`
- `wasm_layout_places_static_tables_after_struct_region`

### Validation

- `cargo fmt --all`
- `cargo test -p codegen wasm_`
- `cargo test -p compiler wasm`
- `cargo run -p compiler --bin faust-rs -- -lang wasm tests/corpus/rep_25_with_local_defs.dsp -o /tmp/faust_rs_step3_rep25.wasm`
- `xxd -l 8 /tmp/faust_rs_step3_rep25.wasm` → `0061 736d 0100 0000`

## feat(compiler): wire WASM scaffold through compiler facade and CLI

Commit date: 2026-03-26
Source commit: working tree after `d51e95d`

Completed the second bring-up step for the new WASM backend by exposing the
`codegen` scaffold through the `compiler` facade and the `faust-rs` CLI.

### Scope

- Added `backend-wasm` feature plumbing in `crates/compiler/Cargo.toml`.
- Added `Compiler` facade entry points:
  - `compile_source_to_wasm`
  - `compile_source_to_wasm_with_lane`
  - `compile_file_to_wasm`
  - `compile_file_to_wasm_with_lane`
  - `compile_file_default_to_wasm`
  - `compile_file_default_to_wasm_with_lane`
- Added `CompilerError::CodegenWasm`.
- Added CLI support for `-lang wasm`.
- Added binary output handling in `main.rs` so the CLI can write raw `.wasm`
  bytes instead of trying to treat the backend output as UTF-8 text.
- Extended the FIR fixture path so `--fir-fixture ... -lang wasm` also works.

### Deliberate limitations of this step

- The CLI currently writes only the `.wasm` binary. Companion JSON/WAT output
  is still deferred to later backend steps.
- The emitted module remains the scaffold from the previous commit: ABI-shaped
  and structurally valid, but not yet executing real FIR code.

### Regression coverage

Added targeted compiler tests:

- `compiler_compile_source_to_wasm_emits_magic_header`
- `cli_parse_accepts_lang_wasm`

### Validation

- `cargo fmt --all`
- `cargo test -p compiler wasm`
- `cargo run -p compiler --bin faust-rs -- -lang wasm tests/corpus/rep_25_with_local_defs.dsp -o /tmp/faust_rs_step2_rep25.wasm`
- `xxd -l 8 /tmp/faust_rs_step2_rep25.wasm` → `0061 736d 0100 0000`

## feat(codegen): start WASM backend scaffold in `codegen`

Commit date: 2026-03-26
Source commit: working tree after `1b898f7`

Started the WebAssembly backend implementation from
`porting/wasm-backend-plan-2026-03-25-en.md`, limiting this first step to the
`codegen` crate so the backend can be brought up with a typed API and a
structurally valid `.wasm` skeleton before wiring the compiler facade and CLI.

### Scope

- Added `backend-wasm` feature plumbing and the `wasm-encoder` /
  `wasmparser` dependencies in `crates/codegen/Cargo.toml`.
- Replaced the previous placeholder `codegen::backends::wasm` module with:
  - `WasmOptions`,
  - `WasmModule`,
  - `WasmBackendError` / `WasmBackendErrorCode`,
  - `WasmMemoryLayout` scaffold in `layout.rs`,
  - `generate_wasm_module(...)`.
- The generator now emits a valid WASM module skeleton with:
  - a type section,
  - optional imported or internal memory,
  - canonical Faust export names,
  - trivial placeholder function bodies,
  - a data segment containing scaffold JSON metadata.

### Deliberate limitations of this step

- No FIR instruction lowering yet.
- No real struct/table memory layout yet.
- No WAT emission yet.
- Exported DSP functions are ABI-shaped placeholders only; they are meant to
  validate module structure while Step 2/3/4 of the plan are implemented.

### Regression coverage

Added `crates/codegen/src/backends/wasm/tests.rs`:

- `wasm_scaffold_emits_valid_module_for_passthrough_fixture`
- `wasm_scaffold_exports_canonical_faust_api_names`

These tests validate the skeleton with `wasmparser` and assert the current
export surface.

### Validation

- `cargo fmt --all`
- `cargo test -p codegen wasm_scaffold`

## fix(transform): lower `SIGFVAR(count)` in signal FIR Step 2C

Commit date: 2026-03-26
Source commit: working tree after `b601eaa`

Fixed the `faust-rs -pn bsmooth_test tests/signals_tests.dsp` failure reported
from `faustlibraries`, where the transform fast lane aborted with
`[FRS-SFIR-0004] unsupported signal node in Step 2C:
FVar(... sym("count"), sym("<math.h>"))`.

### Root cause

`crates/transform/src/signal_fir/module.rs` already handled `SIGFCONST` and
`SIGFFUN` in Step 2C, but `SIGFVAR` still fell through to the generic
unsupported-node error. In the failing DSP, Faust lowers the smoothing window
expression through the foreign variable `count`, which C++ treats as the
`compute(int count, ...)` function argument.

### Fix

- Added Step 2C lowering for `SigMatch::FVar`.
- Matched C++ `InstructionsCompiler::generateFVar` parity:
  `count` now lowers to `LoadVar("count", AccessType::FunArgs, Int32)`.
- Added deduplicated extern-global declarations for other foreign variables so
  non-`count` `SIGFVAR` nodes follow the same reference path instead of
  remaining unsupported.

### Regression coverage

- Added `foreign_var_count_lowers_to_compute_funarg` in
  `crates/transform/src/signal_fir/tests.rs` to assert that
  `SIGFVAR(int(0), sym("count"), sym("<math.h>"))` lowers to a compute-argument
  load in the emitted FIR.

### Validation

- `cargo fmt --all`
- `cargo test -p transform signal_fir::tests::`
- `cargo run -p compiler --bin faust-rs -- -pn bsmooth_test /Users/letz/Developpements/faustlibraries/tests/signals_tests.dsp`

## fix(eval): fold numeric case arguments before matcher dispatch

Fixed the `faust-rs -pn encoder3D_test tests/hoa_tests.dsp` stack overflow from
`faustlibraries`, and the related `optimBasic3D_test` failure on the same 3D HOA
path. The overflow reduced to the local recursive `p/pcalcul` helper inside
`hoa.lib`, where Rust repeatedly selected the catch-all recursive `case` branch
instead of the `0/1/2` base cases.

### Root cause

Rust already simplified `case` patterns before compiling the matcher automaton,
but `apply_pattern_matcher_value(...)` still matched against the raw evaluated
argument tree. In the HOA selector path, the first argument to `pcalcul(...)`
was a residual arithmetic box like `((l != 0) & ...) + ... * 2 + ... * 3`
instead of a folded numeric literal.

Because the matcher only saw that unfurled arithmetic tree, it could not match
the constant branches `0`, `1`, or `2`, so only the catch-all recursive rule
remained active. That sent `p(...)` back through `pcalcul(...)` indefinitely
until the Rust process overflowed its stack.

### Fix

- `apply_pattern_matcher_value(...)` now simplifies each consumed argument with
  `box_simplification(...)` before passing it to the pattern matcher.
- This restores the same effective behavior as the reference C++ compiler for
  numeric `case` dispatch: selector expressions are reduced to literal ints/reals
  before rule matching, so constant rules win before catch-all recursive clauses.

### Regression coverage

Added two eval tests:

- `eval_process_preserves_parser_case_rule_priority_for_recursive_local_cases` —
  self-contained regression covering the recursive `p/pcalcul` shape reduced
  from `hoa.lib`.
- `eval_process_parser_case_rule_priority_matches_cpp_reference` — file-backed
  differential test that compiles the same fixture with the C++ reference
  compiler when `FAUST_CPP_BIN` (or `/usr/local/bin/faust`) is available, then
  checks Rust eval on the same source.

### Validation

- `cargo test -p eval eval_process_matches_exact_integer_real_case_arguments_like_cpp -- --nocapture`
- `cargo test -p eval eval_process_preserves_parser_case_rule_priority_for_recursive_local_cases -- --nocapture`
- `cargo test -p eval eval_process_parser_case_rule_priority_matches_cpp_reference -- --nocapture`
- `target/debug/faust-rs -pn encoder3D_test /Users/letz/Developpements/faustlibraries/tests/hoa_tests.dsp`
- `target/debug/faust-rs -pn optimBasic3D_test /Users/letz/Developpements/faustlibraries/tests/hoa_tests.dsp`
- `cargo fmt --all`

## fix(eval): preserve captured environments for unapplied local case values

Fixed a second eval parity bug exposed by `faust-rs -pn u_transformer_test tests/wdmodels_tests.dsp`
and `u_transformerActive_test` against `faustlibraries`. The failing path passed a local
multi-clause function (`s`) through another higher-order function (`matrix`) before applying it.
Rust then raised `undefined symbol R0`, even though the reference C++ compiler kept `R0` and `R1`
captured correctly.

### Root cause

Rust already represented evaluated `case` values as `EvalValue::PatternMatcher`, but
`force_value_to_box(...)` still had one non-parity shortcut: for an unapplied matcher
(`state == 0`, no consumed arguments), it returned the original raw `case` tree instead of a
runtime `boxPatternMatcher(...)` node.

That shortcut dropped the captured matcher environments. As soon as a local `case` value escaped
its lexical site before first application, later calls re-entered a bare `case` tree with no
captured bindings, so free variables like `R0`/`R1` became undefined.

### Fix

- `force_value_to_box(...)` now always stores `EvalValue::PatternMatcher` in the side-table and
  reifies it as `boxPatternMatcher(key)`, even before the first application.
- This aligns the Rust representation with the reference compiler model where evaluated `case`
  values remain closure-like matcher runtime objects carrying their lexical environment.

### Regression coverage

Added two eval tests:

- `eval_process_preserves_unapplied_local_case_environment_through_higher_order_call` —
  in-memory regression for a local `transformer_scatter` case function passed through
  `apply_twice(...)`.
- `eval_process_unapplied_local_case_higher_order_matches_cpp_reference` — self-contained
  file-backed differential that compiles the same fixture with the C++ reference compiler when
  `FAUST_CPP_BIN` (or `/usr/local/bin/faust`) is available, then checks Rust eval on the same
  source.

### Validation

- `cargo test -p eval eval_process_preserves_unapplied_local_case_environment_through_higher_order_call -- --nocapture`
- `cargo test -p eval eval_process_unapplied_local_case_higher_order_matches_cpp_reference -- --nocapture`
- `cargo run -p compiler -- -pn u_transformer_test /Users/letz/Developpements/faustlibraries/tests/wdmodels_tests.dsp`
- `cargo run -p compiler -- -pn u_transformerActive_test /Users/letz/Developpements/faustlibraries/tests/wdmodels_tests.dsp`
- `cargo fmt --all`
