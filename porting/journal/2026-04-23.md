# 2026-04-23

## fix(propagate): lift slot_env before propagating Rec feedback branch

Commit date: 2026-04-23

Source commit: pending

### Context

Nested recursions that captured an outer-scope value via `slot_env` (e.g.
`fi.lowpass(f_curr, x)` inside a `train ~ _` loop) produced wrong C++:
the biquad's coefficient `tan(...)` argument referenced the biquad's own
`y[n-1]` (e.g. `fRec366[1]`) instead of the outer loop's feedback signal.

### Root cause

In `FlatNodeKind::Rec` (crates/propagate/src/lib.rs), `slot_env` was lifted
*after* propagating the feedback branch `right` with `l0 =
[delay1(proj(i, DEBRUIJNREF(1)))]`. Because `l0` already uses
`DEBRUIJNREF(1)` meaning "inner group", the feedback signals produced by
`right` live inside the new Rec body — but any slot lookup in `right`
still returned the un-lifted outer-scope signal, so its `DEBRUIJNREF(1)`
ended up being captured by the inner `DEBRUIJNREC` instead of the outer
one.

### Fix

Lift `slot_env` (via `liftn(..., 1, ...)`) *before* propagating `right`,
not only before `left`. Both branches now evaluate in the inner scope
consistently.

### Validation

- Regression repro `/tmp/nested_rec2.dsp`: inner Rec body now shows
  `DEBRUIJNREF(2)` for outer-scope captures (previously `DEBRUIJNREF(1)`).
- `fad_filter1.dsp` C++: `tan(fConst1 * fTemp5)` where
  `fTemp5 = select2(fRec1439[1]==0, 1000, fRec1439[1])` — the learned
  frequency from the outer train loop, matching Faust reference output.
- `cargo test --workspace` green.

### Docs

Updated `porting/faust-rs-supported-faust-subset-en.md`:

- added the fix to the list of 2026-04-23 review targets,
- added an item in section 5.2 describing nested-recursion DeBruijn
  scope correctness alongside the other backend correctness fixes.

## docs(propagate): document read-only table-read FAD semantics and boundaries

Commit date: 2026-04-23

Source commit: pending

### Context

The code step added a real `SIGRDTBL` differentiation rule for read-only table
sources, but the Rustdoc and supported-subset document still described
tables/waveforms wholesale as zero-tangent fallback territory.

That mismatch is exactly the kind of drift that becomes expensive later in a
parity-sensitive compiler: the code is no longer telling the same story as the
maintenance docs.

### Implementation

Updated `crates/propagate/src/forward_ad.rs` to document:

- the new read-only `SIGRDTBL` rule,
- the symmetric finite-difference approximation over the read index,
- the fact that bounds handling is inherited from the existing runtime table
  semantics,
- and the unchanged exclusion of writable tables and soundfiles.

Updated `porting/faust-rs-supported-faust-subset-en.md` to:

- record read-only table/waveform lookup differentiation as current `fad`
  support,
- mark it explicitly as an `adapted` Rust extension rather than C++ parity,
- tighten the remaining zero-tangent boundary to writable tables,
  soundfiles, standalone waveforms, and other unmatched families.

### Verification

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace --all-targets`

## feat(propagate): differentiate read-only table and waveform reads in FAD

Commit date: 2026-04-23

Source commit: pending

### Context

`fad` already handled arithmetic, delays, FFUNs, and a substantial recursive
subset, but `SIGRDTBL` still fell through the generic zero-tangent boundary.

That meant all read-only lookup-driven DSPs behaved as if the read address was
non-differentiable, even when the practically useful quantity is exactly the
lookup slope with respect to the index expression.

### Implementation

Extended `crates/propagate/src/forward_ad.rs` so `SigMatch::RdTbl(table, idx)`
now emits a tangent for read-only table sources:

- `SIGWAVEFORM`
- `SIGWRTBL(size, generator, nil, nil)` read-only generated tables

The rule implemented is the documented symmetric finite-difference model over
the read address:

- primal: `rdtbl(table, idx)`
- tangent: `((rdtbl(table, idx + 1) - rdtbl(table, idx - 1)) / 2) * idx'`

Writable tables remain outside the differentiable subset and still emit zero
tangents.

Added corpus fixtures:

- `tests/corpus/fad_waveform_index_basic.dsp`
- `tests/corpus/fad_rdtbl_index_basic.dsp`
- `tests/corpus/fad_recursive_waveform_index.dsp`
- `tests/corpus/fad_rwtable_index_zero_tangent.dsp`

Extended structural coverage in
`crates/compiler/tests/signal_pipeline.rs` for:

- waveform-backed lookup differentiation
- read-only generated-table lookup differentiation
- recursive read-index compilation
- writable-table non-regression zero tangent

Extended numeric runtime coverage in
`crates/compiler/tests/fad_recursive_runtime.rs` for:

- waveform lookup vs central finite differences
- read-only generated-table lookup vs central finite differences
- recursive read-index lookup vs central finite differences on a linear
  generated table

The recursive numeric case intentionally uses a wide linear read-only table,
not the waveform-square corpus fixture, so the whole-DSP finite-difference
oracle stays well-conditioned and does not confound table boundary effects
with the new FAD rule.

### Verification

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace --all-targets`

## docs(porting): plan FAD through table and waveform reads

Commit date: 2026-04-23

Source commit: pending

### Context

The current `fad` implementation is now strong on arithmetic, transcendental
functions, and a substantial recursive subset, but it still treats
tables/waveforms/soundfiles as outside the differentiable model and emits zero
tangents for those families.

The next sensible extension is not “differentiate everything with state”, but a
narrower and more defensible slice: read-only table and waveform reads.

### Implementation

Added
`porting/fad-table-waveform-read-plan-2026-04-23-en.md`, covering:

- the current support boundary
- semantic options for differentiating table reads
- a recommended first-step model based on symmetric finite differences over
  the read index
- explicit non-goals for writable tables and soundfiles
- implementation sketch, test strategy, and parity impact

### Verification

Documentation-only update. No code changes in this step.

## test(compiler): extend recursive FAD corpus to deeper nested and multi-seed mutual cases

Commit date: 2026-04-23

Source commit: pending

### Context

The existing recursive FAD coverage was already solid on self-recursive,
nested, multi-output, mutual-crossed, and multi-seed shared-recursion cases,
but it still left some more aggressive graph shapes under-tested:

- triple nested scalar recursions,
- genuinely mutual two-state recursions with two explicit seeds,
- and a nested shape where an outer scalar recursion depends on the sum of an
  inner mutual recursion.

Those are the kinds of forms most likely to expose slot-order mistakes,
interleaving bugs, or instability in the recursive FAD lowering when the graph
depth grows.

### Implementation

Added new corpus fixtures:

- `tests/corpus/fad_recursive_triple_nested_parametric.dsp`
- `tests/corpus/fad_recursive_mutual_multi_seed.dsp`
- `tests/corpus/fad_recursive_nested_mutual_parametric.dsp`

Extended structural coverage in
`crates/compiler/tests/signal_pipeline.rs` with new corpus tests for the three
fixtures.

Extended numeric runtime coverage in
`crates/compiler/tests/fad_recursive_runtime.rs` with:

- `fastlane_interp_triple_nested_recursive_fad_matches_central_difference`
- `fastlane_interp_mutual_multi_seed_recursive_fad_matches_central_difference_per_seed`
- `fastlane_interp_nested_mutual_recursive_fad_matches_central_difference`

For the triple-nested case, the finite-difference window was intentionally
limited to the first five frames because the recurrence grows fast enough that
later frames become a poor discriminator of FAD correctness versus finite-step
conditioning noise.

### Verification

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test -p compiler --test signal_pipeline fad_recursive -- --nocapture`
- `cargo test -p compiler --test fad_recursive_runtime -- --nocapture`
- `cargo test --workspace --all-targets`

## docs(propagate): clarify FAD support boundaries and recursive projection semantics

Commit date: 2026-04-23

Source commit: pending

### Context

The refreshed Rustdoc in `crates/propagate/src/forward_ad.rs` was already
substantially better than the temporary shortened header, but three details
were still too implicit for parity-sensitive maintenance:

- what exactly happens when a projection points to an outer de Bruijn
  recursion not rewritten on the current descent path,
- which zero-tangent choices are deliberate support boundaries rather than
  accidental omissions,
- and which signal families are intentionally handled by "preserve primal,
  emit zero tangent" fallback logic.

The supported-subset document also needed to reflect the current multi-lane
recursive FAD state instead of the older 2026-04-21 snapshot.

### Implementation

Expanded `crates/propagate/src/forward_ad.rs` with:

- a clearer explanation of zero-tangent treatment for discrete controls and
  non-differentiable casts
- an explicit mini-example for the `DEBRUIJNREF(level > debruijn_depth)`
  projection case
- a dedicated "zero-tangent fallback boundary" table for unsupported signal
  families

Updated `porting/faust-rs-supported-faust-subset-en.md` to:

- bump the snapshot date to 2026-04-23
- record the unified recursive multi-seed FAD refactor
- update the `fad` corpus/support description to the current recursive and
  multi-seed coverage
- document the current explicit fallback boundary for unsupported
  differentiable families

### Verification

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace --all-targets`

## docs(porting): archive unified-recursive FAD plan as implemented

Commit date: 2026-04-23

Source commit: pending

### Context

The implementation work for the unified recursive multi-lane FAD refactor was
already complete in code, tests, and Rustdoc, but the planning document still
read like an open design note.

Leaving it that way makes later archaeology harder: the next contributor has
to infer from Git history that the plan has already landed and that the
relevant implementation commits are `75404e5`, `304e969`, and `5f8e291`.

### Implementation

Updated `porting/fad-n-lanes-unified-rec-plan-2026-04-23-en.md` to record the
actual completion state:

- marked the plan as implemented/archive-trace rather than active work
- linked the implementing commits
- summarized the observable end state of the transformer and recursive layout
- marked each plan step with its implementing commit or equivalent coverage
- clarified that Step 5 was satisfied by equivalent regression coverage rather
  than a one-to-one fixture list

### Verification

Documentation-only update. The underlying implementation had already been
validated by:

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace --all-targets`

## docs(propagate): restore detailed forward AD Rustdoc after unified-rec refactor

Commit date: 2026-04-23

Source commit: pending

### Context

The multi-lane and unified-recursion refactors replaced most of
`crates/propagate/src/forward_ad.rs`, and the module header had been reduced
to a shorter summary while the new `1 + N` lane carrier was being stabilized.

That was acceptable as a temporary editing state, but not as a maintenance
baseline: the previous Rustdoc carried important semantic detail about the
dual-number model, recursive de Bruijn slot mapping, known differentiation
rules, and the generated recursive shape expected by downstream passes.

### Implementation

Expanded the module Rustdoc in `crates/propagate/src/forward_ad.rs` so it now
documents the finalized multi-lane model instead of the old mono-seed shape:

- detailed `Dual { primal, tangents[...] }` carrier description
- explicit `diff_seeds` / `diff_seed_index` roles and duplicate-seed behavior
- rule tables for arithmetic, chain-rule unary math, binary math, FFUNs,
  delay nodes, casts, control-flow, and pass-through helper nodes
- de Bruijn recursion classification and slot mapping for the interleaved
  `[primal, tangent_0, tangent_1, ...]` layout
- placeholder/cache invariant used while rebuilding recursive groups
- clearer "recursive code generation note" showing the unified-recursion
  effect on the generated AD state

### Verification

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace --all-targets`

Outcome:

- the forward-AD module once again carries the semantic and provenance detail
  needed to maintain parity-sensitive recursion and multi-seed behavior after
  the recent refactor.

## feat(propagate): collapse recursive FAD outputs onto one unified transform

Commit date: 2026-04-23

Source commit: pending

### Context

After the previous carrier refactor, `ForwardADTransform` could already carry
several tangent lanes internally, but `generate_fad_signals_multi(...)` still
instantiated one transformer per seed and stitched the rows together at the
end.

That kept the old recursive-codegen shape alive:

- one exposed primal recursion,
- plus one AD-local primal shadow recursion per seed.

The visible optimization target of the plan was to stop doing that and make
all primal/tangent outputs project into the same differentiated recursive
group.

### Implementation

Updated `crates/propagate/src/forward_ad.rs`:

- `generate_fad_signals_multi(...)` now creates one
  `ForwardADTransform::new(arena, seeds)` instead of one transformer per seed
- the output bundle is now assembled directly from one `Dual` per propagated
  output:
  - `result.push(dual.primal)`
  - `result.extend(dual.tangents)`
- Rustdoc now documents the unified-recursion behavior instead of the old
  temporary per-seed orchestration model

Added regression coverage:

- new corpus fixture:
  - `tests/corpus/fad_recursive_multi_seed_shared.dsp`
- structural tests in `crates/compiler/tests/signal_pipeline.rs` now assert:
  - `fad_recursive_parametric_self.dsp`: primal and tangent project from the
    same recursive group
  - `fad_recursive_multi_seed_shared.dsp`: all three output lanes project from
    the same recursive group
- runtime regression in `crates/compiler/tests/fad_recursive_runtime.rs` checks
  the two-seed recursive case against central finite differences for both
  tangent lanes
- `crates/propagate/tests/core_api.rs` was updated to the new invariant:
  primal and tangent projections now share the same interleaved
  `DEBRUIJNREC`

Concrete generated-code result:

- `fad_bug1.dsp` now lowers to one primal state plus one tangent state:
  the duplicate primal shadow recursion is gone from the emitted C++

### Verification

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace --all-targets`
- `cargo run -q -p compiler -- fad_bug1.dsp`

Outcome:

- the redundant recursive primal duplication is removed;
- recursive single-seed and multi-seed FAD cases remain numerically correct;
- the new observable invariant is "shared recursive group, distinct projected
  slots".

## refactor(propagate): generalize forward AD carrier to multi-lane tangents

Commit date: 2026-04-23

Source commit: pending

### Context

The unified-recursion FAD plan needs the internal transformer to carry
`1 + N` lanes (`primal + one tangent per seed`) before the public
multi-seed entry point can collapse the current "one transformer per seed"
orchestration.

The pre-existing implementation hard-coded a one-seed carrier:

- `Dual { primal, tangent }`
- `diff_seed: SigId`
- de Bruijn recursion interleaving fixed to `[primal, tangent]`

That shape forced every later optimization to duplicate most derivative
rules, because the carrier itself only knew how to represent one tangent
lane.

### Implementation

Updated `crates/propagate/src/forward_ad.rs`:

- introduced a multi-lane carrier:
  - `Dual { primal, tangents: SmallVec<[SigId; 2]> }`
- generalized `ForwardADTransform` to:
  - `diff_seeds: Vec<SigId>`
  - `diff_seed_index: AHashMap<SigId, SmallVec<[usize; 2]>>`
- generalized the node rules so they now build one tangent lane per seed
  internally:
  - arithmetic
  - unary chain rules
  - delays
  - `select2` / `prefix`
  - helper pass-through nodes
  - known differentiable `FFun` cases
- generalized de Bruijn recursion handling to interleave
  `[primal, tangent_0, tangent_1, ...]` for the active seed set
- kept `generate_fad_signals_multi(...)` on the old external orchestration
  shape for this step:
  - still one transformer per seed
  - still one tangent row gathered at a time

Supporting dependency update:

- added `smallvec` to `crates/propagate/Cargo.toml`

### Verification

- `cargo test -p propagate`
- `cargo test -p compiler --test signal_pipeline fad_recursive -- --nocapture`

Outcome:

- recursive FAD coverage still passes;
- the transformer now has the internal lane model required for the next
  step, without changing the public output expansion strategy yet.

## docs(porting): tighten unified-recursion FAD refactor plan

Commit date: 2026-04-23

Source commit: pending

### Context

`porting/fad-n-lanes-unified-rec-plan-2026-04-23-en.md` already pointed at
the right optimization target: collapse the current "one recursive AD shadow
per seed" model into one unified recursive group carrying the primal and all
tangent lanes.

The initial draft was directionally correct, but a few invariants were still
too implicit for a parity-sensitive refactor:

- seed lookup cost and shape were under-specified once `diff_seed` becomes a
  vector;
- the temporary recursive placeholder invariant needed a stronger statement;
- duplicate seed handling was not documented;
- "one recursion accumulator" could be read too literally at the backend code
  emission level;
- validation criteria leaned too much on structural C++ shape and not enough
  on numeric checks.

### Implementation

Refined the plan in
`porting/fad-n-lanes-unified-rec-plan-2026-04-23-en.md`:

- added an explicit `diff_seed_index` recommendation for multi-seed lookup;
- clarified the `DEBRUIJNREC` placeholder invariant and the required
  regression coverage around back-edge resolution;
- documented duplicate-seed expectations and recommended preserving duplicate
  tangent lanes in the first implementation;
- clarified that success means one unified recursion group at the
  signal/FIR boundary, not necessarily one literal backend array variable;
- strengthened Step 4/5/Validation to require numeric regression checks
  alongside structural assertions.

### Verification

Documentation-only update. No code or tests changed in this step.

## test(compiler): validate recursive forward-mode AD numerically

Commit date: 2026-04-23

Source commit: pending

### Context

`fad_bug1.dsp` raised a concern that the generated C++ for a seeded
self-recursive system was wrong because the AD lowering materialized a second
recursive state next to the exposed primal state.

The produced code does duplicate the primal recurrence inside the AD row, but
that alone is not evidence of a semantic bug. For a seeded recursion such as:

```text
y[n] = p * y[n-1] + 2
```

forward-mode AD legitimately needs both:

- the process-visible primal recurrence `y[n]`,
- and an AD-local primal shadow feeding the tangent recurrence
  `dy/dp[n] = y[n-1] + p * dy/dp[n-1]`.

What was missing was hard evidence across more difficult recursive families:
single-state feedback, nested recursions, multi-output recursive groups, and
genuine mutual recursion.

### Implementation

Added Rustdoc and regression coverage:

- `crates/propagate/src/forward_ad.rs`
  - documented the current recursive codegen model explicitly:
    generated AD code may duplicate primal recursion state while remaining
    semantically correct.
- `tests/corpus/`
  - added:
    - `fad_recursive_parametric_self.dsp`
    - `fad_recursive_nested_parametric.dsp`
    - `fad_recursive_multi_output_parametric.dsp`
    - `fad_recursive_mutual_crossed_parametric.dsp`
- `crates/compiler/tests/signal_pipeline.rs`
  - added structural acceptance tests for the new recursive corpus fixtures.
- `crates/compiler/tests/fad_recursive_runtime.rs`
  - added runtime checks through the interpreter fast lane:
    - closed-form recurrence validation for the `fad_bug1`-style self-recursive
      case,
    - central finite-difference validation for nested recursion,
    - central finite-difference validation for multi-output recursion,
    - central finite-difference validation for mutual recursion.

### Verification

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace --all-targets`

Outcome:

- the `fad_bug1` pattern is numerically correct under the current lowering;
- the current concern is redundancy/optimization, not a proven semantic error;
- the new recursive fixtures now guard against regressions in
  `crates/propagate/src/forward_ad.rs`.
